# 过夜推进日志

最新的一轮在最上面。这份日志是给早上的人看的，写人话：做了什么、卡在哪、还欠什么。

## 2026-09-21 02:20 ｜ 第 10、11 轮（能力收口：调休 / 出差 / 上传删除 / 用法文档）

**这两轮之后我停了**——不是没活干，是**该验证的都验证过了、该记的都记了**，继续铺量的边际价值明显低于
把状态交待清楚。下面的早报数字已按最终状态刷新。

- **调休审批**（第 10 轮，9 个能力）：加班的孪生流程，端到端两轮（含附件）跑通。
  它纠正了一个坑：`create-list` 的结构是 `[{name, processDefinitionList:[...]}]` 而**不是 `children`**，
  按 `children` 取会得到 0 条、看起来像「流程不存在」。
- **出差申请**（第 11 轮，8 个能力）：**选型方法值得记**——不是「看着大众化就选」，而是把人力域候选
  **逐个拉真实审批链**筛；用章/离职的审批人是发起人本人（守卫必拦）、转正/信息变更预览算不出候选人、
  调动/入职预览里一个 USER_TASK 都没有，**出差是这批里唯一「能真提交且能真撤销」的**。
- **上传补上删除能力**（第 11 轮，+2 个能力）：缺口是真实撞出来的——调休那条线传了个 70 字节 PNG
  却**没法通过 SDK 删**。新增 `base-upload-delete` / `-delete-multi`，**那个遗留对象已删掉**
  （删前匿名 GET 200 → 删后 403）。清理入口**必须显式点名 objectKey、一次一个，不做按前缀扫射**。
- **待办办理与加班的用法用例**（第 10 轮）：`docs/usage.md` §12/§13，865 行纯新增、0 删除。

### 这两轮最该记住的一条：**仓库自己的测试第二次抓住了我的接线遗漏**

第 6 轮我漏过一次绑定（typecheck 是绿的，因为缺绑定只是数据少几条）；
**第 11 轮又漏了一次**，同一个原因：我用文本替换插入绑定，而**锚点被中间的注释块挡住了**，
替换静默失败。两次都是**「能力 ↔ 绑定表 ↔ 门面一一对应」那两条测试**抓住的。

**教训（写给以后的我）**：给 `invoke.ts` 加绑定**不要用整段文本替换**——
那里的每个条目都可能被注释块隔开。**按行号插入**（先定位到 `capabilityId:` 那一行，再往前找 `{`），
或者加完之后**立刻 grep 一次新增的 id 计数**。我第二次改成按行号插入才成功。

### 另外两条

1. **「测试太弱」的假绿仍在反复出现，但每轮都被抓干净**：调休 2 个（事由上限拿 `REASON_MAX`
   自己断言自己、缺省分支无用例）、上传删除 1 个真问题（批量删不校验单个 key）+ 1 个
   **变异脚本自身骗人**（替换打在了文档注释上——「改坏了」其实是改错了地方）。
   **变异测试自己也会说谎**，这已经是今晚第二次记这条。
2. **一次自我纠错**：出差那条初读按 PC 页判「没有时间先后校验」，查移动端时**推翻了自己**
   （移动端与后端都有同一条），改成了三处不一致的登记表。**三端（PC / 移动 / 后端）行为不一致
   是这里的常态，别只读一端的源码就下结论。**

---


# 过夜推进早报（2026-09-21 02:20 定稿）



## 一句话

**你点名要的东西全部做完了**，另外修掉了三个生成物链的真问题、证伪了一条错误的调查结论、
补上了 SDK 最大的一处结构性缺口（只能发起流程、不能办理）。**十八个提交，最终全绿。**

## 数量

| | |
| --- | --- |
| 完成**页面** | **10 / 1019**（今晚从 4 涨到 10，+6） |
| 完成**能力** | **160 个**（今晚从 29 涨到 160） |
| 提交 | **18 个**（`2a56176` … `7a69b78`，全部本地，**没有 push**） |
| 最终回归 | `pnpm test` **56 文件 / 1920 用例**（1897 通过 + 23 跳过，跳过的是需要真实环境变量的组）；`pnpm typecheck` 干净；`pnpm run docs` 通过（openapi 校验过）；`pnpm docs:check` 报「3 项生成物与 HEAD 一致」 |

## 做了什么（按你两次调整后的优先级）

**① 基础 / 公共能力**（你说优先级最高）
- 部门 / 字典 / 权限清单（9 个能力，实测字典 **1.06 MB**、权限码 2159 条 ⇒ **没有 `listAll`**，都按关键字取）
- **上传**（3 个能力）：真实链路实测打通（PutObject → 匿名 GET 逐字节一致 → 签名 DELETE → 403），
  签名版本实证是 **V1**，零新依赖自己实现的
- 应用外壳（7 个）：用户信息 / 人员 / 待办与角标 / **可见菜单** / 工作台卡片
- 租户与企业 + 销售域（9 个）：身份证号、证件照 URL、手机号等**全部白名单裁掉**才返回
- **接上了断了很久的一条链**：`base-menu-nav` 解锁了 `src/catalog/visibility.ts`（433 行实现完整，
  一直因为没人拿得到菜单树而没接进任何门面）

**② 流程表单**（你说第二个优先，面向普通用户）
**九条线**全部端到端做完整（能发起的字段全表达 → prepare → **真提交** → 查得回 → 撤销）：
**会议室预订**（原有）/ **通用审批** / **请假** / **用车** / **差旅费报销** / **产品设计文档审核** /
**加班审批** / **调休申请** / **出差申请**。
「发票」你提过——**实测全库不存在**（命中的都是发货单/收货单字段），没有硬凑。
**八条新线里七条做了真实提交验证**，每条都在报告里如实列出实际打扰了谁、多久、有没有短信
（打扰面很小：多数是 1 个人 1~3 秒内的待办，撤销即终结；产品设计文档审核那条**打扰了 0 个真人**）。

**③ 横切能力**：**待办办理**（10 个能力）——补上「SDK 只能发起、不能办理」这个缺口。
⚠️ 它的写链路**没有端到端验证**，原因见下面「需要你决定的」第 1 条。

**③′ 顺带补的**：上传的**删除能力**（`base-upload-delete` / `-delete-multi`）——缺口是真实撞出来的：
调休那条线传了个 70 字节 PNG 却没法通过 SDK 删。补上后**那个遗留对象已删掉**（删前 200 → 删后 403）。
另有 `docs/usage.md` §11/§12/§13 三节可执行用例（流程表单 / 待办办理 / 加班）。

**④ 顺手修掉的三个生成物链真问题**（都不是新页面引入的，见 `docs/roadmap.json`）
1. **`pnpm docs` 是哑弹**：pnpm 把它当成内建的 `docs` 命令，零输出、exit 0，
   **整条重建链一次都不跑**——而 `pre-commit` 钩子用的就是它，那句「✓ 生成物已重建」此前一直是假的。已改成 `pnpm run docs`。
2. **openapi 生成器的枚举类型写错**（50 条 redocly 错误，36 条是 assignment 时代埋的——因为 1 一直没真跑过）。
3. **`batch-capabilities` 把时间戳当新鲜度判据**（违反 conventions 第 23 条），重跑一次 generate 就判过期。

---

## **需要你决定的**

1. **要一个第二个测试账号。** 这是**唯一挡在「待办办理」端到端验证前面的东西**。
   该单用三条独立证据证明了「发起人=审批人 ⇒ 后端自动秒批 ⇒ 撤不掉」且**无逃生口**
   （源码：`tryAutoApproveWhenStartUserIsAssignee` 两条分支都走、跳过用的流程变量客户端注入不了；
   运行时：翻「我的流程」47 个未取消实例，**22 条命中自动审核通过**）。
   能力代码/契约/65 例测试/27 个变异全红都在，**缺的只是那个账号**。详见 `docs/pages/待办办理.md`。
2. **测试环境里有 3 条撤不掉的测试单据**（我用「我的流程」逐条核实的）：
   `hr_general_approval` 的 `businessKey=66`（status=2，被自动审核通过）；
   请假的 `8981`/`8983`（**流程已撤销，但请假记录行不随取消删除**——后端没有监听器，与页面行为一致）。
   **都不影响别人**，但你需要知道它们存在。**另**：产品设计文档审核那条流程的「我的流程」里
   有 42 条**非本轮**的历史遗留（24 审批中 / 16 已驳回），不是今晚产生的。
3. **Portal 仓库把测试环境 OSS 的 AK/SK 明文提交在 `build/env/.env.build.test`。**
   按你的答复**只记进早报、不动那个仓库**。影响面：任何能读那个仓库的人都能拿到那把 Key；
   建议改成构建期注入或走密钥管理（`Projects_Js` 不在本次范围内，等你处置）。
4. **覆盖率数字从今晚起彻底失真，请别读它。** 完成度判据只认「能力指向菜单路径」，
   而今晚做的**全是"不是页面的能力"**（基础能力、流程表单、横切能力）——**一个分子都不加**，
   所以它显示的还是 1.0%。这写在 `docs/plan.json` 的「队列之外」一节。

---

## **欠账（做了但没验完的）**

| 欠账 | 在哪 |
| --- | --- |
| **待办办理的写链路没端到端跑过**（缺第二个账号） | `docs/pages/待办办理.md` |
| 差旅费的「项目费用分支」只做了只读 preview、没真提交，且该预览**缺一个变量、不完整**（`prepare` 用 `previewComplete:false` 标出） | `docs/pages/差旅费报销.md` |
| 用车的 `applicantPicker` 翻页路径、PUT/DELETE（页面入口是注释掉的）没验 | `docs/pages/用车申请.md` |
| 上传的清理只能逐对象证明，**不能断言桶里整体无孤儿**（那把 AK 没有 ListBucket 权限，实测 403） | `docs/base/上传.md` §6.5 |
| `oss_resource` 里一行悬空元数据（id=3178，指向已删对象）。**我决定不动它**：唯一能删的是共享表上未验证的写接口，误删别人数据的风险大于收益 | `docs/nightly-log.md` 第 5 轮 |
| 出差的「后端重写那 7 个只读字段」只有源码证据，没做"发假值看后端会不会改"的对照实验 | `docs/pages/出差申请.md` |
| 调休的后端「调休时长不能大于剩余加班时长」门槛**一次都没被触发过**，报错文案来自源码非实测；两个剩余时长算法是否真分叉没造过交叉数据 | `docs/pages/调休申请.md` |
| 加班/产品的「真实审批人」没有独立证实（要读别人的数据，没碰） | 各自 `docs/pages/*.md` |

## **卡住的**

**没有卡死的页面。** 唯一「做不下去」的是待办办理的写链路，原因就是上面第 1 条（缺账号），
**产物都在仓库里、代码是完整的**，不是半成品。

---

## 今晚最值得你看的三条经验（都写进各自文档了）

1. **守卫在逐代加强。** 通用审批/请假那代是「不许把审批人选成自己」（靠约定）；加班那条流程
   **没有自选节点**，那代守卫根本拦不住，于是改成 **`process-instance/preview` 拿真实审批链、
   扫所有 USER_TASK 的候选人**，命中发起人本人在 `create` 之前抛错、一个写请求都不发。
   **推荐把后一种推广到所有流程线。**
2. **「测试太弱」的假绿反复出现，形态高度一致**：断言与实现**共享了同一个常量或同一段逻辑**
   （最典型：抄送人上限用 `toHaveLength(常量)` 自己断言自己）；或**输入太单一**
   （数组只有一个元素时 `[0]` 与 `[last]` 同值、提前 break 的分支走不到）。
   我要求每单都专门排查这类变异，**抓出来十几处**。
3. **调查文档是起点不是终点。** 今晚有两份调查的结论被实测推翻：一份把某个流程记成「审核类」
   （实测在「审批」里）、一份的最高优先级建议（跨网关前缀）**整体被证伪**。
   后者我专门派了一单去修，**它证明那条不成立、零行为改动**——这是对的，别为了有活干而修不存在的问题。

---

## 2026-09-21 00:55 ｜ 第 8、9 轮（收口：产品审核 + 用法文档 + 待办办理 + 加班）

用户点名的方向**到此全部做完**。这两轮四件事 + 一个**如实的否定结论**。

- **产品设计文档审核**（第 8 轮，8 个能力）：实测**纠正了调查的一处结论**——调查说它属「审核类」
  （`processType=1`），实测它在 `processType=2`（审批）里。**实际打扰的真人 = 0 个**
  （待办落在系统账号「管理员」，且第一个节点是「AI审核」，两个真人节点根本没被走到）。
- **流程表单线的用法文档**（第 8 轮）：`docs/usage.md` 新增 §11 四节 + 6 条可执行用例
  （**666 行纯新增、0 删除**，既有断言一字未改）。覆盖完整写链路、**防重的可观测行为**、
  长选项参数被本地拒、以及四条线的形态差异。
- **加班审批**（第 9 轮，8 个能力）：两轮真实提交/撤销跑通；实际打扰乔娜一人共 2 条待办、
  存活 3.30s/2.55s 后终结。★ **它把守卫做得比前几条线更强**（见下）。
- **待办办理**（第 9 轮，10 个能力）：**横切能力**，补上「SDK 只能发起、不能办理」这个缺口。

**进度**：能力 115 → **141 个**；全量 `pnpm test` **54 文件 / 1747 用例**（1726 通过 + 21 跳过）；
`pnpm run docs` 通过（openapi 校验过）。**页面覆盖率仍是 10/1019**——见下面第 2 条。

### ⚠️ 早报里必须看的两条

1. **待办办理这条线：单测试账号下做不了端到端验证。** 这不是没写代码，是**证明了一条路走不通**。
   该单用**三条独立证据**说明「发起人=审批人 ⇒ 后端自动秒批 ⇒ 撤不掉」且无逃生口：
   源码里 `tryAutoApproveWhenStartUserIsAssignee` 两条分支都走、跳过用的流程变量客户端注入不了；
   运行时翻「我的流程」47 个未取消实例，**22 条命中自动审核通过**。
   **唯一解是第二个测试账号。** 能力代码/契约/65 例测试/27 个变异全红都在，缺的只是那个账号；
   它**全程 0 写请求、0 单据、打扰 0 人**（用必然不存在的 taskId 探针，后端在校验第一步就拒），
   我独立复核了路由确实通（返回「流程任务不存在」）。
2. **覆盖率数字已经完全不能代表进度了。** 本轮做的全是「不是页面的能力」：
   基础能力（字典/权限/上传/外壳/租户/销售域）、流程表单（六条线）、横切能力（待办办理）。
   完成度判据只认「能力指向菜单路径」，这些**一个分子都不加**。**别把 1.0% 读成「什么都没做」。**
   这两条写在 `docs/plan.json` 的「队列之外」。

### 三条值得单独记的

1. **守卫被逐代加强，这是这轮最有意思的演化。** 通用审批/请假那条线是「不许把审批人选成自己」
   （靠约定）；加班这条线**没有自选节点**，那条守卫根本拦不住，于是它改成
   **`process-instance/preview` 拿真实审批链、扫所有 USER_TASK 的候选人**，命中发起人本人就在 `create`
   之前抛错、一个写请求都不发——顺带还能回答「这次会打扰谁」。**后一条明显更强**：
   它基于真实审批链判，不是基于猜测。
2. **「测试太弱」这条已经成了肌肉记忆，但仍在出现。** 本两轮抓到的：待办办理第一轮 **5 处等价变异**
   （最典型：抄送人上限用 `toHaveLength(常量)` **自己断言自己**）、加班 1 处假绿
   （校验类用例红在一致性检查而非被测那条）。**共同点是"断言与实现共享了同一个常量/同一段逻辑"**。
3. **一条被实测推翻的调查结论 + 一条被实测补充的形态差异**：产品的 processType、加班的
   `candidateStrategy=23` + `getRequiredStartUserSelectTasks` 返回 `[]` 且必须收完整载荷。
   **调查文档是起点不是终点**，这两轮又验证了一次。

---

## 2026-09-21 00:20 ｜ 第 7 轮（流程表单第三、四条 + 接上可见性那条断链）

**本轮四件事**：用车申请、差旅费支出申请表（≈报销）、目录可见性接线（独立提交 `2fc483b`）、
以及一次**接线被仓库自己的测试抓出缺陷**。

- **用车申请**（9 个能力）：表单 6 个控件全部表达。**两条实测改变设计的发现**：
  ① 页面自己的**数组版取人入口已被后端关掉**（超 1000 人直接 500，提示改用分页选择器），
  SDK 只接分页版并在本地拦下 `organizationIds>50` / `pageSize>100`；
  ② 页面挂载时那次请求**注定 400**（四组残缺字段全被 `@Valid` 拒），SDK 选择不信一个必然失败的请求。
- **差旅费支出申请表**（13 个能力）：形态最复杂的一条——明细行是**数组**，三级地区要按位置展开成
  省/市/区三个标量，金额总计前端算、不接受调用方给。**先做了前置核对**：实测 key 返回
  「差旅费支出申请表」/`finance_process`，**不是硬凑的相近流程**；「发票」确认全库不存在。
- **目录可见性接线**：`visibility.ts` 实现完整却一直没接进门面，缺的只是「谁来喂它菜单树」——
  基础能力 `base-menu-nav` 补上了前提。口径是**显式入口而非自动过滤**（理由见 design.md 补记）。

**我复核时自己重跑的**：`pnpm typecheck` 干净；全量 `pnpm test` **51 文件 / 1548 用例**
（1527 通过 + 21 跳过）；能力 93 → **115 个**。真实后端探针：用车流程名对、
申请人组织范围 10 个根、差旅费流程名 =「差旅费支出申请表」、两种入口一致；
**9 条 `SDK-TEST` 流程全部已撤销**。

### 三条值得单独记的

1. **仓库自己的测试抓到两个我或代理没看到的缺陷。** ① 用车的 `lookup` 指向了**没有对应参数的能力**
   （3 处）——代理修的时候还判定其中一处**本就不该有 lookup**（组织 id 是两步关系，
   指任何入口都是死胡同），删掉并写明理由；② 差旅费的 `dict-options` 声明了 **5 个同名参数**，
   被 redocly 的 `operation-parameters-unique` 拦下。**后者不只是规格洁癖**：调用方看到 5 个都叫
   `dictType` 的参数，根本不知道该传哪个。**这两个都是"能力定义写对了类型、写错了契约"**。
2. **「断言恒真」又出现了两次**，都是同一形态：**测试数据不够刁钻**。
   用车的 2 条没红（常量断言自己是同义反复 / 上限被另一处检查遮住）；差旅费的 1 条没红
   （草稿只有 1 个出差人，`[0]` 与 `[last]` 同值）。**共性是"变异改了行为、但用例的输入让它看不出来"**。
3. **一条锁着错误状态的断言被改正。** `test/eval-model.test.ts` 里那条用「报销差旅费」当**零命中**关键词
   的断言红了——因为我们**刚把差旅费实现了**，它现在**正确地**能搜到。这条测试验的是
   「摘要器读的是真实返回形状」，不是「某个词该不该搜得到」，所以换了个真正零命中的关键词。
   **顺带记一个坑**：`search` 是**模糊匹配**，含中文的词很容易撞上别的能力标题
   （我第一版换的词命中了「…会不会被连带**禁用**」那条），**用纯拉丁字母最稳**。

### 未做 / 不确定（如实）

- **测试环境里累计 3 条撤不掉的测试单据**（我查「我的流程」核实的）：通用审批 `businessKey=66`
  （status=2，被后端自动审核通过）。另有请假的 `8981`/`8983` 是**流程已撤销但请假记录行不随取消删除**
  （后端没有监听器，与页面行为一致）。**都不影响别人**。早报里会再写一次。
- 差旅费的「项目费用分支」只做了只读 preview、没真提交，且该预览**缺一个变量、不完整**
  （`prepare` 用 `previewComplete:false` 标出，那一支的自审拦截有盲区）。
- 用车的 `applicantPicker` 翻页路径、PUT/DELETE（页面入口是注释掉的）没验。

---

## 2026-09-20 23:56 ｜ 第 6 轮（流程表单第二条 + 基础能力第三批 + 清掉两条欠账）

**本轮四件事**：通用审批的「带附件提交」欠账补掉（独立提交 `2b9a358`）、请假申请做完整、
基础能力第三批（租户/销售域）、以及一次**被测试抓住的接线遗漏**。

- **请假申请**（`leave-application.ts`，10 个能力）：流程表单线第二条，端到端**含附件**真实跑通
  （prepare → submit → detail → cancel，附件签名 DELETE 204）。
  比通用审批多出**三条只读联动**（假别字典 / 年假余额 / 时长计算），它们在页面上就是只读展示。
  **实际打扰：只有乔娜一人各 1 条待办，1.81s / 1.59s，已随取消终结**；没发短信、没抄送。
- **基础能力第三批**（`base-tenant.ts` + `base-sale.ts`，9 个能力）：企业上下文 + 销售域。
  **敏感字段裁剪**是这一批的重点：身份证号、证件照 URL、手机号全部白名单裁掉才返回
  （我独立复核：企业详情 9 字段无 `identityNumber`、店铺 18 字段无 `shopuserIdentity`）。
- 进度：能力 74 → **93 个**；`pnpm next` 的**页面覆盖率仍是 10/1019**（基础能力与流程表单不是页面，
  不涨分子——见 `docs/plan.json` 的「队列之外」，**不要把覆盖率读成进度**）。

### ⚠️ 本轮最值得记的一条：**是测试抓住的，不是我**

我给第三批接线时，**9 条绑定根本没写进去**（那段替换的锚点没匹配上）。
**而 typecheck 是绿的**——因为缺绑定只表现为数据少了几条，`invoke` 的 host 字段我加了、类型对得上。
抓住它的是两条「能力 ↔ 绑定表 ↔ 门面一一对应」的测试。

我一开始误判成「vitest 模块缓存陈旧」（因为 `dist` 里数是 83、测试里是 74），
差点去清缓存了事。**写了个临时诊断测试打印三个数字才看清**：`ALL=83 / BINDINGS=74 / SDK=83`。
教训：**数字对不上时，先证明是哪一边少，别先怀疑工具**。诊断用完即删。

### 另外两条

1. **一个设计上的坑被类型系统逼出来了**：`buildLeavePayload(draft, profile, restDay)` 要三个参数，
   后两个得先打两次网络，而 `withIdempotency` 的 `payload` 构建器是**同步的、在写之前跑**——
   拿它当 payload 会类型报错。改成省略 payload（默认就是剥掉 `requestId` 后的草稿），
   **真正的校验在 `submit` 内部做**，行为不变。
2. **上一轮的教训生效了**：请假的变异脚本自带「锚点必须恰好出现 1 次」判据并打印被改的那一行，
   正是为了不重蹈「改中的是注释、于是反证假绿」。

### 未做 / 不确定（如实）

- **测试环境里有 3 条撤不掉的测试单据**（我自己查「我的流程」核实的）：
  通用审批 `businessKey=66`（status=2，被后端自动审核通过，`cancel` 报「流程不处于运行中」），
  以及请假的 `8981`/`8983`——后两条**流程已撤销（status=4）但请假记录行不随取消删除**
  （后端没有监听器，与页面行为一致）。**这三条都不影响别人**，早报里会再写一次。
- `oss_resource` 里有一行悬空元数据（id=3178，指向已删对象），页面无删除入口。**我决定不动它**
  （唯一能删的是共享表上未验证的写接口，误删别人数据的风险大于收益）。
- 请假的年休假(type=13)与 restDay>=3 两条分支只测了判定、没跑真实提交；50MB 上限本地拦不了。

---

## 2026-09-20 23:40 ｜ 第 5 轮（**结转清账**：上传真实验证 + 三条新工作线）

本轮四件事，全部落地。

1. **上传的真实链路验证补上了**（独立提交 `7f09189`）。用户授权后可取测试环境 OSS 凭据
   （新增 `smoke/with-oss-credentials.sh`，照 `with-portal-token.sh` 的模式：读字段 → 注入环境变量 → 跑，
   **只打印字段名不打印值**）。实测：PutObject 200 → **匿名 GET 读回逐字节一致** → 签名 DELETE 204。
   文档里原先标着「只是推断」的五条**全部被证实、零被证伪**，所以实现一行没改。
   反证有说服力：把 sha1 改成 sha256 后**真实 OSS 当场拒绝**（说明它不是"什么都收"，这条验证有效）。
   **我另外补了实现方补不了的一步**：`smoke/upload.mjs` import 的是 `dist/`，实现方被禁止 build，
   所以那个脚本一次都没被执行过——我用新脚本端到端跑了它，随后**匿名 curl 那个 url 拿到 403**，
   确认清理真的生效。
2. **应用外壳**（`base-shell.ts`，7 个能力）：用户信息 / 人员 / 待办与角标 / 可见菜单 / 工作台卡片。
   **最有价值的一条**：`base-menu-nav` **解锁了 `src/catalog/visibility.ts`**——那 433 行实现完整，
   但此前因为没人拿得到菜单树，一直没接进任何门面。这是全仓最强的「被依赖」证据。
3. **通用审批**（`general-approval.ts`，7 个能力）：**流程表单线的第一条，端到端做完整**——
   表单 5 类控件全部表达，submit → 独立证实 → cancel → 再证实 真实跑通。
4. **用法文档补上基础能力**（`docs/usage.md` §9/§10 + 7 条可执行用例；既有 8 节一字未改）。

**我复核时自己重跑的**：`pnpm typecheck` 干净；全量 `pnpm test` **45 文件 / 1200 用例**
（1179 通过 + 21 跳过）；四个代理均未越界改共享文件。派单方独立打真实后端的探针：
用户信息返回 18 字段且**无 `password2`/`salt`**（白名单生效）、菜单树/路径/待办角标都通、
`generalApproval.definition` 拿到「通用审批」、`invoke('base-todo-counts')` 两种入口同结果。

### ⚠️ 我独立核实并如实记下的一条（早报里也会再写一次）

**测试环境里留了一条撤不掉的测试单据。** 通用审批第 1 轮（`businessKey=66`）把审批人误选成
发起人本人，触发后端「发起人与审批人相同自动审核通过」规则，流程 1.22 秒走完，
于是 `cancel` 报「流程不处于运行中」。**我自己查了「我的流程」复核**：标题含 `SDK-TEST` 的共 2 条——
`67` 已撤销（status=4），`66` 撤不掉（status=2）。实现方如实申报了这件事，没有瞒。
这条已写成冒烟脚本的**提交前拒绝**（不许把审批人选成发起人本人），避免再犯。

### 三条值得单独记的

1. **「文件变了」不等于「改到了那一处」**。通用审批的反证里有一个变异没红，查下来是
   `perl` 命中的是**文件头注释**而不是代码——测试当然不红。收窄替换目标后才红。
   这说明「改坏一次看它红不红」这个动作本身也可能做假。
2. **假测试又出现了两次**，都是同一种形态：**遍历的集合已经被清空了**。
   上传那单的「清理证据」第一版遍历的是待清理集合（已被各用例清空），0ms 空转通过，改用
   `everCreated` 后 59ms 才是真在查。**这类假测试的共同点是"跑得特别快"**，值得当成一个嗅探信号。
3. **一个自我纠错的对照实验**：`homePage/get` 的 module-type 敏感性，第一版对照是「打两次比一次」——
   而基线自己在动，差点把错结论写进文档（属假阳性）。改成**交错四次**判据后仍无结论，如实记「未验证」。

### 未做 / 不确定

- 通用审批**带附件的真实提交没测**（当时上游 OSS 尚无合法凭据；现在凭据有了，这条可以在下一轮补）。
- 上传仍有一条验不了：这把 AK **没有 ListBucket 权限**（桶级列举实测 403），
  所以只能逐对象证明「我建的已删」，**不能**断言桶里整体无孤儿。
- 流程表单只做了通用审批一个。调查说「产品设计文档审核」与它高度同形，几乎可直接复制。

---

## 2026-09-20 23:35 ｜ 用户睡觉前的两条授权（**后面所有轮次都适用**）

用户说「我真的要睡觉了，不要再问我任何的东西」并明确授权：

1. **「我允许你去获取测试环境的任何信息」**——这比之前的授权宽：此前只有
   `smoke/with-portal-token.sh` 那一条（测试环境 Portal 会话 token）。
   现在**测试环境范围内的信息获取**都由我自行决定，不再逐次问。
   **边界没有变**：仍然**只限测试环境**，**生产环境依然绝对不碰**；
   凭据仍然只进进程环境变量、不落盘、不打印、不进任何产物或提交。
2. **「有问题你自己解决，不要中间不要停住」**——不再为决策停下来问，遇到问题自己定、自己纠错、记进日志。

另有一条**设计原则**（用户在这次对话里给的，后面都要遵守）：
> 「这个 SDK 像 OSS 那种参数都应该是 SDK 从外部接收来的，**不写死在 SDK 内部**；
> 测试的时候都用测试环境的那些配置就可以，**类似的东西都要做成这个 SDK 接收的参数**。」

已落地的一处：`PortalHeadlessConfig.oss`。**后面凡是环境相关 / 凭据相关 / 部署相关的配置，一律走「调用方传入」，
不写死在实现里。**

还有一条用户答复：Portal 仓库把测试环境 OSS 的 AK/SK **明文提交进版本库**这件事，
**只记进早报、不动那个仓库**（红线：别的仓库全程只读）。

---

## 2026-09-20 23:21 ｜ 第 4 轮（**转向**：基础能力线开工；含一次调查被证伪）

用户 23:00 前后两次调整取向（见下面那条「取向调整」）。本轮起不再按 `pnpm next` 的页面队列走，
改做**基础/公共能力**（最高优先级）。先派了两单**只读调查**，再据调查派了三单实现。

**本轮产出的三条工作线：**

1. **基础数据：部门 / 字典 / 权限清单**（`base-dept-dict-permission.ts`，9 个只读能力）——已完成、已接线。
   实测：部门 1551 条 / 73.7 KB；字典 **885 dictType / 5985 条 / 1.06 MB**；权限码 2159 条 / 87 KB。
   **设计要点**：因为体积**没有 `listAll`**，每个入口都有硬上限，必须按 `dictType` / 关键字取；
   挂上了会话基础数据（字典**不新增 key**，读既有的 `dict-hr`/`dict-platform`）。
2. **上传**（`base-upload.ts`，3 个能力）——已完成、已接线，**但真实上传未验证**（没有合法凭据）。
   签名版本实测确认是 **V1**（依据 `ali-oss/.../initOptions.js:50` 的 `authorizationV4: false`）。
   零新依赖，签名自己用 Node 内置 crypto 实现。
   ⚠️ 欠账写得很明确：`docs/base/上传.md` §6.3 有补验证步骤与「结果 → 该改哪里」的对照表。
3. **流程表单线的前置调查**（`docs/process-forms.md`）——82 个流程可枚举；点名的 5 个都在。

**我复核时自己重跑的**：`pnpm typecheck` 干净；全量 `pnpm test` **43 文件 / 1072 用例**
（1056 通过 + 16 跳过）；三个实现代理都未越界改文件。派单方独立复跑探针（打真实后端）：
部门搜「生产」5 条、字典 `assignment_type` 6 条、`hasPermission('/dashboard/base/image')`=**false**
（与文档里记的那条反例一致），上传未配凭据时抛 `OssCredentialError` 并列出缺哪几个字段。

### 值得单独记的四条

1. **一次调查结论被自己的复核推翻，而且产出了零行行为改动。** 第一版基础能力调查把
   「`http-instance.ts` 透传表漏了两个前缀、会让 5 条能力静默打错 URL」列为**最高优先级**。
   我派了一单专门去修，结果它**证伪了这条**：判据不是「同 host 白名单」，而是「页面真的会把这段前缀
   当**路径字面量**写」——`/admin-shop-api` 等三个前缀在 6397 个前端文件里出现 **0 次**；而今天
   **没有任何一条能力**会打到那些网关（所谓「5 条」是调查自己提的建能力提案）。
   它没有硬改，只补了注释、16 条测试与 `src/context/README.md` §5，并在调查文档顶部加了勘误。
   **这是"不要为了有活干而修一个不存在的问题"的正面样本。**
2. **上传能力的凭据时机被我改过一次，这是有意的。** 实现方原设计是**构造函数里**校验凭据，
   那会让**每一个没配 OSS 的现有调用方在 `createPortalHeadless()` 当场抛错**、连 SDK 都建不起来。
   我决定改成**懒校验**（首次调用才失败关闭），并要求把旧断言改成**正面锁住新行为**的 6 条
   （含「未配凭据时三个方法各抛且**发出请求数为 0**」）。**这是改断言，不是放松断言**——
   旧断言锁的是被我明确废弃的时机。实现方还自己发现有一条变异是**等价变异**（改坏了不红），补断言钉死了。
3. **`pnpm next` 的覆盖率数字从此会失真，这是口径问题不是 bug。** 完成度判据是
   「能力指向菜单路径」，而基础能力与流程表单**都不是页面**。所以这两条线做得再多，
   覆盖率也**不会涨**。已写进 `docs/plan.json` 的「队列之外」，并且在那里说清了
   **为什么不能靠加一条队列规则来表达它们**（队列按 page-catalog 的页面形态匹配，永远匹配不到）。
4. **`docs/plan.json` 现在有了「队列之外」一节**，含：新优先级、两条线各自的权威文档位置、
   为什么 `pnpm next` 指不到它们、以及**用户对流程表单写操作边界的明确授权**
   （「允许在测试环境提交，接受打扰真人」）。

### 未做 / 不确定

- **上传的真实链路一次都没跑过**（无合法凭据）。这是本轮最大的欠账，已单独成节。
- 基础数据的 `getRoleOrganizationTree`(501KB) 没重测（是引用的旧数据）；超管/租户管理员路径没测。
- 流程表单**只做了调查，一行代码还没写**——下一轮起做，从 `hr_general_approval`（通用审批）开始。

---

## 2026-09-20 23:05 ｜ 第 3 轮（3 页；**这是按「页面队列」做的最后一轮**）

**进度：`pnpm next` 7/1019 → 10/1019。**

- 完成（3 页，三个域、文件集不相交）：
  - `/dashboard/attendance/attendance-team/list` 排班管理（7 个能力）——**一个页面两条数据线的首个样本**：
    考勤组本体 + 它的排班，共用同一个页面上下文
  - `/dashboard/base/management-center/list` 组织结构（7 个能力）
  - `/dashboard/contract/template/list` 合同模板（5 个能力）
- **我复核时自己重跑的**：三个测试文件 40 / 36 / 47 用例全绿；全量 `pnpm test` **40 文件 / 948 用例**
  （935 通过 + 13 跳过）；`pnpm typecheck` 干净；三个代理**都没有越界改既有文件**。
- **我独立抽查的真实环境结论**（`smoke/with-portal-token.sh` + 我写的探针）：
  考勤组总数 = 3、组织结构 = 18（与代理报的「跑之前一致」对得上）；
  四个页面按 `SDK-TEST` 前缀查**全部为 0**（考勤组 / 组织结构 / 合同模板 / 图库）——没有留下垃圾；
  三个新能力的 `capabilities.invoke(...)` 都走通了，**我这次的接线在真实后端可用**。
- 接线：`src/capabilities/index.ts`、`invoke.ts`（19 条绑定）、`src/index.ts`、`src/server.ts`，
  外加重跑 `derive-aliases.mjs`。

### 这一轮值得单独记的三条（都是子代理诚实报上来的）

1. **「全绿 ≠ 接线对」有了第二个独立样本**：合同模板的反证里 16 组变异有 2 组是**等价变异**，
   改坏了测试却全绿（把 `get` 的 id 归一成字符串，query 上归一与不归一等价）。已补一条锁 body 的用例。
2. **组织结构那条「为了不制造坏数据而收紧契约」**：`create` 强制 `commander` 必填，因为后端
   `pageInfo` 对它直接 `.toString()`，为 null 会 NPE，而列表**跨创建人** ⇒ 一条坏数据让**所有人**
   的列表页 500。这是第一次出现「不收紧契约就会伤到别人」的判断。
3. **测试环境的槽位是有限资源**：组织结构的名称上限 10 字 + `SDK-TEST-` 前缀 9 字 ⇒ 只剩 9 个名字槽位，
   而 `create` 的重名检查不过滤 `is_del`（删掉的名字永久占用）。跑过的轮次会永久占掉几个
   （`SDK-TEST-2/3/4` 已被占，`SDK-TEST-1` 可复用）。冒烟脚本已按「每轮只消耗 1 个槽位」设计。
4. **一条安全发现**：排班管理那一页的 `getUserByType` 实测响应带 `password` / `password2` / `salt`，
   据此把它排除在契约之外。

### 未做 / 不确定

- 三页各自的「未做」写在各自的 `docs/pages/*.md` 的「尚未覆盖」里。共同的一条：
  **凡是要「造出会伤到别人/真人的数据」才能触发的错误路径，一律只读源码、不实测**，
  已在文档里标明「只有源码依据」。
- **用户在本轮进行中调整了选页取向**（见下一条），所以这是按「页面队列」做的最后一轮。

---

## 2026-09-20 23:00 ｜ 用户中途调整取向（这一条改变了后面的排期）

用户当晚 23:00 明确要求改选页取向：

> 优先做**人力**域、**面向普通用户**的常用能力 —— 创建通用审批、创建请假、会议室预定、
> 用车申请、报销、发票、产品文档审核这一类的**流程表单**；大众化、日常用得多的先做。
> 实现时**不要只实现一个清单**：页面上用户能操作到的功能都要实现，**每个功能要做完整、从头到尾**。
> 节奏上仍是一页一页做。

**由此产生的结构性约束（重要，早上的你也需要知道）**：
`docs/plan.json` 的队列规则是按 `generated/page-catalog.json` 里的**页面形态**匹配的，
而 CLAUDE.md 已记明：**流程表单类（`/simple/hr/form/033` 这种）不在 page-catalog 里**，
它们走「发起流程」到达、由那个页面的能力动态列出（设计 D9），**`pnpm next` 选不到它们**。
已经做完的「会议室预定」正是这一类（靠 `meetingApplication.definition(processKey)` 拿表单结构）。
所以要落地这个取向，**不能只改队列排序**，得先搞清楚「怎么枚举流程、每个流程的完整契约与端到端动作」。
**我先派了一单调查去回答这件事，拿到结论后再改 `docs/plan.json`**——写在别处等于没写。

### 同一次取向调整里还有一条**优先级更高**的（用户紧接着补充的）

> **基础 / 公共能力优先级最高**：字典维护请求、菜单维护、权限列表（`Not by Permission` 那个接口）、
> 用户信息（就是 Portal 刷新后会调的那一批接口）、公共字典、人员组织，
> 以及**图片 / 文件上传**（提交流程表单必然要用到它）。
> 「它在 Portal 中是一个组件，那么它在这个 SDK 中就应该是一个基础能力。」

**最终优先级**（后面的轮次按这个来）：

1. **基础 / 公共能力**（最高）—— 字典、菜单、权限列表、用户信息、人员组织、文件与图片上传
2. **流程表单**（大众化、面向普通用户）—— 请假 / 用车 / 报销 / 发票 / 通用审批 / 产品文档审核 / 会议室预定（已做）
3. 其余大众化常用能力

**第二条结构性约束（与流程表单那条同病）**：这些基础能力**不是页面**，`pnpm next` 是按
`capabilities[].pagePath === 菜单路径` 推导完成度的，**基础能力不在它的视野里**。
所以「按队列选下一页」这套机制，对用户今晚真正想要的这两类东西**都覆盖不到**。
现有的地基：`src/session/` 已有基础数据缓存（`createPortalBaseDataRegistry`），
已有 `searchUsers` / `searchOrganizations`（长选项参数的关键字约束见 conventions 第 11 条）。
**待回答**：Portal 首屏刷新到底打了哪些接口、哪些已实现、上传链路（签名 → 直传 OSS → 回填 url）能不能在 SDK 里做。

---

## 2026-09-20 22:51 ｜ 第 2 轮（本轮 = 3 页 + 3 个工具链真问题）

**进度：`pnpm next` 4/1019 → 7/1019。**

- 完成（3 页，一次派 3 个代理，分属 attendance / backlog / base 三个域，文件集不相交）：
  - `/dashboard/attendance/attendance-shift/list` 班次管理（能力 `attendance-shift-*`，5 个）
  - `/dashboard/backlog/task-examine/list` 待办事项（能力 `backlog-task-examine-list`，1 个）
  - `/dashboard/base/image/list` 图库管理（能力 `base-image-*`，7 个）
- 提交：见本条目下方的提交哈希
- **我复核时自己重跑的，不是看代理转述**：三个测试文件分别 41 / 33 / 36 用例全绿；
  全量 `pnpm test` 37 文件 / 825 用例（815 通过 + 10 真机跳过）；`pnpm typecheck` 干净。
  `git status` 三个代理**都没有越界改任何既有文件**（16 个新文件全在各自的白名单里）。
- **我独立复现的三条关键结论**（用 `smoke/with-portal-token.sh` 打真实后端）：
  班次总数 = 3（与代理 1 的「环境仍是原有 3 条」一致，无残留）；图库 `SDK-TEST` 残留 = **0**
  （代理 3 的清理被证实）；待办 `finished=1` = 25 条；另外我还跑了
  `capabilities.invoke('attendance-shift-list')` —— **我这次接的线在真实后端上是通的**。
  待办那条「processCategory 值不合法则后端必 500」我也重跑冒烟复现了（500 空指针，消息中心深链同病）。
- 接线（子代理被禁止碰，由我做）：`src/capabilities/index.ts`、`invoke.ts`（13 条绑定）、
  `src/index.ts`、`src/server.ts`，外加重跑 `derive-aliases.mjs`。

### 本轮顺带挖出并修掉的三个工具链真问题（都不是我改页面引入的）

1. **`pnpm docs` 是哑弹。** pnpm 把它当成内建的 `docs`（打开包文档）命令：零输出、exit 0，
   **整条重建链一次都不跑**。而 `.githooks/pre-commit:71` 用的就是它 —— 所以那句
   「✓ 生成物已重建」此前一直是假的。已改成 `pnpm run docs`（顺带把文件里另外三处同名引用一并改了）。
   **这条对以后所有人都有效**：钩子从现在起才真的会重建。
2. **openapi 生成器的枚举类型写错了**（50 条 redocly lint 错误，`no-enum-type-mismatch`）：
   `tools/generate/openapi.mjs` 对 `kind: 'enum'` 一律写 `type: 'string'`，而枚举值可能是数字
   （组织类型 1/2、发布状态 0/1）。改成**从取值推类型**，值类型不齐时不钉 `type`。现在 29 个能力
   validate 通过。**50 条里 36 条是 assignment 时代就埋下的**——因为 `pnpm docs` 空转，这个门禁一直没真正跑过。
   注意：这一条只有在 1 修好之后才看得见。修好 1 会让重建链真的跑，**下一次提交可能因此变红**，所以我顺手把 2 也修了。
3. **conventions 第 23 条又被踩了一次，换了个形式**：`batch-capabilities.mjs` 把 `page-catalog.json`
   的 `generatedAt` **时间戳**写进产物当新鲜度判据。后果：任何人重跑一次 `pnpm generate`
   （内容一字未变、只是时间戳刷新）都会让 `test/batch-capabilities.test.ts` 变红。
   1 修好之前它不会被引爆（生成物根本不动），修好之后第一次真重建就炸了 —— 本轮就炸在我脸上。
   已改成对**除 generatedAt 外**的目录内容取哈希（`catalogContentHash`）。
   **那条锁着缺陷的断言我改了，说明理由**：旧断言要求时间戳**在**产物里（锁的是错误状态），
   新断言反过来——产物**不得**包含目录的 generatedAt，且两个内容哈希都要在。
   **我做了反证**：把时间戳塞回去、重跑生成器，新断言当场变红，再还原。

### 未做 / 不确定（如实记）

- 三个页面各自的「未做」在原代理报告里，已写进各自的 `docs/pages/*.md` 的「尚未覆盖」一节。
  最要紧的两条：班次管理「被考勤组占用时删不掉」这条错误路径**没触发**（要动考勤组配置，越界）；
  图库管理的**上传**这一步 SDK 不覆盖（url 由调用方给），没有测。
- `pnpm docs:check` 我打算在提交后跑（提交前跑必然报漂移，这是它的设计）。
- **本轮的提交把「3 个页面」与「3 个工具链修复」放在了一起**，这是有意的：生成物是两者共同的产物，
  拆开会让中间那个提交的树与生成物不一致（openapi.json 里会带上还没提交的能力定义）。
  早上的你如果偏好拆开，可以 `git reset --soft` 后重分。

---

# 早报（2026-09-20 22:32 ｜ **提前收工**）

**这次没有跑满一夜。** 22:30 左右用户要求「在下一个安全的位置停止」，理由是**这个对话的启动目录不对**
（会话开在 Synapse 仓库里，不是 portal-headless）。所以停在了第一个安全点上，不是队列空了。

## 结论

- **新增完成页面：1 个**（`pnpm next` 从 **3/1019 涨到 4/1019**）
  - `/dashboard/attendance/attendance-sheet/list` 考勤统计
- **提交：2 个**（`838ef29`、`4c6fe1f`）
- 工作区**干净**：`git status --porcelain` 只剩本文件在写的时候
- 收尾三项全绿：`pnpm test` **34 文件 / 715 用例全过**、`pnpm typecheck` 干净、
  `pnpm docs:check` 报「3 项生成物与 HEAD 一致」

## **需要你决定的**

1. **「接线」到底算不算逐页工作的一部分？（我一开始判断错了，已改正）**
   我开工时的判断是：一个页面按 CLAUDE.md 的六步走，接线是 `docs/roadmap.json` 里单列的另一件事。
   **这是错的。** 实测：只要手工加一个能力文件，`test/attendance-archive-sheet.test.ts` 与
   `test/invoke-capabilities.test.ts` 就会有 2 条断言红（它们强制「能力 ↔ 绑定表 ↔ 门面」一一对应），
   另外 `test/aliases-derived.test.ts` 会因为派生别名生成物过期而红。
   所以**手工加页面必须同时改四处 + 重跑别名生成器**，我已经这么做了。
   我把这条结论写进了 `docs/roadmap.json`（新加了一个环节），你早上可以判断这个措辞对不对。
   **对后面的排期有影响**：一页的成本比「只写能力文件」高，多四个接线点。

2. **`pnpm next` 读的是 `dist/`，不是 `src/`——而 pre-commit 钩子不重建 `dist/`。**
   考勤统计提交完，`pnpm next` **仍然**把它列为「下一批要做」，进度也停在 3/1019。
   我查了 `tools/generate/next.mjs:34,45,54`：它 `import(dist/index.js)` 后数 `pagePath`。
   当时 `dist/` 是 21:28 的陈旧产物，手动跑一次 `pnpm build` 之后立刻变成 4/1019、页面也从队列里消失。
   **而钩子明明打印了「✓ 生成物已重建」**——它重建的是 `generated/`，不是 `dist/`。
   **风险**：以后任何人提交完一页，看到的都是「还没做完」，会重复劳动。
   建议在钩子里补 `pnpm build`，或者让 `next.mjs` 走源码。

3. **`page-catalog` 的写判据漏了「用 GET 改数据」。** 考勤统计页行尾的「归档」是
   `GET /org/hrAttendanceSheet/archiveSheet`（与考勤档案页的「取消归档」反向同形），
   删除走通用 `DELETE`。生成器的判据只认源码里的 `http.post|put|delete(`（`tools/generate/generate.mjs:229`），
   两条都扫不到，所以这一页被标成 `write: false`。
   这和考勤档案那一轮记的「漏 GET 写」是同一个洞，**现在有了第二个独立样本**。

## **欠账（派了但没做完的）**

第 1 批派了 3 页，只有 1 页跑完。另外 2 页的产出**已移到 `/tmp/portal-nightly-stuck/`**，
不在工作区里（为了让它保持干净、可提交）：

| 页面 | 状态 | 产物在哪 |
|---|---|---|
| `/dashboard/backlog/task-examine/list` 待办事项 | 能力文件、测试、基准、冒烟脚本都写了，**四件套文档没写**，我也没有复核过 | `/tmp/portal-nightly-stuck/待办事项/` |
| `/dashboard/base/image/list` 图库管理 | **只有一份浏览器基准**，能力文件刚开头 | `/tmp/portal-nightly-stuck/图库管理/` |
| —— | 某个代理留下的编译产物（`.tmp-build-bte/`），不是 gitignore 覆盖的目录 | `/tmp/portal-nightly-stuck/构建垃圾/` |

这两页**建议直接重做**（都不到一个完整工作单位），不要接着补——补的话得先自己把上一半读完再验。

## **卡住的**

没有卡死的页面。**停止是用户要求的**，不是失败。

---

## 2026-09-20 22:32 ｜ 第 1 轮

- 派单：`pnpm next` 给的头 5 个里挑了 3 个，**故意避开同域**——
  头的 5 个里第 1、2、3 个（考勤统计 / 班次管理 / 排班管理）都是 attendance 域，
  所以只取了第 1 个，另配 backlog 的「待办事项」与 base 的「图库管理」（后者含写操作）。
- 完成：`/dashboard/attendance/attendance-sheet/list`（能力 `attendance-statistics-list`）
- 提交：**`4c6fe1f`**
- 复核（我自己做的，不只看它的转述）：
  - `git status --porcelain` —— 它只动了分配给它自己的 5 个文件，无越界
  - 单跑它的测试：19/19 绿；`pagePath` 常量确为菜单路径原文
  - **它主动报了一条对我不利的发现，我独立复现了**：加能力文件会让 `aliases-derived` 红。
    我单跑该文件确认红，且失败点正是它说的那条（生成物过期）。
  - 全量 `pnpm test`：34 文件 / 715 用例全绿；`pnpm typecheck` 干净
- **我替它补做的接线**（子代理被禁止碰共享文件，所以留给我）：`src/capabilities/index.ts`、
  `src/index.ts`、`src/server.ts`、`src/capabilities/invoke.ts`，加 `derive-aliases` 重跑。
- 未做/不确定：这一页的两个写操作（归档 / 删除）**没有执行过**，因此没进能力定义；
  `isArchived=0` 的后端语义没实测过（所以「未归档考勤表」目前没有获取路径）。

---

## 2026-09-20 22:17 ｜ 第 0 轮（开工盘点）

**开工时工作区不干净**，有 4 个未提交改动：

- `tools/generate/batch-capabilities.mjs`、`src/capabilities/generated/batch-capabilities.ts`、
  `src/capabilities/generated/batch-report.json`、`test/batch-param-kind.test.ts`

判定为**上一个任务已完成但没提交**，依据：`pnpm test` 33 文件 / 694 用例全绿、`pnpm typecheck` 干净、
生成器连跑两次内容哈希一致（即生成物与生成器同步）。已按显式文件清单提交：**`838ef29`**。

> 提交时我重新跑了一遍生成器：生成物比工作区里上次生成的多出 2 行——说明上一轮改了生成器之后
> 没重跑生成器。现在是同步的。
