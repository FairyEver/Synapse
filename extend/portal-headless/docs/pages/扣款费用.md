# 扣款费用（`/dashboard/salary/person-tax/list`）

## 页面四件套

| 项目 | 记录 |
| --- | --- |
| 菜单 | `人力 → 个税专项扣款 → 扣款费用` |
| 页面权限 | `/dashboard/salary/person-tax` |
| 模块类型 | `14`（薪酬管理） |
| Portal 页面 | `app/portal/views/dashboard/hr/salary/person-tax/list.vue`、`[mode]/[id].vue` |
| 能力文件 | `src/capabilities/salary-person-tax.ts` |
| AI 说明 | `src/catalog/contracts-salary-person-tax.ts`，通过 `catalog.describe(id)` 和 `-llm` 返回 |
| 回归测试 | `test/salary-person-tax.test.ts` |

结论来自 Portal 菜单、列表、详情表单、人员选择器、导入组件、Java `SalaryPersonTaxController`、DTO、Service、Mapper、资格 DTO 和 Excel 类的逐字段对照。尚未执行真实测试环境浏览器读、保存、删除、导入、模板下载、导出和归档 smoke；下面明确区分已锁定的请求事实与未实测项。

## 页面可达能力

| Portal 动作 | SDK 能力 / 方法 | 请求 |
| --- | --- | --- |
| 筛选、分页和合计 | `salary-person-tax-list` / `.list` | `GET /salary/salarypersontax/pageNew` |
| 读取详情 | `salary-person-tax-get` / `.get` | `GET /salary/salarypersontax/{id}` |
| 修改前准备、修改 | `salary-person-tax-prepare-update` / `.prepareUpdate`、`salary-person-tax-update` / `.update` | 本地校验后 `PUT /salary/salarypersontax` |
| 删除前准备、批量删除 | `salary-person-tax-prepare-remove` / `.prepareRemove`、`salary-person-tax-remove` / `.remove` | 本地确认后 `DELETE /salary/salarypersontax` |
| 导入前准备、导入 | `salary-person-tax-prepare-import` / `.prepareImport`、`salary-person-tax-import` / `.importExcel` | xlsx multipart `POST /salary/salarypersontax/import` |
| 模板下载 | `salary-person-tax-download-template` / `.downloadTemplate` | `GET /salary/salarypersontax/download` |
| 选中记录导出 | `salary-person-tax-export` / `.export` | `GET /salary/salarypersontax/export` |
| 归档前准备、归档 | `salary-person-tax-prepare-archive` / `.prepareArchive`、`salary-person-tax-archive` / `.archive` | 本地确认后 `POST /salary/salarypersontax/updateArchive`，固定 `status=2` |
| 人员资格检查 | `salary-person-tax-eligibility-check` / `.eligibilityCheck` | `POST /salary/staff-eligibility/check` |

列表没有“新建”按钮；因此 SDK 不把 Controller 的 POST 保存分支注册为本页面的通用新建能力，POST 只用于 Portal 的导入流程。编辑页面的 PUT 分支和列表可达的保存动作仍完整覆盖。

## 列表、合计与筛选规则

Portal 请求默认参数为：

```text
GET /salary/salarypersontax/pageNew
name=&staffCode=&idCard=&archiveStatus=1&orgId=
pageNo=1&pageSize=20&costDateStart=&costDateEnd=&order=desc&orderField=id
```

Portal 后端响应不是普通 `{ list, total }`，而是：

```text
{
  total: { childEducation, housingRent, housingLoanInterest,
           elderlyCare, continuingEducation, infantCare, sickChildEducation },
  list: { list: object[], total: number }
}
```

SDK 对外返回 `{ summary, list, total }`，其中 `summary` 是7类金额合计，`total` 是分页记录数；不能把合计对象误当成分页总数。列表每行保留员工、组织、岗位、归属月份、归档状态、操作人、创建时间和 `specialList`。Portal 的七个金额列从专项类型 `1` 至 `7` 映射，不是后端独立字段。

`archiveStatus` 默认 `1=未归档`，`2=已归档`；`orgId` 是角色组织树节点 ID，服务端再展开下级组织并叠加当前用户数据权限。`costDate` 月份范围按 Portal 的 `convertFetchForm` 转成 `costDateStart`、`costDateEnd` 的 `YYYY-MM` 字符串；未选择月份发送两个空字符串。排序固定为 `order=desc`、`orderField=id`，不接受调用方改写成其他排序。

## 表单、导入和归档规则

详情加载时，Portal 将后端数值 `staffCode` 转为字符串，并把每个专项的 `costStart`、`costEnd` 投影为 `costTime`；两端不完整时回填空数组。提交时保留根字段和专项归档状态，但移除表单专用 `costTime`：

```text
costTime = [任意日期A, 任意日期B]
→ costStart = A所在月第一天
→ costEnd   = B所在月最后一天

costTime = [] 或两端不完整
→ costStart = null, costEnd = null
```

表单固定七个且不重复的专项类型，金额按 Portal `a-input-number` 规则限制为非负且最多两位小数。保存、删除和归档都必须先准备草稿、用户确认、提交，再重新查询；成功空回执不能替代回查。已归档行在列表中不可勾选归档或删除，归档请求固定为 `{ ids, status: 2 }`。

导入文件字段固定为 `file`，MIME 必须是标准 xlsx 类型；导入和归档都返回员工资格回执。`processedCount` 是实际处理数量，不是请求数量；`excludedStaffList` 的原因必须保留并展示，不能把请求成功描述成全部保存成功。模板名固定为 `个税专项扣款归档导入模板`，导出使用 `selectState.join(',')` 生成 `idList`；空选中状态保持 Portal 的空字符串语义。

## 权限与边界

- 所有请求使用 `platform` axios 实例，并由 SDK 会话注入 `module-type=14`；权限码固定为 `/dashboard/salary/person-tax`。
- Portal 人员选择器会无关键字分页拉取人员，再按当天业务日期分批调用资格检查；SDK 不复制无界全量候选。调用方应先用已有员工搜索能力取得并核实 `staffCode`，再调用表单或显式的 `eligibilityCheck`。
- `id`、`staffCode` 和导出 ID 必须来自当前用户可见且已核实的记录；服务端数据权限仍是最终边界，SDK 不扩大范围。
- `idCard` 是敏感字段，按原值消费并避免写入日志。

## 验证状态

- 已完成：Portal 菜单、列表嵌套分页/合计、详情字段、七类专项映射、月份筛选、月份首末日转换、保存/删除/导入/模板/导出/归档/资格请求、Java Controller/DTO/Service/Mapper/Excel 与 SDK 能力/AI 说明/离线测试逐项对照。
- 已完成：非法月份、状态、分页、工号、详情 ID、专项数量/重复类型、文件类型、重复删除/归档/导出 ID、空文件和坏回执的负向断言。
- 未完成：真实测试环境的浏览器列表、详情 `prepare → submit → 回查`、删除、导入、模板下载、导出和归档闭环；未将其表述为已实测。
