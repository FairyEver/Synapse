# 成本中心汇总（`/dashboard/manage/cost-center/list`）

## 页面范围与证据

| 项 | 结论 |
| --- | --- |
| 页面权限 | `/dashboard/manage/cost-center` |
| HTTP 实例 | `platform` |
| module-type | `14` |
| Portal 静态锚点 | `CodeReview_Projects_Js@test/portal/main:d3cf56bdc7` |
| Java 静态锚点 | `CodeReview_Mall_Platform_Java@test/test:dcb3f360194` |
| 能力文件 | `src/capabilities/manage-cost-center.ts` |
| 回归测试 | `test/manage-cost-center.test.ts` |

固定检出均未执行 `pull --ff-only`；本页结论来自源码、Java Controller/DTO 和离线测试。

## 能力定义

| 页面动作 | 能力 / 方法 | 请求或行为 |
| --- | --- | --- |
| 筛选、分页 | `manage-cost-center-list` / `.list` | `GET /salary/costcenter/page` |
| 组织树 | `manage-cost-center-organization-tree` / `.organizationTree` | `GET /org/organization/getRoleOrganizationTree` |
| 导出 | `manage-cost-center-export` / `.export` | `GET /salary/costcenter/export` |
| 进入维护子页 | `manage-cost-center-maintenance` / `.maintenance` | 返回Portal相对路由 `./maintenance/list`，不发请求 |

当前列表页没有写接口；维护子页不在本页能力范围内。

## 参数契约与逐字段基准

列表默认发送 `order=''`、`orderField=''`、`name=''`、`insuranceCost=''`、`fundCost=''`、`salaryCost=''`、`organization=''` 和 `pageNo=1&pageSize=20`。组织字段是ID，不是名称。

导出沿用页面通用 `actionExport`，只发送五个表单字段，不发送 `order/orderField/pageNo/pageSize`；文件名优先取响应 `Content-Disposition`，缺失时回退为 `成本中心.xlsx`。

列表行字段为 `name`、`staffCode`、`idCard`、`insuranceCost`、`fundCost`、`salaryCost`、`fullPath`。三个成本中心字段是页面显示文本，SDK不从文本推断成本中心ID。

## 操作步骤、失败处理与验证边界

1. 需要组织筛选时先读取角色组织树，再提交节点ID。
2. 按 `total` 分页；导出前沿用相同表单筛选。
3. 用户明确要求维护时调用本地路由动作，由支持Portal路由的调用方进入子页；不能把汇总列表当维护表单。
4. 非法组织ID、分页、树、分页响应或空文件在本地失败；权限、网络和后端业务错误原样抛出。

已完成页面源码、Java字段、分页/导出参数和路由语义的离线核对；本页没有写接口，因此不存在本页的 `prepare → submit → cancel` 写闭环；维护子页尚未扩展。
