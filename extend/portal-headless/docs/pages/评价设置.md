# 评价设置（`/dashboard/base/teacher-appraise-setting/list`）

> 这一页的**共享机制**（参数顺序怎么来的、空值发不发、时间区间怎么算）写在
> [学习管理域](./学习管理域.md) 里，这里只记这一页自己的东西。

| 项 | 值 |
| --- | --- |
| 菜单路径 | `/dashboard/base/teacher-appraise-setting/list` |
| 权限码 | `/dashboard/base/teacher-appraise-setting` |
| 路由文件 | `app/portal/views/dashboard/education/base/teacher-appraise-setting/list.vue` |
| 接口 | `GET /admin-api/study/base/studyappraiseteacher/list`（无参数） |
| module-type | 12 学习管理 |
| 能力 id | `study-appraise-setting-list` |
| 基准 | `baseline/study-teacher.browser.json` |

## 这一页不是列表页

`page-catalog` 把它归成「其他/自定义页面」：挂载时就是一句

```js
const { state } = useAsyncState(() => http.get('/study/base/studyappraiseteacher/list'), [])
```

拿到全部配置后交给一个表单组件渲染。**没有任何参数**（所以能力定义里的 `params` 是**空表**，
那是有意的，不是漏写），也没有分页。

## 真实验证（2026-09-21）

- `listAppraiseSettings()` → 3 条；URL 是 `/admin-api/study/base/studyappraiseteacher/list?_t=…`，
  与基准逐字段一致（`_t` 是 platform 拦截器给 GET 加的，无参也加）。

## 尚未覆盖

- 那一页的**写入口**（表单里的保存）没做。保存走哪个接口没查。

<!-- ai-contract-business:start -->
## SDK AI 说明契约（2026-09-22）

本节由当前 SDK 描述结果整理；说明权威来源为 `src/catalog/contracts-business.ts`。调用 `catalog.describe(id)`（或 `id-llm`）可得到参数来源、返回字段/状态、数据消费与后续映射；`catalog.describePage(页面路径)` 用于选择页面功能。以下更新以本节为准；前文历史请求/实测记录仍保留其记录日期。

### `study-appraise-setting-list`

读取讲师评价问卷题目设置；没有分页，也不是评价成绩。

- 执行：`sdk.studyTeacher.listAppraiseSettings`；通过 `capabilities.invoke('study-appraise-setting-list', args)` 调用时按 `params` 填参。性质：read。
- 参数：无参数。完整来源、枚举、条件必填与默认值由 SDK 同次返回。
- 返回：`object[]`。[] 表示本次查询没有条目；这是数组，不读取 list/total。
- 字段：`[].id`、`[].sort`、`[].content`、`[].isDel`、`[].createTime`、`[].updateTime`。嵌套字段、可空性及业务含义见同次返回的 `returns.fields`。
- 消费：按 sort 排序展示题目 content；本能力不提交评价，不修改问卷。
- 完成条件：交付题目清单与排序即完成。
- 失败/不确定：权限或会话错误要交由用户恢复权限/登录后再读；不要通过换租户、换页面上下文绕过。业务校验失败按原错误修正参数，网络失败的只读查询可重试。


验证：`test/ai-contract-business.test.ts` 直接检查 SDK 描述输出及业务夹具；本批未重放真实写操作。逐能力状态、证据与反证见 [补齐清单](../ai-contract-business-evidence.md)。
<!-- ai-contract-business:end -->
