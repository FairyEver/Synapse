# 讲师统计（`/dashboard/statistics/teacher/list`）

> 这一页的**共享机制**（参数顺序怎么来的、空值发不发、时间区间怎么算）写在
> [学习管理域](./学习管理域.md) 里，这里只记这一页自己的东西。

| 项 | 值 |
| --- | --- |
| 菜单路径 | `/dashboard/statistics/teacher/list` |
| 权限码 | `/dashboard/statistics/teacher` |
| 路由文件 | `app/portal/views/dashboard/education/statistics/teacher/list.vue` |
| 接口 | `GET /admin-api/study/statistics/teacherList` |
| module-type | 12 学习管理 |
| 能力 id | `study-statistics-teacher-list` |
| 基准 | `baseline/study-statistics.browser.json` |

## 时间字段是 `startTime` / `endTime`

⚠️ 与**学习管理**页的 `startStudyTime`/`endStudyTime`、**班课统计**的
`startTimeCondition`/`endTimeCondition` 都不同名 —— 同域三页三个样，别串。

参数顺序：`order → orderField → staffCode → name → startTime → endTime → pageNo → pageSize → _t`，
结束端是 +1 天的开区间（`buildStudyStatisticsTeacherTimeRange`）。

实测 URL：`?order=&orderField=&staffCode=&name=&startTime=&endTime=&pageNo=1&pageSize=20&_t=…`

## 真实验证（2026-09-21）

- `listTeachers()` → **111 条**，URL 与基准逐字段一致（含键顺序）。

## 尚未覆盖

只读页。页面列里有「讲授课程数量 / 讲师班课数量 / 评价人数 / 整体评价」，
行结构的字段名没逐个核对（能力里按"其余原样透传"处理）。

<!-- ai-contract-business:start -->
## SDK AI 说明契约（2026-09-22）

本节由当前 SDK 描述结果整理；说明权威来源为 `src/catalog/contracts-business.ts`。调用 `catalog.describe(id)`（或 `id-llm`）可得到参数来源、返回字段/状态、数据消费与后续映射；`catalog.describePage(页面路径)` 用于选择页面功能。以下更新以本节为准；前文历史请求/实测记录仍保留其记录日期。

### `study-statistics-teacher-list`

按讲师汇总授课数量、班课数量及评价。

- 执行：`sdk.studyStatistics.listTeachers`；通过 `capabilities.invoke('study-statistics-teacher-list', args)` 调用时按 `params` 填参。性质：read。
- 参数：`staffCode` 可选 (string)；`name` 可选 (string)；`startTime` 可选 (string)；`endTime` 可选 (string)；`pageNo` 可选 (number)；`pageSize` 可选 (number)。完整来源、枚举、条件必填与默认值由 SDK 同次返回。
- 返回：`{ list: object[], total: number }`。list=[] 为当前页无记录；total=0 表示该筛选无结果。保留筛选条件检查页码，不将空结果解释成权限判定。
- 字段：`list`、`total`、`list[].staffCode`、`list[].name`、`list[].courseNum`、`list[].lessonNum`、`list[].appraiseNum`、`list[].appraiseScore`。嵌套字段、可空性及业务含义见同次返回的 `returns.fields`。
- 消费：展示课程数、班课数、评价人数及平均分；平均分不可在缺少权重时跨讲师直接平均。
- 完成条件：按用户要求交付本次筛选范围的结果；需要完整清单时继续翻页至覆盖 total，不能把当前页当全集。
- 失败/不确定：权限或会话错误要交由用户恢复权限/登录后再读；不要通过换租户、换页面上下文绕过。业务校验失败按原错误修正参数，网络失败的只读查询可重试。


验证：`test/ai-contract-business.test.ts` 直接检查 SDK 描述输出及业务夹具；本批未重放真实写操作。逐能力状态、证据与反证见 [补齐清单](../ai-contract-business-evidence.md)。
<!-- ai-contract-business:end -->
