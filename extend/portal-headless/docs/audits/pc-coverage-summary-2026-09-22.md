# 人力、平台、门户 PC 能力覆盖审计

**结论：未达到100%，而且不能用旧报告的页面覆盖统计代表功能覆盖。** 用户本轮要求的三个系统全部PC Web操作是目标；旧D3/计划里的异实例、iframe跳过规则不能作为本次缩小分母的理由。未接入异实例必须如实列缺口，仍遵守显式baseURL和会话隔离。

前端 `test/portal/main@d3cf56bdc76c73c5eb9252be84b9b39b3900b48e`，后端 `test/test@dcb3f360194e63c33cfd7ef9df4360355ff13533`，本轮开始已pull --ff-only。审计包含开始时工作区已有AI模块实现；这些在途文件不由本次提交收走。

## 当前可重复统计

`pnpm coverage:pc` / `pnpm coverage:pc --json`。工具按真实index.js导入图、test条件和系统选择器求值，不扫描未导入旧mall.js，不漏PATH_TASK_MY常量，不包含DEV工具箱。

| 系统 | 活跃菜单叶子 | 至少一个可调用能力挂载 | 完全无能力入口 |
| --- | ---: | ---: | ---: |
| 人力 | 139 | 59 | 80 |
| 平台 | 177 | 17 | 160 |
| 门户 | 123 | 1 | 122 |

这是页面挂载率，**不是功能完成率**。一个查询能力不能代替新建、编辑、删除、审批、导入、导出、打印和可达弹窗。全部动作分母尚未穷尽，不能给出虚假的“功能百分比”。外壳非菜单能力如登录设备另列，不给首页虚增覆盖。人力原140统计多算的“系统核算参数”实际位于门户系统设置；完整归属说明见工具文档。

详细入口和动作线索：[人力](hr-pc-coverage-2026-09-22.md)、[平台](platform-pc-coverage-2026-09-22.md)、[门户](portal-pc-coverage-2026-09-22.md)。三份报告为静态入口盘点，不声称已逐个走完所有动态表单。

## 本次已补代码

- 考勤统计/档案：9个能力，编辑资料、当前用户部门、班组/人员候选、保存、归档/取消归档、删除、统计簿；保存接短窗口防重，补列表上下游说明。
- 岗位类别：7个能力，查询/详情/新建/修改/删除前检/短信发送/验证删除；保持占用和敏感操作保护。
- 平台单位管理：7个能力，查询/详情/准备新建/准备修改/新建/修改/删除；保留编辑隐藏字段，修正详情inline query键序。
- 登录设备管理：4个能力，列表/数量/准备移除/移除复查；保护本机，设备上下文按会话提供，显式走zhdj-app实例。

共27个新增能力，已接单用户、多用户、invoke、describe/-llm及相应方法说明。工作区注册总量318→345。说明结构通过不等于线上验收。别名生成改用实际注册表，原按源码格式扫描漏了全部27个新增定义；旧失败断言保留，修复后通过。

## 测到与未测到

已测：新增模块离线请求/参数/保护/多用户/AI契约；关键语义变异均红后恢复。考勤编辑和人员搜索浏览器基准与SDK逐字段比对。平台单位管理浏览器新建测试记录51359（SDK-TEST-U）、排序9999→9998、删除并相同筛选回查0条，已清理。浏览器行为不是SDK真实环境调用，不混淆证据。

真实SDK验证：用户明确授权后使用现有受控脚本，凭据只进入子进程环境。岗位类别24完成新建→详情核对→修改→详情核对→删除→同名查询0条；考勤表155完成创建/编辑→详情核对→统计→归档并在档案回查→取消归档→删除后列表消失。两条测试记录已清理，来源考勤表144只读未改。

平台单位SDK列表与prepare通过；创建失败code500，数据库唯一键报重复编码0054。浏览器测试删除最大编码后，后端分配器仅从未删除记录取MAX+1，而唯一键仍包括逻辑删除行；换新名称仍失败。两次失败创建均经同名回查0条，未生成待清理单位。此项是实测发现的后端阻塞，不能标成SDK写闭环通过。

未测：岗位类别短信链、设备真实读及踢下线、新增人力写动作浏览器逐字段基准。设备实例不在受控脚本允许的主后端host范围，不扩大凭据用途。考勤簿及打印仍未实现；其他无能力页面及已挂载页大量动作未补齐。`pnpm ai:check:complete` 按真实gaps失败，`pnpm coverage:pc:check` 因未覆盖和功能验证unknown失败；未放松门禁。

离线验证：仅本次暂存内容的隔离检出全套2406测试通过；完整工作区曾出现一个并发运行造成的eval-model 5秒超时，单独复跑33测试全通过。新增能力的离线回归、typecheck及说明检查通过；不以结构检查替代业务验证。
