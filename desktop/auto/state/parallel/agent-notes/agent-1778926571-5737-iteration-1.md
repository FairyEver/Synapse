# Agent Note - agent-1778926571-5737 - iteration 1

## 调查摘要

继上轮 `triggerSource` 日志补齐后，跟进三条后续建议：

1. **workflow-dispatcher.ts success log correlation** (P3): 成功日志 `"workflow mcp dispatch succeeded"` 只记录 `action`，缺少 correlation 数据。而起始日志和错误日志都包含 `dispatchCorrelation(params)`。

2. **task-scheduler → workflow triggerSource**: 调查发现 `task-scheduler` 不直接调用 `WorkflowEngine`，而是通过 `MainActionRegistry` 分发 action，无直接 workflow 触发路径。此项不构成可实施的 P0/P1 修复。

3. **RunSnapshot error 字段**: `WorkflowRunSnapshot` 类型缺少顶层 `error` 字段，从 nodeResults 重建。修复需要改核心数据模型——违反"不得改变核心数据模型字段"规则。

## 修改

- `desktop/electron/capabilities/workflow-dispatcher.ts`：将 `dispatchCorrelation(params)` 添加到 success log，与 dispatch start log 和 error log 保持一致

## 验证

- `pnpm exec vitest run electron/capabilities/__tests__/workflow-dispatcher.test.ts` — 15 tests passed
- `pnpm exec eslint electron/capabilities/workflow-dispatcher.ts` — 0 errors

## 后续建议

- 下轮可检查 `execution-service.ts` 中 `persistableActionError` 是否丢失错误诊断信息（另一个 worker 已有 claim）
