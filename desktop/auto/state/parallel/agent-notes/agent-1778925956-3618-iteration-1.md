# Agent Note - agent-1778925956-3618 - iteration 1

## 调查摘要

- 扫描了三轨：对话消息区、工作流/调度链路、日志缺口
- 工作流/调度方向发现 P2 日志缺口：`triggerSource` 未传播到节点级和工作流终态日志

## 修改

- `desktop/electron/services/workflow/workflow-engine.ts`：在 node started/succeeded/failed/cancelled 及 workflow failed/completed/cancelled 日志中添加 `triggerSource` 字段

## 验证

- `pnpm exec vitest run electron/services/__tests__/workflow-engine.test.ts` — 13 tests passed
- `pnpm exec vitest run electron/services/__tests__/workflow-scheduler.test.ts` — 12 tests passed
- `pnpm exec eslint electron/services/workflow/workflow-engine.ts` — no errors

## 候选放弃

- 对话消息区：未发现高置信问题，`agent-message-event.tsx` 代码稳健
- 工作流执行正确性：`workflow-scheduler.ts` 拓扑排序正确，异常传播覆盖，abort 处理完整
- `switch/executor.main.ts` 分支匹配逻辑覆盖了 exact/normalized/substring 三策略 + default fallback

## 后续建议

- 下轮可检查 `workflow-dispatcher.ts` 成功日志是否携带 correlation（当前 success log 仅含 action 字段）
- 可检查 `task-scheduler` 触发 workflow 的路径是否传递 triggerSource
