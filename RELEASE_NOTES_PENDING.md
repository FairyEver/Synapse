# Pending Release Notes

## 新增功能

- Agent 项目菜单新增“在终端中打开”，可直接在项目目录中新建 Synapse 终端并切换到该会话。
- Console 设置新增 API 秘钥管理，用户可以选择开放接口权限、一次性复制和撤销秘钥，并查看固定摘要的使用记录。
- 新增开放 API，可将 Drive 分享链接、Site 和公开素材转换为十分钟有效的文件或 ZIP 下载地址。
- 文档站已迁移到 `/document/`，顶部菜单仅保留首页和开放接口，并随服务端版本统一发布。

## 功能优化

- Console API 权限选择项新增文档入口，可直接打开对应的开放接口文档。
- Console API 秘钥列表改为自适应卡片布局，秘钥、权限、使用时间和管理操作在窄窗口中也能完整显示。
- Console API 秘钥支持创建后修改或清空权限；权限选择改为带说明的可滚动列表，移除权限后新请求和尚未开始的临时下载会立即失效。
- 获取分享链接文件接口现在直接接收完整 URL，带密码分享无需拆分密码；单文件和 ZIP 内文件均保留云盘原名，单个 Markdown 继续按原文件下载。
- 文档站首页已改为当前跨编辑器 AI 能力管理定位，并保留开放接口入口。
- 开放接口文档补齐快速开始、权限范围、支持的 Drive URL、响应字段、下载响应头、快照与失效行为，可直接按当前服务端契约完成集成。
- 文档站已移除外部下载入口。
- Agent 项目菜单中的“自定义对话”调整为“创建自定义对话”，操作含义更加明确。
- Agent 输入框附件菜单中的操作名称调整为“附加文件”和“附加文件夹”，更准确地表达文件会随消息一同发送。
- 云盘 Markdown 可视化编辑器现在支持在表格单元格内按 `Shift + Enter` 换行，保存和重新打开后会继续保持单元格内的多行内容。

## 问题修复

- 修复开放接口处理不存在或已失效的普通 Drive 分享链接时返回服务器内部错误的问题；现在会按公开契约返回链接不存在。
- 修复云盘单次上传恰好 1,000 个文件时可能因数据库事务超时而失败的问题；受支持的边界数量现在可以正常完成准备。
- 修复 Console API 秘钥列表中操作列表头与行内操作未对齐的问题。
- 修复云盘 MDXeditor 表格单元格末尾第一次按 `Shift + Enter` 不显示新行的问题；现在按一次即可立即换行并继续输入。
- 修复公开素材图片无法嵌入本地文件或其他域名页面的问题；图床响应现在允许跨域资源加载。
- 修复云盘 MDXeditor 文档以图片结尾时，图片后的输入光标难以发现、难以继续输入的问题；末尾图片后现在会保留可编辑空行，并提供更充足的底部点击区域。
- 修复云盘 MDXeditor 粘贴图片时，混合剪贴板内容可能留下仅当前页面有效的临时图片地址，以及空图片直到保存时才提示“文件大小无效”的问题；保存前也会阻止未完成上传的临时图片写入文档。

## 技术调整
