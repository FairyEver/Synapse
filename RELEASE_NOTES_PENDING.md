# Pending Release Notes

## 新增功能

## 功能优化

- 优化 Markdown 评论卡片的信息层级、选中反馈与评论者信息展示；回复现在可直接在当前评论卡片内完成，分享页和云盘阅读页保持一致。

## 问题修复

- Windows 上通过 Terminal MCP 恢复环境变量继承时，变量名现在按大小写不敏感匹配；macOS 和 Linux 仍保持大小写敏感。
- Console Web 会话 Cookie 现在与 `USER_REFRESH_TOKEN_DAYS` 配置及服务端实际过期时间保持一致，浏览器重启后不会因固定 30 天期限而提前失效或继续携带过期会话。
- Terminal MCP 更新启动环境时，受保护、重复或超限的设置现在会返回可处理的参数校验错误，不再误报为内部故障，也不会在错误或审计中暴露环境变量值。
- 管理员查看全局云盘存储汇总时现在会留下安全审计记录，审计元数据仅包含聚合数量，不记录文件路径或原始存储值。
- 修复生产环境直接打开或刷新管理后台深层页面时可能返回 404 的问题，部署检查现在会覆盖深层管理路由。
- 修复从应用启动台打开工作流、云盘和自动化时出现两条重复顶栏的问题；应用操作和云盘容量信息现在会合并到同一条顶栏，独立窗口仍保留自身工具栏。

## 技术调整
