# Pending Release Notes

## 新增功能

- Agent 输入框支持通过粘贴或拖拽添加图片、文件和文件夹附件；图片按 `[Image #1]` 展示，文件和文件夹显示为路径上下文，并可在发送前删除。

## 功能优化

- Agent 附件发送到运行时后，图片会直接交给模型识别；项目外部的文件和文件夹路径会在新会话中加入可访问目录，旧会话遇到新增外部路径时会提示重新开启会话。

## 问题修复

- 修复 Agent 附件消息只点发送或按 Enter 后没有真正发送的问题，并让带尾斜杠的粘贴路径按文件夹处理。
- 修复 Agent 附件进入 Electron 后被丢弃或无法通过校验的问题，图片和文件路径附件会继续交给 Agent 运行时处理。

## 技术调整
