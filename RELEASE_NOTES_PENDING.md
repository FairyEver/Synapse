# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- 手机远程看电脑终端时，输出期间**画面会反复上下乱滚**：视口先突然跳到上面很远的地方（一屏甚至好几屏之前的内容），再花将近一秒滚回最新一行，如此一批接一批。电脑每发来一段更早的终端内容（重连后同步整屏、往回翻页取历史）就会来一次，输出密集时几乎不停。根因是「停在最新一行」的读者被跳过了位置补偿：上方新插进来的内容让画面凭空错位一整块，紧接着平滑跟随又把这一整块滑了回来。现在停在最新一行时会先把错位补齐再去跟随，纯属上方插入的情况更是直接一步到位、不再产生任何滑行。

- 终端里的**中文会变成乱码**（同一个格子里叠着两个字、缺笔画，或者混进英文字母碎片）：终端渲染器把汉字字形存在一张图集里，长时间的中文会话会把这张图集撑到需要整理，整理之后有一部分汉字仍按旧位置取图，于是画出别的字。这种乱码滚动、刷新、切走再切回都冲不掉，只有拖着改一下窗口大小才会恢复。现在渲染器整理过图集之后会自动重建一次字形图集（等终端安静下来再做，不会打断正在刷屏的输出），这类乱码会自己消失，不用再拖窗口。没有触发整理的会话不会有任何变化。

- 录音**转写会一直卡在「转写中」不动**：录的时候没拾到说话声的录音（整段只有环境底噪，例如误触开始又很快停下），提交给语音识别服务之后会一直停在「处理中」——既不返回结果也不报错，进度条贴在 95%，看起来只是「快好了」。实测这种任务要拖到几十分钟之后才会由识别服务判失败（两条分别拖了 47 分钟和 52 分钟），失败原因还是一句英文的 `Failed to recognize audio!`。现在这类任务超过 15 分钟没有任何进展就直接判失败，并写明原因是「这段录音里没有检测到说话内容，无法转写。」，用户可以按重试或重新录一条。有说话内容的录音完全不受影响，长会议该等多久还是等多久。
- 录音中途**App 被系统回收或强退，整条录音会丢**：录音文件原先要等按下「完成」那一刻才把索引写进去，进程没了索引就永远写不上，本机那份文件谁也读不出来——恢复机制会把这份读不出来的字节照原样再传一次，服务端只能判「音频文件不完整」，用户丢掉整段音频（实测撞过一次 12 秒的）。现在录音期间磁盘上一直是一份**已经写完索引、每 2 秒一个分片**的文件，进程被杀最多丢最后那 2 秒，前面全部保留，恢复后照常转写。
- 录音的**时长显示不准**：列表和详情页原先直接采信手机上报的时长，而这个数有两种偏法——一种是一条 6 秒的录音报成 0 秒（详情页因此显示「0 秒的录音，预计 1 分左右完成」），另一种是按文件大小折算时把开头那段占位区也算成音频，6 秒的能报成 13 秒。现在时长改由服务端在合并音频时**直接从音频文件里量**，手机报的数只在量不出来时兜底。

## 技术调整
