# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复
- 统一 Agent 权限请求展示里的敏感信息脱敏逻辑，减少规则不一致导致的泄露风险。
- 修复 HTTP 定时任务选择认证类型但未填写凭据时仍以无认证方式发送请求的问题。
- 修复数据库导出在 WAL checkpoint 忙碌时仍继续复制数据库文件，可能导出不完整备份的问题。
- 修复内容图标图片缓存无上限增长的问题，长时间浏览带图片图标的内容时会自动回收旧缓存。
- 修复 MCP HTTP 诊断探测缺少超时导致诊断页可能永久等待的问题。
- 修复主进程运行时未配置 logger 时后台 run 崩溃缺少兜底告警的问题。
- 修复 HTTP 请求错误信息可能暴露 URL 中用户名或密码的问题，运行历史和测试反馈会隐藏这类凭据。
- 修复应用退出时服务停止失败缺少日志、并可能让后续服务得不到停止机会的问题。
- 修复定时任务保存失败时只显示通用提示的问题，现在会展示脱敏后的具体失败原因。
- 修复内容索引遇到卡住的 Git 命令时可能长期阻塞的问题，超时后会自动中断并走恢复流程。
- 修复定时任务后台自动执行后列表不会自动刷新的问题，运行结束、跳过或停止后会触发列表刷新。
- 修复数据库 timestamp 字段接受不存在日期的问题，避免写入类似 2 月 30 日这类语义错误的时间。
- 修复定时任务 Command/Script 动作环境变量输入中间态会被清空的问题，逐字输入 KEY=value 时不再回退光标。
- 统一 HTTP URL 脱敏规则，权限请求、审计、日志和网络诊断中的用户名密码与敏感查询参数会按同一规则隐藏。
- 修复内容索引同步缺少事务保护的问题，索引重建或增量同步中断时不会留下半更新的索引表。
- 限制主进程 tracer 保留的已完成 span 数量，长时间运行时不再无限积累观测数据占用内存。
- 修复 Agent 会话创建过程中激活写入失败会留下 inactive 占位会话的问题，失败时会重试并清理未完成记录。
- 修复 Token Usage 中 Claude Code 项目路径含连字符时被错误拆成多级路径的问题，项目排行和详情会保留真实项目名。
- 修复内容收藏侧栏计数不随当前仓库过滤的问题，切换仓库后“我的收藏”数量会和实际列表保持一致。
- 修复 MCP Server 注册后刷新列表失败会让设置页一直停在加载状态的问题。
- 修复 Agent 发送消息时项目解析失败缺少阶段事件的问题，失败时会留下可观测的发送失败状态。
- 修复身份恢复对话框输入 ID 时过早显示格式错误的问题，改为失焦或提交前再校验。
- 修复编辑器扫描移到废纸篓成功但列表刷新失败时没有提示的问题。
- 修复数据库侧栏文件夹操作失败时提前退出编辑状态且没有错误提示的问题。
- 修复数据库导入导出成功日志记录本地完整路径的问题，降低诊断日志暴露用户目录信息的风险。
- 修复定时任务执行内容未填完整时保存按钮禁用但没有提示的问题。
- 修复定时任务命令或脚本被内层权限策略拒绝时运行历史只显示异常类名的问题。
- 修复内容安装进行中仍可点击取消关闭弹窗、但写入继续执行的问题。
- 修复编辑 Prompt、Rule、Skill 时替换图片图标后关闭弹窗不会提示未保存修改的问题。
- 修复定时任务导入或创建时会保存未知执行动作的问题，非法任务会在创建阶段被拒绝。
- 修复内容创建或编辑保存失败但错误消息为空时没有任何失败提示的问题。
- 修复 Usage Analysis 刷新遇到部分文件处理失败时没有提示的问题，报告可能不完整时会显示提醒。
- 修复 Usage Analysis 快速切换范围或刷新时旧请求可能覆盖新报表的问题。
- 修复 Usage Analysis 遇到零成本模型时可能反复重建聚合数据的问题，减少不必要的查询卡顿。
- 修复 Agent 错误事件中独立出现的 API Key 未被脱敏的问题，聊天界面和事件记录会隐藏这类敏感信息。
- 修复仓库维护会跳过图片图标内容历史的问题，整理历史和过期软删除清理会正确处理这类内容。
- 修复编辑器扫描复制失败后仍刷新列表的问题，失败时只保留复制错误提示，不再触发多余刷新。
- 修复新建 Prompt 时重复标题不提示的问题，现在会按已有 Prompt 标题检查并阻止重复创建。
- 修复删除 Agent 会话时关闭运行时失败却已删除历史数据的问题，失败时会保留会话供重试。
- 修复内容列表切换排序保存失败时没有提示、并可能产生未处理错误的问题。
- 修复收藏状态保存失败时可能产生未处理错误的问题，失败时会给出提示并保留原状态。
- 修复 MCP 工具错误可能向外部客户端暴露本地路径或密钥片段的问题，错误返回会先脱敏。
- 修复定时任务配置失效后只在界面显示禁用、重启后又恢复启用的问题，发现无效任务会同步持久禁用。
- 修复 Agent 定时任务选择无超时时会被立即终止的问题，无超时配置会保持运行而不是传入 0ms。
- 修复编辑器扫描详情里“重新安装”在仓库或身份未就绪时仍可打开安装弹窗的问题。
- 修复错误脱敏漏掉带前缀环境变量密钥和部分平台 token 的问题，减少日志与界面泄露风险。
- 修复内容安装状态初始化快照可能覆盖实时变更的问题，减少安装状态短暂回退或闪烁。
- 修复批量恢复已删除内容遇到冲突时只显示笼统提示的问题，现在会列出发生变化的内容标题。
- 修复最近删除列表在仓库索引同步失败时直接报错的问题，异常仓库状态下会降级为空列表。
- 修复内容列表卡片不显示用户填写的“用法提示”的问题，索引缓存会保留这段文本。
- 修复从源目录导入 Skill 时附件读取失败仍提示保存成功的问题，避免生成缺附件的 Skill。
- 修复快速连续点击 Prompt 运行按钮可能创建重复 Agent 会话的问题。
- 修复本地网络服务在端口探测后被抢占时不会自动换端口的问题。
- 修复数据库侧栏刷新并发时旧请求可能覆盖新表和文件夹列表的问题。
- 修复数据库空更新会只改写更新时间并显示成功的问题，现在必须提供至少一个业务字段。
- 修复 Agent 定时任务存量配置缺字段时只提示检查执行内容的问题，现在会指出需要补项目、提示词、会话策略或超时时间。
- 修复编辑器扫描页并发刷新时旧结果可能覆盖最新列表的问题。
- 修复 Agent 首次停止请求失败时没有用户反馈的问题，现在会提示停止失败。
- 修复 Agent 定时任务连续多次失败后可能丢失上次成功会话的问题。
- 修复 Codex Usage Analysis 已有明细页但没有入口的问题。
- 修复数据库单行保存或删除未命中时仍显示成功的问题，现在会提示行已不存在并刷新列表。
- 修复替换同名 Skill 后旧内容仍显示已安装的问题，安装状态徽标会同步清理被替换的旧内容。
- 修复项目级 Agent 运行时停止时没有关闭实时会话的问题，项目容器关闭会同步清理仍存活的 Agent 会话。
- 修复 Prompt 运行弹窗在项目被删除后仍可发送到旧项目的问题，项目列表为空时发送按钮会正确禁用。
- 修复仓库同步后多个内容安装状态只刷新最后一项的问题，批量变化的安装徽标会逐项更新。
- 修复最近删除读取失败或加载中时被显示成空列表的问题，现在会展示对应的加载或错误状态。
- 修复外部 Scheduler 接口只显示任务被禁用却不说明配置失效原因的问题，任务摘要会返回需要更新的字段提示。
- 修复数据库表移动到文件夹时可写入不存在表或文件夹的问题，失败时会保留原有归属并返回明确错误。
- 修复 Rule 更新后列表安装状态仍显示已安装的问题，已安装到编辑器的旧版 Rule 会提示可更新。
- 修复编辑器扫描打开大型 Skill 详情时关联文件列表无上限递归的问题，避免详情页长时间停在加载状态。
- 修复 Skill 扫描目录读取失败时被误显示为未检测到的问题，现在会显示扫描读取失败。
- 修复创建或编辑 Skill 时拖入大目录会先整理全部文件的问题，附件收集会按数量上限提前停止。
- 修复外部回复 outbox 会持久化原始 Agent 错误的问题，失败记录只保存错误摘要。
- 修复编辑器 Skill 目标里损坏的身份文件会暴露底层解析错误的问题，现在会按目标冲突处理。
- 修复 HTTP 定时任务使用内置认证字段时权限和审计上下文缺少 Authorization 信息的问题，且不会暴露凭据原文。
- 修复数据库文件夹重排接受缺失、重复或不存在 ID 仍返回成功的问题，避免文件夹顺序被写乱。
- 修复项目容器关闭失败后仍从运行时注册表移除的问题，失败状态会保留以便诊断并避免重复打开新容器。
- 修复定时任务列表并发刷新时旧请求覆盖新结果的问题，避免任务状态在刷新后回退。
- 补齐知识库创建、资料列表和 raw 文件操作的主进程日志，批量操作跳过文件时会记录原因摘要。
- 修复知识库 URL 资料导入写入失败时审计日志可能误标为网络失败的问题。

## 技术调整
- 补齐 EventBus 的 `block`、`drop-oldest`、`drop-newest` 背压策略，避免声明了策略却退回即时分发。
