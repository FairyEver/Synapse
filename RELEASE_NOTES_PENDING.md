# Pending Release Notes

## 新增功能

- 基础设置新增「设备名称」，macOS 和 Windows 均可自定义手机端显示的电脑名称，改名不影响设备身份。

- 新增 Portal Headless Test 测试环境连接器，支持授权回调验证、按账号隔离的安全凭据保存、断开与重新连接。
- 新增 Portal Headless 测试扩展：AI 可通过本机 MCP 获取短期授权，携带 SY 与 Portal 双重凭证发现并调用固定 SDK 的完整读写能力目录；实际业务操作仍受当前 Portal 账号与企业权限约束。

- 终端里项目分组的加号支持按住 ⌘ 点击（Windows/Linux 为 Ctrl）：按设置中的默认模型在项目目录里直接启动一条 Claude Code 会话，与对话侧栏项目行上按住 ⌘ 点加号是同一件事。只对来自项目的分组有效；电脑上没有可用的模型时会提示先配置供应商，不会新建空标签。

## 功能优化

- Portal Headless Test 连接器使用独立的 Portal 图标。
- 终端选中快捷输入后，不再显示底部重复内容及“待执行 · Enter 执行”提示条，仍由用户按回车发送。
- 终端设置里的 Agent 原生通知拆成两颗开关：总开关不变，新增「弹系统通知」。关掉它之后终端里的 Agent 状态与等待输入标记照旧记录，只是不再弹系统通知；直接打开总开关的用法和以前一样。
- 从 Synapse 里启动的 Claude Code 会话（新建对话、项目分组按住 ⌘ 点击、手机端）现在和手动敲 `claude` 一样会上报状态：任务完成时弹通知，等待你操作时侧栏出现标记。通知总开关关着时仍然什么都不注入。

## 问题修复

- 修复在 Synapse 终端里 zsh 的 `.zlogout` 不会执行的问题：登录会话退出时用户自己的收尾逻辑现在照常运行。
- 终端 Agent 通知不再连带影响 `codex` 命令本身：Codex 版本更新改动它用到的开关名时，通知功能可能失效，但 `codex` 仍照常启动。
- 修复凭证使用缩写变量名时可能在对话工具详情、复制或导出中漏脱敏的问题。
- Portal 扩展提供可靠调用脚本：查询失败及时退出，分页异常不再无限循环，授权刷新仅重试一次；参数错误明确指出缺失或无效字段。

- 修复年度双赢协议等页面因旧导航菜单与实际页面路径不同而无法查询的问题，扩展目录与执行检查改用 Portal 网页同源的页面权限。

- Portal 扩展区分服务端已配置能力与当前连接可见目录，避免将目录不可见误报为部署缺失；补充跨天会议室占用时段和简洁回答指引。

- 修复电脑迁移后因设备 ID 和名称相同而互相顶掉连接的问题：macOS 和 Windows 通过机器标识区分迁移后的设备；机器标识暂时不可用时保留原设备身份。

- 修复应用长时间开着时，「发现新版本」会一直停在同一个版本号上的问题；没有点下载的更新会定期重新确认，版本已被更晚的版本取代或已经下线时，界面提示会跟着更新。
- 手机上点「快捷输入」里的一句话、再按发送，句子现在发得出去。这类句子是多行的，之前会被回一句「一次只能提交一行命令」，什么也没送出去；现在整句话会送进终端并提交。终端里的程序不收多行内容时，提示会说明这一点，而不是只说没法提交。

## 技术调整
