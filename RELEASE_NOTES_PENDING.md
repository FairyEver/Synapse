# Pending Release Notes

## 新增功能

- 终端可选择为 Codex 和 Claude Code 开启原生通知；需要操作或任务完成时会提醒，点击通知可直接定位到对应终端会话，并兼容最终调用 `codex` 或 `claude` 的命令别名。

## 功能优化

- 左下角软件更新入口支持原地下载、查看进度并安装重启，不再打断当前任务跳转到设置页。

## 问题修复

- 修复网盘 Markdown 文档保存后可能跳回顶部的问题，包含表格的长文档会保持当前阅读和编辑位置。
- 修复终端会话或应用长时间处于后台后，重新打开时可能快速回放历史动态画面的问题；终端现在会直接恢复到最新画面并继续显示实时输出。

## 技术调整
