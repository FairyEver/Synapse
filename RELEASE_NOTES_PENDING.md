# Pending Release Notes

## 新增功能

- 可让 Codex 在 Synapse 中直接新建对话：默认进入“本地对话”，也支持查询可用供应商和模型、指定模型创建、指定分组或沿用已有对话的分组，并继续发送消息和读取回答；重复创建请求会复用已创建的对话。

- Agent 对话右键菜单新增“复制深度链接”。链接采用与 Codex 对齐的 `synapse://threads/<thread-id>` 格式，可交给安装了 Synapse Skill 的 Codex，在本机 Synapse 中直接定位对应对话。

- 将 Agent 对话深度链接交给安装了 Synapse Skill 的 Codex 后，可读取并总结完整可见执行记录、监视运行状态，并在用户对话中异步发消息、引导当前任务、停止或处理待回答问题；权限批准与强制停止仍要求针对当前操作明确授权。

- Agent 任务运行时，可以从待发送消息中选择「引导」，让 Agent 在下一个处理节点调整当前任务；普通回车排队和停止操作保持不变。

## 功能优化

- 修复图片读取被误判为上下文超限而中断的问题；空间不足时保存进度并自动在新会话中继续读取同一原图。停止未获确认、原件变化或图片仍无法容纳时保留恢复记录，不重复执行已完成操作，也不把未交付图片记为完成。

- 减少 Agent 计时和重复运行状态引起的历史区更新；开发模式下限制 React 性能记录的累积。长对话整体性能和长时间运行表现仍待进一步验收。

- Agent 长任务在上下文空间不足或整理效果不佳时，会保存任务进度并自动切换干净会话继续；完整交接资料可按需读取，避免反复整理后中断。

- 修复可选 MCP 离线导致加载全量工具、挤占上下文的问题；第三方模型默认使用工具按需加载，保留用户明确关闭的选择。

- 上下文提示改为显示实际占用、SDK 真实整理触发值和模型上限；动态控制工具结果容量，并在整理释放空间后恢复可用额度。

- Agent 会把大型文本工具结果安全保存后，只将有界片段送入后续模型请求，并按单结果、单批和当前上下文控制容量；普通回复和附件原件保持不变。

- Agent 长回复改为有界批量刷新，超长历史内容先显示预览并支持分段查看全文；正常逐字显示最多增加约 50 毫秒延迟。

## 问题修复

- 修复窗口渲染帧失效后持续刷出消息发送错误的问题；页面恢复后继续正常接收事件。
- 修复本地停止服务时因缺少 PDF 渲染器密钥而无法完成清理的问题。

- 修复 Agent 工具结果无法完整交付时，可恢复标志在失败处理和历史保存中丢失的问题；重开对话后仍保留真实失败状态。
- Agent 诊断导出区分未采集记录与读取失败，未知事件数量不再显示为零。

- 修复 Agent 自动交接丢失真实工作路径和早期要求的问题；后续会话继续使用同一任务清单身份，并获得最近尚未确认处理的工具结果。
- 修复大型 Read、Bash 工具输出的截断替换未被 SDK 接受的问题；文本结果无法完整保存或引用无法交付时明确停止，避免带着缺失证据继续执行。
- 长任务累计处理量不再受到 96 KiB 工具输出额度限制；上下文仍按当前占用和请求体预算治理。
- 修复运行时只返回空页或部分时间线时，对话导出出现空文本和零计数的问题；未收到终态的 SDK 退出不再被视为完成。

- 修复 Agent 历史追加与会话标题、SDK 状态和费用并发保存时可能覆盖已有记录的问题。
- 超长内容窗口关闭后释放已加载正文；快速切换或重新打开时忽略旧请求，加载失败可重试，长时间翻页不再无限累积回退记录。

- 修复对话链接经过 Markdown 复制后，分隔点带有转义符时被误报为“对话链接无效”的问题；新复制的链接也会避免这类标点转义影响。

- 修复在新 Codex 对话中首次读取 `synapse://threads/...` 链接时可能返回“Agent 对话操作失败”的问题；完整深度链接现在可以稳定解析并读取对应时间线。

- 修复 Codex 读取 Agent 深度链接时可能漏掉长对话标识字符，或把短定位符中的下划线转义后误报“对话不存在或已删除”的问题。新链接只包含更短且带校验的 thread id，并对容易被 Markdown 改写的字符编码；首次读取由 Synapse 跨项目解析完整链接。

- 修复 Agent 工具详情内容较长时无法在详情区域内滚动的问题。

- 修复 Agent 流式回复时对话内容上下抖动的问题，贴底浏览时会稳定跟随最新内容。

- 修复 Agent 引导消息缺少 SDK 回放信号时，任务完成后仍可能一直显示“处理中”的问题。

- 修复点击发送后按钮焦点被运行中的停止按钮继承，导致后续按空格或回车时误停止任务的问题。

- 修复百炼长对话因 6 MiB 请求体上限中断后无法继续的问题。现在可在原对话中整理上下文并由用户确认继续，不会自动重放已执行的操作，也不会改变附件原件。

- 修复自动整理后被连续大型工具结果迅速填满而中断的问题。百炼会为每次文本工具结果保留更安全的上下文余量；极端情况下仍会安全结束旧上下文，并允许在原对话中整理后继续，不再显示英文 SDK 错误。

- 修复长任务只看 token、不计算请求字节和连续工具结果总量的问题。现在会结合 SDK 上下文快照与新增内容字节提前保护请求，并记录原生工具结果淘汰和整理结果，降低再次触发 6 MiB 限制或快速回填熔断的概率。

- 修复高频 Agent SDK 事件导致界面白屏、应用失去响应且后台任务继续运行的问题。界面异常时会停止该窗口发起的任务并自动恢复；重复异常时可重新打开或退出应用。

- 修复开发环境中长对话触发 React 性能诊断时，Electron 因序列化大型组件数据而意外退出的问题；崩溃恢复也不再因同步重载导致整个应用退出。

## 技术调整
