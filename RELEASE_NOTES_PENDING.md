# Pending Release Notes

## 新增功能

- 新增 JavaScript 和 Node.js Workflow 节点及 Automation Action，可直接运行普通脚本文件，并把严格 JSON 输入和单一 JSON 结果接入后续步骤。
- 新增 JSON Repair 系统应用、MCP 工具与 Workflow 节点，可将一次文本输入修复为经过最终解析和资源边界验证的完整 JSON 文本。
- 新增“问题反馈”，支持在用户逐次确认后通过内置 Synapse Skill 匿名提交纯文本问题，并由管理员在 Dashboard 私下查看和单条删除。
- 新增 System Notifier 系统应用、原生系统通知 MCP 工具与 Workflow 节点，支持跨平台的单向通知触发、默认启用/静音设置和测试通知。

## 功能优化

- Agent 助手消息中的本地文件引用现在支持通过右键菜单使用默认应用打开，或在文件夹中显示。
- 完善 Synapse Agent 的工作流操作指南，能更准确地区分 Workflow 节点与 App 工具直调，并在替换节点、生成 HTML 和校验工作流时减少误判与并发覆盖风险。
- 优化无法在线预览文件的分享页，将下载提示居中展示，并提供更清晰的状态说明和下载入口。

## 问题修复

- 修复条件显示应用的入口、Dock 与独立窗口判断不一致，以及工作流独立窗口可能显示空白的问题。
- 修复导入工作流脚本确认、重新运行和子工作流调用可能执行审阅后新版本的问题，并修复 Node.js 临时脚本文件晚创建后残留、UTF-8 日志跨块乱码及结构化错误信息不准确的问题。
- 修复系统通知工作流节点可能在运行历史和复制报告中保留通知正文或解析后变量值的问题。
- 修复 macOS 正式包中新建终端始终启动失败、只显示“终端画面无法恢复”的问题。
- 修复服务端部署时未同步 pnpm 补丁文件、导致 Docker 镜像构建中断的问题。

## 技术调整
