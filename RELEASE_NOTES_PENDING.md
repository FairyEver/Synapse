# Pending Release Notes

## 新增功能

- 工作流新增 Claude Code 节点，可以调用用户本机终端里的 Claude Code CLI，并在运行历史中保留调试记录。
- 新增“应用”启动器，把资源仓库、本地数据库、IDE 管理、用量监控和价格管理收纳为系统应用，并以独立窗口打开。
- Synapse Drive MCP 新增云盘整理基础能力，Agent 现在可以快速获取云盘文件/文件夹总数和递归元数据，预检整理方案后按计划移动文件；整理过程不提供批量读取文件内容能力，仍需少量、逐个读取文本文件。

## 功能优化

- 工作流 Claude Code 和 Codex 节点的配置分组已对齐，设置、执行、权限和调试选项现在按一致顺序展示，更容易找到对应参数。
- Synapse Drive MCP 的工具名称和说明重新整理，外部 AI 现在可以按更清晰的文件管理、预览、下载和分享语义操作普通用户云盘。
- 云盘上传限制调整为更适合办公文档：单文件最大 100MB，账号容量默认 5GB；桌面端会提前拦截超大文件，已有高于 5GB 的云盘配额会随迁移收敛到 5GB。
- 云盘页面标题旁的容量进度条改为更克制的灰底样式，并简化为“当前容量 / 总额度”的显示。
- 云盘公开页面的未找到和密码输入页面改为更清晰的居中布局，打开失效链接或输入密码时不再只看到空白文字页。
- 诊断包现在会记录 Codex 进程启动时间和 Codex MCP 配置修改时间，帮助判断旧 Codex 会话是否没有加载最新的 Synapse MCP。
- 应用启动器改为紧凑入口列表，保留系统应用图标并补充简短用途说明，打开入口更清晰。
- 内置技能列表移除了“Synapse 价格规则 MCP”和“Synapse 定时任务 MCP”，新用户不再看到这两个预置安装项。
- 旧“定时任务”模块已下线，顶部不再显示独立“定时”入口；需要 cron 或 interval 触发时请使用“自动化”。
- 顶部菜单顺序调整为先显示“对话、工作流、云盘、自动化、技能、规则、提示词”，其余入口继续排在后面。
- 顶部“工具”模块和内置文件转 Markdown 工具已下线，主导航不再显示独立工具入口。
- Webhooks 管理页改为更紧凑的表格视图，列表中不再直接显示 Public ID 和 URL；加载、空数据和翻页状态现在与后台其他列表页保持一致，操作列会固定在右侧，横向滚动时也能直接操作当前行。
- 网盘里直接打开文件时会进入更专注的阅读布局，顶部保留文件信息和下载操作，正文区域不再显示多余的左右分栏。
- 网盘承接页重构为 Finder 和 Renderer 两层：目录浏览、文件预览、新窗口打开和分享文件访问的布局更清晰，文件页可以直接进入阅读或预览视图。
- 网盘公开访问收敛为分享链接：新链接使用 `/share/...`，HTML 文件自己打开或分享给别人时默认进入网页渲染视图，也可以切换源码和下载；旧的网页发布、站点发布、重新发布和已发布管理入口已下线。
- 网盘 Markdown 文件预览移除了内部“预览 / 源码”切换，阅读区域更干净；同级文件为空时会明确提示“暂无同级文件”。
- 网盘文件页里的“新标签页打开”会进入独立阅读模式，不再重复打开当前分栏预览页；文件阅读和分享访问加载时都会显示阅读页骨架。
- 网盘控制台的路径导航会在最前面显示可点击的“我的空间”，从文件或文件夹详情页可以直接回到根目录。
- 网盘打开文件时不再显示空的左侧文件栏，文件内容会直接进入阅读视图。
- 网盘 Markdown 阅读器现在会自己限制正文宽度，宽屏下阅读长文档时行长更稳定。
- 云盘主界面移除了只筛选当前视图的搜索框，工具栏保留上传、新建、我的分享和刷新等实际管理操作。
- 自动化编辑器的触发条件和执行操作现在把“更换”放到面板标题区，已选内容卡片只展示当前配置，界面层级更清楚。
- 顶部主导航改为在标题栏居中显示，不会因为右侧账号入口存在而向左偏移。
- 本地数据库应用新增“数据表、服务状态、管理、MCP”顶栏切换，数据服务相关设置集中到应用窗口内，设置页不再单独显示“数据服务”分类。
- 系统应用窗口的顶栏切换统一居中显示；价格管理去掉了左侧多余标题，右侧刷新和筛选操作保留。
- 用量监控窗口移除了 CC 和 Codex 页面内重复的来源标题，只保留顶部来源切换和页面筛选。
- 独立系统应用窗口的原生标题栏现在会显示具体应用名称，多个窗口同时打开时更容易区分。

## 问题修复

- 应用启动器里的系统应用入口改用“新窗口打开”图标，不再误用表示进入下一级的右箭头。
- 修复桌面端打开云盘主界面时可能直接显示“query is not defined”的问题，云盘列表现在可以正常进入。
- 修复价格规则窗口变窄后工具栏和表格右侧溢出的问题，操作按钮会自动换行，表格会留在窗口内横向滚动。
- 修复网盘中自有 HTML 文件切换到“网页”视图后无法直接渲染的问题，未分享的网页也可以在登录后的控制台里预览。
- 网盘 Markdown 文件预览的视图切换入口改为“打开方式”，当前视图会用对号标记，也修复了缺少切换入口的问题。
- 网盘独立阅读页的文件菜单新增“在云盘中查看”，可以从阅读模式回到对应云盘文件页。
- 网盘独立阅读页的文件菜单默认移到右上角，并支持拖拽调整位置，阅读时可以避开正文内容。
- 网盘独立阅读页的文件菜单不再显示多余的“新窗口打开”，避免在已经独立打开的页面里重复打开同一文件。
- 修复网盘代码和网页预览在独立阅读页、控制台分栏中尺寸异常的问题，编辑器会稳定铺满预览区域。
- 修复从网盘文件页点击“我的空间”后直达 `/console/drive` 时可能看到开发服务器 base URL 提示的问题。
- 修正公开文件预览页的阅读布局，顶栏标题、下载按钮和正文内容现在会按同一宽度对齐。
- 修复工作流 Claude Code 节点未继承工作流默认超时的问题，导入含 Claude Code 节点的工作流包时也会正确要求选择本地项目。
- 修复分享 HTML 渲染链接携带密码参数时会先读取文件再清理地址的问题，现在会先完成访问校验并移除密码参数。
- 修复分享 HTML 文件默认网页视图可能被浏览器安全策略拦截的问题，分享页现在可以正常在阅读器中渲染网页内容。
- 修复网盘图片预览仍会生成对象存储直链的问题，图片预览现在也走网盘下载路由，取消分享或删除源文件后访问语义更一致。
- 修复工作流 Claude Code 和 Codex 节点项目配置区的字段间距，工作目录标签不再贴着项目下拉框显示。
- 注册账号时会明确校验邮箱是否已被使用，重复邮箱会直接提示在邮箱输入框下方。

## 技术调整
