# Pending Release Notes

## 新增功能

## 功能优化

- Agent 分享独立 HTML 时默认生成云盘分享链接；只有多文件网页或明确要求发布整个文件夹时才创建站点，只有一个 `index.html` 的文件夹仍可按需发布。

## 问题修复

- 修复 Secrets 更新仅提供名称时仍返回成功、推进更新时间并广播变更的问题；更新请求现在必须实际包含密钥值或描述。
- 修复超多 Skill 环境变量声明会放大配置弹窗和密钥关联扫描成本的问题；单个 Skill 的声明及单次批量扫描现在统一限制为 100 项，并在渲染或读取密钥前明确拒绝超限输入。
- 修复 Workflow 定义检查会把未来数据版本误报为可用的问题；检查现在与保存共用版本迁移保护，并明确阻止继续解释或编辑受保护数据。
- 修复 Agent 的多问题确认在问题 id 或 key 重复时会覆盖已选答案、最终被误判为未完整回答的问题；答案现在始终按问题顺序使用稳定且唯一的键提交。
- 修复 Agent renderer 的正常会话事件和刷新失败日志仍会记录原始 sessionKey、暴露工作流或调度来源标识的问题；相关日志现在统一保留脱敏标记。
- 修复 Windows/CRLF 格式 `SKILL.md` 的声明名无法被 Skill 卸载器识别、导致按名称找不到或无法复查目标的问题；名称联想、扫描和卸载前校验现在都支持 CRLF frontmatter。
- 修复 Synapse Skill 刷新安装状态时仍可点击行内安装或更新、随后因安装源被释放而失败的问题；刷新期间所有安装入口现在会一致禁用。
- 修复 Skill env 更新弹窗重新扫描时会接受截断结果、让用户误以为所有关联 Skill 都已覆盖的问题；截断扫描现在会保留重扫提示并阻止使用部分结果更新。
- 修复重新安装 Skill 时在配置弹窗明确替换密钥后，目标 `.env` 仍保留旧值的问题；现在只覆盖用户确认替换的键，其它已有配置继续保留。
- 修复从本地身份隐式更新 Skill Repository 时只按“新建仓库”检查权限、可能绕过目标仓库限制且审计对象错误的问题；现在会按实际仓库逐次授权和记录，过期关联退回新建时也会单独检查。
- 修复 Workflow 普通导出和未来版本原文导出可能沿符号链接覆盖其他本地文件的问题；导出现在会拒绝符号链接和写入前发生变化的目标。
- 修复 Document Template 开启覆盖时可能沿符号链接写入其他本地文件的问题；App、MCP 和 Workflow 节点现在都会拒绝符号链接输出路径。
- 修复本地 Skill 安装器在准备安装来源时可能绕过本地文件读取授权与审计的问题；读取 SKILL.md 和附件前现在会先执行统一权限检查并记录结果。
- 修复 Workflow 文件和文件夹参数会裁掉合法路径首尾空格的问题；运行、预设和参数默认值现在会保留用户选择的完整本地路径。
- 修复 Workflow Runner 查询活跃运行时可能忽略 workflowId、返回另一工作流运行信息的问题；内存状态与归档快照现在执行相同边界校验。
- 修复 Agent 会话重命名成功后，迟到的历史、SDK session 或用量保存可能把标题回滚的问题；非标题写入现在会在提交时保留最新标题状态。
- 修复 Secrets App 保存密钥失败时主进程 IPC 日志可能记录明文名称、值和描述的问题；失败日志现在只保留字段是否提供的布尔摘要。
- 修复 IDE 管理复制 Skill 时会把源目录中的符号链接带入目标编辑器目录的问题；复制前现在会按安全边界检查完整目录，并在发现链接或异常规模时停止。
- 修复旧仓库 Workflow 已成功找回但恢复状态写入失败后会被误报为 ID 冲突的问题；恢复过程现在会保存中间状态，并在下次启动核对文档摘要后自动修复状态。
- 修复 Skill Repository 清理旧身份时可能删除刚由其它操作写入的资源仓库关联的问题；清理前现在会复查原始文件快照，发现并发变化时保留文件并提示重新扫描。
- 修复 Skill 环境变量关联扫描在目录权限被拒绝或读取异常时仍提示成功的问题；失败扫描会保留重试入口，不再让已安装 Skill 静默继续使用旧配置。
- 修复 Workflow 多资源参数从文件改为文件夹或反向修改后仍沿用上次运行值的问题；运行弹窗会提示重新选择，不再到运行阶段才失败。
- 修复 Agent 停止待回答问题后当前会话仍显示旧卡片状态的问题；取消状态写入后会立即刷新会话时间线。
- 修复升级后旧仓库工作流恢复失败、版本过高或 ID 冲突时列表没有提示的问题；现在可直接查看恢复诊断和处理建议。
- 修复反复打开 Synapse Skill 安装与更新入口后主进程持续保留旧安装源的问题；检查、关闭或安装流程结束后会及时释放缓存。
- 修复 Workflow 定义检查会放过缺少数据版本信息、随后保存才失败的问题；检查与完整定义更新现在执行相同的版本元数据校验。
- 修复 Skill 发布会把 `.ENV.EXAMPLE` 等大小写变体误当成标准配置声明的问题；现在只有根目录精确命名的 `.env.example` 会进入发布与安装配置流程。
- 修复 Agent 提问中的重复选项会被合并为同一个答案的问题；重复选项现在会被识别为无效请求，不再展示无法区分的回答卡片。
- 修复从本地目录发布 Skill 时可以保存超大 `.env.example`、直到安装才失败的问题；超过 1 MiB 的配置声明现在会在导入前直接拒绝。
- 优化 Workflow 运行参数预设加载：打开弹窗不再逐项访问所有已保存资源，只在选中预设时检查对应路径，避免大量或离线路径拖慢弹窗。
- 修复 Workflow 单文件或单文件夹默认路径可以通过检查和保存、运行时才失败的问题；路径不可访问或类型不匹配现在会在保存前提示。
- 修复 Agent 可在未经过 Terminal 权限检查和审计的情况下创建或重命名终端分组的问题；这两类持久化操作现在与其它 Terminal MCP 操作使用同一安全边界。
- 修复 Skill 密钥更新队列在写权限被拒绝前仍会打开可写 `.env` 句柄的问题；现在权限拒绝会发生在取得任何文件写入能力之前。
- 修复 Skill Repository 上传或重试关联时会无上限读取本地身份文件的问题；超过 64 KiB 的当前或旧版身份文件现在会在解析和云端写入前明确拒绝。
- 修复发布、上传或本地安装 Skill 时会在应用附件限制前无界枚举源目录根层的问题；超过 1,000 个根层条目现在会快速返回明确错误。
- 修复 Agent 对话刷新后已完成、取消、失败、超时或中断状态可能消失的问题；终态与对应模型用量信息现在会完整保留。
- 修复 Skill 上传到云仓库后本地关联失败只显示笼统提示的问题；现在会显示具体失败原因，便于处理权限、磁盘或并发冲突后重试。
- 修复 Workflow 单文件或单文件夹参数改换类型后，旧运行预设仍会被当作可用的问题；选择预设时会先提示类型不兼容，不再等到运行后才失败。
- 修复 Workflow Call 可选择迁移失败或未来版本工作流、加载失败却没有提示的问题；受保护工作流现在不可选择，已有引用也会显示具体原因并清空旧参数。
- 修复编辑器扫描和 Skill 快速发布会无上限读取本地关联文件的问题；异常大的 `.synapse.json` 现在会在 64 KiB 边界处安全拒绝。
- 修复 Agent 自动标题与手动重命名同时发生时，自动标题仍可能覆盖用户会话名的问题；同一会话的标题写入现在会按顺序提交并保留最后的手动选择。
- 修复 Workflow MCP 参数更新 schema 会放行文本、数字或选项参数的 `allowMultiple` 字段、随后运行时才拒绝的问题；非法组合现在会在工具参数校验阶段拦截。
- 修复 Skill Repository 云端变更成功后，浏览器或深链打开失败会把整个操作报告为失败的问题；现在保留成功结果并返回非致命打开警告，避免误重试上传、可见性修改或安装会话创建。
- 修复 Codex 主目录存在同名外部 Skill 时，兼容目录中的 Synapse Skill 会被安装状态检测遮蔽的问题；状态检查和批量更新现在会定位真实安装目录。
- 优化内容页单个全局 Skill 或 Rule 的卸载流程，只扫描目标编辑器的全局目录，不再遍历其它编辑器和所有已配置项目。
- 加固旧仓库 Workflow 自动找回：版本文件会通过防符号链接的文件句柄读取并复查同一快照，文件在读取期间被替换时会跳过并继续尝试旧版本。
- 修复 Agent timeline 运行时回退日志会记录原始会话标识的问题；诊断信息现在保留必要关联字段，但会脱敏 `sessionKey`。
- 修复 Workflow Call 切换子工作流后仍保留旧参数映射的问题；面板加载新子工作流时会只保留当前参数的映射，卡片摘要与实际运行配置保持一致。
- 修复损坏、不可读或不安全的 Skill `.env` 会被误算成任意密钥关联项的问题；无法确认键名的文件现在单独作为扫描警告，不再影响关联数量和更新列表。
- 修复 Skill env 关联扫描会把普通子目录计入 Skill 数量上限的问题；受信任目录中的非 Skill 文件夹不再导致后面的真实关联项被提前截断。
- 修复 Workflow Call 的文本、数字或选项 value 绑定可引用不存在的父工作流参数并保存的问题；定义检查和保存现在会在运行前阻断无效引用。
- 修复本地 Skill 安装源会长期保留完整附件并随重复选择持续占用主进程内存的问题；成功安装后会立即释放，未完成来源也受 TTL、条目数和总字节预算限制。

## 技术调整
