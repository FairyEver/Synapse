# Pending Release Notes

## 新增功能

- 系统设置新增“MCP”分类，可集中查看 MCP Server 状态、连接地址和各客户端注册信息，并支持打开配置文件、注册或重新注册。
- Git 工作台新增远程分支发现与检出：默认显示本地缓存的远程分支，可手动刷新，并从远程分支创建本地跟踪分支。
- Git 工作台新增按文件丢弃改动；操作前会再次确认，新增和未跟踪文件只移入系统废纸篓。

## 功能优化

- MCP 注册管理统一迁移到系统设置；本地数据库中不再显示重复入口。
- Git 工作台现在可以稳定读取超大改动列表，并在仅展示部分文件时明确显示完整改动数量。
- Git 工作台现在会自动刷新外部改动，提交历史支持继续加载，并能正确识别完整未跟踪文件、重命名路径、RSA SSH 公钥和已删除的上游分支。
- Git 工作台现在能正确展示替换文件和合并提交差异，并支持名称包含 `/` 的远端。
- Git 工作台现在能识别进行中的 merge、rebase、cherry-pick、revert 和 bisect，在外部操作完成前保护提交、丢弃、切换与同步入口。
- Git 在大型仓库中的被忽略文件保护和复杂状态刷新改为限流处理，避免路径列表或并发任务过多导致读取失败。
- 云盘本地同步现在按账号完全隔离，支持初始化中断恢复、游标过期后的完整校验、传输进度和断网重试；已有同步绑定和排除规则在升级后保留。
- 云盘同步状态中心现在区分离线、加载失败、重试、冲突和终止错误，可查看传输进度并直接打开对应云端位置；离线期间保持只读，联网后自动追平。
- 云盘同步的推荐排除规则对新绑定默认开启，`.gitignore` 默认不导入；导入前可预览原始规则，否定、转义、目录和通配符规则都能正确生效。

## 问题修复

- 修复云盘同步状态模块加载失败可能导致桌面界面无法打开的问题。
- 修复 Agent 长对话只能查看最近一部分记录的问题；现在可以向上加载完整历史，刷新和上下文压缩后已加载内容不会消失。
- 修复 Git 文件在提交确认后被外部修改仍可能一并提交的问题，并避免选中文件提交影响无关的预暂存内容。
- 修复切换分支或同步时可能静默覆盖被忽略文件的问题；发生路径冲突时会在修改工作区前停止并列出文件。
- 修复同一路径同时出现暂存删除和未跟踪文件时，预览、选择、提交与丢弃结果不一致的问题。
- 修复内容仓库同步失败时可能误中止其它 Git 客户端 rebase 的极端竞态，并限制异常历史分页请求。
- 修复未解决冲突路径解析错误、外部 merge 可能被无关提交意外完成，以及普通文件与符号链接互换无法正确处理的问题。
- 修复克隆地址中的账号、密码、Token 或查询参数可能写入仓库配置的问题，并加强 Git 凭据与 SSH 主机校验子进程的输出、超时和并发写入保护。
- 修复长时间连续执行 Git 操作时已结束任务状态持续占用内存的问题。
- 修复 Git 克隆取消或失败后遗留半成品目录、导致同名重试被阻止的问题；完整仓库克隆成功但列表登记失败时会保留仓库并提示恢复方式。
- 修复本地已有待推送提交但缺少同步队列记录时，推送失败状态在重启后丢失、无法继续自动重试的问题。
- 修复内容文件已写入但 Git 提交失败时可能遗留未保存历史、附件、图标或永久删除结果的问题；失败后会自动恢复，异常中断后也会继续处理。
- 统一系统设置左侧分类的文字字重，避免全大写英文分类在视觉上显得异常加粗。
- 修复 Git 差异预览过度信任界面参数、可能读取非当前改动文件的问题。
- 修复直接访问平台管理后台根地址 `/admin/` 时误加载普通用户控制台并显示 404 的问题。
- 修复 HTTP 仓库克隆成功后界面误报失败、分叉分支执行必败同步、Worktree 分支名异常，以及提交详情参数校验不足的问题。
- 修复云盘同步可能上传变化中的文件、同大小内容误判一致、账号切换串用绑定、初始化未完成却显示成功、停止后仍继续写入、目录冲突恢复不完整、根目录缺失误判删除，以及失败状态无法自动恢复的问题。
- 修复云盘同步在暂停、监听漏报或规则重新纳入后无法追平，部分上传遗留重复文件或云端残骸，旧冲突记录类型判断错误，以及 Unicode、Windows 路径和符号链接替换带来的同步风险。

## 技术调整
