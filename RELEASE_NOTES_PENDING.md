# Pending Release Notes

## 新增功能

- 知识库资料管理支持上传文件夹、批量导出，以及拖拽整理文件；文件夹结构会保留，同名文件会自动保留副本，避免覆盖。

## 功能优化

- Agent 对话完成后会显示新的用量统计卡片，集中展示本轮费用、会话累计费用、各类 token 累计、本轮增量和对应人民币费用；token 数字 hover 停留 1 秒后会说明累计和本轮新增含义，复制时也会生成可读摘要。
- Agent 用量统计卡片的 token 分类会按含义和成本影响配色，输入、输出、缓存读写更容易区分。
- Agent 和用量分析的费用估算会统一规整小数精度，避免浮点计算尾巴影响展示和汇总。
- 知识库资料管理进入多选后可以一键全选当前可见文件，搜索过滤时不会误选隐藏项。
- 用量分析内置价格补全了 GPT、Claude、DeepSeek、Kimi、GLM 和 MiniMax 等常用编码模型，新建或重新扫描用量时费用估算更完整。

## 问题修复

- Agent 对话在持续收到工具调用或 SDK 事件时，会保持停止按钮可用，不再退回到禁用的发送按钮导致无法打断循环。
- Agent 用量统计卡片不再显示没有真实数据来源的“最近 5 轮”柱状图，新会话只展示本轮实际统计。
- Agent 历史对话里的用量统计会保留本轮 token 明细，重新打开对话后不再把增量和占比显示成横线占位。
- Agent 用量统计卡片的费用会按 Synapse 价格规则匹配当前模型估算，不再把第三方模型误按 Claude SDK 自带价格显示；未知价格的模型只显示 token。
- 使用第三方 Anthropic-compatible 模型时，如果模型反复提交相同的 Todo 更新，Agent 会更早停止重复工具回合，不再一直重试到耗尽回合数。
- 知识库资料管理拖动已上传文件整理位置时，不再误显示“松开上传”的上传提示。

## 技术调整
