# Pending Release Notes

## 新增功能

- 工作流现在可以导出为一个 `.synapse-workflow` 分享文件，自动包含递归引用的子工作流；同事导入时可按六步流程批量映射模型、项目、文件和运行环境。
- 同一来源的新版分享包会原地更新已导入工作流，保留运行历史和参数预设，并提供一次撤销；重复导入同一版本不会产生副本。

## 功能优化

- 模型档位统一显示为 `#1` 至 `#4`，选择模型和配置模型时可悬停查看原来的主模型、Opus、Sonnet、Haiku 名称。
- 智能体现在在新建对话时选择，并作为该对话的固定身份；名称和智能体采用紧凑的左右布局，模型列表移除表头并增加可用高度，绑定模型提示与输入框保持一致高度，普通和未绑定智能体可直接选择模型，绑定模型则自动锁定。
- Agent 的侧边栏新增、闲置提醒、权限模式新会话和独立窗口新增统一使用同一创建弹窗；智能体失效时会保留历史并引导新建对话，不再静默切回普通模式。
- 工作流导入的六步面板重新整理了内容层级，短步骤会随内容收起，较长的风险和映射列表可在固定操作区内滚动查看。
- 导出前会提示敏感配置、高风险权限、本地文件和未包含的 Automation；本地文件不会被打进分享包，需要分享者另行转交。
- 更新工作流时会保留仍有历史或外部引用的旧子工作流，只清理可安全删除的子工作流；不兼容的关联 Automation 会停用但保留配置和历史。
- 删除已导入的入口工作流时，默认可同时清理没有外部引用和运行历史的子工作流；需要保留的子工作流会提前说明。
- 旧版 V1–V3 工作流文件也会进入新的六步预检流程；导入、更新、撤销和整组删除发生中断时会自动恢复到完整状态。

## 问题修复

- 修复 Windows 安装资源仓库 Skill 时附件路径被误判为越界、安装后只剩主文件的问题；附件无法完整复制时不再报告成功或覆盖已有 Skill。
- 删除仍被其它工作流调用的工作流时现在会被阻止，避免留下失效的子工作流引用。
- 工作流删除失败时会显示具体原因，便于解除引用、等待运行结束或按提示处理异常数据。

## 技术调整
