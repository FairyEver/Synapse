# Pending Release Notes

## 新增功能

- 云盘分享支持可编辑权限。创建分享时可以选择获取链接的人可阅读、获取链接的登录用户可编辑，或指定邮箱用户可编辑；文字类文件可在代码编辑器里保存修改，并生成新的文件版本。遇到他人先保存的新内容时，会提示用户下载自己的本地编辑版本或重新加载。
- 云盘新增“公开素材”和“回收站”固定入口。图片可以生成稳定公开链接；删除后的云盘文件和公开素材会先进入回收站，恢复后原链接继续可用。
- Synapse Drive MCP 新增公开素材直链和回收站工具。Agent 可以按“上传到图床”“生成直链”等请求上传图片公开素材、替换或恢复公开链接，并管理用户可见的云盘回收站。
- 管理后台用户列表新增用户名和管理员备注。管理员可以为用户维护仅后台可见的备注，便于区分账号来源、用途或服务状态。
- 管理后台云盘新增公开素材和存储视图。管理员可以查看全部公开素材、访问日志、历史版本，并下载或恢复需要处理的云盘文件。

## 功能优化

- Git 问题诊断日志更完整。导出 Syslog 后，开发者可以更快定位仓库状态异常、同步失败、认证权限和网络问题，同时日志会避免记录提交正文、diff、凭证和敏感远端地址。
- 内容商店列表会批量获取安装量和最新版本号，大页列表不再因为逐条查询拖慢打开速度。
- 知识库资料管理窗口会按需加载文件夹树，不再在打开大资料库时自动扫描所有根级文件夹。
- Git 工作台会在进入历史页后再加载提交列表，并且只在选择提交时读取完整 patch，打开改动页更轻量。
- Database MCP 的操作日志读取会限制单次返回数量，避免长期使用后一次请求拉取过多历史记录拖慢本地进程。
- Git 仓库列表在 Windows 上会识别大小写不同但指向同一目录的路径，避免同一个仓库被重复登记。
- Workflow MCP 内置 Skill 会正确说明脚本节点的默认 shell：Windows 留空时使用 `cmd`，macOS/Linux 使用 `posix`。
- Usage Analysis 的原文搜索加载更多会从下一批会话继续扫描，并对输入做短暂去抖，减少重复读取历史 JSONL 的开销。
- Workflow MCP 指南补齐 Claude Code 节点说明，Agent 通过指南创建本机 Claude Code 工作流时不再误以为只有 Codex 节点可用。
- 内容商店 Skill 编辑器会在选择文件时立即拦截超出大小上限的附件，避免先读入大文件再到保存阶段失败。

## 问题修复

- Drive MCP 取消分享现在可使用记录 id 或公开 shareId，按分享结果里的 `shr_...` 也能正确关闭链接。
- 工作流 Prompt、Switch 和 Script 节点会在保存或运行前检查已删除的项目引用，不再等到执行期才失败。
- 拖拽多个本地文件夹上传到云盘时，会按真实文件数累计上限，不再先完整扫描超量目录后才失败。
- 网盘浏览“加载更多”会忽略同一分页的重复触发，不再把同一批文件夹子项追加两次。
- 知识库资料导出会阻止把目标目录选到当前 `.raw` 资料目录内部，避免导出时递归复制自身。
- 后台云盘删除审计写入失败时，服务端日志会脱敏错误摘要，避免 token、Cookie 或本地路径进入日志。
- 文件夹上传在全部文件都失败时，会清理本次新建的根目录，避免刷新后留下空文件夹。
- 网盘公开链接列表不再显示已过期分享，避免复制或打开必然失效的链接。
- 网盘整理应用后会在审计记录中保留脱敏的移动摘要，方便追踪哪些条目从哪个父级移动到了哪里。
- Git 克隆和添加本地仓库失败时，弹窗会直接显示失败原因，不再让用户停在表单里却不知道哪里出错。
- 云盘分享页切换到代码编辑时，文件操作浮动按钮不再遮挡保存按钮。遇到文件已有新内容时，用户可以正常触达“下载本地版本”和“重新加载”操作。
- 云盘列表中的上传中、上传失败和删除中条目不再提供必然失败的预览或进入文件夹操作，状态按钮会和后端可用性保持一致。
- 网盘浏览加载更多失败后切换目录，会清空上一目录的分页错误提示，不再把旧错误显示到新目录。
- 管理后台用户、团队和邀请列表隐藏列后，可以在当前页面直接恢复列显示，不再需要刷新页面。
- 管理后台云盘列表中的删除中条目不再显示必然失败的删除按钮，避免管理员触发无效操作。
- 管理后台邀请列表改为展示服务端计算的邀请状态，不再受管理员本机时间偏差影响。
- Code X 节点配置面板的帮助文案更克制，只保留必要配置提示，不再展示实现说明和过长风险解释。
- Code X 关闭保存调试文件后，运行历史不再展示已删除的临时 last-message 路径。
- 云盘分享中的 HTML 文件不再能以分享访问者的会话执行脚本或访问同源 API，降低恶意分享页面读取账号数据的风险。
- 生产环境和本地开发环境直达或刷新 `/drive` 时会停留在网盘根页面，不再跳回控制台首页。
- 云盘分享页从“登录后编辑”进入登录页后，会正确回到原分享链接，不再跳到控制台下的无效分享地址。
- 网盘整理会阻止把两个同名文件夹移动到同一目标目录，避免整理后出现难以区分的重复文件夹。
- 修复本地 Drive 存储模式下，文件上传完成后创建历史版本可能失败的问题。使用本地 fallback 存储时，云盘文件现在可以正常完成上传、分享、在线编辑和版本保存。
- 工作流 Code X 节点的运行历史会脱敏指令中的 token、Cookie、Authorization 和本地路径，避免敏感内容进入快照文件。
- 工作流实时事件和运行状态会脱敏 Code X 节点结果，避免 token、Cookie、Authorization 和本地路径通过 live 通道泄露。
- Workflow MCP 读取工作流定义和运行历史时会经过权限检查并写入审计记录，避免敏感工作流内容被静默读取。
- Workflow MCP 创建节点时会在工具契约层要求提供节点配置，不再先接受空配置再在保存时失败。
- MCP 工具返回 `ok:false` 时会标记为工具调用失败，不再把 Drive、Workflow 等调度失败包装成成功响应。
- Code X 历史运行重新运行时，不会把历史快照里的 `[redacted]` 配置覆盖值当作真实参数执行；可用时会改用当前工作流定义，否则会提示无法安全重跑。
- 自动化触发包含 Code X 的工作流时，Code X 进程审计会保留自动化执行身份和运行编号，不再退回系统身份。
- Agent 列出网盘目录树时会由服务端分页返回结果，不再为了第一页目录先把整个网盘加载到应用内存。
- 只包含 Code X 的工作流导入时不再要求无关的模型供应商映射；没有配置供应商的环境也可以正常导入这类工作流。
- 工作流导入确认时会校验文件是否仍是预览时的内容；如果文件在确认前被替换，会提示重新选择，不再导入未审阅过的包。
- 工作流调用节点会在子工作流被删除后显示失效状态，并在保存或运行前阻止继续使用已不存在的子工作流。
- Automation 和 Data Store MCP 变更审计会先清理外部传入的标识，避免非法参数或权限拒绝时把 token、路径等敏感片段写入审计记录。
- 安装已下载更新时会尊重知识库存储迁移的退出拦截；迁移进行中会先聚焦迁移窗口，不再直接重启安装更新。
- 知识库存储迁移在切换后校验失败时会立即恢复旧位置，避免失败后继续写入未验证的新存储目录。
- 知识库资料导出会参与存储迁移互斥，迁移期间不会继续从旧目录导出过期或不完整的资料。
- 知识库存储迁移进行中会阻止继续向已有知识库 Agent 对话发送消息，避免迁移期间重新启动会话写入旧目录。
- 知识库资料管理添加 URL 失败时会保留弹窗和输入内容，方便修正后重试，不再直接清空刚输入的地址。
- 知识库 `/wiki-ingest` 会在收尾阶段为新页面补齐稳定地址，Windows 环境不再依赖 Bash 地址分配脚本才能完成摄入。
- 知识库 `/wiki-ingest` 会把大量新资料拆成有边界的批次发送给 Agent，不再因为一次性追加上千个来源路径而拖慢或卡住摄入。
- 知识库 `/wiki-ingest` 模板会跟随 Synapse 的 SHA-256 增量判断，不再指导 Agent 用旧的本地 hash 命令重复处理已摄入资料。
- 通过 Content MCP 创建或更新 Skill 时，附件不再允许覆盖 `SKILL.md` 或 `.synapse.json`，避免安装后主文件或 Synapse 跟踪信息被破坏。
- Windows 上从同一个本地 Skill 反复发布到内容商店时，会按大小写不敏感路径复用未发布草稿，不再因为路径大小写变化生成重复草稿。
- Windows 上从内容商店安装标题为系统保留名的 Skill 时，会自动使用安全目录名，不再在写入编辑器目录时失败。
- Windows 上运行工作流里的 Codex / Claude Code 节点时，会正确解析 npm 安装的 `.cmd` 启动器，不再因为裸命令无法启动。
- 知识库存储迁移恢复失败后会持续阻止知识库操作并保留阻塞提示，不再允许关闭提示后继续写入未确认的知识库位置。
- 自定义知识库存储会检查真实数据目录是否仍可用，外接盘或同步目录丢失 `knowledge-bases` 时不再误判为可用并创建新的空知识库目录。
- Windows 上恢复默认知识库存储位置时，会识别仅大小写不同的等价路径，不再把目标知识库目录当作旧位置清理。
- Automation 列表和详情查询不再持久化禁用配置失效的自动化，避免只读查看或 Agent 盘点绕过权限审计修改用户配置。
- Automation MCP 读取自动化、运行历史、运行状态和 Webhook 列表时会经过读取权限检查并写入审计，不再静默枚举账号级自动化信息。
- Automation MCP 内置 Skill 的创建示例不再写死 POSIX shell。Agent 创建命令或脚本自动化时会优先使用平台感知默认 shell，避免 Windows 自动化被示例带偏后运行失败。
- 自动化停止请求超时等待运行落库时，不再误报“已停止”；界面会提示停止请求已发送，并保留运行中的真实状态。
- 私人令牌设置页新增、编辑或删除变量时会走 `secret.write` 权限检查和审计，不再绕过敏感变量写入记录。
- Bridge 外部适配器不能再注册为本地 renderer 平台，使用保留平台前缀创建的会话也会按外部 Bridge 身份处理。
- Agent 自定义执行命令返回 stdout/stderr 前会脱敏 token、Authorization 和 Cookie，避免 side-channel token 进入对话记录。
- Agent slash 命令会保留 Windows 路径里的反斜杠，`/show` 和自定义执行命令工作目录不再把 `C:\...` 路径解析坏。
- Windows 上 Agent 消息里的本地路径会渲染为可点击引用，支持盘符路径、UNC 路径和反斜杠相对路径。
- Windows 上初始化本地仓库时，会按大小写不敏感规则识别用户目录和 Synapse 源码目录，避免高风险目录被误放行。
- Git 变更列表快速切换文件时，旧 diff 请求返回后不会再覆盖当前选中文件的差异内容。
- 带密码的网盘分享页跳转登录时，会先移除分享密码参数，避免密码进入登录页 URL 和浏览器历史。
- Markdown/MDX 分享预览里的“登录后编辑”会跳转到后台 `/console` 登录页，不再误跳到根路径登录地址。
- 公开素材上传准备阶段生成的临时 backing file 不再出现在普通云盘列表、统计和树接口里，避免 Agent 或用户把未完成图床上传当成普通文件处理。
- Drive MCP 内置 Skill 的 API 参考补齐普通文件、分享、版本、用量和整理工具，Agent 查附件时不再误以为这些云盘能力没有字段说明。
- 知识库 `/wiki-ingest` 会按 Agent 报告的页面做定向校验，不再在每次摄入前后全量读取并哈希整个 wiki。
- 普通网盘接口遇到登录过期时会触发统一登录恢复流程，历史版本、上传、分享和回收站操作不再只停留在本地错误提示。
- 公开素材上传或替换失败、取消、过期时会释放对应的 session URL 缓存，避免长期运行的服务进程积累无效上传状态。
- 设置诊断会保留知识库存储详情，导出的诊断报告不再丢失存储模式、根目录、运行目录数量和旧路径引用统计。
- Automation MCP 列表会先按启用状态、范围和 limit 收敛结果，再计算运行态摘要，避免小列表请求仍处理全部自动化。
- 内容商店 Skill 文件路径会提前拒绝 Windows 不支持的字符和保留文件名，避免发布后 Windows 客户端安装失败。
- Git 环境诊断会真正检查 SSH 客户端是否可执行，不再把 Git 可用误显示为 SSH 可用。
- 知识库资料导出取消或全部跳过时，不再显示“已导出”，会按真实结果提示跳过或静默结束。
- 内容商店本地安装成功但完成记录响应丢失后，重试会识别同一客户端已完成记录，不再被已消费会话永久拒绝。
- 从内容商店点击安装唤起已关闭的桌面端时，只打开独立安装窗口，不再额外弹出无关的主窗口。
- 知识库存储诊断会有界检查旧路径引用，不再为了普通诊断递归读取所有 wiki 文本。
- 管理后台查看公开素材列表、详情、访问日志和历史版本时会写入审计记录，便于追踪管理员读取跨用户素材元数据的行为。
- Skill 下载 zip 会使用标准 `SKILL.md` 作为主文件，解压后可直接作为 Skill 目录复用。
- Drive MCP 重整预览现在允许把条目移动回根目录，内置 Drive skill 也会提示使用 `targetParentId: null`。
- Agent 会话导出会先校验会话归属再打开保存对话框，默认文件名也不再回退使用 sessionKey。
- Skill 名称校验会和编辑器安装规则保持一致，不再允许创建带点号的 Skill 名称导致安装后无法识别。
- 回收站列表会直接分页查询被删除的根条目，不再为了返回第一页而加载整棵已删除文件夹子树。
- Drive MCP 创建分享链接时，审计记录会保留内部分享记录 ID，不再持久化公开分享链接里的 shareId。
- 新建本地仓库会提前拦截 Windows 非法文件名和保留名，避免到目录创建阶段才失败。
- 新增内置 Synapse 价格规则 MCP skill，AI 管理模型价格时会按 ruleId、人民币每百万 token 和不重算历史用量的规则执行。
- 模型价格 MCP 的权限检查和审计会脱敏外部传入的规则 ID，避免异常入参把 token 片段写入审计记录。
- 从设置页整库导入数据库成功后，已打开的数据库页面会自动刷新，不再停留在导入前的数据。
- Usage Analysis 今日数据自动刷新临时失败后，会允许后续自动重试，不再在 10 分钟内只能手动刷新。
- Usage Analysis 启动时不再同步递归扫描 Claude Desktop 历史会话目录；刷新用量时会按上限发现 Desktop 会话根目录。
- 模型价格估算会优先匹配更具体的模型版本规则，百炼等预设不再被前面的泛化规则抢先命中。
- 网盘上传完成、创建分享等操作已生效后，即使审计日志写入失败，也不会再向用户误报操作失败。
- 知识库资料重命名、移动、删除和摄入收尾会串行同步资料清单，避免并发操作用旧 `.raw/.manifest.json` 覆盖彼此。
- 配置备份导入脱敏备份时会保留当前机器已有的同名变量值，不再把本机 token 或私有变量静默清空。
- Drive MCP 公开链接列表不再返回分享密码或带密码链接，避免普通列表查询把分享密码写入 Agent 结果和会话记录。
- 知识库地址分配器和内置 Bash helper 会使用同一个项目本地锁，避免并发摄入时分配出重复页面地址。
- 知识库 `/wiki-ingest` 会在资料清单损坏时提前停止并提示修复，不再让 Agent 先写 wiki 页面后又跳过 manifest 写回。
- 知识库 `/canvas` 现在会按内置说明获得必要的 shell 工具权限，添加 URL 图片、本地文件、PDF 和导入近期生成图片时不再因为工具契约缺失而卡住。
- 公开素材访问日志会在写入和展示前遮蔽来源 URL 中的分享路径、公开素材 ID、密码和 token 参数，避免敏感 Referer 长期留在后台记录里。
- Git 工作台同步会在 fetch 后重新判断远端落后状态，不再因为同步前的旧状态为 0 而漏拉远端提交却提示已同步。
- 云盘文件底层对象删除失败后会进入定时重试清理，不再长期停留在已删除但存储对象未清理的 pending 状态。
- 本地网盘删除当前文件时会忽略同名 legacy 版本目录，不再把已经删除成功的对象误标记为待清理。
- 工作流触发托管知识库 `/wiki-ingest` 时会和界面会话一样执行资料预检与 manifest 收尾，不再出现 wiki 已更新但 `.raw/.manifest.json` 未同步的情况。
- 工作流里的 Script、Codex 和 Claude Code 节点选择托管知识库项目时，会使用真实知识库运行目录，不再把 `synapse-kb://...` 虚拟路径当作 cwd。
- Windows 上运行诊断时会用 PowerShell 检查 Codex 运行态，不再因为缺少 POSIX `ps` 而漏掉旧 Codex 会话需要重启的提示。
- 管理后台网盘存储概览改为由数据库直接聚合统计，不再为几个汇总数字把所有文件、公开素材和历史版本行读入服务端内存。
- Markdown 文件默认使用 MDXeditor 打开，不再误落到代码编辑器；可编辑文件会显示保存状态，并在版本冲突时提示下载本地版本或重新加载。
- Markdown 文件在 MDXeditor 和代码编辑器之间切换时，不再因为编辑器初始化同步而误显示为未保存。
- 管理员现在可以正确删除公开素材文件；公开素材上传入口的可选图片类型也会跟随后端规则保持一致。
- 管理后台公开素材详情的历史版本列表支持翻页，替换超过 20 次的旧版本也可以继续查看和下载。
- 管理后台公开素材详情的访问日志和历史版本加载失败时会显示错误和重试入口，不再误显示为空列表。
- 网盘 Markdown 预览会按文本预览上限读取和渲染，不再因为打开超大 Markdown 文件而让服务端全量加载整份文件。
- Data Store 表导入导出会提前限制行数、文件大小和单元格体积，导出时不再一次性读取整张表，避免大表或大 BLOB 拖垮桌面主进程。
- 删除工作流时会同步清理该工作流历史运行关联的 Code X 调试文件，避免 workflow 删除后在本地 userData 留下孤立调试内容。
- 管理后台设备列表会把在线未落库客户端和已落库设备放在同一套分页里计算，不再出现第一页超出每页行数或后续页空转的问题。
- 管理后台设备页收到当前页外设备的实时事件时会重新拉取分页数据，不再把页外设备插入当前页。
- 设备列表按设备名、平台、版本或首次连接排序时，服务端会保留当前排序语义，不再把结果重新改回最近在线排序。
- 团队邀请、加入和成员操作失败时，即使审计日志写入异常，也会保留原本的业务错误提示，不再被审计存储错误覆盖。
- 修改仓库本地路径后，后台目录监听会同步切换到新路径，不再被旧目录删除误触发仓库状态刷新。
- Drive MCP 上传本地文件夹时会提前限制文件数量和目录深度，不再无上限递归扫描大目录后才失败。
- 网盘历史版本弹窗支持继续加载更多版本，超过前 100 条的旧版本也可以在界面里下载、恢复、保留或删除。
- 知识库资料管理器通过选择文件上传时也会遵守文件数量、单文件大小和总大小限制，不再绕过文件夹上传的保护。

## 技术调整
