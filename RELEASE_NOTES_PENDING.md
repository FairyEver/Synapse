# Pending Release Notes

## 新增功能

## 功能优化

- 手机端录音的**锁屏卡片和灵动岛按 Apple 的实时活动规范重做**：录音名让位、计时成为主角，两者不再抢同一行；波形与 App 内是同一个粗细和间距（原先锁屏上那排又粗又扁，和 App 里不是一条波形）；锁屏内容边距改为规范里的 14 点；灵动岛上那圈过重的白色描边去掉，交给系统；图标文字统一用系统的黑白色，不再出现和应用不一致的蓝色。

## 问题修复

- 手机端**录音锁屏卡片和灵动岛上的「取消」「完成」按下去没有反应**：这两个按钮的动作原先落到了 Widget 扩展进程里，而那里没有录音机，按了等于什么都没发生。现在改为在 App 进程里执行，按下去就是真的取消或保存。

## 技术调整
