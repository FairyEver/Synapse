# Pending Release Notes

## 新增功能

## 功能优化

- 终端顶部会话标签支持右键菜单，可直接重命名或关闭会话。
- 终端左侧的分组现在可以直接拖拽调整顺序，顺序会被记住，重启后仍然生效；分组「⋯」菜单也提供上移 / 下移。
- 对话侧栏的项目分组现在可以直接拖拽调整顺序，顺序会被记住；「本地对话」固定在第一项，「已归档」固定在最后一项。
- 终端会话支持复制深度链接：在左侧会话列表或顶部会话标签的右键菜单中选择「复制深度链接」，即可把链接交给其他对话或工具，直接定位到 Synapse 中对应的终端会话；会话结束或 Synapse 重启后链接不再有效。

## 问题修复

- 修复终端里 emoji 与状态符号（如 `⏺`、`◼`、`⚡`）被按 1 格宽度计算，导致运行 Claude Code / Codex 等 Agent 时中文与 emoji 混排的行出现错字、重影、乱码的问题。

## 技术调整
