# Pending Release Notes

## 新增功能

## 功能优化

- 工具、工作流和云盘页面统一为紧凑表格与单层内容面板样式，顶栏、间距和操作区与自动化页面保持一致。
- 工具页列表副标题字号下调，标题和说明层级更清楚。
- 自动化页面列表改为更紧凑的表格样式，副标题字号也下调一号；顶栏、加载、空状态和错误状态与云盘等内部工具页面更一致，长内容在窄窗口下也更稳定。
- 模型与供应商页面的默认设置区域改为铺满内容区，供应商操作按钮尺寸也统一为同一套控件节奏。
- 模型与供应商页面顶部的默认设置控件收窄并靠右，避免在宽窗口下拉得过长。
- 选择供应商和模型弹窗收窄了右侧模型列表区域，整体尺寸更紧凑。
- 顶部导航顺序调整为技能、规则、提示词、对话、定时、自动化、工作流、云盘、工具、CC、Codex、设置，并把显示文案和默认入口集中到桌面端配置里。
- Agent 对话空闲超过 1 小时后，会在输入框内轻量提示继续可能无法复用缓存，并可直接新建同项目同模型的对话。
- Agent 对话消息滚动到输入框下方时会自然渐隐，长对话里输入区和消息区的层次更清楚。
- 模型与供应商里的默认设置区域移除了长对话金额和 token 阈值配置，提醒规则改为固定的一小时空闲判断。
- 部署流程会集中备份远端 `.env`、Postgres 角色配置、在线数据库快照、最终切换前数据库快照，并在启动新服务前验证最终备份可恢复；未配置 COS 时，本地 Drive 文件也会落到持久目录并在部署窗口备份。
- 服务端 COS 配置拆分为用户云盘文件桶和后台备份桶，避免上传文件与备份归档混用同一个存储桶。
- 管理后台备份升级为轻量灾备包，除数据库外会包含 PostgreSQL globals、Drive COS 对象清单、备份校验信息和恢复说明，恢复时继续使用本机保存的 `server/.env`。

## 问题修复

- 修复管理后台登录、注册、侧栏和浏览器标签页仍显示模板图标或缺少 Synapse 图标的问题，统一显示 Synapse Dashboard 品牌。
- 修复选择供应商和模型弹窗在供应商名称较长或模型较多时文字挤压、左侧选择项靠上显示、选择圆点与名称间距偏大的问题。
- 修复 Agent 空会话里误显示“滚动到底部”按钮的问题，空时间线不会再因为滚轮操作进入离底部状态。
- 修复 Agent 对话导出在事件/用量记录较多时选择保存位置后可能卡住无响应的问题；导出包生成会先按当前会话筛选数据，并能跳过异常的运行态引用。

## 技术调整
