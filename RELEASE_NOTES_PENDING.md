# Pending Release Notes

## 新增功能

## 功能优化

- Agent 会话侧栏现在会优先显示正在输出的转圈状态；已完成但未读的会话改为蓝色小圆点，不再显示未读数字。
- 自动化编辑窗口中，已选择的触发器和执行器会作为独立摘要显示，并移除冗余状态标签。
- 自动化编辑窗口在未选择触发器或执行器前不再展示默认配置摘要，避免误以为已经完成选择。

## 问题修复

- 修复 DOCX 转 Markdown 时把内嵌图片写成超长 base64 导致部分 Markdown 工具无法识别的问题；文件转换工具现在会为每个输出文件生成独立图片附件目录，知识库摄入则默认省略这类内联图片。
- 修复工作流运行结果详情中内容较长时，右侧 Markdown / 文本切换按钮可能超出面板的问题。
- 修复手动运行单个自动化时，列表中其它自动化也显示加载或禁用状态的问题。
- 自动化列表对齐工作流列表的交互样式，鼠标悬停列表项时不再改变背景色。
- 修复自动化编辑窗口中右侧命令等长内容可能撑开页面并出现横向溢出的问题。
- 修复自动化手动运行后旧定时器仍可能按原时间触发的问题，完成后锚定的固定间隔会按本次完成时间重新计算下一次运行。
- 修复自动化 HTTP 请求测试和运行历史可能显示 Authorization、Cookie、token 等敏感内容的问题；JSON 请求现在会自动使用 `application/json`，GET 请求也会提前拦截 Body 配置。
- 修复安装包内置 Claude Code runtime 缺失时 Agent 对话只显示底层 SDK 报错的问题；现在会在运行和诊断中明确提示更新或重新安装，并在发版校验中拦截缺少内置 runtime 的包。
- 修复本地项目目录被移动或删除后，Agent 对话可能继续进入 SDK 并显示不透明错误的问题；现在会直接提示项目路径不存在或不可访问。

## 技术调整

- 本地打 macOS / Windows 安装包时会先重新构建应用，并在打包校验中拦截主进程代码过旧但版本号已更新的安装包，避免覆盖安装后仍带旧 Agent runtime。
