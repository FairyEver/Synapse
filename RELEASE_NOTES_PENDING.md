# Pending Release Notes

## 新增功能

## 功能优化

- 自动化页面列表改为更紧凑的表格样式，顶栏、加载、空状态和错误状态与云盘等内部工具页面更一致，长内容在窄窗口下也更稳定。
- Agent 长对话达到费用阈值后，会在本轮输出结束时提示开始新对话，并保留当前项目和模型，减少继续沿用超长上下文带来的额外费用。
- Agent 长对话新开提示的金额和 token 阈值可以在模型与供应商设置中调整。
- 模型与供应商里的默认设置区域改为更紧凑的对齐方式，金额和 token 阈值的单位跟随输入框显示，配置时更容易扫读。
- 部署流程会集中备份远端 `.env`、Postgres 角色配置、在线数据库快照、最终切换前数据库快照，并在启动新服务前验证最终备份可恢复；未配置 COS 时，本地 Drive 文件也会落到持久目录并在部署窗口备份。

## 问题修复

- 修复 Agent 对话导出在事件/用量记录较多时选择保存位置后可能卡住无响应的问题；导出包生成会先按当前会话筛选数据，并能跳过异常的运行态引用。

## 技术调整
