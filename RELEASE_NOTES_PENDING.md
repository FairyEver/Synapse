# Pending Release Notes

## 新增功能

- 工作流运行结果新增 Token 消耗视图，可以按节点查看模型、输入、输出、缓存读写、思考 token 和运行时估算费用，并在表格底部汇总本次运行成本。

## 功能优化

- 自动化页面改为更轻量的列表入口，新建和编辑会打开独立编辑窗口；编辑器支持选择触发器和执行器、重新选择配置，并提供“仅保存”和“保存并启用”。
- 自动化编辑窗口改成更紧凑的双栏工作台，窗口默认高度更低，中间分隔线与内容区上下对齐；新建时会自动生成不易重复的名称，点击标题即可重命名。
- 自动化 Cron 触发器现在复用定时任务的高级 Cron 编辑器，可以用常用计划快速生成表达式。

## 问题修复

- 修复新建自动化时编辑窗口打不开、只提示重试的问题。
- 修复新建自动化编辑窗口在有未保存修改时无法关闭的问题，现在关闭窗口会给出放弃确认，不会再卡住。
- 修复自动化 Agent 执行器无法稳定填写和保存的问题，现在可以选择项目、选择供应商模型并保留提示词配置。
- 修复自动化运行中无法停止的问题，现在运行态的停止按钮可直接中止当前运行，不会误打开编辑窗口。
- 修复自动化实际运行成功后列表仍偶发显示上次失败或下次运行未知的问题，现在运行结果和下一次触发时间会保持一致。
- 修复今日时段图把小时数据画在右侧整点刻度上的问题，现在柱子位于对应小时区间中间，避免还没到整点却看起来已有整点数据的误解。
- 修复 Agent 对话用量卡片刚生成和切换会话后价格、累计 token 不一致的问题；现在实时显示和历史重载都使用同一套 Synapse 本地价格规则估算。
- 工作流运行中即使关闭运行窗口，也可以从工作流列表或运行历史重新打开进度；运行历史会把“执行中”的记录显示在顶部。

## 技术调整
