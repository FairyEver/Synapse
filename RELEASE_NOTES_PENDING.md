# Pending Release Notes

## 新增功能

## 功能优化

- 云盘 Markdown 评论改为逐条删除。主评论和回复都只删除当前所选评论，不再混合显示“删除评论”和“删除讨论”。

## 问题修复

- Terminal 的一次性启动覆盖现在可以明确移除继承的环境变量，避免敏感变量继续传入新会话。
- Text File Writer 的 MCP 请求现在会在客户端取消或断开时停止等待和写入，避免继续提交本地文件。
- HTML Generator 的 MCP 请求现在会在客户端取消或断开时停止渲染，并阻止尚未提交的 HTML 文件写入。
- 修复生产环境直接打开或刷新平台管理后台深层页面时被错误重定向到用户控制台的问题。

## 技术调整
