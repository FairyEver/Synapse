# Pending Release Notes

## 新增功能

## 功能优化

- 手机端终端键盘：**方向键和回车搬到第一页**。Claude Code 弹出选项时按上下键选、回车确认，这套动作现在不用再翻页；`Esc`、`Tab`、空格和回车挪到同一行，空格与回车各比原先宽一倍多，方向键四颗都是大键。
- 手机端终端键盘第一页补上 `,` `.` `/`（真键盘字母底行的末尾三颗）；第二页的符号、编辑键、功能键重新分成三组，两页都铺满，不再留空行。
- 手机端录音的**锁屏卡片和灵动岛现在只讲两件事：录了多久，怎么停**。原来那条点阵去掉了——它右半边是补出来的占位圆点，看着像波形，其实读不出音量；按钮也从两枚减到一枚，锁屏和灵动岛都只剩一颗圆形的停止键（形状照系统「语音备忘录」那枚）。空出来的地方给了内容：锁屏上的计时放大一档，停止键从 34 点放到 52 点；灵动岛收起时左边一颗红点表示在录、右边是计时，长按展开后左边是录音名压着计时、右边是停止键。停止键仍然不需要解锁就能按；取消录音改在 App 内做——点一下卡片就进录音页。这一套红**只出现在锁屏和灵动岛上**，App 内仍是原来的单色。
- 手机端录音的**锁屏卡片点一下会打开 App 的录音界面**（原先点它没有任何反应）：卡片是「带我去看正在录的那一条」，结束或丢弃录音仍然走卡片上那两个按钮。卡片打开不会结束这条录音，也不会另起一条。

## 问题修复

- Windows 和 Linux 上**终端里按 `Ctrl+V` 粘不进任何东西**：粘贴键原先只给 macOS 接（`⌘V`），其它平台按下去字节被原样丢给终端，看上去像没反应。现在这两处的 `Ctrl+V` 和 `Ctrl+Shift+V` 都会把剪贴板内容送进终端，和 Windows Terminal、VS Code 的习惯一致；`Ctrl+C` 仍然是中断，`Alt+V` 不受影响，继续用来把截图交给终端里的 AI。
- 手机端终端键盘上原先打不出减号：符号页那一格画的是 `±`。现在换成真键盘上本来就有的 `-`/`_`，`Shift` 一下就是下划线。至此键盘能打出全部可打印 ASCII 字符。
- 手机端**录音锁屏卡片和灵动岛上的停止键按下去没有反应**：这个按钮的动作原先落到了 Widget 扩展进程里，而那里没有录音机，按了等于什么都没发生。现在改为在 App 进程里执行，按下去就是真的保存。

## 技术调整
