# Pending Release Notes

## 新增功能

- 终端侧栏里多了「项目:XXX」这类分组，点进去开终端就直接站在那个项目的目录里（这个工作目录自带好了，要改成别的在分组设置里改）：你在「对话」里有的每个项目，终端这边都有一个对应的分组，名字就是项目名。在项目里按 Command 点加号开的 Claude Code 对话，直接落进它自己的那个分组，不再挤到别人那儿去。项目改名，这个分组跟着改；删掉项目，这个分组连同里面的终端一起消失（删之前会告诉你这里有几个终端、几个还在跑）。分组本身长得和你自己建的一模一样，只是名字是「项目:」加项目名——所以它的改名和删除在菜单里没有，那两个动作由项目决定。
- 在终端里自己开的终端（以及 AI 通过 MCP 开的终端）永远落在你自己的分组里，不会再被塞进某个项目。以前「没有指定分组」等于「放到列表最上面那个分组」，现在项目分组也在列表里，那套规则就会把终端塞进别人的项目。
- 终端里每个会话的头部多了一颗「复制引用」，点一下就把这个会话的引用复制走，不用再对着标题点右键——会话名很短的时候（比如就叫「一」）那颗标题很难对准。复制出来的还是那三行引用，粘给另一个 Claude Code 会话，它就能认到具体是哪一个。
- 按 Command+R 可以重命名当前会话，和双击标题一样弹出那个小窗口。Windows 上是 Ctrl+Shift+R（那里 Ctrl+R 归命令行的历史搜索）。
- AI 现在能在一个指定的标签里再开一格分屏，也能整个关掉一个标签。以前它只认得被指到的那个会话，标签本身动不了——想加一格分屏、或者把一个标签连同里面的会话一起收掉，都得你自己在界面上做。关标签走的是正常的结束流程，不会强杀正在跑的东西；里面还有没停完的，它会如实告诉你还剩哪些。

## 功能优化

- 手机端「新建」面板上的两段，名字从「对话 / 终端」改成了「项目 / 终端分组」——第一段是挑一个项目开对话，第二段是挑一个分组开终端。第二段里现在也能看到「项目:XXX」那些分组，和电脑上一样。
- 终端里的三层统一了叫法：分组下面是「标签」，标签里分屏出来的每一个才是「会话」。以前同一个东西在不同地方叫「终端」「会话」「对话」——侧栏那一行右键叫「重命名终端」，分屏里右键却叫「重命名对话」，复制出来的引用说的又是「会话」。手机端上对应的几处也跟着改成「会话」。
- 会话右键里的「编辑描述」去掉了。那段备注只有把鼠标停在侧边栏那一行上才看得到，而那一行本来就写着会话标题——要看的内容得先悬停才出现，实际没起到作用。侧栏的「置顶」不受影响。
- 终端跑长输出时不再越用越卡。输出超过一定长度后，手机每收到一帧都会把整个缓冲区重新排版一遍（命令行工具、构建日志、TUI 界面都会触发），现在只处理真正变化的那一段；终端页每帧重复做的大块计算也一并去掉了。
- 终端里连按按键的响应更跟手：每条发出去的消息原来都要新建一次时间格式化器和编码器，现在复用。
- 手机端终端的键盘面板重做成一块完整的电脑键盘。顶上多了「组合键模式」开关和 Control / Shift / Option / ⌘ 四颗修饰键，下面的键位分两页：第一页是数字与字母（含空格、回车、退格），第二页是符号行、F1–F12 功能键，以及 Ins / Home / End / PgUp / PgDn / Del 和方向键。右下角新增 Caps，按一下锁住大写输入。
- 这块键盘打开时，屏幕下半部分就只剩键盘本身了：它上面的工具栏和输入栏会一起让位，不再压着键盘。要收键盘，点一下键盘外侧的终端画面就行；要用手机键盘打字，收掉之后点输入框。
- 面板上有几颗键在终端里没有对应的输入（PrtScr、ScrLK、Pause，以及被 macOS 终端自己占用、送不进终端的 ⌘）：它们照常显示在键盘上，但呈灰色，按下去只有一次振动提示，不会让人对着一个没反应的键怀疑键盘坏了。
- 修饰键的用法没有变：点一下锁住，再按字母完成组合，发完自动弹回；连点两下锁定，可以连续按同一个组合。「组合键模式」关掉之后修饰键那一行收起，面板就是一块普通字符键盘。
- 录音回放区摆正了。「后退 15 秒、播放、前进 15 秒」三颗按钮现在居中占一行，播放按钮正好落在屏幕中线上；播放进度挪到它们下面单独一行。以前三颗按钮贴着左边、进度在最右边，一左一右互相拽着。
- 终端分屏的头部多了一个复制按钮，一次把这段终端里的全部内容复制成纯文本。行尾空格和结尾的空行都会去掉，粘到哪都是干净的一段。
- 按住 Command 点某个项目的加号来开一个 Claude Code 对话时，直接切到「终端」里那个新对话上，不再弹出新窗口。以前每点一次就多一个窗口，现在和点普通加号一样留在当前窗口里。
- 可以指着另一个终端说「去盯着它」。在一个 Claude Code 会话里粘上另一个终端的引用，它就能认出那个终端里跑的是哪个会话、对方此刻是在干活、在等人回答还是空着，然后直接给它派活或传话。以前 AI 只能看到「那个终端存在」，认不出里面是谁、在忙什么，人得自己在两边转述。
- 终端跟到最新一行的动作变成平滑滑动，不再一跳一跳。新输出是一批一批到的（桌面端大约每 1/8 秒送一批），原来每一批都把画面瞬间拽到底，看着就是每隔一百多毫秒跳一下；现在按屏幕刷新率把画面朝最新一行滑过去，目标一直在动也跟得住。手指按住屏幕时不做插值——那一下画面归手指。开了系统「减弱动态效果」的仍然一步到位。
- 会话头部的右键菜单整条都能呼出来了，不再只认标题那一小块——标题旁边的空白、以及挤在右边的那排按钮，右键都能用。标题上右键则不再弹菜单：那一条同时是拖着挪动分屏的把手，菜单会跟拖拽抢同一个手势；双击标题重命名没有变。
- 键盘面板翻页时终端不再跟着动。两页现在一样高，都按第二页（符号行加功能键）的高度来，翻到第一页时空格键底下会空出两行 —— 换来的是翻页时上面的终端一行都不跳。电脑那边的终端一直不会被这件事影响。
- 让 AI 去照看另一个终端时，它现在能看出那个会话还有没有同伴。以前它只认得被指到的这一个，不知道同一个标签下是不是还分屏开了别的会话，只能靠人一个个转述；现在查询会话时会一并给出它所在的标签，同一标签下分屏出来的几个会话能认出是一伙的。

## 问题修复

- 录音列表左滑出来的「删除」不再是一块空白方块。深色模式下那颗按钮的底色和上面的垃圾桶图标取的是同一个颜色，图标画在白底上等于没画，只剩一块看不出是什么的白方块；现在是正常的红色按钮。终端会话列表早就修好了，录音这一处漏了。
- 按住说话之后很快松手（在权限弹窗或连接还没回来的时候），麦克风不再留在后台开着、也不再把这段没人要的录音传上去。
- 退出账号再登回来之后，终端的心跳不再停掉。以前这会让电脑认为手机已经离开并收回写入权，之后每次输入都被当成「电脑正在使用这个终端」。
- 退出账号时会一并注销本机的推送登记。以前换账号后，上一个账号的终端审批和转写完成通知还会继续发到这台手机上，通知正文里带着会话标题和最后一行输出。
- 录音上传的结尾那一段没发出去时，不再把它当成完整录音提交（提交上去的会是一段打不开的音频，还会开始计费转写），本机那份完好的录音也不再被删掉；下次打开 App 会自动补齐。
- 同一个文件不会再被上传两次。以前在上传途中再选一个文件，会让同一份内容传两遍，多出来的那份会一直留在云盘里没人回收。
- 终端键盘面板连续快速开关时，不再把一个动画中途量到的终端尺寸报给电脑（那会让远端重排、整屏重绘一次）。
- 网络切换（家里 Wi-Fi 和蜂窝之间来回）或线路静默太久之后，终端不再停在「已连接」却什么都不动。以前这种半死的连接不会报错，屏幕一直说连上了，只有杀掉 App 重开才能恢复。
- 一次选多张照片、或选一个很大的视频发到终端时，界面不再卡住。照片的转码和文件的拷贝原来都在界面线程上做，选九张就是九次背靠背的全尺寸解码。
- 录音过程中 App 异常退出（被杀掉、崩溃）之后，重新打开不再卡住。上一次的录音收尾原来会在界面线程上把整个音频解一遍，长录音下就是几十秒没有响应。
- 在两条录音之间来回切换时，不再把上一条的声音装到当前这条上。
- 点开一条陈旧的推送通知后退出账号，下一个人登录不会被带到别人的终端上。
- 手机上点开输入框、键盘升起来的时候，终端画面不再整个缩小。以前键盘占掉的那块高度会被当成终端变矮了，整屏内容按缩小的画布重排一遍（宽终端的字会小到看不清），收起键盘又长回去；现在键盘只是占了屏幕下方的位置，画面保持原来的大小，少看的那几行滚一下就到。
- 键盘面板上每一行之间的缝不再比每一颗键之间的缝宽一倍，整块键盘重新读成一张网格。以前上下方向的缝是 12 点、左右是 6 点，看着像几排凑在一起的按钮；同一个原因还让第二页整体高过面板，底下一行要上滑才够得着。
- 键盘面板的两页现在能左右滑动切换了。以前两页画的其实是同一块板子，滑动途中看着没动，一松手整块板子突然换成另一页；点底下那两个点一直是可以用的。
- 深色模式下「组合键模式」的开关不再是一整块白色。它的底色原来和白色圆点同色，看不出开着还是关着；设置页的「记录诊断日志」开关是同一个问题，一并有正常的样子了。
- 键盘或键盘面板升起来之后再按下的键，结果不再看不见。以前画布一被压矮，终端就停在原来的位置不再跟着最新一行走（它把「屏幕变矮」当成了「你往上翻去看历史了」），新输出全落在可视区外面；现在它继续跟。这条只管跟随，往上翻历史时不被拽回底部那条行为没有变。
- 终端不再一阵一阵地闪。切出去再回来、从别的页面回到终端、以及在一个跑了很久的终端里，画面原来会突然跳到前面某处、再几下闪回最新。原因是每次重新连上，电脑会把整个屏幕的内容切成几块发过来，而手机拿到第一块就当成全部：它先落在这一段里**最老**的那几行上，后面几块每到一个就往下走一段，屏幕上就是连着闪几下。跳的幅度跟着窗口大小走，所以对话越长越明显，短会话里根本看不出来。现在最新的那块先到、更老的部分从上方补齐，落点一次到位。
- 转屏、收放工具栏或键盘之后，终端里不再出现一批不属于当前位置的行（显示成空白，或者上一次刚好用过的那一行）。画面尺寸一变就要按新字号重画每一行，而重画之前用来查内容的表被清空了、没人补上；终端闲着的时候不会再有任何一帧来纠正它。现在重画和查表是同一步，不会再对不上。
- 按 Command+R 不再整个界面重新加载。以前那一下会把「当前在哪个应用」一起丢掉——这个状态没有被记下来，重新加载后就回落成默认的「对话」，终端里正在跑的东西全从眼前消失；菜单里的「重新加载」也一并去掉了。
- 在很长的会话里往回翻着看时，画面不再一帧一行地往前滑。手机端终端最多记六千行，到顶之后每进一行就要丢掉最老的一行，而丢掉的那些全在视口上方；位置补偿原来只认「之前的第一行」——那恰好就是刚被丢掉的那一行，于是补偿整体失效。

## 技术调整

- Synapse 内置的 Claude Code 升到 2.1.278，并显式打开了它的跨会话消息开关。此前这个开关的默认值随版本变来变去（同一份设置，旧版默认关、新版默认开），现在由 Synapse 写死，行为不再随将来升级忽明忽暗。
- 手机端可发送的按键新增 F1–F12 与 Ins（38 → 51 项）。这三端同步的键名表要求**服务端与桌面端先升级**，否则手机上按这些键会发送失败；服务端与桌面端升级完成前，这一版手机 App 的其它键位不受影响。
