# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- 修复工作流 DAG 视图中已产生 Agent 对话的节点缺少入口提示的问题，现在可直接从节点打开关联对话。
- 修复费用币种迁移缺少诊断日志的问题，迁移成功或失败时会记录关键上下文，便于排查历史费用数据更新。
- 修复模型价格规则能力调用缺少运行日志的问题，定价规则变更成功或失败时更容易追溯。
- 修复金手指状态读写缺少主进程日志的问题，工作流入口等隐藏状态变化更容易排查。
- 修复 Claude Code 对话记录解析遇到文件流读取异常时会中断的问题，现在会返回可见解析错误。
- 修复 Claude Code 对话详情窗口加载失败只显示笼统提示的问题，现在会显示脱敏后的具体错误信息。
- 修复 Claude Code 对话记录查询和原文搜索缺少主进程日志的问题，文件缺失、解析失败和查询结果现在更容易追溯。
- 修复桌面端账号登录成功链路缺少日志的问题，登录、刷新和退出状态现在更容易排查。
- 修复知识库上传源文件时文件选择对话框可能出现在窗口后方的问题。
- 修复工作流 MCP 查询历史运行时返回结构不一致的问题，旧快照也会补齐运行耗时。
- 修复桌面端登录按钮在等待浏览器登录时仍可重复点击的问题。
- 修复桌面登录交换失败审计缺少原因的问题，便于区分过期、重放和并发竞争等失败。
- 修复桌面端注册登录回调协议失败时没有诊断日志的问题。
- 修复知识库资料管理窗口加载失败后再次打开仍停留在空白窗口的问题。
- 修复桌面账号接口失败日志缺少 HTTP 状态和端点信息的问题，排查登录与刷新失败时更容易区分原因。
- 修复从旧运行历史打开工作流 Runner 时 DAG 可能空白的问题，旧快照缺少结构信息时会自动补加载工作流结构。
- 修复 Claude Code 对话详情窗口加载失败后再次打开静默失败的问题，失败窗口会被清理并允许重试。
- 修复管理后台退出登录在会话撤销异常时可能缺少审计记录的问题。
- 修复工作流节点执行器日志缺少工作流和节点上下文的问题，按 workflowId 或 nodeId 排查运行失败更直接。
- 修复桌面端登录回调临时失败后无法利用已有会话恢复的问题，旧 refresh token 仍有效时会自动恢复登录状态。
- 修复知识库图片摄入记录保存本机原始路径的问题，现在只写入托管附件相对路径。
- 修复知识库 `/save` 可能把保存笔记变成原始资料摄入的问题，现在保存只写结构化 wiki note。
- 修复管理后台系统日志部分清理失败仍显示成功的问题，现在会记录失败审计并提示清理异常。
- 修复知识库资料管理无法直接添加网页 URL 的问题，现在可以从资料管理窗口添加 URL 资料。
- 修复工作流运行中节点详情缺少已解析输入的问题，长时间运行的节点开始后也能查看本次输入变量和 Prompt。
- 修复工作流 Runner 复用窗口切换运行后刷新回到旧记录的问题，切换运行时地址栏会同步到当前 runId。
- 修复桌面登录外部打开审计记录保留 OAuth state 和 PKCE 参数的问题，现在这些登录握手参数会被脱敏。
- 修复工作流导入包格式无效时缺少失败审计的问题，现在格式校验失败会留下明确日志和审计记录。
- 修复团队退出或解散失败缺少审计记录的问题，成员关系变更失败时也能追溯触发人和失败原因。
- 修复管理后台只允许最早创建的管理员登录的问题，后续添加的管理员现在也可以用自己的邮箱和密码登录。
- 修复工作流导入成功后自动打开编辑器失败会误报导入失败的问题，现在会保留导入成功状态并单独提示打开失败。
- 修复托管知识库创建和删除接口向界面暴露真实运行目录的问题，现在界面只接收虚拟知识库路径和必要元数据。
- 修复定时任务手动运行完成后列表状态不刷新的问题，运行后会重新读取任务状态和上次运行结果。
- 修复通过 MCP 触发工作流运行后审计记录缺少 runId 的问题，现在可直接关联触发记录和运行历史。
- 修复管理端失败审计可能保存敏感底层错误的问题，现在审计日志会脱敏令牌、内部 URL 和本机路径。
- 修复工作流运行快照路径可能被异常 workflowId 越界的问题，现在快照读写删除会校验工作流 ID。
- 修复系统日志目录读取失败被显示成空日志的问题，现在目录不可读会返回错误并进入失败审计。
- 修复变量重命名时权限检查和审计记录仍指向旧名称的问题，现在会按最终变量名校验和追踪。
- 修复工作流取消后未启动节点被显示为跳过的问题，现在会保留取消状态，运行历史更准确。
- 修复 Windows 文件操作审计可能保留本机绝对路径的问题，现在审计记录会统一脱敏 Windows 路径。
- 修复工作流节点详情直接显示敏感输入输出的问题，现在详情面板会像运行报告一样脱敏令牌、Cookie 和本机路径。
- 修复知识库资料只读列表会悄悄重建 `.raw` 目录的问题，现在资料目录缺失会返回可诊断错误。
- 修复知识库资料重命名、移动、删除后日志缺少条目路径的问题，现在可以通过 raw 相对路径复盘具体改动。
- 修复系统日志下载失败时审计记录可能保存底层错误消息的问题，现在只记录安全的失败类型和错误代码。
- 修复认证失败审计会信任未验证 JWT 内容的问题，现在无效 token 不会伪造操作者或目标。
- 修复 Agent 问答卡片点击“不回答”时返回英文跳过原因的问题，现在会使用“未收到选择，已停止操作。”的统一停止提示。
- 修复桌面账号退出时本地凭据清理失败仍显示已退出的问题，现在会返回可重试的退出失败状态。
- 修复团队邀请创建权限失败缺少审计记录的问题，现在非所有者尝试创建邀请会记录失败原因。
- 修复 Agent 消息中打开代码引用只按文件读取审计的问题，现在外部编辑器或系统打开动作也会经过 shell 权限与审计。
- 修复知识库资料转换失败后残留原始附件的问题，现在转换失败或 OCR 不可用时会清理已暂存的原始文件。
- 修复用户注册遇到基础设施异常时缺少失败审计的问题，现在会记录安全的失败类型并保留原始错误传播。
- 修复 Agent 片段直接发送和知识库命令发送缺少操作追踪的问题，现在这些发送入口也会记录安全摘要。
- 修复知识库资料管理移到废纸篓失败时提示不准确的问题，现在错误提示会明确指向废纸篓操作。
- 修复知识库热缓存注入状态长期运行可能持续增长的问题，现在会清理过期记录并限制缓存规模。

## 技术调整
