# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- 录音**转写会一直卡在「转写中」不动**：录的时候没拾到说话声的录音（整段只有环境底噪，例如误触开始又很快停下），提交给语音识别服务之后会一直停在「处理中」——既不返回结果也不报错，进度条贴在 95%，看起来只是「快好了」。实测这种任务要拖到几十分钟之后才会由识别服务判失败（有一条是 47 分钟），失败原因还是一句英文的 `Failed to recognize audio!`。现在这类任务超过 15 分钟没有任何进展就直接判失败，并写明原因是「这段录音里没有检测到说话内容，无法转写。」，用户可以按重试或重新录一条。有说话内容的录音完全不受影响，长会议该等多久还是等多久。

## 技术调整
