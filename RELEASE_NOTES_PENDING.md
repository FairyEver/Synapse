# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- 修复本地 JSON 数据仓库并发写入时可能互相覆盖的问题，降低快速连续保存或后台任务同时写入造成数据丢失的风险。
- 修复工作流节点同时遇到失败上游和未激活分支时仍可能被执行的问题，失败路径会稳定阻断下游启动。
- 补齐知识库 DragonScale 语义分片的失败日志，Ollama 连接、模型查询、页面读取、缓存损坏和嵌入失败都更容易排查。
- 修复创建 Rule、Skill 等内容时仅切换图标类型不会提示未保存更改的问题，关闭弹窗前会正确提醒。
- 修复 Skill 安装失败后旧备份恢复失败却不提示的问题，用户会看到恢复失败说明并可按桌面备份检查旧内容。
- 修复仓库同步、维护和手动推送失败日志可能暴露 Git 凭据或本机路径的问题，相关错误写日志前会先脱敏。
- 修复导入损坏备份时结构校验不足的问题，缺少必要字段或 namespace 结构异常的备份会被明确拒绝。
- 修复审计记录把操作错误信息整段屏蔽的问题，失败原因会保留脱敏后的可诊断文本。
- 修复工作流 Switch 节点无法在解释性回复中匹配中文分支 ID 的问题，中文路由键会按预期命中对应分支。
- 修复配置备份导入存在两套重复逻辑的问题，对话框导入和指定路径导入会走同一套校验、回滚和日志流程。
- 修复复制选中日志文件失败时可能产生未捕获异常的问题，失败会被记录并保持既有错误提示。
- 修复管理后台团队角色权限替换失败时审计 action 过于泛化的问题，失败记录可按角色权限操作准确检索。
- 修复管理后台备份下载遇到登录过期时只显示泛化失败的问题，会和其他接口一样清理会话并回到登录页。
- 修复知识库 DragonScale 边界评分静默跳过读取失败页面的问题，评分报告会区分文件过大和读取/编码失败原因。
- 修复应用异常退出后定时任务运行历史永久停留在“运行中”的问题，重启时会把遗留记录标记为失败并同步任务状态。
- 修复管理后台用户列表模块权限偶尔显示技术 key 的问题，列表会等模块权限标签加载完成后再展示。
- 补齐工作流重新运行、运行历史和运行状态查询的主进程日志，生产排查时能看到更完整的运行链路。
- 修复仓库拉取成功后因本地分支无法统计 ahead 数而被误报为同步失败的问题。
- 统一 Git 非快进错误识别，内容提交和仓库维护遇到同类远程分叉提示时会按同步流程一致处理。
- 修复内容提交或待推送记录已成功写入仓库后，索引刷新失败仍向用户显示保存失败的问题。
- 修复管理操作审计日志写入失败被静默吞掉的问题，短暂失败会重试，最终失败会明确中断并暴露错误。
- 加强管理后台日志清理保护，清理日期不能早于最近 30 天，避免单次请求删除全部历史日志。
- 补齐知识库 URL 资料抓取的 SSRF 防护日志，私有地址拦截、重定向异常和请求失败会留下可追踪记录。
- 修复自动化已在运行时再次手动运行只显示泛化失败的问题，现在会明确提示自动化正在运行中。
- 修复自动化编辑窗口再次聚焦时仍显示旧配置的问题，回到编辑器会重新读取最新自动化数据。
- 修复系统日志并发清理时已被其他请求删除的文件仍被计为失败的问题，重复清理不会误报 500。
- 修复知识库资料转换遇到本地 OCR 不可用时丢弃已转换文本的问题，普通 PDF/Office 文档会继续写入可用 Markdown。
- 修复管理后台用户页在模块权限定义加载失败时隐藏用户列表的问题，用户数据可用时会继续展示表格。
- 修复管理后台操作失败时审计写入异常掩盖原始业务错误的问题，管理员会继续看到真实失败原因。
- 修复管理后台读取审计日志和系统日志时审计写入失败会反过来导致页面报错的问题，业务结果会正常返回并记录降级日志。
- 修复系统日志下载或清理失败时产生重复审计记录的问题，失败审计会保留具体日志文件名或清理日期。
- 修复自动化运行已自然结束后再点击停止仍提示失败的问题，运行历史会刷新到最新状态。
- 修复删除自动化后运行历史残留的问题，删除时会同步清理关联运行记录，避免后台数据长期累积。
- 修复定时任务手动运行后旧计时器仍可能按原时间触发的问题，以上次完成时间为锚点的间隔任务会按手动运行结果重新安排。
- 修复自动化运行过程中应用异常退出后历史记录一直显示“运行中”的问题，下次启动会把中断运行恢复为失败状态。
- 修复单条事件触发自动化配置异常或匹配失败时可能影响后续自动化的问题，异常条目会被跳过并留下排查日志。
- 修复数据迁移遇到版本降级时错误提示误导为“缺少迁移”的问题，现在会明确提示不支持降级。
- 修复加入团队接口返回的成员信息缺少角色字段的问题，响应会与前端成员类型保持一致。
- 修复通过团队邀请加入团队时服务端引用旧权限关联导致接口崩溃的问题，邀请加入流程会按当前个人模块权限模型返回成员信息。

## 技术调整
