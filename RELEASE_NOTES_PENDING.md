# Pending Release Notes

## 新增功能

## 功能优化

- 桌面端账号登录现在会在服务器临时不可用时保留本地账号信息，显示离线状态，并在服务恢复后自动重新同步。

## 问题修复

- 部署脚本健康检查更稳了，失败时会指出具体卡点，避免管理后台标题变化导致部署被误判失败。

## 技术调整

- 服务端部署脚本会在升级前自动备份生产数据库，容器文件日志也会保留在服务器部署目录，减少升级和排查时的数据风险。
