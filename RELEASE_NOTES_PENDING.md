# Pending Release Notes

## 新增功能

- Agent 对话在当前项目关联 Git 应用仓库时提供 Git 快捷菜单，可确认后自动生成提交说明并提交全部改动，也可直接提交并推送、拉取、推送、同步或打开对应仓库工作台。
- 知识库支持整库导入与导出：可将旧电脑上的单个知识库文件夹校验后复制到当前 Synapse 存储，也可导出带完整性校验信息的独立备份。

## 功能优化

- Agent 项目菜单新增“清空对话”，确认后仅清空当前来源分类与当前项目下显示的对话，不影响其它分类、项目或归档内容。
- Agent 新建对话后会自动聚焦消息输入框；打开自定义对话时会选中默认名称，完成命名后可直接按 Enter 创建。
- Agent 对话列表的相对时间不再在悬停时显示精确时间气泡，减少列表遮挡。
- Agent 项目侧栏的新建入口改为拆分按钮：点击加号可按默认配置直接创建普通对话，也可通过下箭头进入自定义创建或在文件夹中显示项目。
- Git 仓库工作台的改动文件列表改为紧凑单行布局，以状态字母和悬停提示区分文件状态，长路径会随侧栏宽度自动省略。
- Agent 斜杠选择菜单改为紧凑单行布局，介绍显示在名称后方，超出宽度时自动省略。
- Agent 输入框左下角新增文件与文件夹选择入口，支持从整个对话区域拖入或从剪贴板粘贴附件；附件会横向排列，较多时可通过两侧箭头浏览，并可在悬停后移除。
- Agent 对话导出包新增脱敏的模型流式事件诊断文件，可按会话、轮次和内容块定位异常输出，并通过分层体积上限避免诊断数据无限增长。

## 问题修复

- 修复部分华为浏览器和微信内置浏览器打开分享网页时，页面只显示顶部窄条的问题。

## 技术调整
