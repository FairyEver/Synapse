# Pending Release Notes

## 新增功能

- 手机连着电脑的终端时，右上角菜单里多了一个「Git」：能看当前在哪个分支、有多少改动没提交，也能直接切分支、新建分支、提交、推送、同步和合并分支。操作的是**终端当前所在的目录**，你在终端里 `cd` 到哪它就跟着到哪；不是 Git 目录时这一项不会出现，首页那一行也照旧显示版本号。合并一旦遇到冲突会**自动取消并退回原样**，并把冲突信息放进剪贴板，你可以直接粘给别的 AI 去处理。

## 功能优化

- 手机上新建终端时，「终端分组」列表多了一个搜索框：分组多了以后可以直接搜名字，位置和外观与选项目、选供应商那两页一致。点分组仍然是立刻建终端。

## 问题修复

- 手机端终端快捷面板里的「清空」不再是一行贴在列表上方、看着不像按钮的裸文字：它按 iOS 的写法移到了面板顶部的工具栏，和其它系统按钮一样有颜色、有足够大的点击范围，列表为空时同样置灰。
- 修复网页分享与预览里，从飞书等文档转出来的链接会多出反斜杠、点开还会跳到打不开的地址的问题：这类链接现在按原样显示，也能正常打开。
- 修复在终端里 `cd` 之后打开文件树仍然停在上一次目录的问题：终端现在会把当前目录报给 Synapse，文件树按的是实时位置，而不是这个终端刚建出来时的位置。

## 技术调整

- 终端启动时会给 shell 注入一小段集成脚本，用来上报当前目录。用户自己的 `.zshrc` / `.zprofile` 等配置照常生效；这一段与「Agent 原生通知」开关无关，关掉通知也会注入。

