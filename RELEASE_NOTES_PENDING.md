# Pending Release Notes

## 新增功能

- Synapse MCP 新增终端分组、会话创建、读取、输入、调整窗口和停止能力，Agent 可在启用控制权限的终端会话中继续操作。
- 新增“终端”系统应用，可在 Synapse 内创建、查看、输入、停止终端会话，并按会话控制 Agent 接管权限。

## 功能优化

- 更新官网文档为开发者视角，补齐桌面端当前能力说明，并将 GitHub 入口指向发布下载页。

## 问题修复

- 修复终端应用嵌入到“应用”页时，顶部操作区可能反复刷新导致点击“新建终端”没有反应的问题。
- 修复终端应用启动时可能因本地 `node-pty` helper 不可执行而导致主进程崩溃的问题。
- 修复 MCP 控制终端时可能绕过权限审计的问题，并让应用退出时未结束的终端会话在下次启动后显示为已丢失，而不是误标记为手动终止。

## 技术调整
