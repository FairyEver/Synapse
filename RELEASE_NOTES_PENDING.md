# Pending Release Notes

## 新增功能

- 新增桌面和 iPhone 共用的消息中心，汇总外部推送、终端待处理与完成、录音转写和 System Notifier 通知；已读与删除状态跨设备同步，历史保留 90 天。
- 新增独立的 API 密钥「发送通知」权限和开放接口，可发送标题、正文、分组、HTTPS 链接及普通、静默、时效性提醒。

## 功能优化

- 录音的显示时长改为取音频文件自身的长度，比过去按文件大小估算更准：一条界面显示 1 小时 25 分的录音，实际音频是 1 小时 22 分，长录音尤其明显。
- iPhone「需要我」改为「消息」，保留待处理会话，并提供全部与未读视图；消息列表按系统原生列表的样子重做——未读标成蓝点，时间不再把年月日和时间写全，而是「17:25 / 昨天 / 周三 / 9月20日」这种一眼能读的形式，左滑删除也是一颗正常的红色按钮。桌面全局入口显示未读数。
- 已登录且在线时，System Notifier 正式通知的完整标题和正文会同步到 Synapse 云端并推送其他设备；离线时仅本机显示，不补发。通知正文以明文保存在服务端，并可能出现在锁屏预览中。

## 问题修复

- iPhone 消息页左上角不再是一片空白：标题「消息」会像终端、录音两页那样显示出来。
- iPhone 消息页列表顶部的圆角回来了：最上面那块卡片过去顶上是一条直角边，第一条消息看着像被截断。
- 长录音不再转写失败：提交识别之前会先检查音频文件有没有写完整，这条检查过去把**五分钟往上的长录音一律误判成「这段录音的音频文件不完整，无法转写。」**，重试多少次都一样，录音越长越必然中。现在判定与录音多长无关，几小时的录音也能正常提交识别。
- 几小时会议的转写结果不再可能整条写入失败：逐字稿现在分批入库，会议越长行数越多，过去有那么一条长度上限会让已经识别成功的文字拿不回来。

## 技术调整
