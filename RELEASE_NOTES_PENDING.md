# Pending Release Notes

## 新增功能

- Synapse MCP 新增仓库发现和本机变量管理能力，AI 可以在指定仓库中安全地增删改查变量；变量列表和写入结果默认不返回敏感值。
- CC 使用分析新增“记录”入口，将明细和对话合并到同一个工作流中，可从会话行展开请求明细，也可打开原始对话窗口查看工具调用和事件字段。

## 功能优化

- 隐藏入口的金手指机制新增持久状态管理，后续可支持一次性触发和可开关状态两类行为，并能在多个窗口间同步状态。
- CC 记录查询改为后台 worker 执行，进入记录页、原文搜索和打开对话窗口时不再把读取任务压在主进程上，降低页面卡死风险。
- CC 记录页在读取列表、原文搜索或展开请求明细时会显示加载状态，避免页面空白等待。
- CC 记录页按批次加载会话和请求明细，并显示已加载数量，数据量大时不用一次性等待全部结果。
- CC 记录列表的标题和项目列会自动去掉共同路径前缀，长路径结果更容易扫读。

## 问题修复

- 修复 CC 记录页没有展示会话时间的问题，展开请求明细时也会在同一列显示请求时间。
- 修复 CC 使用分析打开对话新窗口时无法加载桌面桥接配置的问题。

## 技术调整

- 本机变量更新时，内部配置日志会脱敏变量值，避免 token 等敏感内容进入日志。
