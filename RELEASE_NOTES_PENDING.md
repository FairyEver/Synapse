# Pending Release Notes

## 新增功能

- 云盘文件支持历史版本：上传覆盖会自动保留完整版本，用户可以浏览、下载、恢复、删除或保留指定历史版本，并由系统自动清理过多的旧版本。
- 新增 Git 模块：可以克隆公司仓库、添加本地仓库、查看改动、选择文件提交、同步远程，并查看当前分支历史。

## 功能优化

- Git 克隆仓库和添加本地仓库时，现在可以直接选择文件夹；添加本地仓库会自动带出仓库名称，减少手动输入路径的出错机会。
- Git 仓库列表现在可以删除仓库记录；删除时可选择只移除列表记录，或把本地目录移到系统废纸篓。
- Git 应用图标换成更明确的分支提交图，和资源仓库图标更容易区分。
- Git 独立窗口去掉了顶部只有一个选项的“仓库”标签，保留右侧仓库操作按钮，界面更干净。
- Git 仓库列表去掉了每行的“进入”按钮，常用同步、拉取、推送和删除操作更集中。
- 知识库快捷菜单里的“资料管理”去掉了左侧图标，菜单项对齐更简洁。
- 云盘同一文件夹内不再允许通过重命名或移动产生同类型重名项目；上传同名文件仍会覆盖原文件并进入历史版本，文件和文件夹同名不受影响。

## 问题修复

- Git 页面没有仓库时，中间空状态不再重复显示克隆和添加本地仓库按钮，只保留“暂无仓库”。
- Git 独立窗口的仓库页顶部操作区改为统一的新窗口布局，标签保持居中，添加和克隆按钮固定在右侧操作区。
- Git 仓库列表里同步单个仓库时，不再让其它仓库的同步按钮一起进入加载或禁用状态。
- 删除 Git 仓库的确认弹窗会始终显示本地目录，并固定在选项上方，切换删除方式时弹窗不再跳动。
- Git 改动详情里的长文件路径和长 diff 行不再撑出窗口，代码差异会在右侧详情区内横向滚动。
- Git 历史详情里的长文件路径和长 diff 行不再撑出窗口，代码差异会在详情区内横向滚动。
- Git 历史详情在提交没有文件列表时，不再显示一条空的分隔线。
- 云盘历史版本弹窗内容不再横向错位；版本较多时会在弹窗内部滚动，时间和操作按钮稳定显示在弹窗内。

## 技术调整
