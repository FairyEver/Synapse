# Pending Release Notes

## 新增功能

- iPhone 主屏幕新增小、中、大三种终端组件：小组件提示待处理会话，中组件监看选定会话，大组件概览当前电脑；点击可进入对应终端，组件随系统自动适配深色和浅色模式。

- 在手机上远程指挥 Agent 时，现在可以让它检查 Synapse 桌面端更新、安装可用更新并重启，或单独完整重启桌面端；重新连接后可查询版本和启动状态。

## 功能优化

- Synapse Skill 在应用启动后自动更新已有的全局安装，成功时不再弹窗要求手动确认；更新失败时会提示重试。
- Drive 中的 Agent 现在按固定版本读取和编辑文本；通读大文件时优先下载固定版本，整篇重建后复核新版本内容。编辑冲突会明确提示，网页整篇编辑继续可用。
- Agent 项目操作菜单统一使用纯文字菜单项，移除“上移”和“下移”的图标。
- 终端分屏顶栏按文件夹按钮、会话标题、分支信息的顺序排列。
- 终端分支信息移除图标和无改动提示；有改动时以 `+n -n` 显示增删行数，窄分屏将操作收进更多菜单。

## 问题修复

- 修复已发现的更新版本被清理后仍尝试下载旧安装包的问题；点击下载时会重新确认最新版本。
- 修复 Windows 终端中选中文字后无法用 Ctrl+C 复制的问题；没有选区时 Ctrl+C 仍可中断当前命令。
- 修复 Windows 终端拖入文件路径时反斜杠和空格被错误转义的问题。

## 技术调整

- 日常推送代码和更新 PR 不再自动运行 GitHub CI 或发版；需要时手动启动，发版时先通过 CI 再构建发布。
