# Pending Release Notes

## 新增功能

- 终端分组支持配置多个命名命令；`+` 会新建干净终端，命令可从分组旁的专用菜单启动。

## 功能优化

- 终端应用的左侧分组/会话栏现在可以拖拽调整宽度，和其他可调整侧栏的页面保持一致。

## 问题修复

- 修复 macOS 全屏状态下按 `⌘W` 后可能停在黑屏 Space、只能退出应用才能恢复的问题。

## 技术调整
