# Pending Release Notes

## 新增功能

- 新增“模板生成文档”能力：可以用本地 Word 模板和 JSON 数据生成 `.docx` 文件，并为后续 App、MCP、工作流节点共用同一能力打好基础。

## 功能优化

- 优化“模板生成文档”界面：文件选择、JSON 数据来源、生成设置和结果状态更轻量清晰，减少重复边框和分散状态，让生成前缺什么一眼可见。
- 模板生成文档完成后，可以直接在文件夹中定位生成的 Word 文件。
- 优化云盘站点管理弹窗的表格布局，站点信息、状态、访问方式、时间、大小和操作列更紧凑清晰。

## 问题修复

- 修复“模板生成文档”工作流节点在画布和运行视图中显示为空壳的问题，并补齐模板、输出和 JSON 配置缺失时的校验提示。
- 调整“模板生成文档”的数据处理边界：生成时不再隐式补日期和列表序号，模板字段需要由 JSON 显式提供。
- 修复本机 Claude Code 使用代理且没有显式模型配置时，工作流、Agent 默认模型和定时任务无法选择本机默认模型的问题。

## 技术调整
