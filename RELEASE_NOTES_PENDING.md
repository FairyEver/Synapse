# Pending Release Notes

## 新增功能

## 功能优化

- Agent 对话里的 token 用量改为在回复底部显示当前会话截至该回复的累计分项统计，并用“累计”标明口径，避免误看成单轮消耗。

## 问题修复

- 修复退出应用时选择先同步多个仓库会串行等待的问题，单个仓库推送卡住时不再阻塞其它仓库同步。
- 修复 Usage Analysis 刷新后仍保留已删除 CC/Codex 日志历史数据的问题，清理本地日志后报表会同步移除过期统计。
- 修复内容下载实际写入失败时只留下允许审计的问题，失败结果现在会写入审计记录，便于追踪下载是否真正完成。
- 修复 Side-channel 已认证 HTTP 请求在业务处理失败时缺少失败审计的问题，损坏请求或会话解析失败会留下终态记录。

## 技术调整
