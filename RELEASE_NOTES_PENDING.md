# Pending Release Notes

## 新增功能

## 功能优化

- Claude Code 使用分析刷新改为增量解析，日志文件追加后只读取新增内容，大型历史记录下刷新更快。

## 问题修复

- 修复 Windows 上新建命令或脚本定时任务默认使用 POSIX shell 的问题；Windows 默认任务现在会使用 cmd 执行。
- 修复 Windows 上 Agent 自定义 exec 命令未指定 shell 时按 POSIX 转义的问题；默认执行会使用 PowerShell 语义。
- 修复数据库导入外部 SQLite 文件时忽略源 WAL 的问题；导入前会生成一致快照，避免丢失已提交但尚未 checkpoint 的数据。
- 修复 shell 执行审计可能持久化完整命令参数和凭据的问题；审计记录会脱敏参数字段和常见 token 文本。
- 修复知识库资料扫描会整文件读取大型 raw 来源的问题；扫描现在有单文件和总量上限，并用流式 hash 跳过超限资料。
- 修复运行中的定时任务仍可被删除的问题；现在必须先停止当前运行，避免删除后任务继续在后台执行。
- 修复定时任务失败后运行记录收尾遇到短暂写入失败时可能遗留 running 状态的问题；失败路径会重试收尾并继续更新任务运行结果。
- 修复 Database raw SQL 接口可直接访问表文件夹系统表的问题；文件夹结构只能通过受控的文件夹和表移动操作修改。
- 修复编辑器扫描中“在 Finder 中显示”在桥接层不可用时没有错误反馈的问题；现在会直接提示无法打开，而不会让操作静默失败。
- 修复数据库导入失败且备份恢复也失败后，数据库服务可能保持未打开状态的问题；失败后会尽力保留并重新打开当前数据库。
- 修复 interval 定时任务的活跃星期在非 UTC 时区可能错位的问题；下次运行计算会按本地日历与实际执行校验保持一致。
- 修复受控进程在权限拒绝前仍可能解析登录 shell PATH 的问题；现在会先完成权限判断，允许后才准备真实启动环境。
- 修复备份替换恢复时旧 singleton 数据可能残留的问题；导入备份缺少对应 singleton 时会正确清空本机旧状态。
- 修复 Agent 会话标识可能进入主进程日志的问题；日志记录和导出前会统一脱敏会话 key 与常见凭据字段。
- 修复命令和脚本定时任务超时后可能一直停留在运行中的问题；即使子进程忽略普通终止信号，Synapse 也会在短暂等待后强制结束。
- 修复 Database 只读 SQL 入口会接受写入型 PRAGMA 的问题；现在只读查询会拒绝可能改变数据库状态的 PRAGMA，避免绕过变更记录。
- 修复正式包后台检查更新失败后，关于页面可能展示整段更新错误和内部堆栈的问题；现在后台失败只记录日志，用户手动检查失败时显示简短提示。
- 修复最近删除里的永久删除可能清掉已被其他端恢复内容的问题；删除前会重新确认内容版本和删除状态。
- 修复 MCP 创建或更新 Skill 时源目录导入会被工具 schema 提前拒绝的问题；客户端现在可以按文档只传 `sourceDirectoryPath` 走目录导入。
- 修复 Agent 回复 outbox 写入失败仍被报告为已记录的问题；发送结果现在会反映 outbox 是否真正持久化，外部回复也会先创建 outbox 行再分发。
- 修复 MCP/API 定时任务创建、启停和更新缺少权限检查与审计的问题；外部变更现在会经过 PermissionGuard 并记录允许、拒绝或失败结果。
- 修复 Bridge 权限卡片在多项目环境下无法批准或拒绝的问题；权限卡片会携带来源项目，点击后能定位正确的 Agent 会话。
- 修复定时任务服务停止时运行记录可能长期停在 running 的问题；停止服务会先取消并等待当前运行写入终态。
- 修复 MCP/API 数据库变更缺少权限检查与审计的问题；外部数据库写入现在会经过 PermissionGuard 并记录允许、拒绝或失败结果。
- 修复 Agent 权限列表刷新失败会阻断已有会话选中的问题；权限刷新失败时仍会打开会话并加载历史消息。
- 修复 Agent side-channel 回复环境变量未注入运行会话的问题；Agent 会话和自定义命令现在能拿到 side-channel 回复地址与 token。
- 修复 Agent Relay 运行历史持久化源和目标 sessionKey 的问题；新记录只保存短 hash，诊断列表会脱敏旧记录。
- 修复知识库资料管理器侧边栏目录树缓存不会随目录移动、重命名或删除刷新的问题；目录变更后树节点会重新加载。
- 修复托管知识库运行目录被外部删除后 Agent 会话只显示模糊启动错误的问题；现在会提示知识库运行目录缺失且不会暴露隐藏路径。
- 修复知识库资料上传部分跳过时仍提示“已上传”的问题；现在会显示上传数量、跳过数量和跳过原因。
- 修复知识库 wiki 中不同目录同名页面在边界评分和预检中被静默覆盖的问题；同名页面会分别保留，歧义链接会被报告。
- 修复工作流未连接节点只作为提示而仍允许运行的问题；未连接节点现在会阻止保存和运行，并在校验中明确提示。
- 修复应用重置会先关闭本地日志导致操作无法审计的问题；重置开始前会写入审计记录，并同步输出系统级重置日志。
- 修复工作流删除可能与并发编辑交错导致已删除工作流被复活的问题；删除现在会和其他工作流变更共用同一把锁。
- 修复配置备份导出会丢失仓库变量和自定义 Rule/Skill 目录的问题；备份导入导出都会保留这些仓库设置。

## 技术调整

- 调整发布流水线，macOS 和 Windows 安装包全部构建完成后再统一发布，避免 GitHub Release 短时间缺少部分更新元数据。
