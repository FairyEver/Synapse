# Pending Release Notes

## 新增功能

- 新建自定义对话弹窗新增「创建终端对话」：用所选供应商和模型在终端里直接启动内置 Claude Code，体验与在本机终端运行一致，无需单独安装 Claude Code 或 Node.js；退出即结束该终端会话，不会新增 Agent 对话。

## 功能优化

## 问题修复

- 多文件任务不再被强制要求补登记材料清单：读完多个文件后会直接给出结果，只有回答声称已完整读取却没有可核对清单时才显示一条非失败提示，任务本身不会再因此多跑一个来回。
- 不会再为了登记材料清单打断你要求的步骤：只有需要覆盖核对的多文件/多阶段分析才登记清单，纯编辑或写入任务不再被要求补登记，最多在读到第二份材料时给出一条可选、非阻塞的建议。
- 编辑或写入大文件时不再把整份文件内容另存为任务证据：只保留工具、改动标识与写入后版本，证据体积从数百 KB 降到几 KB。
- 修正编辑失败时同一工具结果的字节被重复计量的问题，上下文预算账目更准。

## 技术调整
