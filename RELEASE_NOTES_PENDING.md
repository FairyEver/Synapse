# Pending Release Notes

## 新增功能

## 功能优化

- 云盘 Markdown 预览和分享页现在使用 GitHub 风格的文档排版，标题、表格、引用和代码块更接近 GitHub README 的阅读效果。
- 云盘 Markdown 在预览和分享中点击完整网页 URL 时，会在新的浏览器页面打开，不再离开当前文档。
- 云盘 Markdown 中无法加载的图片现在显示统一的紧凑占位，不再出现浏览器原生破图图标。
- 云盘 Markdown 评论卡片现在直接显示删除图标，首次点击切换为对号确认，再次点击才会删除；删除仍会同时移除全部回复，删除首条评论时也会移除整条讨论和原文标记。
- 云盘 Markdown 评论卡片的回复与编辑状态现在互斥；编辑时直接替换原评论内容和操作区，不再同时出现两个输入框和两组提交按钮。

## 问题修复

- 修复云盘 Markdown 宽表格在阅读栏内被压缩、中文内容逐字换行的问题；表格现在保留合理列宽，超出正文宽度时可横向滚动。
- 修复表单弹窗中的长链接可能撑宽内容并裁掉右侧操作的问题。
- 修复云盘 Markdown 目录点击后激活项会随正文滚动跳回其他章节的问题；目录选择和正文跳转现在都会立即完成，不再沿途切换。
- 修复云盘 Markdown 文档首次添加评论时，评论输入框可能滚动到远离所选文字位置的问题。
- 修复云盘 Markdown 在列表和硬换行附近添加评论时可能误报“评论位置已失效”的问题；重复内容无法唯一定位时会提示选择更多文字。
- 修复 macOS 更新恢复偶发卡住后 Synapse 进程已启动但主窗口不出现的问题；恢复现在在后台执行，缓存清理带有硬超时，不再阻塞窗口。
- 修复 macOS 将更新任务提交给系统后 ShipIt 实际未启动、应用却直接退出的问题；现在只有确认安装进程已经运行才会关闭 Synapse，失败时保留当前窗口供用户重试。
- 修复服务端部署构建未包含共享界面包、导致 Dashboard 镜像构建中断的问题。

## 技术调整
