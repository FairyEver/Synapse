# Pending Release Notes

## 新增功能

## 功能优化

- 自动化编辑窗口中，已选择的触发器和执行器会作为独立摘要显示，并移除冗余状态标签。

## 问题修复

- 修复工作流运行结果详情中内容较长时，右侧 Markdown / 文本切换按钮可能超出面板的问题。
- 修复手动运行单个自动化时，列表中其它自动化也显示加载或禁用状态的问题。
- 自动化列表对齐工作流列表的交互样式，鼠标悬停列表项时不再改变背景色。
- 修复自动化编辑窗口中右侧命令等长内容可能撑开页面并出现横向溢出的问题。
- 修复安装包内置 Claude Code runtime 缺失时 Agent 对话只显示底层 SDK 报错的问题；现在会在运行和诊断中明确提示更新或重新安装，并在发版校验中拦截缺少内置 runtime 的包。

## 技术调整

- 本地打 macOS / Windows 安装包时会先重新构建应用，并在打包校验中拦截主进程代码过旧但版本号已更新的安装包，避免覆盖安装后仍带旧 Agent runtime。
