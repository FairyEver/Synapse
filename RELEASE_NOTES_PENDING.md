# Pending Release Notes

## 新增功能

- 新增 iPhone 端 Synapse Remote：用同一个账号登录后，可以在手机上查看电脑上正在运行的终端、执行快捷命令、新建与停止终端、输入命令，并在 Agent 请求确认时从锁屏直接批准。终端画面在电脑上完成渲染后以结构化行下发，手机不做 ANSI 解析，也不需要理解租约与修订号。
- 桌面端新增移动终端网关：把本机终端的分组、会话状态与画面中继到手机，并代为托管终端写租约。只有手机正在查看的那一个终端产生持续流量，空闲时不产生任何上行；超出上行预算时降级为发送最新画面而不是排队。
- 云新增手机专用通道与终端中继：按设备路由桌面与手机之间的终端消息，在内存中保留每个桌面的最近一份会话列表供手机冷启动立即展示，不落库任何终端内容。
- 支持 APNs 推送：Agent 从「不需要人」变为「需要人介入」时推送到手机，通知支持在锁屏直接允许或拒绝；点通知正文会直接落到那个等待中的终端。未配置推送凭证时该能力自动关闭，其余功能不受影响。
- 手机可以往回翻阅终端历史：滑到顶部按页加载更早的输出，带样式、直到追溯到电脑上仍保留的最早一行，并明确提示已到最早。历史深度与桌面端可见终端一致（5000 行）。

## 功能优化

- Agent 长会话恢复为 Claude Agent SDK 原生 Streaming Input、会话续接与自动整理机制，不再由 Synapse 预估请求容量、截断工具结果、自动交接任务或阻断最终回答；百炼与其它 Provider 使用一致的运行规则。
- Agent 与内置 Claude Code 终端仅透传 Provider 明确配置的上下文窗口环境变量，上下文状态只展示 SDK 返回的真实用量与整理阈值。

## 问题修复

- 修复百炼 6 MiB 单次请求限制被误报为“模型连接中断”的问题；现在会明确说明请求内容超过供应商限制，并提示减少图片或大型工具结果，或新建对话后重试。
- 修复本地上下文整理、输出结构替换和任务证据校验可能在供应商阈值前提前终止 Agent 对话的问题；真实 Provider 错误现在只结束当前轮，不再触发隐藏恢复流程。

## 技术调整
