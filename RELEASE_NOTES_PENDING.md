# Pending Release Notes

## 新增功能
- 新增“快捷输入”独立应用，原来的提示词片段从设置页迁出，可以在应用启动器中打开管理。

## 功能优化
- Agent 对话中的快捷输入现在统一按直接发送处理，不再区分“插入”和“直接发送”，旧片段会自动迁移到新的快捷输入库。

- 快捷输入列表去掉容易误解的图钉置顶按钮，并移除对应的置顶接口，列表行只保留编辑和删除操作。

- Agent 对话顶部标题现在支持双击重命名，和侧边栏会话列表的重命名方式保持一致。

- 调整了 IDE 管理的页面层级：顶部只负责“内容 / 目录”切换，编辑器选择和内容筛选各归其位，减少误读。

- 模型价格规则导入预设支持多选后一次性导入，批量补齐价格规则更省步骤。

- 快捷输入应用换上了更清晰的专属图标，在应用启动器里更容易识别。

## 问题修复

- 修复分享页预览 3D 漫游内容时浏览器拦截鼠标锁定的问题，Three.js 第一人称视角控制现在可以在安全沙箱内正常响应用户点击。

## 技术调整
- 快捷输入数据迁移到独立应用数据存储，并纳入配置备份导入导出，减少后续系统应用数据混放到全局设置里的风险。
