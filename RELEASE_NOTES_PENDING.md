# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- Swarm Task 运行详情工具现在分页读取 worker 记录，默认仅返回 20 条并提供总数与后续页标记，避免大型蜂群运行产生全量 IO 和超大 Agent 响应。
- 分享的网页现在可以通过页面按钮复制内容到剪贴板，不再被预览安全策略拦截。
- Workflow 完整定义更新工具现在明确要求并提前校验 `meta.schemaVersion`，Agent 不再按工具 schema 生成保存时必然失败的定义。
- 安装器失败日志现在会整体汇总已保存密钥的引用映射，不再展开环境键、占位符键或密钥名称。
- 快速发布 Skill 写入本地关联前的最终复查现在会执行读取权限校验并记录审计，读取被拒绝时不会继续覆盖关联文件。
- Workflow 导入预览现在会先校验包内工作流再读取其标识，损坏或手工编辑错误的包不再被日志语句提前触发底层 TypeError。
- Agent 自动标题保存失败时现在会记录脱敏诊断并继续当前对话，不再让辅助标题写入错误中断 SDK turn。
- Workflow 运行现在会在启动前拒绝未声明的参数键，避免拼写错误或过期参数被静默忽略并写入运行状态与历史快照。
- Skill 环境配置更新遇到文件冲突后，冲突项会退出当前提交选择，只能重新扫描后再更新，不再重复提交已失效的扫描结果。
- Agent 通过 Secrets MCP 写入密钥时，upsert 现在会在调用前要求提供密钥值；仅修改已有密钥描述时应使用 update，不再等到服务执行后才因缺少值失败。
- Agent 查询归档 Workflow 运行状态时，现在会收到历史定义迁移失败或未来版本保护诊断，不再把不可读定义表现成无原因缺失。
- 内容页把 Skill 移到废纸篓后，即使安装状态全量刷新失败，也会立即清除对应编辑器的旧徽标并显示刷新警告。
- Agent 助手消息中的令牌、Cookie 和鉴权信息现在会在界面显示与复制前统一脱敏，并继续保留普通本地路径。
- Workflow 多文件或多文件夹预设现在会在选择时检查已保存资源的真实类型；参数在文件与文件夹之间变更后会提示重新选择，不再等到运行阶段才失败。
- Skill `.env` 更新审计不再保存密钥名称，并加强了共享审计清洗对 camelCase 等敏感字段名变体的识别。
- Secrets App 扫描 Skill 环境关联失败时，renderer 日志不再记录密钥名称；普通扫描与删除前检查均只保留脱敏错误诊断。
- IDE 管理页扫描 Skill 时现在会限制每个目录的条目数、预览读取大小和项目并发数，避免大量 Skill 或超大主文件造成无界磁盘读取与内存占用。
- 用户在 AskUserQuestion 超时边界提交答案时，用户响应与自动超时现在只允许一方处理 pending；已开始保存的答案不会再被迟到的超时拒绝覆盖。
- 已安装 Skill 的 `.env` 超过 1 MiB 时，密钥联动扫描现在会明确提示文件大小限制，不再误报为不可读或不可写。
- Agent 创建或更新蜂群任务时，文件写入模式现在只会提示并接受当前有效的“仅追加”与“更新”选项，不再公开会被运行时拒绝的旧值。
- 从扫描详情快速发布 Skill 时会在写入本地关联前复查完整源文件；预检后只改 frontmatter 也会停止关联，避免刷新后误信任过期状态。
- 旧资源仓库的 Workflow 自动找回现在会拒绝符号链接和越出配置仓库的目录，避免启动扫描误把仓库外的工作流导入 Synapse。
- 关闭、重置或退出 Agent 会话时，待回答的 AskUserQuestion 现在会持久化为已取消，刷新或重启后不再只显示含义不明的“已结束”。
- Workflow 运行历史现在会先按数量上限筛选最新快照文件，再读取、迁移和整理内容；大量残留历史文件不再让列表请求解析全部快照。
- Skill Repository 上传完成后会重新确认本地 Skill 仍与上传快照一致；源目录被删除或替换时不再重新建目录并写入错误的云仓库关联。
- Skill 的 `.env.example` 现在会在安装检查和环境配置物化前执行 1 MiB 上限校验，超限文件不会继续转码、解析或合并。
- 内容页从编辑器卸载 Skill 或 Rule 时，确认弹窗现在明确说明会移到系统废纸篓且可以恢复，不再误写为永久删除。
- 旧内容仓库自动找回 Workflow 时，现在会隔离当前界面无法安全操作的异常 ID，并记录迁移失败诊断，不再恢复出无法打开、删除或导出的条目。
- Workflow Call 使用模板模式参数绑定时，保存和检查现在会提前阻止未绑定的模板变量，不再等到运行子工作流时才失败。
- Agent 读取私有 Skill Repository 列表、详情或通过仓库 ID 解析公开地址时，现在会先经过内容读取权限检查，并记录不含文件内容的审计结果。
- Skill 环境配置里已编辑但为空的密钥值，现在会显示为待保存，关闭弹窗前也会提示确认，不再无提示丢失。
- Skill 环境配置一次保存多个密钥后，现在只遍历并解析一遍受信任安装目录，再按密钥生成独立扫描结果，避免按密钥数量重复全量读取 `.env`。
- IDE 管理批量移除 Skill 后，如果文件已进废纸篓但安装状态刷新失败，现在会保留并显示公共卸载服务的 warning，不再按纯成功静默关闭。
- Synapse Skill 批量安装同时出现失败和清理提醒时，顶部反馈会优先提示“部分安装失败”并保留提醒内容，不再让 warning 掩盖失败结果。
- 标准安装器现在会在用户确认后把空字符串 Skill 环境配置保存到密钥库，后续重装和密钥联动可以继续复用该配置。
- 升级找回旧仓库工作流时会限制扫描的仓库、目录、历史版本、文件大小和总时长，避免异常大的历史仓库长期阻塞工作流列表首次加载。
- Skill 卸载扫描现在会跳过超过 1 MiB 的 `SKILL.md` 并继续检查其下级目录，避免异常大文件占用过多主进程内存和磁盘 IO。
- 批量移除 Skill 部分成功后，“重试未处理项”现在只会再次提交失败目标，不再把已移到废纸篓的 Skill 误报为目标变化。
- Workflow 迁移失败或来自未来版本时，点击异常条目会直接显示具体隔离原因；可原样导出的未来版本同时提供备份入口，不再打开编辑器后只提示重试。
- Agent 用户提问会先保存问题与回答状态，再继续通知模型；保存失败时会明确提示并保留重试机会，刷新后不再丢失已经提交的选择。
- Skill 环境配置现在按大小写精确匹配配置键与密钥名；遇到仅大小写不同的同名密钥会明确阻止关联，不再把值写入错误的环境变量。
- Synapse Skill 批量安装失败后，刷新状态或单项安装成功会清除已解决目标的旧错误，不再继续显示过期失败原因。
- Skill 上传云仓库或重试本地关联时会复查身份文件，避免迟到请求覆盖其它窗口刚更新的云仓库关联。
- Relay 会话自动停止用户提问后会保存“已跳过”终态，刷新或导出记录时不再显示为待回答。
- 多文件或多文件夹运行预设现在会在保存时按真实路径检查重复资源，避免符号链接等价路径保存成功后又无法运行。
- 未来版本工作流现在会通过专用入口原样导出 JSON 文档，并与可重新导入的普通工作流包明确区分。
- 从 Agent 页面继续 Workflow、Swarm 等来源的会话时，现在会沿用目标会话的真实来源处理权限、运行事件与回复，不再误当作普通本地会话。
- Agent 会话现在会持久化标题来源，手动命名后即使标题与自动标题格式相同，也不会再被迟到的自动标题覆盖。
- 蜂群任务的文件写入提示现在只接受项目相对路径，避免 worker 被引导到所选项目之外写入文件。
- Synapse Skill 批量安装、更新和重装现在会在编辑器写入审计中保留各自的操作类型，便于准确追踪敏感目录变更。
- 蜂群运行面板现在分页加载 worker，每次只查询并渲染当前 100 条记录，避免大型运行在自动刷新时反复传输和渲染全部 worker。
- 修复 Workflow Call 将上游节点输出绑定到单文件或单目录参数时传入空值的问题；运行时现在会按节点 ID 读取真实上游输出。
- Terminal MCP 重命名和调整会话尺寸时现在会检查终端控制权限，并记录允许或拒绝结果，避免这两项操作绕过安全策略与审计。
- 修复 Skill 改名后重新安装会丢失旧目录 `.env` 和用户附加文件的问题；现在新目录会继承原有环境配置，旧目录会保留为桌面备份。
- Workflow 的运行事件、状态和历史快照现在会限制超大节点输出的大小、集合项数和嵌套深度，避免大响应拖慢事件传输与历史加载；工作流内部下游节点仍使用完整输出。
- Skill 上传到云仓库后若本地关联写入失败，现在可以只重试本地关联，不会再次上传或重复创建云仓库；重试前会复查本地内容和关联状态。
- Workflow 定义检查现在会执行与保存相同的本地多资源默认值校验，不再出现检查通过、保存时才提示资源无效的情况。
- 修复 Workflow 多文件或多文件夹默认值可通过符号链接重复选择同一资源的问题；保存时会按真实路径检查重复项，避免保存成功后默认运行立即失败。
- 安装内容时，已保存密钥改为在主进程安全读取，不再自动显示在变量输入框中。
- 蜂群任务 MCP 的查询、修改和运行操作现在会先校验权限，并记录不含提示词正文的审计结果。
- 密钥 App 的查看、复制和管理操作现在统一经过权限校验与脱敏审计。
- 蜂群任务 MCP 创建和更新任务时，参数说明现已与当前项目及提示词注入配置保持一致。
- 通过 MCP 查看单次蜂群运行时，现在会一并返回可定位关联 Agent 会话的 worker 明细。
- 升级密钥库时，旧配置中的不兼容变量名不再阻断密钥功能，未迁移项会保留以便修正后重试。
- 蜂群 worker 失败信息在写入运行历史前会统一脱敏并限制长度，避免错误详情携带密钥或令牌。
- Skill 仓库的可见性修改、派生和安装会话创建现在会先校验本地权限并记录脱敏审计结果。
- 同名密钥的并发创建与更新现在会按名称串行处理；历史重复记录不再被静默读取，删除时会一并清理。
- 蜂群 worker 未提供结构化摘要时不再把原始输出写入摘要；显式摘要会先脱敏并限制长度。
- Agent 文件夹附件会检查依赖和构建目录中的符号链接，避免外部路径绕过附件访问边界。
- Terminal MCP 读取分组及保存的命令前会先校验终端权限，并记录不含命令正文的审计结果。
- 密钥新增、更新或写入失败时，IPC 错误日志不再记录密钥名称、值和描述正文。
- Skill 安装失败时，renderer 与主进程 IPC 日志只记录环境值和变量替换表的数量，不再记录其中的名称和值。
- Agent 用户提问的多选回答会过滤空白项；只有空白项时会停止操作，不再把问题误记为已回答。
- 从密钥 App 更新 Skill `.env` 时会将临时文件和最终文件权限收紧为仅所有者可读写。
- Secrets MCP 对同一密钥的不同大小写名称会使用同一个权限与审计资源，避免策略被大小写绕过。
- 通过 MCP 或自动化运行工作流时，实时事件和运行状态现在会统一脱敏，不再向界面传递节点输出中的密钥、令牌或本地路径。
- 历史运行中的工作流结构无法迁移或来自更高版本时，现在会明确提示并停止复盘与重跑，不再套用当前工作流结构显示错误结果。
- 升级工作流时会先在内存完成整批迁移，再一次性原子写回；升级或找回期间若检测到原文件并发变更会停止写入，避免覆盖较新数据或留下半迁移状态。
- 清空工作流的多选文件或文件夹参数后会阻止运行并提示重新选择，不再静默恢复为默认资源。
- 查询工作流运行状态时会按真实工作流进行权限检查，并校验运行记录归属，避免跨工作流读取历史快照。
- 查询蜂群任务运行历史时必须指定任务，并按该任务执行权限检查和审计，不再通过泛资源读取全部任务的提示词快照。
- 工作流运行快照会脱敏参数默认值中的令牌、密码和本地路径，包括多选资源数组，避免这些内容进入长期运行历史。
- 发布或上传 Skill 时会把附件读取绑定到已检查的文件对象；若文件被替换、变成符号链接、移出源目录或在读取期间变化，会停止发布以避免带入目录外内容。
- 扫描或更新 Skill 的 `.env` 密钥关联前会执行密钥级权限检查和审计；策略可分别阻止读取密钥和写入关联文件，审计不会记录密钥值。
- 工作流升级会在写回前完整校验文件和文件夹参数默认值；空多选、超限、重复、类型不匹配或单选/多选不一致的数据会保留原文并进入迁移失败隔离。
- Workflow Call 缺少子工作流必填参数模板或绑定时会在检查和保存阶段直接提示，不再等到运行后失败。
- 从本地 Skill 导入云仓库前会校验并审计身份文件读取；身份文件是符号链接、目录外文件或读取期间被替换时会在云端更新前停止，避免误覆盖其它仓库。
- 升级找回旧仓库工作流时会按版本号中的数字选择最新可解析文件，避免 `v_10` 被误排在 `v_9` 之后并永久恢复旧内容。
- 回答 Agent 的 AskUserQuestion 后会先保存回答状态再继续处理后续事件，避免并发写入覆盖回答后的工具结果或助手消息。
- 数据库“复制结构”生成的 Skill 和 MCP 速查会明确批量更新/删除的 dry-run 步骤，先核对受影响行再执行真实写入。
- 桌面端与管理后台的文件大小显示统一使用共享格式化规则，负值和无效值不再显示成错误的字节数，并支持到 TB 单位。
- 安装内容时如果保存密钥失败，会保留保存确认和输入内容供用户重试或改为仅本次使用，不再擅自继续安装。
- Secrets MCP 会在调用前按真实命名规则校验密钥名称，避免合法 schema 输入到运行时才被拒绝。
- 蜂群任务后台调度或收尾失败时会保存脱敏后的运行错误并记录诊断，运行详情不再只显示泛化失败状态。
- 从 App 或 MCP 新建蜂群任务时，缺省并发数和轮次数现已统一为 1×1，避免未填写参数就计划启动 9 个 worker。
- 导出的诊断包不再包含本地密钥名称，只保留不暴露供应商、环境或用途信息的数量统计。
- 蜂群任务“停止补位”的 MCP 工具名已统一为规范的小写下划线形式，避免能力发现与实际调用名称不一致。
- 蜂群任务向下一批 worker 注入上一轮交接时会限制总上下文大小并标记截断，避免高并发任务把提示词膨胀到兆字节级。
- Secrets App 显示或复制密钥失败时，持久化日志不再记录密钥名称或错误正文。
- Agent 文件夹附件会限制附件数量、目录深度和扫描条目总数，超出时直接提示缩小范围，不再在发送前无界扫描。
- 本地 Skill 的资源仓库关联文件会拒绝符号链接、非普通文件和读取期间替换，避免扫描结果指向错误的覆盖发布目标。
- 蜂群任务运行详情读取会校验所属任务，避免通过运行编号读取其它任务的提示词和 worker 信息。
- Workflow 运行取消会校验所属工作流，避免通过运行编号中止其它工作流的活动运行。
- Workflow Call 会在执行任何子节点前完整校验子工作流，避免失效配置继承父工作流项目后在错误目录运行。
- Agent 归档会话刷新只读取最近 200 条轻量摘要，不再把所有会话的完整历史载入主进程。

## 技术调整
