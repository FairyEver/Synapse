# Pending Release Notes

## 新增功能

- 云盘 MCP 新增链接读取能力，Agent 可以解析他人发来的 `/share`、`/sites`、`/files` 链接，读取 Markdown/HTML 文本，或落盘到本地缓存后继续分析。

## 功能优化

## 问题修复

- 修复知识库资料管理中新建或重命名资料时，带前导空格的名称可能被放行并造成跨平台文件名不一致的问题。
- 修复数据导入替换模式在中途失败后可能残留部分新数据，导致恢复后的数据混入旧内容和新内容的问题。
- 修复本地 Drive 存储在服务重启后丢失文件类型信息的问题，已上传文件的预览和下载响应更稳定。
- 修复 Drive 文档图片扫描在服务端累积无用调试数据的问题，降低长时间使用后的内存增长风险。
- 修复管理员查看和下载服务日志时 URL 与用户路径脱敏不完整的问题。
- 修复 Agent 取消当前执行时，下一轮消息可能复用正在关闭的 SDK 会话的问题。
- 修复知识库资料上传遇到非法文件名时只显示通用失败提示的问题，现在会保留具体跳过原因。
- 修复 Agent 切换活跃会话失败后，旧活跃会话状态可能残留异常的问题。

## 技术调整

- 统一服务端下载文件名响应头的生成逻辑，减少 Drive 与备份下载之间的编码维护差异。
- 改进 Agent skill 发现失败时的路径脱敏，保留 `/wiki-ingest` 等 slash 名称便于诊断。
- 移除 Agent command 路由中的空 catch，避免事件持久化异常被静默吞掉。
- 修复 Agent 会话 reset/delete 后 SDK session 去重缓存未清理的问题，避免长期运行时缓存持续增长。
- 改进知识库 manifest 串行变更失败后的日志记录，后续变更继续执行时会保留前次失败诊断。
- 为 Agent skill 解析增加实例缓存，减少重复 slash 解析时的文件扫描和读取开销。
