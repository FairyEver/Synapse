# Pending Release Notes

## 新增功能

## 功能优化

- auto 并行运行改为连续补位：某个 worker 完成后会立即启动下一次任务，不再等待同批次全部结束；停止操作会等待当前任务跑完后收尾。

## 问题修复

- 修复工作流文件转换节点配置面板中英文混杂的问题，字段标签与输出模式选项已统一为中文。
- 修复损坏的内容附件摘要可能让附件预览或导出读取到仓库外路径的问题。
- 修复编辑器扫描结果缺少当前编辑器数据时内容区空白的问题，现在会显示可操作的空态提示。
- 修复 Token Usage 图表在切换深浅主题后仍沿用旧颜色，导致坐标轴和图表内容对比度不正确的问题。
- 修复工作流取消超时后，未及时归档的运行节点可能被误标记为跳过的问题；现在会标记为已取消，运行结果更准确。
- 修复 Agent 后台会话发送中的状态跟踪缺失，避免后台 phase 事件影响当前会话的同步判断。
- 清理工作流导入导出的无消费者 data-only IPC 入口，保留带文件权限检查与审计的导入导出路径。
- 修复 MCP/API 内容更新或删除遇到冲突时仍返回成功响应的问题。
- 修复 MCP 发布 Skill 附件时非法 base64 可能被当作有效内容保存的问题。
- 修复 MCP 触发工作流运行异常退出时，运行快照可能把节点输入中的敏感信息写入磁盘的问题。
- 修复查找工作流运行记录时，某个损坏快照会阻断其他正常快照被找到的问题。
- 修复通过外部工作流能力删除不存在连线时仍提示成功并刷新版本的问题。
- 修复导入含模型引用的工作流时，本机没有供应商却只禁用按钮、不说明原因的问题。
- 修复工作流编辑窗口加载失败后残留窗口状态、可能阻塞同步的问题。
- 修复新建脚本节点默认配置会立即触发校验错误的问题。
- 修复复用已最小化的工作流编辑器或运行器窗口时不会自动恢复的问题。
- 修复 HTTP 工作流节点中未绑定模板变量不能在运行前发现的问题。
- 修复内容编辑保存前未同步远端更新，可能绕过冲突提示并覆盖他人新版本的问题。
- 修复内容保存、删除或恢复已成功后，pending push 状态刷新失败却反向提示操作失败的问题。
- 修复系统日志下载在响应开始前失败时，审计记录无法关联到实际下载文件名的问题。
- 修复系统日志下载在 zip 流中途失败后响应连接可能悬挂的问题。
- 修复 Webhook shell 执行失败时错误信息可能把本地路径或密钥片段返回给调用方的问题。
- 修复管理后台多处会话、导航、权限面板、下载导出、日志筛选和错误反馈问题；会话过期或服务端异常时提示更准确，重复点击不再触发并行操作，系统日志日期筛选会同步影响列表结果。
- 修复多项工作流 P1 问题：MCP 全量更新和节点删除不再绕过同一套变更锁，缺少工作流 ID 的更新会被入口拦截；运行快照和复制报告会在写入磁盘或剪贴板前脱敏；导入工作流会清理外部项目引用，节点空项目也会正确继承默认项目；Switch 路由键编辑和自动布局使用更稳定的本地状态。

## 技术调整
