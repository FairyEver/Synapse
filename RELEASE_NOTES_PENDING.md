# Pending Release Notes

## 新增功能

- Agent 输入框支持通过粘贴或拖拽添加图片、文件和文件夹附件；图片按 `[Image #1]` 展示，文件和文件夹显示为路径上下文，并可在发送前删除。
- 定时任务现在可以逐个迁移到自动化；迁移会保留执行配置，成功后删除原任务，运行中的任务会先停止再迁移。

## 功能优化

- 云盘文件列表现在支持把本机文件或文件夹直接拖进去上传，按钮上传和拖拽上传都会上传到当前所在目录，并保持文件夹层级。
- Agent 附件发送到运行时后，图片会直接交给模型识别；项目外部的文件和文件夹路径会在新会话中加入可访问目录，旧会话遇到新增外部路径时会提示重新开启会话。
- Agent 会话导出现在会包含附件诊断信息，能看到每条带附件消息的附件类型、大小、MIME、图片哈希和是否已准备为模型输入，方便排查图片是否真的进入发送链路。
- Agent 对话调试包导出成功后会自动打开保存位置，方便直接找到导出的 ZIP 文件。

## 问题修复

- 修复正式环境云盘分享链接打开后被导向管理后台的问题，分享文件链接会继续进入公开文件访问流程。
- 修复顶部导航里 IDE 入口缺失的问题，用户可以继续从主窗口进入本机 IDE 扫描页面。
- 修复 Agent 附件消息只点发送或按 Enter 后没有真正发送的问题，并让带尾斜杠的粘贴路径按文件夹处理。
- 修复 Agent 附件进入 Electron 后被丢弃或无法通过校验的问题，图片和文件路径附件会继续交给 Agent 运行时处理。
- 修复第三方模型供应商下 WebFetch 容易因 Claude Code 域名预检 403 中断的问题；遇到同类失败时，对话里会显示更短、更可操作的提示。

## 技术调整
