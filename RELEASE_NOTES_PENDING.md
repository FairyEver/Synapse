# Pending Release Notes

## 新增功能

## 功能优化

- 关于页的当前版本号现在可以直接点击复制，排查版本信息时更方便。

## 问题修复

- 对话页右上角的模型信息现在会同时显示供应商和模型名，新建会话不会只看到供应商名称。

## 技术调整

- 部署脚本现在会显式同步本机 `server/.env` 到服务器，并在远端校验 Docker Compose 配置，避免云盘 COS 配置更新后线上仍使用旧环境变量。
