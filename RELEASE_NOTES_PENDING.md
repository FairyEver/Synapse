# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- 修复从 IDE 管理复制 Skill 时会遗漏或覆盖丢失 `.env` 等本地运行配置的问题；复制现在会完整保留安装实例，同时排除版本控制元数据。
- 修复 Workflow Call 同一子参数同时配置模板和绑定时可以保存、运行时才失败的问题；保存和检查现在会提前阻止冲突映射。
- 修复 Workflow 迁移备份首次失败后必须重启应用才能恢复的问题；修复磁盘、权限或备份文件后可在同一进程内安全重试。
- 修复通过 MCP 上传或更新本地 Skill 时会绕过云仓库写权限和审计的问题；未获授权时不再执行上传，成功与失败结果都会留下脱敏审计记录。
- 修复数据迁移失败或由更高版本创建的 Workflow 仍可被删除的问题；受保护原文和迁移诊断现在会保留，未来版本仍可先导出原文备份。
- 修复导入 Workflow 包时只校验文件读取权限、无法被工作流变更策略阻止的问题；导入保存前现在会检查变更权限，并记录脱敏的成功、拒绝或失败审计。
- 修复 prepared、云端或历史 Skill 包中的 `.env.local` 等运行配置可能被安装到编辑器目录的问题；安装前与最终落盘前现在都会拒绝除根 `.env.example` 外的 `.env`/`.env.*` 文件。
- 修复覆盖或替换不同 Skill 时会继承旧目标 `.env` 密钥的问题；只有再次确认属于同一 Skill 的目录才会保留运行配置，不同 Skill 的备份仍会完整保留。
- 修复多个项目出现相同 Agent 权限请求编号时，待处理卡片可能被隐藏或响应发往错误项目的问题；权限与提问响应现在按项目隔离。
- 修复 Workflow 编辑器可绕过数据迁移保护运行未来版本或迁移失败定义的问题；运行前现在统一完成版本迁移并阻止不兼容数据进入引擎。
- 修复 Workflow 迁移会把节点配置不符合当前 schema 的文档误判为可用的问题；无效配置现在会在迁移边界被隔离并保留原始数据。
- 修复文档模板 MCP 参数校验与实际生成行为不一致的问题，同时传入文件数据和内联数据时会在调用前明确拒绝。
- 修复 Synapse Skill 首次打开会重复准备安装源并扫描全局安装状态的问题，减少不必要的磁盘读取并避免旧刷新结果覆盖新状态。
- 修复工作流运行参数预设会先保存无效文件或文件夹路径再运行失败的问题，预设现在会在落库前完成与运行一致的资源校验。
- 优化工作流历史运行状态和重新运行的快照读取，已知工作流时不再扫描所有工作流目录。
- 修复 Skill 云仓库公开地址 MCP 的参数约束缺失问题，缺少完整仓库身份的调用会在执行前被拒绝。
- 修复 Skill 卸载扫描和执行复查会无上限读取异常身份文件的问题，损坏或过大的元数据不再阻塞卸载流程。
- 修复工作流导入失败时丢失具体原因的问题，包过大、格式错误或文件已变化时会显示可操作的安全提示。
- 修复 Skill 云仓库管理地址工具绕过本地读取权限和审计的问题，未获授权时不会构造或打开私有仓库管理页面。
- 修正工作流选项参数的 MCP 默认值说明：允许自定义只作用于运行输入，保存的默认值仍须来自选项列表。
- 修复同一个 Skill 配置文件批量更新多个密钥时，后续密钥会被自身前序写入误判为文件冲突的问题。
- 修复仅包含目录的文件夹上传准备失败后无法从任务面板重试的问题。
- 修复工作流定义预检会拒绝省略参数默认值、但同一内容又能被更新工具保存的问题。
- 限制 Skill 环境变量关联扫描的单根目录规模，超出范围时停止继续读取并提示整理后重新扫描。
- 修复 Agent 提问超时后自动拒绝失败会被记录成用户停止的问题，刷新或重启后会保留真实的已超时状态。
- 修复 Rule 和 Prompt 可被其他仓库身份从资源仓库界面更新、删除、恢复或永久删除的问题；所有入口现在统一执行创建者权限校验。
- 修复不同来源复用同一 Agent 会话标识时可能串用并改写其他来源会话的问题；活跃会话现在按平台与工作区隔离。
- 修复导入 Workflow 包时可能通过符号链接读取未授权目标文件的问题；预览和导入现在只接受读取期间未变化的普通文件。

## 技术调整

- Synapse Skill 安装源服务改由主进程统一管理，IPC 与安装流程现在共享同一个受生命周期控制的服务实例。
