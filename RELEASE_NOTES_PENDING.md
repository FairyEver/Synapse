# Pending Release Notes

## 新增功能

- 工作流参数新增“选项”类型，运行时可以从预设值中选择，也可以按参数设置输入一次性的自定义值。
- Agent 对话现在可以显示 Claude Code SDK 工具返回的图片结果，例如读取 PNG 后会在工具结果中展示图片预览，并在导出会话时带上对应图片文件。

## 功能优化

## 问题修复

- 修复工作流自定义选项参数带默认值时，下拉菜单会被默认值预先过滤的问题。
- 修复 Agent 对话顶部偶尔显示其它会话“待回答”数量，导致点击后无法定位到当前对话问题的情况。
- 修复 Agent 对话中工具读取到的图片不易发现、预览地址在开发态不可用，以及 Markdown 本地图片引用显示为不可用占位的问题。
- 修复云盘同步根文件夹在云端被移动后，后续子文件变更不再同步到本地的问题。
- 修复云盘子项在云端移出同步文件夹后，本地同步根仍残留旧文件的问题。
- 修复应用关闭期间产生的本地云盘同步变更，在下次启动后不会自动补传到云端的问题。
- 修复云盘远端下载过程中产生的内部临时文件可能被误当作本地新增文件上传的问题。
- 修复云盘同一文件先有未拉取的远端修改、随后本地修改时，本地上传可能覆盖远端且不产生冲突的问题。
- 修复云盘同步绑定初始上传或下载尚未完成时，手动同步或后台轮询可能并发进入并造成错误状态的问题。
- 修复云盘远端同一轮先重命名再更新内容时，本地可能留下新旧两份文件的问题。
- 修复本地文件夹初次上传后，同名嵌套文件可能让同步基线指向错误云盘条目的问题。
- 修复从云盘子目录发起本地同步上传后，绑定可能保存成文件名而不是真实云盘条目 ID 的问题。
- 修复管理后台直接打开 Markdown 文件时无法查看或创建评论的问题。
- 修复云盘同步排除规则中 `**/*.pem` 等递归通配写法无法正确排除文件的问题。
- 修复本地文件夹创建云盘同步失败后，云端已创建内容失去同步记录、难以恢复的问题。
- 修复云盘链接本地缓存 manifest 可能保存 token、签名等敏感查询参数的问题。
- 修复并发恢复云盘历史版本时可能突破容量配额的问题。
- 修复同步远端文件夹后，刚下载的子文件可能被误判为本地修改并重新上传成新版本的问题。
- 修复网页端上传文件夹时空目录可能静默丢失的问题。
- 修复重新分享已过期云盘链接时可能继续返回不可访问旧链接的问题。
- 修复本地同步移动或重命名遇到云端名称冲突/非法名称时，可能先移动远端条目再失败的问题。
- 修复保存已受保护 Drive Site 访问设置时可能静默更换站点密码的问题。
- 修复本地文件夹创建云盘同步时，可能静默合并云端同名文件夹并遗漏旧云端子项的问题。
- 修复导入 `.gitignore` 时 `/config.yml` 这类根目录规则会误排除嵌套同名文件的问题。
- 修复导入 `.gitignore` 时 `!` 例外规则被忽略，导致本应同步的例外文件漏传的问题。
- 修复网盘上传空文件或包含空文件的文件夹时，服务端会拒绝 0 字节文件的问题。
- 修复历史版本刚被标记为保留后，仍可能被后台自动清理删除的问题。
- 修复已停用的 Drive Site 重发后会被静默恢复公开访问的问题。
- 修复云盘同步冲突选择“保留两份”时可能覆盖已有 `.local` 本地副本的问题。
- 修复 Drive link 下载到本地时，写权限审计可能记录带密码链接的问题。
- 修复 Drive link materialize 使用 entry 范围时，可能落盘站点列表中的首个资源而不是入口页的问题。
- 修复从云端创建本地同步时，初始下载期间发生的云端修改可能被 latest 游标跳过的问题。
- 修复从本地创建云盘同步时，本地路径和云端根名称的首尾空格会被静默裁剪的问题。
- 修复受密码保护的云盘分享下载和 HTML 预览缺少私有缓存头，可能被共享缓存复用的问题。
- 修复 Agent 通过云盘工具上传本地文件夹时，嵌套空目录会在上传成功后丢失的问题。
- 修正内置 Drive skill 对删除后恢复分享链接的说明，避免 Agent 误判旧分享链接不可用。
- 修复云盘历史版本弹窗多次加载更多后，最新版本页会从列表中消失的问题。
- 修复 Markdown 文件保存成新版本后，评论侧栏仍显示旧版本批注的问题。
- 修复云盘同步执行期间，同步状态里看不到正在处理的上传、下载、移动或删除记录的问题。
- 修复从云盘同步目录外移动进来的远端文件或文件夹不会出现在本地同步目录的问题。
- 修复云盘同步根在云端重命名后，本地新增文件可能失去真实远端身份的问题。
- 修复云盘同步状态会被长期累积的历史操作记录拖慢的问题。
- 修复 Markdown 文件切到新版本后，旧版本评论线程仍可回复、编辑或删除的问题。
- 修复 MDX 编辑器保存草稿图片期间继续编辑时，下一次保存可能把本地 blob 图片地址写入云盘文件的问题。
- 修复管理后台从回收站恢复公开素材时会误走普通文件恢复流程的问题。
- 修复从云端同步到本地时，已授权写入后仍可能被额外读权限拦截的问题。
- 修复发布 Drive Site 时没有根目录 `index.html` 就无法从界面选择入口页的问题。
- 修复本地文件夹同步预览忽略排除规则，导致已排除的不可同步条目仍阻断绑定的问题。
- 修复本地同步失败后过快重试并不断生成重复“等待重试”记录的问题。
- 修复手动同步云端变更发现冲突时仍提示“已同步”的问题。
- 修复云端文件夹下载到本地时，目标目录仅包含默认排除文件却被误判为已有内容的问题。
- 修复同步状态页只显示云端名称不显示云端路径，导致同名绑定难以区分的问题。
- 修复同步冲突列表缺少本地/云端摘要，导致用户难以判断保留哪一侧的问题。
- 修复匿名或只读分享 Markdown 仍显示不可用“图片来源”入口的问题。
- 修复远端分页同步中重命名后更新又写回旧本地路径的问题。
- 修复 Drive Site 静态资源被上传 MIME 覆盖，导致 HTML/CSS/JS 响应类型错误的问题。
- 修复从回收站恢复已同步文件夹后，同步重复下载整棵子树的问题。
- 修复管理后台对不可恢复的 legacy_missing 云盘记录仍显示恢复入口的问题。
- 修复 Drive Site 链接落盘时空文件夹不会写入本地缓存和 manifest 的问题。
- 修复管理后台回收站只能操作前 50 条记录，后续文件无法恢复或永久删除的问题。
- 修复单文件云盘同步遇到无法定位文件名的本地监听事件时，可能重复上传当前文件的问题。
- 修复停止云盘同步后，停止前已排队的本地变更仍可能在后台反复重试的问题。
- 修复 Drive Site 创建、访问设置、启停、重发和删除缺少审计记录的问题。
- 修复只读云盘分享的登录访问者仍可创建持久化评论的问题。
- 修复 Markdown 评论弹窗打开时切换文件，旧选区可能被提交到新文件的问题。
- 修复云盘文本读取按字节截断时可能损坏中文或 emoji 等多字节字符的问题。
- 修复恢复云盘历史版本时可能绕过未保存修改确认并清空编辑草稿的问题。
- 修复 Drive MCP 上传或下载本地路径时会裁掉首尾空白，导致读写到另一个本地路径的问题。
- 修复 Drive link materialize 遇到大小写碰撞路径时可能覆盖本地缓存文件、导致 manifest 与磁盘内容不一致的问题。
- 修正 Drive 版本删除的 Agent 指南，明确保留版本需先取消保留再删除。
- 修复 Drive 文件夹上传工具结果缺少成功文件和保留目录相对路径，导致 Agent 无法按指南核对上传结构的问题。
- 优化分享 Markdown 批注读写的权限校验，避免每次评论操作都重复构建完整预览。
- 优化本地文件夹首次上传后的同步基线记录，避免按每个子文件夹重复拉取远端递归目录树。

## 技术调整
