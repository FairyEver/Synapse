# Pending Release Notes

## 新增功能

## 功能优化

- 跟 AI 说「做完通知我」，通知不再只弹在电脑上：只要电脑上的 Synapse 登录着，同一条消息也会进消息中心并推到手机。走的是桌面登录，不用先去建 API 密钥。System Notifier 里因此多了一颗「同步到手机」开关，原来的「启用通知」改名「本机通知」——两颗开关各管一个去处，人在外面时可以把电脑调安静、只留手机响。以前关掉过通知的，升级后仍是关着的。
- Workflow 里的通知节点从「系统通知」改名「发送通知」，与上一条走同一套去处（本机 + 手机）。
- 手机端底栏改为「主页 / 终端 / 我的」三格，不再为单个功能占位置，以后的新功能会陆续出现在主页的功能清单里。
- 手机端通知不再占一格底栏：主页右上角的铃铛随时可打开通知面板，点一条直接去到对应的会话或录音，不再需要先打开通知再点进详情。
- 手机端「我的」改成分类列表，账号、电脑、终端、录音、通知、诊断、关于各自一页，不再把全部设置铺在同一屏。
- 手机端剪贴板历史移到主页的功能里（终端页设备行上那个入口取消），列表上方会写明是哪台电脑的。
- 手机端「我的 → 通知」可以关闭 App 图标上的角标。
- 手机端「会话资源」会把打不开的链接直接标出来，不用点进去才发现。
- 分享链接改用不容易看错的字符：新链接里不再出现容易混淆的字母和数字，转录时读错一个字符就打不开的情况少很多。已经发出的旧链接照常有效。
- 手机端主页右上角会显示你正在哪一台电脑上，点一下就能换到账号里的另一台，不用先切到终端页。只有一台电脑时它只是一行字，正在用的那台掉线时名字仍然留着。
- 手机端终端工具栏的前一排改为固定的 `↑` `↓` `Tab` `回车` `Ctrl+C` `/exit` `/clear`，你自己配的指令排在它们后面。以前这一排完全照搬电脑，手机上因此没有方向键和 `Tab`——在 Claude Code 里用方向键选项、或按 `Tab` 接受它预填的建议，都得先打开键盘面板多点一下。现在两端各自定义自己前面那一排，电脑上不必为了手机先加几颗键；已经在用的电脑和新装的手机之间不会有重复的按钮。

## 问题修复

- 手机端从主页点「录音」，标题、加号和返回键回到同一行，列表紧接在下面。以前这一页顶着两条导航栏，标题和加号被挤到第二行，中间空出一大片；iPad 上列表与详情并排的右栏也会一起消失。现在两处都恢复了。
- 手机端终端输入栏弹出面板（剪贴板 / 自定义命令 / 快捷输入）里，分段选择器和下面列表之间的空白收紧了。以前这里空出一大块：那一条栏按大标题排版，给一个并不存在的标题留着位置，字号却一个字都不画。现在这一段就是系统标准的间距。

- 手机端从「终端」列表点一个会话进去，底部不会再多出一句「这个会话已经结束了」，也不会多震一下。以前点一下会**多发一次**打开请求，那一次带的是一个不是会话号的东西，于是被判成「那个会话已经结束了」——你点进去的那个会话其实好好的。同一个根因，也是更早那次「从列表点进去只有一片黑、从通知点进去正常」的真正原因：手机把列表那一行的编号看错了。现在从列表、从消息、从通知点，走的是同一个正确的编号。附带修好的是：那句话以前还会画在**别的**会话的画布上，看起来像你正用着的会话死了——现在它只出现在会话列表上。

## 技术调整
