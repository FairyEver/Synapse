# Pending Release Notes

## 新增功能

- 新增“智能体”系统应用，可以为内置“中英翻译”智能体配置模型，并管理自己创建的智能体名称、简介、系统提示词、工具能力和可选模型。
- Agent 对话现在可以在输入框选择智能体人格，并在同一会话中保持或切换；普通对话仍是默认模式。
- Web 端网盘补齐了文件上传、文件夹管理、分享、站点、公开素材和回收站能力，和客户端的云端操作保持一致；网页端仍不提供同步。
- 云盘 MCP 新增链接读取能力，Agent 可以解析他人发来的 `/share`、`/sites`、`/files` 链接，读取 Markdown/HTML 文本，或落盘到本地缓存后继续分析。
- 云盘本地同步补齐绑定向导、状态管理、文件/文件夹双向同步、排除规则和冲突处理；绑定本地路径后可以同步云端与本地变更，冲突会暂停等待用户选择。

## 功能优化

- Agent 会话侧边栏右侧状态和新建按钮对齐更稳定，Loading、未读提示和操作按钮切换时不再跳动。
- 服务端新增 Platform Media COS 配置入口，后续用户头像、智能体头像等平台承担的媒体资源可以和云盘文件分桶管理。
- 百炼内置价格预设补齐 Kimi k2.7 code 和 GLM 5.2 系列模型，GLM 缓存命中价格会按最新官方折扣估算。
- Agent 输入框底部的智能体选择入口统一为普通菜单按钮样式，和快捷输入、知识库、权限模式入口对齐。
- Agent 输入框底部的“快捷输入”和“知识库”入口去掉左侧图标，给后续菜单留出更多横向空间。
- 新增智能体的工具白名单改为勾选配置，常用工具可以直接选择，已有自定义工具会保留为可移除标签。
- 新增和编辑智能体弹窗的表单布局更紧凑，短字段会并排展示，填写系统提示词和工具白名单时空间更充足。
- Agent 对话待发送队列会逐条保留发送时选择的智能体，运行中连续发送也会按各自智能体依次执行；普通模式会在队列中显示为“普通”。
- 内置“中英翻译”智能体会更严格地只输出翻译结果，遇到问候、问题或轻微拼写错误时也不会切回普通聊天回复。
- Agent 拉到云端智能体更新后，会同步刷新已打开窗口里的智能体列表，不需要手动重开页面。
- 应用窗口的最小宽度调整为 880px，小屏或分屏时窗口可以收得更窄。
- 智能体应用改为“系统内置 / 我的”顶部切换，新增入口只在“我的”页面显示，内置智能体去掉只读查看入口，模型配置更直接。
- 智能体配置改为跟随账号云端同步；未登录时需要先登录，离线时可继续使用上次同步的智能体但不能编辑，旧本地智能体不会自动迁移到云端。
- 智能体应用换成新的专属图标，在应用列表和 Dock 里更容易识别。
- 控制台字体改为随应用本地加载，不再运行时访问 Google Fonts。
- 云盘应用图标更新为新的云端存储视觉。
- 云盘同步状态页会直接显示错误原因，并按状态给出“重试同步”“处理冲突”“继续同步”等主操作；低频操作收进更多菜单，停止同步前会确认且说明不会删除本地或云端文件。
- 云盘同步状态弹窗改为先展示同步对象列表，可以按状态筛选文件或文件夹；排除规则、冲突和同步记录移到单个同步对象详情里处理。
- 云盘同步状态弹窗列表和详情页更紧凑，路径、状态、操作和同步记录更容易扫读。
- 云盘同步状态弹窗改为固定高度，筛选入口移到顶部中间；同步对象较多时只在主体区域滚动，空态或少量内容时弹窗不再收缩。
- 云盘“我的分享”弹窗改为固定高度，并可在顶部按文件、文件夹切换查看公开链接。
- 固定高度弹窗统一标题、右上关闭按钮、居中切换项和底部操作布局，减少不同弹窗之间关闭按钮位置不一致的问题。
- 云盘路径面包屑改成完整路径条，当前目录更清楚，进入多级文件夹时也更容易扫读和返回。
- 云盘路径面包屑去掉独立背景和多余上下间距，文件列表顶部更紧凑。
- Web 端网盘移动文件或文件夹时可以选择目标文件夹，网页端和客户端的云盘移动体验更一致。
- Web 端网盘页面右侧内容区对齐 Dashboard 标准宽度和切换样式，行内低频操作收进更多菜单，列表扫读更稳定。
- Web 端网盘行内操作文案和列表布局做了收紧，重命名、分享取消和站点操作在长名称或长链接下更清楚。
- Web 端网盘和 Dashboard 按钮细节做了微调，按钮按下反馈更自然，动态数字显示更稳定。

## 问题修复

- 修复 Desktop 在网络抖动、Live 重连或并发刷新账号时可能误掉登录的问题；临时失败会保持离线已登录并自动重试，只有服务端明确判定凭据失效时才退出登录。
- 修复智能体列表表头、内容和操作按钮视觉对不齐的问题，列表扫读更稳定。
- 修复 Web 端网盘文件预览嵌入 Dashboard 时继承普通页面间距的问题，预览顶栏分隔线现在会贴合内容区边界。
- 修复 Web 端网盘根目录新建文件夹和上传文件可能失败的问题；回收站永久删除也会先二次确认，避免误删。
- 修复 Web 端网盘部分异步操作失败时无提示或弹窗直接关闭的问题；分享、站点、公开素材、移动和删除失败都会保留当前上下文并提示原因。
- 修复 Web 端网盘公开素材和站点删除缺少二次确认的问题，避免误操作。
- 修复 Web 端网盘公开素材上传已完成后刷新失败仍尝试取消上传会话的问题，避免把成功上传误处理成待取消状态。
- 修复 Web 端网盘站点切换为密码访问后看不到可用访问链接的问题；站点列表现在会显示带密码的访问链接。
- 修复云盘站点列表 MCP 会返回历史站点密码的问题；列表只返回普通访问 URL，新生成的站点密码仍会在创建或更新访问设置时返回。
- 修复云盘 Markdown 评论中文件 owner 无法删除分享参与者单条评论的问题；评论菜单会区分删除评论和删除讨论。
- 修复内置 App skill 仍提示终端分组支持已移除启动命令参数的问题，Agent 更新终端分组设置时不会再按过期说明传入无效字段。
- 修复内置 App skill 漏写终端分组保存命令工具的问题，Agent 可以按文档创建、更新、删除并启动已保存的终端命令。
- 修复内置 Drive skill 把文件历史列表误写成支持搜索参数的问题，Agent 查看历史版本时不会再传入无效字段。
- 修复内置 Drive skill 漏写公开素材列表和回收站列表搜索参数的问题，Agent 可以按文档使用搜索筛选这些列表。
- 修复云盘链接下载文件工具接受相对本地路径的问题；传入 `outputPath` 时必须是绝对路径，审批和审计会指向明确的本地目标。
- 修复 Drive 分享列表搜索参数描述错写成公开素材语义的问题，Agent 搜索分享时会按分享 ID 或文件名理解参数。
- 修复 Agent 通过云盘分享工具只修改过期时间或权限时，已有分享的密码要求、访问模式和可编辑用户被意外重置的问题。
- 修复 Drive 回收站恢复工具没有在 schema 中表达公开素材必须传 `assetId` 的问题，Agent 会更早得到参数约束提示。
- 修复 Automation 摘要在 trigger 或 executor 定义缺失时静默降级的问题，现在会记录结构化日志方便排查无效自动化配置。
- 修复 Server 非生产环境处理 http-like 500 错误时仍返回通用错误文案的问题，本地和预发调试可以看到实际错误消息。
- 修复内容商店发布弹窗在发布失败时没有反馈且可重复点击的问题，现在失败会提示错误并阻止 pending 期间重复提交。
- 修复内容商店编辑器切换公开/私有失败时静默吞掉错误的问题，现在失败会保留确认弹窗并提示错误原因。
- 修复 Skill 和 Rule 通过新安装器安装时不弹出变量替换确认的问题；内容仓库、内容商店和扫描详情重新安装现在会统一在安装前处理 `${{ NAME }}` 占位符。
- 修复终端首行提示符偶尔显示不完整的问题；新开、切换或清空终端后会先同步显示尺寸，避免调整窗口大小后才恢复。
- 修复终端清空后首行提示符可能贴住顶部工具栏、看起来被裁掉的问题。
- 修复 Agent 思考过程折叠后耗时可能显示为 0 秒的问题；现在会按实际收到的思考流首尾时间展示处理耗时。
- 修复云盘同步弹窗尺寸偏小、空态宽度不一致的问题；同步绑定现在区分“绑定已有本地项”和“下载到本地”，本地已有文件或文件夹与云盘内容一致时可以直接建立绑定，不再被卡在排除规则和不可绑定提示上。
- 修复云盘“绑定同步”弹窗里的同步方式看起来不可选择、路径字段含义不随模式变化的问题，现在“绑定已有本地项”和“下载到本地”会显示为标准可切换选项，并同步更新字段名称、选择按钮和主操作文案。
- 修复云盘同步可通过符号链接路径重复绑定同一物理目录的问题；预览和创建都会按真实路径识别已有同步目录。
- 修复云盘同步在本地文件监听事件缺少文件名时可能把同步根目录当成子文件夹上传的问题。
- 修复较大的 Markdown 文件执行图片转存时可能用截断预览覆盖完整正文的问题；超过内联编辑上限的文档不会进入转存保存。
- 修复云盘同步冲突处理失败后仍把冲突标记为已解决的问题；上传、下载或删除失败时冲突会保持待处理并显示错误状态。
- 修复本地文件或文件夹作为同步起点时，首次上传失败会把本地路径占位值保存成云盘 ID 的问题；失败会回滚未完成绑定并保留错误记录。
- 修复受密码保护的 Drive Site 静态资源仍可被共享缓存保存的问题；受保护资源现在使用私有不缓存响应头。
- 修复云盘文件“下载并绑定”完成后可能触发后台回传错误并导致应用退出的问题；同步后台操作失败时会记录错误状态，不再让主进程退出。
- 修复云盘文件夹“下载并绑定”初始化时可能误触发本地删除同步的问题；首次下载会先完成本地文件和同步基线，再开始监听本地变更。
- 修复云盘文件夹下载时嵌套文件可能额外出现在目标根目录的问题；现在会按云端目录路径还原本地结构。
- 修复云盘文件夹打包下载会丢失空目录的问题；导出的 zip 会保留云端已有的空文件夹和空子目录。
- 修复云盘文件夹同步冲突选择“用云端”时可能把远端文件夹当成文件下载的问题，文件夹冲突现在会按文件夹恢复。
- 修复新建云盘同步绑定后第一次同步云端变更可能回放历史记录、导致绑定进入错误状态的问题；新绑定会从当前云端变更位置开始跟踪。
- 修复本地同步目录里新增带文件的文件夹时，云端可能只创建空文件夹的问题；实时同步会补齐子文件上传。
- 修复同步云端变更前本地扫描失败仍继续下载或删除本地内容的问题；扫描失败会让绑定进入错误状态并阻止远端操作。
- 修复云盘同步按钮在历史失败后持续显示红色的问题；顶部同步入口现在只把冲突作为强提醒，错误绑定可在同步状态弹窗中恢复。
- 修复云盘文件绑定在远端删除后，本地文件未正确移入同步回收目录的问题。
- 修复云盘同步对象本地文件已删除后仍显示“同步关系正常”的问题；已不存在的本地或云端删除目标也会按已删除处理，不再产生误导性错误。
- 修复云盘文件夹同步的本地根目录被删除后仍可被手动同步误判为正常的问题；现在会保持错误状态，不会自动重建目录或继续上传内部回收目录。
- 修复云盘文件同步的本地文件缺失后，手动检查本地变更可能删除云端文件的问题；现在会保持错误状态，等待用户恢复本地路径或停止同步。
- 修复云盘在线编辑后本地同步文件不会自动更新的问题；同步绑定现在会定期检查云端变更并拉取到本地。
- 修复云盘历史版本删除在底层存储清理失败时仍释放容量、版本从列表消失的问题；清理失败会保留待清理状态，重试成功后再释放容量。
- 修复云盘同步同一路径冲突被重复记录的问题；重复扫描会更新已有冲突，不再让冲突数量膨胀或处理后仍停留在冲突状态。
- 修复云盘“下载到本地”同步在校验后本地目标又出现文件或内容时仍继续下载的问题；创建同步前会复查目标，避免覆盖或混入用户新建的本地内容。
- 修复初始同步失败后仍提示“已创建同步绑定”的问题；如果首次上传或下载失败，弹窗会保留并显示错误原因。
- 修复云盘文件夹同步遇到仅大小写不同的远端路径时可能写到同一个本地路径的问题；下载前会阻止本地无法区分的远端目录树。
- 修复 Agent 解析受密码保护的云盘站点链接时仍提示需要密码的问题；提供正确密码后可以正常解析站点内容。
- 修复受密码保护的云盘站点访问凭证可被伪造的问题；站点密码访问现在会使用签名凭证，知道站点链接也不能绕过密码。
- 修复 Web 端网盘 Markdown 自有文件的“图片来源”扫描和转存接口返回 404 的问题，自己的文档也可以正常使用图片来源功能。
- 修复 Web 端网盘无法从界面上传文件夹的问题；选择文件夹上传会保留浏览器提供的目录层级并走后端文件夹上传流程。
- 修复云盘在线编辑接近配额时并发保存可能突破配额的问题；保存会用原子配额占用保证用量可信。
- 云盘同步新增本地起点入口，可以从本地文件或文件夹直接创建同步绑定并上传到云盘。
- 修复云盘文件夹同步远端变更时嵌套路径和文件夹类型丢失的问题；远端新增文件夹会在本地创建目录，嵌套文件也会落到正确位置。
- 修复从嵌套云盘文件夹行菜单创建同步绑定后，后续云端变更可能被跳过的问题；同步绑定现在会保存完整云盘路径。
- 修复云盘同步遇到删除与修改冲突时界面没有确认删除入口的问题；用户现在可以直接接受删除侧来完成冲突处理。
- 修复云盘同步把缺少路径信息的无关云端变更误套到当前同步目录的问题，避免同名远端删除误移走本地文件。
- 修复云盘远端同步失败后仍推进变更游标的问题；下载或写入失败的云端变更现在可以在后续重试中继续处理。
- 修复云盘远端要求全量重扫时仍静默推进变更游标的问题；现在会保留原游标并进入明确错误状态，避免漏同步历史远端变更。
- 修复 Agent 整理云盘时创建文件夹或移动条目不会触发桌面同步的问题，远端整理结果现在会进入同步变更记录。
- 修复本地同步目录里重命名文件或移动文件夹后被同步成新上传加远端删除的问题；可确认身份的本地移动会保留原云盘条目。
- 修复云盘本地监听器启动失败或运行异常时可能被误判为同步根目录删除的问题；监听异常现在会让同步关系进入错误状态，不会触发云端删除。
- 修复云盘本地变更处理临时失败后事件被直接丢弃的问题；失败的本地变更会重新排队重试，并留下等待重试记录。
- 修复云盘同步可以用大小写变体重复绑定同一本地路径的问题，避免同一个本地目录被多个云端目标同时消费。
- 修复云盘同步失败信息可能原样保存并展示 token、密钥或本地路径的问题；同步错误记录现在会先脱敏再落库和展示。
- 修复本地起点云盘同步上传后按名称反查远端 ID 可能串到深层同名子项的问题；同步基线会只匹配直接上传的远端条目。
- 修复云盘同步排除规则只匹配根目录的问题；`node_modules/**`、`secrets/**` 这类规则现在会排除任意层级的同名目录。
- 修复云盘同步默认排除规则只在预览中展示、没有写入绑定的问题；`node_modules`、`dist` 和日志文件不会在新建本地文件夹同步时被默认上传。
- 修复云盘同步下载远端文件时可能跟随本地符号链接写到同步目录之外的问题；遇到这类路径会停止同步并进入错误状态。
- 修复云端把已有内容的文件夹移入已同步目录后，本地只出现空文件夹的问题；远端新增或移入的文件夹会递归下载子内容。
- 修复嵌套云盘文件夹“下载到本地”时会把云盘父级目录一并写入本地同步根的问题；本地根目录现在只保留所选文件夹内部结构。
- 修复 Agent 通过云盘链接落盘工具处理公开素材链接时会报“没有目录”的问题，图片和二进制公开素材现在可以按工具说明下载到本地缓存。
- 修复 Agent 通过云盘链接落盘工具处理单文件分享时可能返回空缓存的问题；单个 Markdown、文本或可下载文件会正确写入本地缓存。
- 修复 Agent 通过云盘链接落盘工具处理具体站点文件链接时可能得到空目录的问题；`/sites/.../file.html` 链接会正确落盘对应文件。
- 修复云端移动或重命名已同步文件夹后，子文件同步基线仍留在旧路径的问题，避免后续误判为新建和删除。
- 修复云端重命名或移动已同步条目后，本地同步操作仍使用旧路径的问题。
- 修复本地文件夹首次上传到云盘后只记录第一页远端树的问题，大文件夹后续不会因基线缺失而重复上传或重复建目录。
- 修复本地文件夹创建云盘同步时导入的 `.gitignore` 规则没有保存的问题，避免后续上传或监听同步到本应忽略的本地文件。
- 修复云盘上传文件夹时空目录会丢失的问题；拖拽文件夹或选择文件夹上传时会保留空文件夹结构。
- 修复云盘父文件夹和其中子项可以分别创建同步绑定的问题；现在会阻止远端父子条目被多个绑定同时同步，避免相互覆盖。
- 云盘同步访问本地路径前会经过主进程权限检查并记录审计，后续自动上传、下载、删除或移动本地文件也会留下权限记录。
- 修复 Agent 对话切换到带模型配置的智能体后仍沿用原会话模型的问题；同一轮里出现多个模型用量时，也会按各自模型价格计算费用。
- 修复 Agent 续聊时模型用量快照可能被当成本轮增量计费的问题，用量统计里的本轮费用和累计费用不再重复计算历史缓存命中成本。
- 修复 Agent 待发送队列或会话切换到智能体后，部分路径把智能体系统提示词当作用户消息发送，或在新建会话后重复热切导致“不支持切换智能体”的问题。
- 修复选中“中英翻译”等智能体后仍按普通 Agent 执行指令的问题；翻译智能体现在会在主线程系统提示词和工具权限层生效，指令文本只会被翻译。
- 修复云端内置“中英翻译”智能体提示词落后于客户端的问题，遇到问题句时不再容易切回普通问答。

## 技术调整
