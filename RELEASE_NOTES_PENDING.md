# Pending Release Notes

## 新增功能

- 云盘 Markdown 预览现在可以将 Mermaid 流程图、状态图和时序图渲染为适配深浅主题的图形，公开分享页也会使用相同效果。

## 功能优化

- 云盘 Markdown 预览中的图片现在支持适合窗口、缩放、拖拽和多图切换，并可通过鼠标、触摸或键盘操作。
- 云盘 Markdown 和代码编辑器现在支持在 macOS 使用 `Command+S`、在 Windows 和 Linux 使用 `Ctrl+S` 保存；实时协同编辑时会保存当前版本。
- Auto 工具新增 GPT-5.6 Sol、Terra、Luna 模型选项，并将最大并发数提升到 20。

## 问题修复

- 云盘 Markdown 预览和分享现在兼容 Windows 编辑器生成的 `.\`、`..\` 相对图片路径，同时继续拦截模糊路径和越界访问。

## 技术调整
