# Pending Release Notes

## 新增功能

- 桌面端新增可选账号登录，登录后会在顶栏显示账号，并可在设置里的“账号”面板同步或退出登录。

## 功能优化

- 技能、规则、提示词页面内现在直接提供仓库同步和切换入口，顶栏右侧改为账号区域。

## 问题修复

- 桌面端账号登录误用管理员账号时，现在会结束客户端的“登录中”状态，并提示改用普通用户账号。
- 桌面端账号登录打开浏览器时会经过权限检查并记录审计，账号登录码也会随服务端清理任务回收，避免长期堆积。
- Agent 对话、定时任务和工作流运行的 token 统计现在按 Claude SDK result 原始用量统一落账，避免重复计算或把 thinking 额外算入总量。
- 修复工作流入口状态读取失败时可能继续显示入口的问题，异常情况下会默认隐藏入口。
- 模型价格规则的新增、修改、启用、停用和删除现在会经过权限检查并留下审计记录。
- 变量写入失败时不再留下误导性的成功审计记录，失败原因会按失败审计记录保留。
- 通过能力接口读取仓库列表和本地路径时会先经过权限检查，并记录访问审计。
- CC 对话详情会提示转录解析失败的行，避免用户误以为缺失事件的对话是完整的。
- CC 原文搜索结果被截断时会提示结果可能不完整。
- 管理后台查看用户、团队、邀请、权限和系统概览等敏感信息时会记录成功审计。
- 修复管理后台用户、团队、邀请、审计日志等列表页在开发模式下可能一直停留在“加载中”的问题。
- 管理后台移除了模板调试入口和商业化菜单项，页面不再显示 TanStack Router 标识或 Upgrade/Billing 等模板广告文案。
- 管理后台恢复模板里的外观设置入口，可在后台页面切换主题、侧边栏样式、布局和阅读方向。

## 技术调整
