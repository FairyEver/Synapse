# Pending Release Notes

## 新增功能

- 云盘 MCP 新增链接读取能力，Agent 可以解析他人发来的 `/share`、`/sites`、`/files` 链接，读取 Markdown/HTML 文本，或落盘到本地缓存后继续分析。
- 云盘本地同步补齐自动运行闭环：云盘文件行上可以直接绑定到本地路径，启动后会恢复活动绑定、扫描本地变更并按分钟拉取云端变更；同步状态里可以查看绑定、冲突和失败操作，并支持确认删除、暂停/恢复、重新扫描、排除规则设置、解除绑定和手动重试。

## 功能优化

## 问题修复

## 技术调整
