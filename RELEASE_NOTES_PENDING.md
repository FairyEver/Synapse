# Pending Release Notes

## 新增功能

- 自动化触发器现在会展示可复制的变量，Agent、命令、脚本和 HTTP 请求执行器可以在运行时使用这些变量填入触发信息。
- 管理端和用户端新增 Live 客户端在线状态查询与实时状态流接口，为后续页面展示设备连接状态打基础。
- 桌面端新增 Live 长连接接入能力，登录用户的客户端可向服务端上报在线、心跳和断开状态。
- 桌面端账号设置新增服务器连接状态，用户可以直接看到 Live 连接是否已连接、重连中、未连接或未登录。
- 管理后台用户列表新增客户端在线状态，并可查看单个用户的桌面客户端列表。
- 用户后台个人设置新增客户端列表，可查看自己账号下桌面客户端的在线状态。
- 用户后台新增 Webhook 管理接口，后续页面可创建、停用、重置密钥并查看最近投递记录。
- 用户后台新增 Webhooks 页面，登录用户可以创建 Webhook、复制一次性完整 URL、停用或删除 Webhook、重置 secret，并查看最近投递记录。
- 服务端现在可以接收用户 Webhook 公网请求，并把通过校验的投递实时转发给在线桌面客户端。
- 桌面端现在会接收服务端转发的 Webhook 投递，并交给本机自动化事件入口处理。
- 自动化新增 Webhook 触发器，可选择用户 Webhook，并在执行器变量中使用请求方法、query、header、body 等投递信息。

## 功能优化

- 桌面端账号登录现在会在服务器临时不可用时保留本地账号信息，显示离线状态，并在服务恢复后自动重新同步。
- 桌面端账号等待浏览器登录时可以直接取消，取消后会回到未登录状态，下次登录会重新打开浏览器。

## 问题修复

- 部署脚本健康检查更稳了，失败时会指出具体卡点，避免管理后台标题变化导致部署被误判失败。
- 公网 Webhook 地址现在会通过 Nginx 转发到服务端接收接口，不会被错误重定向到管理后台。
- Webhook 请求体超过限制时现在会正确返回 413，不再被通用错误处理成服务器内部错误。
- Webhook 接收链路对异常内容和转发失败更稳了，错误请求不会泄露敏感信息，桌面在线转发失败也会记录为失败投递。
- 修复后台服务启动时的鉴权依赖问题，管理后台登录接口不会再因此无法连接。
- 管理员停用用户后，服务端会立即断开该用户已有的 Live 连接，避免已停用账号继续接收 Webhook 实时投递。
- Webhook 投递状态更新遇到短暂数据库失败后会保留真实广播结果，不再把已成功转发的投递误显示为失败。

## 技术调整

- 服务端部署脚本会在升级前自动备份生产数据库，容器文件日志也会保留在服务器部署目录，减少升级和排查时的数据风险。
- 服务端部署流程更稳了：上线前会扫描危险数据库迁移、用线上备份恢复临时库预演迁移，切换失败时自动回滚上一版服务镜像，同时保留最终切换前数据库备份供人工确认恢复。
