# Pending Release Notes

## 新增功能

## 功能优化
- Figma 连接器文档增加画板还原为本机 HTML 的提示词示例，并明确图片与 SVG 的本地保存规则。

## 问题修复
- 自动更新现在在启动完成后立即检查一次，后续每 10 分钟检查一次；设置页的主动检查行为保持不变。
- 连接器 MCP 加载失败时会提示检查本机服务、重新连接并新建对话。

## 技术调整
- 内置 MCP 连接器改为声明式定义和统一 Driver，连接状态与 Agent 会话快照不再依赖 Figma 专属逻辑。
