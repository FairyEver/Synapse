# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- 修复长任务里「覆盖检查」把已完成的回答判成失败的问题：现在只在回答声称已通读却没有对应证据，或已登记的材料既没有读取也没有修改证据时给出提示，且提示不再算执行失败、不再影响自动化结果；大文件可按实际看到的内容分次累计，只处理部分内容的单元可以声明范围，成功的编辑也可作为处理证据。
- 修复长任务覆盖检查在材料被改动后无法收尾的问题：文件读过又被编辑或重写时，覆盖改为按单一版本的区间判定，按新版本重读即可恢复，不会再出现「永远缺证据」而反复要求补读；只有覆盖、处理状态或证据冲突真的变化时才会再补正一次。读取 Synapse 已保存的工具结果也不再重复存档，避免生成层层嵌套的副本。
- 多文件任务不再被强制要求补登记材料清单：读完多个文件后会直接给出结果，只有回答声称已完整读取却没有可核对清单时才显示一条非失败提示，任务本身不会再因此多跑一个来回。

## 技术调整
