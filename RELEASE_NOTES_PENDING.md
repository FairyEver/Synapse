# Pending Release Notes

## 新增功能

## 功能优化

- 终端里的链接现在会使用系统默认浏览器打开，不再在 Synapse 内部弹出新窗口。
- Agent 工具调用成功完成后会默认收起，失败和权限请求仍保持展开，减少工具记录对对话主线的干扰。

## 问题修复

- 修复 Agent 在一轮回复中先显示正式回答、随后执行工具时，结束后正式回答从对话中消失的问题。

## 技术调整

- 发版上传到腾讯云 COS 后会自动清理旧安装包目录，默认只保留最近 3 个版本，并保护仍被更新元数据引用的版本。
