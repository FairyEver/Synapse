# Pending Release Notes

## 新增功能

- 新增 Synapse 自动化 MCP 能力和内置技能，安装后 Agent 可以通过 `synapse-mcp` 创建、修改、启用、禁用、删除、运行和排查自动化，同时读取结果会隐藏命令正文、Agent prompt、HTTP body 和敏感配置。
- 自动化触发器现在会展示可复制的变量，Agent、命令、脚本和 HTTP 请求执行器可以在运行时使用这些变量填入触发信息。
- 自动化动作新增“工作流”，现在可以用 Cron、固定间隔或 Webhook 触发已保存工作流，并把触发数据传入工作流参数。
- 管理端和用户端新增 Live 客户端在线状态查询与实时状态流接口，为后续页面展示设备连接状态打基础。
- 桌面端新增 Live 长连接接入能力，登录用户的客户端可向服务端上报在线、心跳和断开状态。
- 桌面端账号设置新增服务器连接状态，用户可以直接看到 Live 连接是否已连接、重连中、未连接或未登录。
- 管理后台新增一级设备入口：管理员可以查看全部用户设备在线状态，普通用户可以在“我的设备”里查看并重命名自己的设备；离线设备也会保留在列表中，方便识别历史连接过的客户端。
- 用户后台新增 Webhook 管理接口，后续页面可创建、停用、重置密钥并查看最近投递记录。
- 用户后台新增 Webhooks 页面，登录用户可以创建 Webhook、复制完整 URL、停用或删除 Webhook、重置 secret，并查看最近投递记录。
- 服务端现在可以接收用户 Webhook 公网请求，并把通过校验的投递实时转发给在线桌面客户端。
- 桌面端现在会接收服务端转发的 Webhook 投递，并交给本机自动化事件入口处理。
- 自动化新增 Webhook 触发器，可选择用户 Webhook，并在执行器变量中使用请求方法、query、header、body 等投递信息。
- Webhook 管理新增全局历史入口，用户和管理员都可以查看外部请求的接收、广播和客户端确认状态；删除 Webhook 后，历史记录会继续保留用于排查。
- 工作流新增“调用工作流”节点，可以在一个工作流里调用另一个工作流，并用上游节点输出或当前工作流参数自动填充子工作流输入。
- 新增 Synapse 云盘：客户端可上传文件和文件夹、管理目录、分享链接，管理员可在后台查看和删除云盘内容，Agent 可通过 Drive MCP 上传并生成交接链接。

## 功能优化

- 管理后台顶栏现在滚动时会固定在顶部，并沿用模板的半透明背景效果。
- 管理后台分页列表默认每页显示 10 条，列表首屏更紧凑。
- Webhooks 页面改成卡片列表，主要信息直接展示出来，不再需要横向滚动表格。
- Webhooks 列表卡片更紧凑，Public ID、URL、创建时间等低频信息移到详情页，列表首屏可以看到更多 Webhook。
- Webhook 投递状态文案更清楚地区分服务端接收、发送到桌面端和桌面端确认。
- 工作流 MCP 和内置工作流技能现在会说明“调用工作流”节点，Agent 创建嵌套工作流时能按子工作流参数正确传参。
- 自动化编辑器的触发器变量入口改为专用弹窗，长变量列表、搜索和 Webhook 动态路径查看更清楚。
- Webhook 自动化变量弹窗现在可以直接复制完整 payload，Agent 可以一次拿到投递里的所有 Webhook 信息。
- 自动化列表现在会把最近新建或编辑过的自动化排在最上方，运行状态刷新不会打乱这个顺序。
- 工作流列表现在会把最近新建或编辑过的工作流排在最上方。
- 桌面端账号登录现在会在服务器临时不可用时保留本地账号信息，显示离线状态，并在服务恢复后自动重新同步。
- 桌面端账号等待浏览器登录时可以直接取消，取消后会回到未登录状态，下次登录会重新打开浏览器。
- Webhook 完整 URL 现在只会在创建或重置 secret 后展示，列表中只保留掩码地址，避免误暴露一次性 secret。
- Webhook 投递记录现在会区分无在线客户端、已投递和客户端已收到，并展示哪些桌面客户端收到了通知。
- Webhook 客户端确认到达得很快时，投递记录会保持“已投递”状态，不再被稍后的广播状态写回覆盖成“已发送”。
- 管理后台页面内容与顶栏之间的间距更紧凑，列表首屏信息更贴近页面标题。
- 管理后台和用户后台侧边栏顶部不再显示无实际作用的团队切换菜单，只保留当前后台标识。
- 部署脚本遇到 Prisma migration 风险项时默认不中断，会在日志里保留待发布 migration、风险数量和 SQL 明细，方便失败后复制日志排查。
- 云盘文件操作菜单去掉了多余图标，分享、重命名、移动和删除入口更统一。
- 云盘文件列表现在用文件和文件夹图标区分类型，面包屑也收成一条完整路径，进入子目录后更容易判断当前位置。
- 云盘文件列表的分享和删除现在直接显示在每一行右侧，重命名、移动和取消分享保留在更多菜单里，常用操作不用再多点一次。
- 云盘移动文件时现在可以用树形目录选择目标位置，深层文件夹不用先切换到对应目录才能移动。

## 问题修复

- 用户后台 Webhooks 页面现在可以正常打开，触发状态为空时不会再导致页面加载中断。
- 部署脚本健康检查更稳了，失败时会指出具体卡点，避免管理后台标题变化导致部署被误判失败。
- 公网 Webhook 地址现在会通过 Nginx 转发到服务端接收接口，不会被错误重定向到管理后台。
- 本地开发环境中，用户后台生成的 Webhook 地址现在可以直接触发服务端接收接口和桌面自动化，不会被前端 dev server 当成页面路由。
- 自动化编辑页在账号未登录或 Webhook 列表暂时加载失败时，会继续显示已绑定的 Webhook 名称，避免误以为配置丢失。
- 自动化编辑器打开新建或编辑窗口时不再产生 IPC 返回值校验错误。
- 自动化编辑器在重复打开新建窗口时更稳了，偶发页面加载中断不会再直接报错关闭。
- Webhook 快速连续触发同一个自动化时会逐条排队执行，不会因为上一条还在运行而丢掉后续投递。
- 桌面端正式包的登录、Webhook 和 Live 连接不再依赖写死的服务器地址，发版时会按公开服务地址写入配置。
- 服务端部署的 WebSocket 转发配置已补齐，桌面端 Live 连接上线后可以正确握手。
- Webhook 请求体超过限制时现在会正确返回 413，不再被通用错误处理成服务器内部错误。
- Webhook 接收链路对异常内容和转发失败更稳了，错误请求不会泄露敏感信息，桌面在线转发失败也会记录为失败投递。
- 修复后台服务启动时的鉴权依赖问题，管理后台登录接口不会再因此无法连接。
- 工作流“调用工作流”节点卡片现在显示已选择的工作流名称，不再把难以识别的内部 ID 展示给用户。
- 管理员停用用户后，服务端会立即断开该用户已有的 Live 连接，避免已停用账号继续接收 Webhook 实时投递。
- Webhook 投递状态更新遇到短暂数据库失败后会保留真实广播结果，不再把已成功转发的投递误显示为失败。
- 自动化 Webhook 触发器在桌面端未登录时会提示先登录，并会在登录成功后自动刷新可选 Webhook 列表。
- 桌面端登录时如果无法打开浏览器，会提示检查默认浏览器设置，不再误报为登录状态保存失败。
- 桌面端已登录后如果系统重放旧的登录回调，不会再把账号状态误切换成登录失败。
- 服务端会清理心跳超时的桌面端 Live 连接，避免失效连接长期占用资源或错过实时投递。
- 本地 Webhook 自动化运行异常时会更可靠地收尾；应用重启后也会把中断的运行标记为失败，避免一直显示运行中。
- 自动化服务停止或重启后会清理旧的运行中标记，避免列表误显示运行中或后续触发被误判为重复运行。
- Dashboard 删除 Webhook 后会立即从列表移除对应项，即使确认框在请求完成前关闭也不会留下旧数据。
- 管理后台用户管理页在模块权限定义加载失败时会显示错误状态，不再把模块列静默显示为空。
- 管理后台用户管理页的客户端在线状态在 SSE 重连期间会保留较新的增量状态，不再被较旧快照短暂覆盖。
- 桌面端网络不可用且账号处于离线重试时，服务器连接状态会显示重连中，不再误显示为未登录。
- 管理后台管理员直接访问普通用户 Webhooks 页面时会回到系统概览，不再进入一个所有请求都 403 的页面。
- 自动化 Webhook 触发器引用已停用 Webhook 时会显示提示，避免用户误以为自动化仍会触发。
- 桌面端并发刷新账号失败时会保留已成功刷新的登录状态，避免网络抖动下被错误登出。
- 自动化 Webhook 触发器的请求 Body 动态变量现在可以读取嵌套字段，不再因 `trigger.request.body.*` 报未知变量而运行失败。
- 自动化 Webhook 触发器遇到没有请求体或缺少某个 query/header/body 子字段的投递时，会把对应变量渲染为空值，不再导致整次 Agent 运行失败。
- 服务端校验用户访问令牌遇到数据库等基础设施故障时会记录可排查日志，并返回服务暂不可用，不再误报为登录过期。
- 服务端 Live WebSocket 现在会记录认证成功、客户端注册、断开、错误和心跳超时等生命周期日志，排查实时连接问题更容易。
- Webhook 投递进入桌面自动化时会保留请求 URL 和请求接收时间，后续变量和运行记录可拿到完整请求上下文。
- Webhook 自动化事件日志现在会带上投递 ID 和 Webhook Public ID，排查投递到本机自动化链路时更容易串联。
- 用户后台 Webhooks 列表现在会显示最近投递状态，不用打开记录也能看出最近一次是否转发成功。
- Webhook 投递记录新增“已接收”中间状态，服务端异常中断时不再留下“转发失败但仍待转发”的矛盾记录。
- Webhook 请求日志现在会掩码完整触发 URL 中的 secret，只保留 publicId，避免排查日志时误暴露一次性密钥。
- 工作流 MCP 写操作在校验失败时不会再留下半保存的无效节点或连线，避免后续编辑器打开异常或定义状态被污染。
- 桌面端 Live 连接在开发代理或服务端短暂重启后会主动检测心跳超时并重连，不再出现界面看似在线但 Webhook 投递找不到在线客户端的情况。
- 服务端关闭或重启时会主动断开旧的桌面端 Live 连接，避免旧进程残留连接导致 Webhook 投递误判为没有在线客户端。
- 本地开发停止命令现在会按目标服务精确清理进程，`quit:server` 不会再把桌面端或官网开发进程一起停掉。
- 服务端部署同步和 Docker 构建现在会包含共享包源码与构建依赖，避免远端构建时因为缺少 `shared/` 或共享包类型依赖而中断。
- 工作流“调用工作流”节点现在会保留子工作流运行快照，父运行里显示的 childRunId 可以继续查看子运行详情和历史记录。
- 工作流“调用工作流”失败时会带出子工作流里的具体失败原因，例如循环调用链或嵌套深度超限，不再只显示笼统的执行失败。
- 云盘上传现在在本地开发环境没有配置对象存储时也可以正常完成，方便开发和 E2E 测试直接验证完整上传流程。
- 云盘上传失败时会显示具体失败原因，并把未完成记录标成明确状态，不再让失败文件长期显示为普通文件或一直卡在“上传中”。
- 云盘新建文件夹、重命名、移动和删除改为应用内确认窗口，Electron 环境下不会再因为系统 prompt/confirm 不可用而无法操作。
- 云盘分享取消入口现在会在已分享文件上正确显示；本地开发环境复制出的分享链接也可以直接访问文件内容，不再被前端开发服务误当成页面路由。
- 云盘客户端文件区改成更轻量的列表体验，分享页也去掉无效搜索、下载全部和图标宫格，只保留清晰的文件列表与下载入口。

## 技术调整

- 服务端部署脚本会在升级前自动备份生产数据库，容器文件日志也会保留在服务器部署目录，减少升级和排查时的数据风险。
- 服务端部署流程更稳了：上线前会扫描危险数据库迁移、用线上备份恢复临时库预演迁移，切换失败时自动回滚上一版服务镜像，同时保留最终切换前数据库备份供人工确认恢复。
- 服务端生产环境现在必须配置公开访问地址，团队邀请、密码重置和 Webhook 地址会使用同一套公开地址规则生成。
