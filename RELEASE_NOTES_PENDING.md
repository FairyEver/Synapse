# Pending Release Notes

## 新增功能

## 功能优化

- 用量分析会为已有用户补齐缺失的内置模型价格，保留已自定义的规则；价格规则也支持一键恢复内置默认，刷新后费用估算会按当前规则重新计算。

## 问题修复

- 修复 Windows 版本打开 CC 的“记录”页时可能读取失败的问题；打包时会带上记录页后台查询所需的解包依赖，避免出现缺少 `agent-redaction` 模块的错误。

## 技术调整
