# Pending Release Notes

## 新增功能

- 手机端终端的 Git 面板多了一个「迁出远端分支」：点开能看到这个仓库的所有远端分支，按远端分组、可以搜名字；点一条就把它迁出来 —— 在本地建一条跟踪那条远端分支的分支并切过去。从前远端有、本地没有的分支在手机上够不着，只能回电脑处理。本地已经有同名分支时，如果它跟踪的正是这条远端分支就直接切过去，如果跟踪的是别的分支或没有上游，会问你另起一个本地名；有未提交改动时和切分支一样先问你怎么处理这些改动。
- 这一页打开时只读电脑上已经缓存的远端分支，不联网；下拉一下才会去获取最新的。

## 功能优化

## 问题修复

- 用中文让 AI 操作 Synapse 时，很多说法找不到对应的能力：像「终端会话列表」这样的指令会返回一堆不相关的工具，「回收站里有什么」则什么都找不到。现在中文说法能正确匹配到对应的工具。英文一直正常，未受影响。

## 技术调整

- 本次新增的三个 Git 动作需要电脑端与云端一起升级才生效，**顺序是服务端 → 电脑端 → 手机端**：手机比服务端新时终端会掉线重连，手机比电脑端新时这一行会提示先升级电脑端。
