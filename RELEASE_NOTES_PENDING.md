# Pending Release Notes

## 新增功能

- 新增独立的 Skill 卸载器，可按名称搜索全局安装位置，也可指定目录搜索；选中匹配项后统一移入系统废纸篓。IDE 管理器中的单个 Skill 也会通过该卸载器处理。
- Skill 现在可以用 `.env.example` 声明安装配置；重新安装会保留已有配置值，密钥变更后可扫描受影响的已安装 Skill，并确认后按队列逐项更新。资源仓库新增 Skill 开发指南和两段可复制的本地 Agent 提示词。Windows 当前支持扫描，队列写入会逐项失败并提示手动更新本地 `.env`；运行时 `.env` 超过 1 MiB 时会安全停止处理。

## 功能优化

- Skill 发布现在会先展示准确的文件与排除摘要，再进入确认和编辑；保存后会明确区分本地提交、后台同步和远端完成状态。本地 Skill 只有在内容未变化时才会安全关联到新版本。
- Skill 开发指南现已改为可一键复制的完整提示词，用户可以直接交给本机 Agent 创建新 Skill 或修改已有 Skill。
- Skill 开发提示词现在会进一步检查子进程参数和 dry-run 的密钥边界；Synapse Skill 也会优先用本地目录更新资源，并区分本地版本生成与远端同步完成。
- 已安装 Synapse Skill 的 Agent 现在直接显示“重新安装”，不再重复显示安装状态和更多菜单。
- 移除了截图系统应用及对应的 MCP、工作流节点能力，不再加载相关原生截图运行时。
- 客户端启动后会检查已安装到各 Agent 全局目录的 Synapse Skill；发现内置内容更新时，可在提示中一键更新全部旧版本，失败项可以单独重试。
- Skill 卸载器现在会预先汇总各 Agent 全局目录中的 Skill 名称，输入名称时可直接联想选择；切换搜索目录后，联想选项也会同步更新。
- 调整 Skill 卸载器的操作区与结果区布局，扫描操作、分隔线和批量选择现在更清晰对齐。
- Skill 卸载器现在使用独立图标，与 Skill 安装器更容易区分。
- 精简了云盘顶栏：上传和新建文件夹统一收进“新建”，本地同步、分享与站点管理收进“更多”，同步状态仍会直接显示冲突和错误数量。
- 收紧底部 Dock 图标间距，让图标四周留白更均衡。

## 问题修复

- 修复发布 Skill 时可能误处理 `.env`、泄露高置信度密钥或混用资源仓库与云 Skill Repository 身份的问题；云仓库身份现独立使用 `.synapse.repository.json`，旧身份会在成功上传后安全迁移。
- 修复 Hermes 中的 Synapse Skill 更新成功后仍会反复提示更新的问题。
- 修复部分入口删除 Skill 时仍绕过 Skill 卸载器的问题；现在所有卸载入口都会统一执行目标复查、权限检查并移入系统废纸篓。
- 修复 Agent 待发送列表每行和列表底部留白偏大的问题，消息队列现在更紧凑。
- 修复 Agent 待发送消息较长时会把删除按钮挤出可视区域的问题，消息正文现在会保持单行并在空间不足时显示省略号。
- 修复 Skill 配置文件权限可能被放宽、冲突替换未保留已有 `.env`，以及排队更新可能写入旧密钥值的问题；安装期间检测到目标目录并发变化时也会停止替换。
- 修复拥有编辑权限的用户打开 Markdown 分享链接时页面显示 500 的问题。
- 修复 Windows 终端点击已保存命令后只输入内容却不执行的问题，多行命令现在会按行提交并继续执行。

## 技术调整
