# Pending Release Notes

## 新增功能

- 基础设置新增「设备名称」，macOS 和 Windows 均可自定义手机端显示的电脑名称，改名不影响设备身份。

- 新增 Portal Headless Test 测试环境连接器，支持授权回调验证、按账号隔离的安全凭据保存、断开与重新连接。
- 新增 Portal Headless 只读扩展：AI 可通过本机 MCP 获取短期授权，携带 SY 与 Portal 双重凭证查询会议室占用、个人年度协议列表及协议状态字典；年度协议详情暂未提供，可查能力以当前连接的菜单目录为准。

## 功能优化

- Portal Headless Test 连接器使用独立的 Portal 图标。
- 终端选中快捷输入后，不再显示底部重复内容及“待执行 · Enter 执行”提示条，仍由用户按回车发送。

## 问题修复

- Portal 扩展区分服务端已配置能力与当前连接可见目录，避免将目录不可见误报为部署缺失；补充跨天会议室占用时段和简洁回答指引。

- 修复电脑迁移后因设备 ID 和名称相同而互相顶掉连接的问题：macOS 和 Windows 通过机器标识区分迁移后的设备；机器标识暂时不可用时保留原设备身份。

- 修复应用长时间开着时，「发现新版本」会一直停在同一个版本号上的问题；没有点下载的更新会定期重新确认，版本已被更晚的版本取代或已经下线时，界面提示会跟着更新。
- 手机上点「快捷输入」里的一句话、再按发送，句子现在发得出去。这类句子是多行的，之前会被回一句「一次只能提交一行命令」，什么也没送出去；现在整句话会送进终端并提交。终端里的程序不收多行内容时，提示会说明这一点，而不是只说没法提交。

## 技术调整
