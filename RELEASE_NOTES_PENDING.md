# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- 修复文件夹型云盘同步在无法确定云端条目的本地位置时，会把云端目录结构建到同步目录里的问题；现在会停止本次校验并给出提示，不再改动本地文件。
- 优化文件夹型云盘同步的路径定位：改为依据云端条目自身的父目录关系判断位置，不再依赖绑定的路径记录，避免该记录与实际位置不一致时同步失效。
- 优化文件夹型云盘绑定在云端位置发生变化时的处理：会先做一次完整核对再继续同步，并自动修正绑定的路径记录。
- 修复 Agent 在读取数据量较大的内置工具结果（如云盘目录树、同步状态）时会中断当前对话并提示「输出结构无法安全替换」的问题；现在会截断超出上下文的部分并保留完整副本，供后续按需读取。

## 技术调整
