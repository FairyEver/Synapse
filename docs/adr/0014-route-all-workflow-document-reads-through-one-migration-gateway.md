# 所有工作流文档读取统一经过迁移入口

本地存储、旧包导入、新包导入和运行快照展示统一调用同一个工作流文档迁移入口，并只产生当前文档、迁移失败或不支持的未来文档三种显式结果。MCP、IPC、编辑器、Workflow、Automation 和子工作流调用不得自行解析或兼容原始数据；现有宽松归一化和异常恢复为空工作流的旁路应被移除。通用迁移器只负责内存编排，工作流存储适配层负责备份、并发检查、失败隔离和原子持久化，以防止不同业务入口形成不同的兼容语义。
