# 工作流分享包不分发节点实现代码

`.synapse-workflow` 只携带工作流数据、依赖声明和兼容性元数据，不携带节点实现、插件代码、可执行文件或安装脚本。节点分享契约必须声明能力 ID、最低兼容版本，以及可选的可信安装来源标识；分享包不得提供任意下载 URL，也不得通过未来附件能力夹带可执行内容。

接收方缺少必需节点能力时必须阻止导入。如果 Synapse 的可信能力仓库能够提供兼容版本，导入兼容性步骤可以发起独立的能力安装流程，按正常权限与审计规则确认，安装完成后重新预检。导入工作流本身不得静默安装或执行代码，避免把数据分享文件变成未经确认的软件分发入口。
