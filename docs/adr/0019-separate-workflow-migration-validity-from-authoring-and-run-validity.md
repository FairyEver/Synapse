# 区分工作流迁移有效性与编辑运行有效性

工作流迁移只把无法识别基础文档结构、无法完成显式字段转换或包含未知节点类型等情况判为迁移失败；结构可识别但不满足当前业务校验的历史工作流仍迁移并加载到编辑器，由现有校验界面提示用户修复，修复前禁止保存和运行。这样可以保留早期版本允许保存的空 Prompt 等历史草稿，同时不再用空工作流覆盖异常数据；项目、供应商、文件是否仍存在等运行环境问题同样不属于 schema 迁移失败。
