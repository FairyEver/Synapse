# 接纳旧工作流中合法的同名布局方向字段

工作流 schema 从 `2.7.0` 升至 `2.8.0` 时，迁移为缺失的 `layoutDirection` 写入 `horizontal`，保留已经是 `horizontal | vertical` 的同名未知字段，遇到其它同名值则迁移失败并保护原文。迁移不运行自动布局、不改节点坐标；该策略在建立必填严格枚举的同时遵守未知字段保留约束，避免静默覆盖实验数据或损坏数据。新建工作流显式写入 `horizontal`，旧运行快照只在读取时执行相同的内存迁移。
