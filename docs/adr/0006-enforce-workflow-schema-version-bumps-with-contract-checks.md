# 工作流 schema contract 变化必须通过门禁升版

工作流 schema contract 由稳定快照和摘要自动检测变化；一旦 contract 变化而 schema 版本、迁移声明或历史 fixture 验证缺失，本地测试和 CI 必须失败。不静默自动递增版本，因为程序无法可靠判断 SemVer 级别或自动生成正确的数据转换；开发者必须显式确认升版级别和迁移内容，工具负责同步版本、contract 快照和迁移入口，以防止遗漏升版和“只改版本号”的伪迁移。
