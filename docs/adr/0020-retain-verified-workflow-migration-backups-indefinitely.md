# 永久保留经过校验的工作流迁移备份

每次工作流持久化数据需要首次执行新的 schema 迁移链前，先把原始 `workflows.json` 字节写入专用迁移备份目录，备份名称记录版本范围和源文件 SHA-256，并在落盘后重新校验字节数与 SHA-256；校验失败时禁止任何迁移写回。相同源摘要只生成一份备份，所有成功备份永久保留且不自动清理，以现有工作流 JSON 体积换取最高的数据恢复确定性。
