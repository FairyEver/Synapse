# 保留工作流自动布局 MCP 输入并统一持久化方向

UI 自动布局和共享布局函数都以工作流持久化的 `layoutDirection` 为方向来源，只在 Dagre 边界映射为 `LR | TB`。既有 MCP `direction?: "LR" | "TB"` 保留为兼容适配层：省略时使用当前持久化方向，显式传入时先映射产品方向，再将方向与全部节点坐标原子更新；同方向只重算坐标，计算失败不写入。不新增并存的产品方向参数，以免出现两个方向来源；实现时必须同批更新 MCP 工具描述和 API 文档，明确显式 `direction` 会改变工作流布局方向。
