# 文档提取只接收主进程验证后的字节

文本提取的文件权限检查、审计和安全读取统一由 Electron 主进程完成。主进程只接受普通非符号链接文件，在读取前后校验文件身份与大小，并把已验证字节转移给受资源限制的独立 Worker；Worker 不接收、解析或重新打开用户路径。

该边界把路径竞态、符号链接、权限决策和审计留在可信入口，同时把第三方文档解析器隔离在 Worker 中。输入大小、输出大小、PDF 页数、执行时间和 Worker 内存必须有明确上限，超限或解析失败返回稳定错误码，不得静默截断。日志与审计不得记录正文、内容片段或未脱敏完整路径。
