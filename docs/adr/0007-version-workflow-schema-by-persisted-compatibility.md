# 工作流 schema 按持久化兼容性使用 SemVer

工作流 schema 的 `major` 用于删除、重命名、类型或基数变化、可选改必填、允许值收窄或既有语义变化；`minor` 用于新增节点类型、可选字段、允许值或具有确定默认语义的配置；`patch` 用于不改变领域含义的持久化规范化和迁移结果修正。已发布的迁移不得原地改写；纯 UI、文案、布局、性能、日志，以及不改变持久化结构或既有数据解释方式的执行器修复不升 schema 版本。
