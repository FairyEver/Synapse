# Pending Release Notes

## 新增功能

- Agent 每轮完成后可查看本轮修改的文件与 Diff，并在文件未被再次编辑时安全撤销最新一轮文件修改；宽屏在会话右侧审查，窄屏自动切换到独立详情视图。

## 功能优化

## 问题修复

- 修复添加项目必填校验失败后，键盘焦点仍停在提交按钮的问题。
- 修复项目名称重复时仍可保存、导致项目选择入口难以区分的问题；重复名称现在会被阻止并回到名称输入框。
- 修复新建 Rule、Skill 或提示词校验失败后，键盘焦点仍停在保存按钮的问题。
- 修复取消放弃 Rule、Skill 或提示词的未保存编辑后，键盘焦点跳到窗口开头的问题。
- 修复新建同名提示词被阻止后，错误只显示在窗口顶部且键盘焦点丢失的问题。
- 修复 CC 对话详情窗口在 macOS 窗口菜单中使用通用标题、无法与 Synapse 主窗口区分的问题。
- 修复 CC 用量记录把未定价请求显示为零费用的问题；记录汇总和展开明细现在会明确区分未定价、部分定价与已计价费用。
- 修复删除唯一价格规则草稿后，键盘焦点跳到页面开头的问题。
- 修复价格规则保存校验失败后，键盘焦点没有回到对应行和字段的问题。
- 修复关闭 API 密钥重命名弹窗后，键盘焦点跳到页面开头的问题。
- 修复关闭 API 密钥权限弹窗后，键盘焦点跳到页面开头的问题。
- 修复关闭 API 密钥使用记录后，键盘焦点跳到页面开头的问题。
- 修复取消或完成撤销 API 密钥后，键盘焦点跳到页面开头的问题。
- 修复取消创建 API 密钥后，再次打开弹窗仍保留上次名称和权限草稿的问题。
- 修复工作流编辑器节点面板无法通过键盘聚焦和添加节点的问题；节点现在支持点击或键盘添加，同时保留拖拽操作。
- 修复取消删除工作流后，键盘焦点跳到页面开头的问题。
- 修复删除工作流成功后，键盘焦点因原操作行消失而跳到页面开头的问题；现在会回到“新建”。
- 修复工作流编辑器窗口使用通用标题、在多窗口环境中无法辨认具体工作流的问题。
- 修复取消关闭有未保存修改的工作流编辑器后，键盘焦点跳到页面开头的问题。
- 修复工作流名称为空导致保存失败后，键盘焦点没有回到名称输入框的问题。
- 修复取消或完成删除自动化后，键盘焦点跳到页面开头的问题。
- 修复关闭自动化运行历史后，键盘焦点跳到页面开头的问题。
- 修复取消关闭有未保存修改的自动化编辑器后，键盘焦点跳到页面开头的问题。
- 修复关闭 Cron 编辑器后，键盘焦点跳到页面开头的问题。
- 修复取消自动化重命名后，键盘焦点跳到页面开头的问题。
- 修复 Cron 编辑器对同一无效表达式重复显示三次错误的问题。
- 修复删除本地数据库文件夹后，键盘焦点跳到页面开头的问题。
- 修复删除本地数据库当前数据表后，键盘焦点跳到页面开头的问题。
- 修复关闭本地数据库表结构弹层后，键盘焦点跳到页面开头的问题。
- 修复取消删除本地数据库数据表后，键盘焦点跳到页面开头的问题。
- 修复取消或完成删除本地数据库记录后，键盘焦点跳到页面开头的问题。
- 修复关闭或应用本地数据库筛选条件后，键盘焦点跳到页面开头的问题。
- 修复本地数据库行新增或编辑保存、取消后，键盘焦点跳到页面开头的问题。
- 修复取消新建数据库文件夹或提交空名称后，键盘焦点跳到页面开头的问题。
- 修复数据库文件夹重名时显示内部错误信息且键盘焦点离开名称输入框的问题。
- 修复新建数据表的表名校验失败后，键盘焦点仍停在提交按钮的问题。
- 修复新建数据表的列名为空、格式不合法、重复、使用系统保留名、缺少选项或服务端返回列错误时，键盘焦点仍停在提交按钮或跳到表名的问题。
- 修复按 Escape 或取消关闭新建数据表弹窗后，键盘焦点跳到页面开头的问题。
- 修复在表结构中按 Escape 取消列说明编辑时同时关闭整个弹窗、且列说明无法通过键盘进入编辑的问题。
- 修复取消新增或编辑密钥后，键盘焦点跳到页面开头的问题。
- 修复密钥名称校验失败时显示内部通道信息且键盘焦点丢失的问题。
- 修复关闭密钥明文查看后，键盘焦点跳到页面开头的问题。
- 修复取消或完成删除密钥后，键盘焦点跳到页面开头的问题。
- 修复取消新增、编辑或删除智能体与快捷输入，以及关闭 Agent 快捷输入菜单后，键盘焦点跳到页面开头的问题。
- 修复在 Dock 设置中上移或下移应用后，键盘焦点跳到页面开头、无法连续调整顺序的问题。
- 修复从 Dock 移除当前应用后，键盘焦点跳到页面开头的问题。
- 修复切换 Agent 会话后，上一个会话尚未发送的文字草稿可能出现在当前会话并被误发送的问题。
- 修复 Agent 会话重命名保存、取消或按 Escape 关闭后键盘焦点跳到页面开头的问题。
- Agent 会话右键删除和“删除其他”现在会先明确确认，取消后回到原会话，删除完成后聚焦仍保留的当前会话，避免一次误触永久清除或键盘操作中断。
- 修复终端会话重命名后键盘焦点落到页面开头的问题；保存、取消或按 Escape 关闭后会回到原会话行。
- 终端会话删除前新增确认，避免误触后立即移除会话及其保留输出。
- 修复终端删除会话、删除分组及放弃未保存更改的确认按钮缺少危险操作样式的问题。
- 修复取消删除终端分组后键盘焦点落到页面开头的问题。
- 修复 Agent、Git、终端等独立窗口在 macOS 窗口菜单中都显示相同标题、无法辨认窗口内容的问题。
- 修复 Git、终端等独立窗口切换项目或会话后，刷新可能回到上一次内容的问题。
- 修复从 Agent 打开 Git 独立窗口并关闭后，键盘焦点跳到页面开头的问题。
- 修复云盘 Markdown 在控制台紧凑布局中丢失有序列表编号的问题；非 1 起始和多层编号会与独立预览保持一致。
- 修复云盘 Markdown 在控制台紧凑布局中切换布局后 Mermaid 流程图退回源码的问题；图表会随正文布局重新渲染。
- 修复文档首页“复制 Markdown”只复制页面配置、无法得到可复用正文的问题。
- 修复中文文档站的 404 页面仍显示默认英文标题、说明和返回链接的问题。
- 修复启用 Agent 文件检查点后，新建 DeepSeek、百炼等会话因 SDK 会话存储冲突而发送失败的问题。
- 修复 Agent 项目位于另一个仓库目录内且自身不是 Git 仓库时，运行环境可能把上层仓库误当成当前项目、导致文件写入错误目录的问题；现在会始终以项目设置中的目录作为工作区根，并校验符号链接的真实目标，阻止直接文件工具写出当前项目或已授权目录。
- 修复导出 Agent 对话调试包时，本机绝对路径可能出现在会话、事件、时间线或 Markdown 记录中的隐私问题。
- 修复错误日志中的路径末段包含 token、secret 等字样时，绝对路径可能只脱敏凭据而继续暴露目录的问题。
- 修复扩展路径脱敏后，Codex 工作流调试信息中的正常工作目录和输出文件位置被误删的问题。
- 修复 Agent 已完成并显示文件检查点后，左侧会话仍持续转圈且输入框错误显示停止按钮的问题。
- 修复 Agent 新增文件时，文件检查点总计的新增行与删除行方向显示相反的问题。
- 修复取消文件撤销确认后，再次撤销会被错误阻止的问题。
- 修复 Agent 文件撤销可能忽略同内容替换、权限模式变化或父目录真实路径变化的问题；撤销前现在会校验完整文件身份。
- 修复 Agent 多文件撤销中途失败后仍显示可撤销、且成功权限检查和逐文件结果缺少审计的问题；中途失败现在会明确标记为部分撤销。
- 修复 Agent 文件撤销确认可能被错误会话复用，以及终端或子智能体修改未提示不在撤销范围的问题。
- 修复单轮超过 1000 个文件时检查点被静默丢弃的问题；现在会保留不可撤销状态和真实文件数量。
- 修复文件 Diff 中新增内容显示为灰色、无法与删除内容直观区分的问题。
- 修复主动停止 Agent 回复后被误报为“发送失败”，并把已发送内容重复恢复到输入框的问题。
- 修复主动停止恰逢工具交接或终止正在运行的工具时，已取消的工具仍显示为失败、真实失败可能误显示为取消，以及重新进入会话后处理耗时缩短的问题。
- 修复用户拒绝 Agent 工具权限后，该工具仍显示为失败的问题；拒绝结果现在会明确显示为 Denied。
- 修复同一文件可在一轮消息中被重复添加和重复发送的问题。
- 修复打开 Git 仓库时在真实状态返回前短暂误报“目录不可访问”和“无分支”的问题。
- 修复在 Markdown 表格单元格内按 Shift+Enter 换行后，保存状态要等到离开单元格才更新的问题。
- 修复 Markdown 编辑保存后，非 1 起始的嵌套有序列表可能退化为普通段落的问题。
- 修复公开素材删除成功后列表仍保留旧记录、再次删除才提示资源不存在的问题。
- 修复网页网盘目录超过首批 100 个文件后没有入口继续加载的问题。
- 修复进入账号设置时，服务器连接状态在读取完成前短暂误报为“未登录”的问题。
- 修复 macOS 导出的对话调试包等 ZIP 中混入无意义 `._` 元数据文件的问题。
- 修复 Agent 文件审查面板无法用 Escape 返回会话、关闭后键盘焦点未回到原入口的问题。
- 修复取消 Agent 文件撤销确认后，键盘焦点跳到页面开头、无法就地再次撤销的问题。
- 修复 Agent 文件撤销进行中，确认层短暂把实际文件数显示为 0 的问题。
- 修复从应用列表打开独立应用窗口并关闭后，键盘焦点未回到原应用入口的问题。
- 修复 Agent 会话独立窗口读取历史时短暂误报“供应商不可用”和“暂无消息”的问题。
- 修复已完成的 Agent 会话在切换或重新打开后，“已处理”耗时可能缩短为 0 秒的问题。
- 修复取消 App Reset 确认后，键盘焦点没有回到“重置”入口的问题。
- 修复配置备份导出会因重复校验或运行历史过大而失败，并导致刚导出的备份无法重新导入的问题。
- 修复编辑项目时只漏填名称却同时误报名称和路径都为空的问题。
- 修复按 Escape 取消项目编辑后，键盘焦点没有回到原“修改”按钮的问题。
- 修复取消配置导入确认后，键盘焦点没有回到“导入”按钮的问题。
- 修复 Agent 草稿重复添加同一文件夹、移除附件后键盘焦点跳回页面开头，以及已发送或恢复后的文件附件无法打开的问题。
- 修复切换 Agent 会话后，上一个会话尚未发送的附件可能出现在当前会话并被误发送的问题。
- 修复按 Escape 取消 Markdown 新评论后，键盘焦点跳到页面开头、无法继续操作评论的问题。
- 修复按 Escape 取消创建 API 密钥后，键盘焦点跳到页面开头、无法继续操作的问题。
- 修复取消配置导出后，键盘焦点没有回到“导出”按钮的问题。
- 修复配置备份校验失败后，键盘焦点没有回到“确认导入”按钮的问题。
- 修复关闭运行中的 Agent 会话独立窗口后，主窗口可能把多轮历史合并为一个超长“处理中”分组的问题。
- 修复 Agent 会话调试包可能保留运行目录与自动记忆文件绝对路径的问题。
- 修复 Agent 文件审查面板在窗口宽窄切换后重置 Diff 选项并丢失键盘焦点的问题。
- 修复二进制文件差异只显示通用变更提示、无法说明正文不可预览的问题。
- 修复 Agent 文件变更列表的展开状态无法被辅助功能识别的问题。
- 修复 App Reset 等延迟确认弹窗的最终危险操作显示成普通主按钮、危险语义不清晰的问题。
- 修复点击危险确认弹窗遮罩后键盘焦点掉到页面开头的问题。
- 修复取消 Git 丢弃确认后键盘焦点没有回到“丢弃改动”入口的问题。
- 修复取消 Git 提交确认后键盘焦点跳到页面开头的问题。
- 修复新建 Git 分支失败后键盘焦点离开名称输入框的问题。
- 修复取消新建 Git 分支后键盘焦点没有回到入口的问题。
- 修复切换 Git 分支后键盘焦点离开分支选择器的问题。
- 修复丢弃最后一个 Git 改动后键盘焦点落到页面开头的问题。
- 修复桌面和网页端取消或完成云盘删除后，键盘焦点没有回到对应文件操作的问题。
- 修复网页云盘中对行内操作按回车时错误打开文件夹的问题。
- 修复网页云盘的公开素材和回收站视图在刷新、深链或浏览器前进后退时丢失的问题。
- 修复网页云盘取消公开素材重命名后键盘焦点丢失的问题。
- 修复添加重复项目路径后，键盘焦点跳到页面开头的问题。
- 修复刷新 IDE 扫描结果后，键盘焦点跳到页面开头的问题。
- 修复取消删除项目后，键盘焦点跳到页面开头的问题。
- 修复取消或完成 IDE 管理批量移到废纸篓后，键盘焦点跳到页面开头的问题。
- 修复 Git 无提交仓库历史页同时显示空态和底层错误，以及重新打开提交确认仍残留上一次成功状态、阻断后续提交的问题。

## 技术调整
