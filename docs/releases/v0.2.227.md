# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- 修复 macOS 更新在腾讯云 CDN 下可能回退为全量下载的问题，后续版本更新会恢复使用增量下载。
- 修复工作流强制重新运行或删除时旧运行取消超时后仍继续执行的问题，避免同一工作流出现并发运行或删除后残留运行数据。
- 修复工作流运行报告中“快照”字段显示反转的问题，完成后的运行会正确标记为已有快照。
- 修复工作流校验错误通过 IPC 返回时丢失节点名、字段路径、重试标记和详情的问题，后续错误提示可以拿到完整上下文。
- 修复工作流取消宽限期边界上已完成节点结果被误标为取消的问题，保留节点输出、Token 消耗和费用信息。

## 技术调整
