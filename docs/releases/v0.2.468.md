# Pending Release Notes

## 新增功能

## 功能优化

## 问题修复

- 修复云盘同步绑定进入错误状态后，即使后续恢复也一直不再自动同步的问题；现在完整校验成功会自动解除错误状态。
- 修复云盘同步绑定出错后只能等用户手动恢复的问题；现在应用启动（或重新登录）时会自动重试一次，网络波动或登录过期导致的错误可自行恢复，真实故障仍会照常提示。

## 技术调整

- 云盘变更记录新增「移动前父目录」，让被移出同步目录的文件也能被及时感知，而不用等到下一次完整校验。
