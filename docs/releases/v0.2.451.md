# Pending Release Notes

## 新增功能

- 终端分屏顶栏新增最大化操作，可将当前会话展开并保留其它分屏为 100px 边栏；切换焦点或工作区后自动恢复原布局。

## 功能优化

- 桌面底部的更新下载进度显示更加紧凑，减少状态栏占用空间。
- Agent 在模型连接中断后会明确显示未完成状态，并提供“继续”操作。
- Agent 对话诊断包会汇总失败轮次、可恢复中断、API 错误和整段会话费用。

## 问题修复

- 修复 Agent 侧边栏空项目展开后产生轻微高度变化的问题。
- 修复模型流式连接中断可能被误记为正常完成、错误提示可能混入 Agent 回复的问题。
- 修复含带空格绝对路径的对话诊断 JSON 可能损坏，以及工具结果中的内嵌图片数据导致诊断内容膨胀的问题。
- 修复终端分屏退出最大化后没有恢复到此前比例的问题；点击其它分屏或主动还原时，现在会恢复最大化前的布局。

## 技术调整
