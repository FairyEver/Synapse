# Pending Release Notes

## 新增功能

- 供应商配置支持导出为文件并在另一台设备导入，导出的文件包含密钥，导入后会作为新的供应商加入列表，不会自动切换默认供应商。

## 功能优化

## 问题修复

- 修复 Agent 对话已回复完成后，发送按钮偶发停留在“停止”状态的问题。
- Agent 对话已有用量卡片时，底部 hover 工具栏不再重复显示 token 用量和美元费用，避免信息重复和价格口径误读。
- 修复 Agent 历史权限卡已失效后仍可点击的问题，并在权限请求过期时给出明确提示。

## 技术调整
