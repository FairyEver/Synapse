# 手机端录音

> 原型（可点，浏览器打开）：`~/Desktop/手机端录音/原型.html`
> 状态：2026-09-19 设计定稿待实施
> 界面用词、状态口径全部沿用电脑端 `docs/superpowers/specs/2026-09-19-meeting-recording-redesign-design.md`。两份文档冲突时以电脑端那份为准，除了本文件第 3 节明确推翻的两条。

---

## 1. 为什么要做

手机端「录音」Tab 已经有列表和详情，但**只能看，不能录**——没有麦克风采集、没有上传、没有权限申请、没有编辑。用户原话是"它有录音这个 tab，但它实际上没有录音功能"。

这一版补上采集这一头，把详情页做成和电脑端一致的两个平级视图，并让录音住进 iOS 系统里（灵动岛、实时活动、控制中心、主屏快捷操作）。

---

## 2. 现状（实施前必须核对，不要凭印象）

| 位置 | 现状 |
|---|---|
| `SynapseMobile/SynapseMobile/Features/Meeting/` | 4 个文件 348 行：`MeetingListView` / `MeetingDetailView` / `MeetingModels` / `MeetingStore`。**只读**，`MeetingDetailView.swift:4-7` 注释明写「不做播放、不做编辑」 |
| `Features/Root/RootView.swift:73-78` | Tab 已经是「录音」，符号 `waveform`。四个 Tab 是 终端 / 录音 / 需要我 / 我的 |
| `DesignSystem/Theme.swift` | `ink = Color.primary`、`paper`、`attention`（**唯一强调色，只表示「需要人」**）、`attentionFill`、`failure`、`running`、`surfaceCard(radius:)`、`Metrics.minimumTapTarget = 44`、`Haptics` |
| `Features/Voice/` | 终端输入栏的「按住说话」，用 `AVAudioEngine` 采 PCM 走腾讯云**实时**识别。**和录音是两件不相干的事**，不要复用、不要改、不要合并 |
| `Info.plist:72` | 注释写着 "No UIBackgroundModes on purpose"——App 现在故意不声明任何后台模式。见第 6 节 |
| 服务端 `server/src/meeting/` | 分块上传、转写提交与轮询、完成推送全部已上线，手机端可直接复用 |
| 桌面端 `desktop/src/modules/meeting/` | 两栏重构已上线，左列表右详情 + 语音/文字两视图 |

服务端已有、手机端直接复用的接口（`server/src/meeting/meeting.controller.ts`）：

| 方法 | 路径 | 用途 |
|---|---|---|
| `POST` | `/meetings/recordings` | 开一条录音。body `{ title?, startedAt? }`，`title` 可省 |
| `PUT` | `/meetings/recordings/:recordingId/parts/:partNumber` | 传一个分片。**裸字节，不套 multipart**。单分片上限 `MEETING_PART_BYTES` = 1 MB |
| `POST` | `/meetings/recordings/:recordingId/complete` | 收尾。body `{ durationMs, peaks }`，`peaks` 是 base64 |
| `DELETE` | `/meetings/recordings/:recordingId` | 取消。服务端会**中止**分块上传并丢弃已传分片 |
| `GET` | `/meetings` | 列表 |
| `GET` | `/meetings/:meetingId` | 详情 |
| `GET` | `/meetings/:meetingId/audio-url` | 音频的下载签名地址（播放用） |
| `GET` | `/meetings/:meetingId/peaks` | 波形数据（0–255 字节数组） |
| `POST` | `/meetings/:meetingId/transcription/retry` | 转写失败重试 |
| `PATCH` | `/meetings/:meetingId` | 重命名 |
| `DELETE` | `/meetings/:meetingId` | 整条删除（级联） |

**不需要新增任何服务端接口。** 服务端本次只改一行推送文案（见第 6 节）。

---

## 3. 这一版推翻的两条旧结论

| 旧结论 | 出处 | 现在 |
|---|---|---|
| **手机端录音是非目标** | `docs/superpowers/specs/2026-09-19-meeting-recording-design.md` §9，被两栏重构 §8 沿用 | 就是这一次要做的事 |
| **手机端不做播放** | 同上 §4.7，以及 `MeetingDetailView.swift:4-7` 的注释 | 改成做「语音 / 文字」两个平级视图（见决策一） |

要认下来的四条链路：**麦克风权限与后台音频 mode**、**AVAudioSession 中断**、**挂起状态下的分块上传**、**两端共享同一条服务端记录**（手机录的和电脑录的在同一个列表里）。

---

## 4. 决策

实施时不要再问，除非发现了推翻它的新事实。

### 决策一：详情页做「语音 / 文字」两个平级视图

和电脑端右栏一致。切换粘性（切到别的录音仍保持当前视图）。**转写失败时默认落在文字视图**（沿用电脑端 §4.5）。

这是本次**代价最大**的一项：手机上要新增音频下载、边下边播、播放会话管理，而播放会话和录音会话在 `AVAudioSession` 上是相互打架的（要 `AVAudioSessionCategoryOptionMixWithOthers` 与路由切换处理）。

**被否决的替代方案**：只做文字视图（退回现状，`MeetingDetailView` 现在就是这样）。

> **这一项可以单独砍掉。** 如果实施时觉得不划算，砍掉「语音」视图等于砍掉实施计划的阶段 5，其余全部不受影响。砍之前问一次用户。

### 决策二：麦克风被别的 App 抢走 → 自动暂停、自动恢复、不询问

来电、闹钟、其他 App 放视频，iOS 会直接掐掉录音会话。电脑端不存在这个场景，没有先例。

- 中断发生：暂停采集，界面提示行写明「录音已暂停，麦克风被其他应用占用」
- 中断结束且 `AVAudioSession.interruptionEnded` 给出可恢复提示：自动继续录**同一条**
- **界面不出现任何询问**

**为什么不是直接结束这条**：一场四十分钟的会，一个电话就断了，而且不能续。
**为什么不是静默录进一段空白**：那会让转写结果里多出一段不存在的静音，而 0.8 元/时照算。

注意：**用户不能手动暂停**。暂停只由系统中断触发，所以和「没有暂停」这条产品口径不冲突。

### 决策三：不引入红色，全部用主题色

电脑端的口径是「语音备忘录的红色是它自己的品牌主色，Synapse 不引入自定义颜色」，桌面端波形用的是主题前景色。手机端跟随：滚动波形、取消/完成按钮、实时活动里的内容全部用 `Theme.ink` / 系统语义色。

唯一的例外是**状态栏那个橙色麦克风指示点**——那是 iOS 自己画的，App 不做也做不了。

> 2026-09-20 修订（用户指示，**只对实时活动开口子**）：锁屏卡片和灵动岛改用系统红，
> 形状也照着语音备忘录那条抄（圆点、播放头、圆形停止键）。用户对照两张截图提的要求
> 是「和语音备忘录从尺寸到颜色完全一致」，那就不可能不引入红。
>
> 边界写清楚，别让它扩散：
>
> - **改的只有锁屏实时活动和灵动岛。** App 内（录音页、列表、详情、顶部胶囊）和电脑端
>   仍然全部是主题色，一个红点都没有。代价是内外不一致，这是用户明确选下的取舍。
> - 用的是 `Color.red`（系统红，深色下就是语音备忘录那个珊瑚红），不是自调的 hex。
> - 波形**不再是两边共用的一套比例**：App 内仍是 1.5 pt 细柱，实时活动是 3.6 pt 圆点。
>   上一版为了「同一个东西两处一样」让 `MeetingAudio` 读共用常量，这次退回去了 ——
>   两处现在有意长得不一样，共用只会互相牵扯。这一条推翻本节上一版的补记。
>
>   2026-09-20 再修一次（见 §5.4 的第二次修订）：实时活动那条圆点整条删了，**那一边不再
>   有任何波形**，所以「共用一套比例」这个问题连同它一起消失。App 内 1.5 pt 细柱照旧。

### 决策四：录音中允许离开录音页

录音页下滑收起，录音继续，App 内顶部出现一枚玻璃胶囊（计时 + 完成）。灵动岛和锁屏实时活动同步。

**被否决的替代方案**：录音期间锁死在录音页。更简单、不会误操作，但四十分钟的会里用户什么都干不了。

### 决策五：开始录音是导航栏右上角的加号

和终端一致（`Features/Sessions/SessionListView.swift:65-78` 的 `.toolbar { ToolbarItem(placement: .topBarTrailing) { Button { } label: { Image(systemName: "plus") } } }`）。**不要自创悬浮按钮。**

点加号**直接进录音页，不先起名字**——电脑端也是点「开始录音」直接进，名字事后在详情页点标题改。

**和终端的一处不同**：终端的加号在「没选电脑 / 电脑离线」时置灰，因为终端得由电脑来建。录音不依赖任何一台电脑在线（手机上采集、服务端转写），所以这个加号**没有禁用条件**，不要照抄终端的 `disabled`。

### 决策六：系统集成的范围

做：**灵动岛三态**、**锁屏实时活动**、**控制中心控件**、**主屏长按 App 图标的快捷操作**、**转写完成的通知**。

不做：**桌面 Widget**（用户明确排除）、桌面小组件、待机模式。

暂缓（等决策一确认后再定）：**锁屏播放控件**（Now Playing）与 **Siri / 快捷指令 / 侧边按钮**。两者和实时活动上的按钮共用同一份 App Intent，加进来很便宜，但只有确定做播放才有意义。

### 决策七：谁本机还留着残片，谁负责收尾

**这不是设计偏好，是不改就会出 bug。** 详见 §6.7。

一言以蔽之：**只有本机还留着这次录音残片的那一端，才有权收尾它。** 桌面看本地 spool 目录里有没有这个 `recordingId` 的分片，手机看本地还有没有那次的 m4a。

---

## 5. 交互规格

### 5.1 列表

沿用现在的 `MeetingListView`，只加两样：

- 导航栏右上角的加号（决策五）
- 列表往下滚时导航栏收成玻璃条，内联标题「录音」淡入

行内容不变：标题 + 副标题（开始时间 · 时长）+ 状态徽标。**不加**搜索、不加筛选、不加分组标题以外的任何东西（与电脑端一致）。

**手势**：
- 点整行进详情
- 向左滑露出「删除」
- 长按：整行抬起 + 其余内容模糊 + 下方浮出三个玻璃圆按钮（重命名 / 复制全文 / 删除）——这是 iOS 的上下文菜单，**不要做成底部动作表**

### 5.2 录音页

```
              新录音
              00:42
     ┌────────────────────────────┐
     │              ▁▃▅█▇▅▃▂▁▃▅▇█ │  ← 贴右边缘，从右往左长
     └────────────────────────────┘
            没有听到声音              ← 只在一直没声音时出现
                                    
           录音会保存，用于转写
        [  取消  ]    [  完成  ]
```

**波形与电脑端同一套口径**（电脑端 §3.6，明确继续有效）：

- **必须是真数据**，麦克风流同时喂给编码器和分析节点，每 28 毫秒取一次时域数据算振幅。不是随机柱状图。
- **密度固定**：一个采样永远占同样宽的槽位，录得越久不会把整段压进同一个框，而是旧的从左边滚出去。
- **贴右边缘、从右往左长**：最新的在右边缘。
- **只回看最近 5 秒**：槽位数同时受时间窗口和画布宽度限制，取小的那个。
- 配色用主题前景色，随深浅色走。

**提示行**（高度固定，不跳）：

| 情况 | 文案 |
|---|---|
| 4 秒没听到声音 | 没有听到声音（听到过一次就永不再提示） |
| 麦克风权限被拒 | 未取得麦克风权限，波形为示意（不阻断录音） |
| 被别的 App 抢占 | 录音已暂停，麦克风被其他应用占用 |
| 分片上传失败 | 红色，带上下文原因 |
| 其余 | 空 |

**没有暂停按钮。** 只有取消和完成。

**点完成立刻回列表**，不出现等待界面。**界面任何位置都不体现上传**。

### 5.3 详情页

头部：标题（可点击就地改，回车提交 / Esc 取消）+ 「开始时间 · 时长」。导航栏右侧一个「⋯」（重命名 / 删除）。

两个平级视图，分段控件切换：

**语音视图**——整段铺满宽度的波形卡片 + 播放头 + 播放键 + ±15 秒 + 「当前 / 总时长」。点波形任意位置跳转。**没有**倍速、时间刻度、缩略图、当前句高亮。

**文字视图**——一张卡片 + 自然段（约 110 字一段，服务端已按此存储）。底部悬浮「复制全文」玻璃胶囊，**没有文字时置灰而不是隐藏**。

**状态**（沿用电脑端 §4.5 与 §4.8）：

| 状态 | 表现 |
|---|---|
| 转写中 | 卡片顶部进度条 +「识别中 · 已用 01:20」+「48 秒的录音，预计 1 分左右完成」+ 已转好的部分 |
| 转写失败 | **默认切到文字视图**，显示失败原因 +「重试」。重试不需要重新上传 |
| 已完成 | 正常 |
| 录音被删（历史数据） | 语音显示「录音已删除 / 文字仍保留，切到「文字」查看。」；文字正常 |
| 没有识别到语音 | 「还没有文字 / 这段录音里没有识别到语音。」 |

**交互**：从左边缘右滑返回（交互式，拖到一半松手会弹回）。

### 5.4 系统集成

全部按系统模板渲染，Synapse 只提供数据和动作。

**灵动岛三态**（`ActivityKit`）：

| 态 | 内容 |
|---|---|
| 紧凑 | 左：红点（在录）；右：计时 |
| 最小 | 同时有两个实时活动时收成一个红点 |
| 展开 | 左：计时；右：一枚停止键 |

**锁屏实时活动**：计时 + 一枚停止键，同一行，左对齐时间、右对齐按钮。

> 2026-09-20 补记（样式按语音备忘录重做，最终口径）：
>
> 用户拿两张真机截图定的：一张讯飞的、一张系统语音备忘录的，要求「和语音备忘录从尺寸
> 到颜色完全一致」。**排布、形状、颜色都改了**，落下来的五条：
>
> 1. **外面那个宽药丸是系统的。** 讯飞（第三方）拿到的宽度和语音备忘录一模一样，所以
>    药丸、圆角、外边距都不用管，能动的只有里面的内容。这一条是「尺寸一致」的边界，
>    别再去试图改药丸。
> 2. **点阵，不是细柱。** 一排分开的小圆点（3.6 pt 宽、2.2 pt 间距），中间一根红色
>    竖线当播放头，落在整条的 59% 处。播放头左边是**已经录到的**（真实振幅，越高越长），
>    右边小一圈、压暗，是**还没到的**。播放头**位置固定**：刚开始录、采样还不够时，
>    左边缺的格子用安静的圆点补满，否则播放头会贴着最后一颗点慢慢往右挪——那是一条
>    进度条，不是这条点阵要的样子。
> 3. **红是系统红**（`Color.red`），只出现在实时活动里，App 内和电脑端不动 —— 见决策三
>    的修订，那里把边界写全了。
> 4. **两个圆形按钮**，照语音备忘录那枚停止键的形状：一圈环套一个实心块，完成的实心块
>    是红的，取消是一个叉。**没有文字**（语音备忘录也没有，文字会把点阵挤窄），读屏靠
>    `accessibilityLabel`。锁屏上直径 34 pt，灵动岛那一行要挤下两枚，28 pt。
> 5. **锁屏内容边距 14 pt**，Apple 给实时活动锁屏形态定的标准布局边距（HIG
>    「Live Activities」Specifications）。系统只画卡片和圆角。`keylineTint` 一并去掉 ——
>    硬漆成纯白会变成一圈很重的白边，那是系统自己该判断的事。
>
> 上一版补记里「波形与 App 内共用同一套比例」和「只用语义色」两条**已被本节推翻**：
> 实时活动现在有意和 App 内长得不一样。

> 2026-09-20 第二次修订（用户看真机截图后的指示，**最终口径**）：**波形整条撤掉，按钮只留
> 一枚。**
>
> 用户原话是「灵动岛和活动卡片设计太小气，并且假的音波图形就不要显示了，都是只显示一个
> 停止按钮就可以」。落下来三条：
>
> 1. **点阵（波形）全部删掉**，锁屏和灵动岛都不再有。理由不是嫌它占地方：那条点阵右半边
>    是补出来的占位圆点、左半边是压成一颗点的振幅，**看着像波形，读不出音量**——它是画出
>    来的音。随之删掉的还有 `ContentState.levels` 以及给点阵算振幅的那套常量：这份载荷每
>    秒推一次，不再有人读它就没有理由继续背着。紧凑态左侧原来那个 `waveform` 图标同属一类，
>    换成一颗红点——它至少说的是真话。
> 2. **按钮从两枚减到一枚**，锁屏和灵动岛都只有停止键。上面第 4 条那两枚挨在一起谁也大不
>    起来，而且锁屏上按它们要先认证解锁——「取消」本来也不该在没解锁的锁屏上做。取消录音
>    仍然做得了，在 App 内（卡片点一下就是录音页）；`CancelRecordingIntent` 保留，快捷指令
>    和 Siri 仍能挑到它，界面上暂时没有入口。
> 3. **元素少了，就把剩下的放大**——这是「太小气」那一句的正面要求，不是顺带：锁屏计时
>    `.title2` → `.title`、停止键 34 → 52 pt；灵动岛展开态计时 `.title3`、停止键 34 pt。
>
> 本节上面那条 2026-09-20 补记里，第 2 条（点阵）与第 4 条（两个圆形按钮）**已被本条推翻**；
> 第 1、3、5 条（药丸是系统的、红是系统红、内容边距 14 pt）继续有效。

> 2026-09-20 第三次修订（用户看真机截图后的指示，**最终口径**）：**一行，左边时间、右边停止键，
> 录音名不显示。**
>
> 用户原话：「这个锁屏的时候，这个活动的这个卡片应该左侧显示的是时间，然后不显示那个什么
> 新录音，左侧是时间，右侧是一个停止按钮就可以了。然后注意把它们对齐，灵动岛也是不需要
> 显示新录音那个名称，左侧是时间，右侧是停止按钮，注意对齐，注意……要符合那个灵动岛或者说
> 那个活动卡片它的苹果的设计规范，主要是注意边距和对齐问题。」
>
> 落下来三条：
>
> 1. **录音名从系统这一层全部撤掉**——锁屏卡片和灵动岛展开态都不再显示。它原来在锁屏上是
>    第一行的左侧小字、在灵动岛上压在计时上面。`ActivityAttributes` 里的 `title` 随之删掉：
>    没有显示位置，留着也是没人读的字段（App 内录音页照常有名字）。
> 2. **锁屏卡片收成一行**：左计时、右停止键，`HStack` 默认垂直居中，两者落在同一条中线上。
>    上一版是「名字 + 计时」一行、停止键另起一行居中，中间隔着一整行空。
> 3. **对齐和边距按规范来**，这一条是用户点名的重点：
>    - 锁屏外边距仍是 **14 pt**（WWDC23「Design dynamic Live Activities」里给锁屏形态定的数，
>      和通知同一套，两侧一致），见 `RecordingActivityLimits.contentMargin`。
>    - 灵动岛**两个区域都不自己加 padding**。每个 `DynamicIslandExpandedRegion` 都带系统默认
>      content margin（`contentMargins(_:_:for:)` 改的就是它），Apple 的要求是内容与岛的形状
>      同心、四周边距一致、别贴边；自己再加一圈只会和默认值叠。两边都不加，计时和停止键的
>      边距自然是同一个数。
>    - 上下对齐靠区域内容的默认垂直居中。上一版灵动岛左侧是「名字压着计时」的两行，计时被
>      压得比右边那枚低一截——看着不齐的根因就是它；名字撤掉之后这条自己就没了。
>    - 灵动岛计时 `.title3` → `.title2`（多出来的宽度原先是名字占的）。
>
> 补记（同一天，用户第二次看真机截图：「没有中心对齐啊，应该圆心重合」；第三次：「应该
> 类似锁屏界面那个活动卡片的布局和尺寸」）。**先记一条走错的路，免得下次再走。**
>
> **走错的那一步。** 第一次只量了当时那一版（34 pt 内容）：计时和停止键的中心在距药丸顶边
> 34.8 pt 处，比药丸中线高 7.0 pt。据此推出「内容在 [顶边距, 高 − 底边距] 这条带里居中，
> 系统默认底边距 ≈ 14 pt」，于是加了 `.contentMargins(.bottom, 0, for: .expanded)`。
> **那是空转**：下一张截图和上一张的灵动岛几何逐像素相同（连白环圆心 y=146.5 都一样，
> 而两张是不同时刻拍的）。一个未知量、一个观测值，本来就不足以定模型。
>
> **真正的模型（两组数据才定下来）。** 拿旧布局（点阵 + 两行计时 + 28 pt 按钮）当对照组：

| 版本 | 内容高 | 内容中心距药丸顶边 | 反推**内容顶边** |
|---|---|---|---|
| 旧（点阵那版） | ~48 pt | 40.8 pt | **16.8 pt** |
| 34 pt 那版 | 34 pt | 34.8 pt | **17.8 pt** |

> 两组的内容**顶边**落在同一处（≈ 17.3 pt），中心却差 6 pt —— 所以展开态的 leading /
> trailing 两块是**顶端对齐**的，内容顶边固定在距药丸顶边约 17.3 pt 处，内容多高就从那里
> 往下长，**不是**在药丸里居中。药丸高 84 pt 也是系统定死的：点阵那版 85 pt、34 pt 那版
> 84 pt、52 pt 这版 84 pt，与内容量无关。
>
> **两条推论，也就是修法：**
>
> 1. **两块必须给同一个高度**，否则中心必然错开——旧结构里 `.title2` 那行计时的中心比 34 pt
>    的按钮高 3.8 pt，就是「圆心不重合」的直接来源。现在两块都框成 52 pt、各自居中。
> 2. **把内容做成 52 pt 高，居中就是顺带的**：52 pt 从 17.3 pt 起落到 17.3…69.3，中心
>    43.3 pt，比药丸中线低 1.5 pt——肉眼即居中。上一版 34 pt 的内容中心在 34.8 pt，高 7 pt。
>
> 所以用户这次说的「类似锁屏界面那个活动卡片的布局和尺寸」，恰好也是对齐问题的正解：
> **锁屏和灵动岛统一成同一套尺寸**——计时 `.title`、停止键 52 pt（原来灵动岛那侧是
> `.title2` / 34 pt）。
>
> 左右自始至终没动过：留白由系统给，实测 19.0 / 18.0 pt（差的 1 pt 是数字字形自带的边距），
> 已经对称。按钮圆心因此落在药丸中线上、但不落在右端半圆的圆心上（横向差 7.2 pt）——要严格
> 同心得把右边留白从 18 pt 加到 25 pt，左边是 19 pt，那就左右不对称了。**取「留白一致」这一边。**
>
> 窄缝里的一个附带风险：`.title` 的「00:00」放得下，「1:23:45」放不下。旧布局就是折成了两行。
> 现在给计时加了 `lineLimit(1)` + `minimumScaleFactor(0.75)`——宁可缩字，不折行。

**控制中心控件**（`ControlWidget`，iOS 18+）：一枚圆角方块，图标 +「录音」，点一下开始。

**主屏长按 App 图标**（`UIApplicationShortcutItem`）：「开始录音」排第一。

**转写完成的通知**：已有推送通道，本次只改正文文案（见第 6 节）。

**状态栏的橙色麦克风指示点**：iOS 自己画的，App 什么都不用做，但截图、客服话术、以及「为什么这里有个橙点」都要能解释。

### 5.5 状态与异常

| 情况 | 处理 |
|---|---|
| 录音中途进程被杀 / 强退 / 更新重启 | 已经录到的部分**按正常录音处理**：下次启动自动收尾、进列表、照常转写，**界面上不出现任何询问** |
| 手机端**没有**「波形丢失」这个缺陷 | 见第 6 节第 4 条 |
| 上传中断 | 分片重传即可，已确认写入的区间不重传 |
| 取消录音 | 服务端**中止**分块上传并丢弃已传分片，不留残留（`DELETE /meetings/recordings/:recordingId`） |
| 音频超过 5 小时 | 服务端按 `MEETING_MAX_DURATION_MS` 截断；手机端在达到上限时自动收尾并提示 |
| 单条超过 200 MB | 服务端拒绝（`MEETING_MAX_RECORDING_BYTES`）。64 kbps 单声道下约 78 小时，远早于 5 小时上限触发，实际不会遇到 |
| **在电脑上删掉了手机正在录的那条** | 用户可以这么做（两端是同一份数据）。手机后续的分片上传会失败，录音随之消失。**这是用户明确要求的删除，不要回滚、不要提示、不要试图恢复**——手机端按上传失败处理，提示行显示红色文案即可 |
| **在电脑上重命名了手机正在录的那条** | 手机端不知情，也**不需要**知情。手机端只在本地持有标题的临时值，收尾后以服务端为准 |

### 5.6 文案表

| 位置 | 文案 |
|---|---|
| Tab | 录音 |
| 页面标题 / 空状态标题 | 录音 / 还没有录音 |
| 空状态按钮 | 开始录音 |
| 列表状态 | 转写中 / 转写失败 |
| 录音页默认标题 | 新录音 |
| 录音页提示 | 录音会保存，用于转写 |
| 录音页 · 没听到声音 | 没有听到声音 |
| 录音页 · 权限被拒 | 未取得麦克风权限，波形为示意 |
| 录音页 · 被抢占 | 录音已暂停，麦克风被其他应用占用 |
| 录音页按钮 | 取消 / 完成 |
| 详情 · 转写中 | 转写还在进行，完成后文字会自动补全。 |
| 详情 · 转写失败 | 转写失败 + 失败原因 +「重试」 |
| 详情 · 无文字 | 还没有文字 / 这段录音里没有识别到语音。 |
| 详情 · 录音已删 | 录音已删除 / 文字仍保留，切到「文字」查看。 |
| 详情 · 按钮 | 语音 / 文字 / 复制全文 |
| 复制成功 | 已复制全文 |
| 删除确认 | 删除这条录音？/「<标题>」的录音和文字会一起删除，无法恢复。/ 删除 |
| 删除成功 | 已删除 |

**一律不出现**：云盘、上传、上传中、上传进度、云端、服务器、纪要、逐字稿、发言人、生成纪要。

---

## 6. 平台能力与代价

> 这一节是本次的真正成本，实施前读完。

### 6.1 要推翻一条有意为之的决定

`SynapseMobile/SynapseMobile/Info.plist:72` 的注释写着：

> No UIBackgroundModes on purpose. The app never receives a silent push: the server sends every alert as `apns-push-type: alert`, and the app delegate has no `didReceiveRemoteNotification:fetchCompletionHandler:`. **Claiming a background mode nothing exercises is something App Review asks about.**

后台录音必须加 `UIBackgroundModes: [audio]`。这条注释和它给的理由要**一起改掉**，写成新的理由（录音在后台继续），而不是留一条自相矛盾的注释。

同时 `NSMicrophoneUsageDescription` 现在是「用于把说的话转成文字填进终端输入框，以及录像时收录声音。」——**没提录音**。权限弹窗是用户第一次见到录音功能的地方，要补。

### 6.2 要新建一个 Widget 扩展 target

灵动岛、实时活动、控制中心控件在 iOS 上**只能放在 WidgetKit 扩展里**，主 App 放不了。用户明确不要桌面 Widget——那个扩展里就是不放主屏 Widget——但 target 本身绕不开。

- 主 App 的 `Info.plist` 要加 `NSSupportsLiveActivities`
- 扩展的 `Info.plist` 要声明 `NSExtensionPointIdentifier = com.apple.widgetkit-extension`
- 扩展的 bundle id 必须是主 App bundle id 的前缀
- `SynapseMobile.xcodeproj` 是**手维护的**，仓库里没有 XcodeGen / Tuist。改 `project.pbxproj` 是本次风险最高的一步，见实施计划阶段 0

**这是本次唯一一处新增构建产物**，打包、签名、TestFlight 全跟着变。用免费开发者账号时，扩展会额外消耗一个 App ID 名额。

### 6.3 全仓没有 App Intents

实时活动的按钮、控制中心控件、主屏快捷操作、Siri 快捷指令都是 App Intent，要从零建一套。四个入口共用同一份 intent。

> 2026-09-20 补记（**强约束，不要改回去**）：实时活动上那两个按钮的意图**必须是
> `LiveActivityIntent`，不能是普通的 `AppIntent`**。
>
> 普通的 `AppIntent` 默认在 **Widget 扩展的进程**里执行。扩展里没有录音机，
> `RecordingIntentRouter.handler` 在那边永远是 nil，于是按下去等于什么都没发生，
> 而且不会有任何报错 —— 这就是它最初上线时的表现。`LiveActivityIntent` 让系统改在
> **App 的进程**里执行，必要时还会在后台把 App 拉起来（不打开界面）。Apple 对实时
> 活动上的可交互元素就是这么要求的。
>
> 配套两点：`RecordingIntentRouter.install(_:)` 会把比处理函数先到的动作攒下来补做
> （系统后台拉起 App 的那一刻，界面和模型都可能还没就位）；按在一条已经不存在的录音上
> 时由 `SynapseAppModel.handleRecordingIntent` 收掉那条孤儿活动，不留一个按了没反应
> 的按钮。
>
> 另外，**锁屏状态下 iOS 本来就不执行**实时活动上的按钮，要先认证解锁 —— 这是系统
> 规则（Apple：「On a locked device, buttons and toggles are inactive…」），不是本
> App 的缺陷，按了没反应时先分辨是哪一种。
>
> 2026-09-20 再补记（卡片点开）：卡片本身和那两个按钮是**两件事**，别让它们互相冒充。
>
> - **卡片点一下 → 落在录音界面**，不结束这条录音、也不起新的一条。机制是
>   `View.widgetURL(_:)` 贴 `synapse://recording`，App 侧在 `RootView.onOpenURL` 解析后
>   走 `NotificationRouter.Destination.liveRecording`。用 `synapse://` 是产品已有的
>   命名空间，App 的 `Info.plist` 因此新增了 `CFBundleURLTypes`（不注册 scheme，
>   `widgetURL` 交出去的链接没有 App 接得住，点一下就是什么都不发生，且无报错）。
> - `.liveRecording` 与 `.newRecording` **不能合并**：前者只在真的在录时把录音页浮
>   出来，后者才起新录音。卡片还在、录音却已经没了（App 被系统杀掉过）时，凭空起
>   一条正是用户没要的 —— 那时落回列表并把孤儿活动收掉即可。
> - Apple 的分工：**要开 App 用链接，要做事用 App Intent**（「An interaction with a
>   button or toggle should do more than open the app. If you want to offer an
>   interaction that opens the app, use `Link` and `View.widgetURL(_:)`」）。所以两个
>   按钮保持 `LiveActivityIntent`，卡片走 URL，两者各归各位。

现有代码里 `ActivityKit`、`ControlWidget`、`AppIntent` **零命中**（grep 命中的全部是 `SynapseMobile/build/` 下的构建产物）。

### 6.4 手机端没有「波形丢失」这个缺陷

电脑端有一个已知缺陷：波形只存在内存里，异常退出后恢复回来的那条录音在语音视图里是一条平线。

手机上不存在这个问题——`AVAudioRecorder` 必须落一个 m4a 文件，所以：

- 正常收尾：从内存的采样算 `peaks` 随 `complete` 上报
- 异常退出：下次启动从本地 m4a 文件重算 `peaks` 再收尾

**这是手机端比电脑端好的地方，实施时不要退化成电脑端那样。**

### 6.5 音频参数（沿用电脑端 §4.6，不要改）

| 项 | 取值 |
|---|---|
| 编码 | AAC / m4a，64 kbps，单声道 |
| 采样 | 16 kHz 单声道（腾讯云引擎要求 `ChannelNum=1`） |
| 体积 | 约 28 MB/小时 |
| 分片 | 编码每 2 秒一片；上传每 1 MB 一片（`MEETING_PART_BYTES`） |
| 上限 | 单条 5 小时（`MEETING_MAX_DURATION_MS`） |

### 6.6 顺手要改的两处用户可见文案

| 位置 | 现在 | 改成 |
|---|---|---|
| `server/src/meeting/meeting-transcription.service.ts:346` | 转写已完成，**逐字稿**可以看了。 | 转写完成，文字可以看了。 |
| `SynapseMobile/SynapseMobile/Info.plist` `NSMicrophoneUsageDescription` | 用于把说的话转成文字填进终端输入框，以及录像时收录声音。 | 加上录音 |

第一条的理由：「逐字稿」正是这次重构文案表里「一律不出现」的词，界面都改成「文字」了，只有这条推送漏在外面。**如果用户对推送文案另有选择，以用户为准。**

### 6.7 跨端收尾冲突（桌面端与服务端必须改）

手机端一上线，现有的「静默收尾」会出事。

**机制**：

- `GET /meetings/recordings/pending` 只按 `userId` 过滤，**没有设备维度**，而且 `findFirst({ orderBy: { updatedAt: "desc" } })` **只返回最新一条**（`server/src/meeting/meeting.service.ts:323-324`）
- 桌面端 `desktop/electron/bootstrap/descriptors.ts:697` 在启动时**无条件**调用 `finalizePendingRecording()`
- 它不检查那条是不是自己录的：`readSpooledParts` 读不到残片就跳过循环，但**仍然往下走到 `completeRecording`**，用 `receivedBytes` 估一个时长、带一个**空的 `peaks`**（`desktop/electron/modules/meeting/service.ts:189-198`）

**后果**：手机正在录音时，用户在电脑上打开 Synapse → 桌面把手机那条强行收尾。

1. 手机后续的分片上传失败——`uploadId` 已经被 complete 消费掉了
2. 时长是按已传字节估的，不是真实时长
3. **波形是空的**——而手机端本来能做到有波形（§6.4），这条优势被电脑端抹掉

**双向的**：电脑在录音、用户打开手机 App，手机同样会把电脑那条抢走。

**规则**：见决策七。残片只在本机，所以只有本机能续传——这本来就是桌面端 §6.5 的原意，只是原来只有一端，没暴露出来。

**要改的地方**：

| 位置 | 改动 | 为什么 |
|---|---|---|
| `server/src/meeting/meeting.service.ts` `findPendingRecording` | 从「返回最新一条」改成「能按 `recordingId` 定位」或「返回全部」 | 只返回最新一条的话，桌面即便加了本地残片判断，只要手机那条更新，桌面就**永远看不到自己那条**，自己那条反而没人收尾 |
| `server/src/meeting/meeting.controller.ts` `GET /recordings/pending` | 跟着上面改 | **线上还有旧版桌面端在调这个接口**，改形状必须兼容，不能直接换返回类型 |
| `desktop/electron/modules/meeting/service.ts` `runFinalize` | 拿到 pending 后先看本地 spool，没有残片就**直接返回，不要走到 complete** | 这一条是本次唯一的桌面端改动 |

**已知代价（要接受，不要试图消灭）**：手机录到一半崩了、之后用户只开电脑，那条会一直是 pending、不会转写。服务端的 `cleanupStaleUploads` cron（每天 4:37，中止超 24 小时的分块上传）会兜底清掉碎片，但**不转写**。用户下次打开手机 App 就会正常收尾。把这个写进已知限制。

---

## 7. 非目标

- **桌面 Widget**（用户明确排除）。扩展里不放主屏 Widget。
- **实时字幕**（边说边出字是语音输入的能力，录音不做）。
- **手动暂停录音**。暂停只由系统中断触发。
- **倍速播放**。
- **播放时跟随高亮当前句**。
- **音频剪辑、导出音频**。
- **纪要、发言人、逐字稿搜索**——不是"先不做"，是这一版明确不要。
- **给「没有波形的恢复录音」设计特殊界面**（手机端不会有这种录音，见 6.4）。

---

## 8. 已定口径

评审中已拍板，实施时不要再问：

1. **删除就是一个动作**，录音和文字一起删，行消失，不可恢复。没有"只删音频"的入口。
2. **异常退出的录音当正常录音处理**，界面上不出现任何询问；照常产生转写费用（0.8 元/时）是可接受的。
3. **界面任何位置都不出现**「云盘」「上传」「云端」「服务器」。音频不进用户云盘。
4. **波形用原生实现，不新增依赖**。手机端用 `AVAudioRecorder` 的 metering 或 `Accelerate` 自算，不引入波形库。
5. **服务端 DTO 字段不删**。`speakers` / `minutes` / `segments` 继续照常返回，只是两端界面不再渲染——手机端旧版本的 `MeetingDetail.speakers` 是非可选数组，服务端一停返回，没升级的 App 详情页就解码失败。
6. **不引入红色**，见决策三。
7. **不改 `server/src/mobile-live/mobile-push.service.ts` 的网关**。`APNS_USE_SANDBOX` 固定 `false`，录音功能与推送环境无关。

---

## 9. 验收基线

逐条验证，不要只跑测试就收工。

**录音本身**

1. 点列表右上角加号，直接进录音页并开始录，**没有**先起名字的步骤。
2. 录音页波形来自真实麦克风：对着麦克风说话波形随之起伏，安静时伏下去。
3. 波形密度固定：录 1 分钟和录 5 秒，柱子宽度和间距一样。
4. 波形贴右边缘、从右往左长；装满 5 秒后旧的从左边滚出去。
5. 录音页**没有**暂停按钮，只有取消和完成。
6. 开头一直没声音时显示「没有听到声音」；出声之后不再出现。
7. 拒绝麦克风权限后不崩，显示「未取得麦克风权限，波形为示意」。
8. 来电或闹钟打断：显示「录音已暂停，麦克风被其他应用占用」，结束后**自动继续录同一条**，用户不需要做任何操作。
9. 点「完成」立刻回列表，不出现上传页、不出现等待。
10. 点「取消」后服务端不留任何分片（含未完成的分块上传）。

**边录边传与异常退出**

11. 录音过程中界面**不出现任何上传相关的显示**。
12. 录制长于 5 秒后，「完成」的响应时间与录音时长无关。
13. 录音中途强杀 App，重新打开：**列表里不出现任何询问**，那次录音已经作为一条正常记录存在，并且正在转写或已转写完成。
14. 第 13 条恢复回来的那条录音，**语音视图有真实波形**，不是一条平线（见 6.4）。

**列表与详情**

15. 列表副标题是「时间 · 时长」，徽标是「转写中」/「转写失败」，没有发言人数。
16. 列表往下滚，导航栏收成玻璃条，加号变成玻璃圆。
17. 向左滑行露出「删除」；长按行时**整行抬起来、其余模糊、菜单浮在下方**，不是底部动作表。
18. 详情页只有「语音」「文字」两个视图，没有第三个入口。
19. 切视图不重置当前选中的录音；播放中切到文字再切回语音，播放头位置正确。
20. 文字视图只有段落文字，没有搜索框、发言人、时间戳、纪要、待办。
21. 转写中的记录，文字视图能看到进度和已经转好的部分。
22. 转写失败的记录，点它**直接落在文字视图**，看到失败原因和「重试」。
23. 重试不重新上传音频。
24. 从屏幕左边缘往右拖能返回，拖不到一半松手会弹回去。
25. 转写中的记录仍会自动刷新到「已完成」（沿用现有 5 秒轮询）。

> 2026-09-19 补记：**详情页原来不轮询**，只有列表页在轮。停在详情页上看进度，条会冻在进这一屏时的那一刻；转完了文字也不会自己出现，得退出去再进来才看得见。所以详情页补了一条自己的 5 秒轮询（转写完就退出循环），进度条那条秒表则用 `TimelineView` 每秒走一帧。

**系统集成**

26. 录音中，锁屏出现实时活动，**一行**：左边计时、右边**一枚停止键**（没有波形，没有录音名，没有取消）；两者垂直居中在同一条中线上，外边距 14 pt；按钮**不需要解锁**就能用。
27. 灵动岛紧凑态左边是红点、右边是计时；展开态（长按）左边是计时、右边是一枚停止键，**不显示录音名**。展开态的**尺寸和锁屏卡片完全一致**（计时 `.title`、停止键 52 pt），两块框成同一个高度所以中心重合；左右留白用系统默认值（实测 19.0 / 18.0 pt），上下落在药丸中线上（52 pt 的内容从 17.3 pt 起，中心 43.3 pt，中线 42 pt）。
28. 同时有两个实时活动时，灵动岛收成最小态（一颗红点）。
29. 控制中心里的「录音」控件点一下能开始录音。
30. 主屏长按 App 图标，快捷操作第一项是「开始录音」。
31. 录音中状态栏出现橙色麦克风指示点；停止录音后消失。
32. 转写完成的通知正文里**没有「逐字稿」三个字**。
33. 转写完成的通知点进去落到对应的录音详情。

**回归（不能坏的部分）**

34. 终端那个「按住说话」的语音输入**完全不受影响**：手势、浮层、识别文案、`AsrRenewal` 的连接寿命全都没动。
35. 手机的推送仍然能收到（`APNS_USE_SANDBOX` 没有被动过）。
36. 电脑端录音功能与重构前完全一致。
37. 拿一个**旧版本**的 App 连上改过之后的服务端，详情页仍然打得开（验证 DTO 字段没有偷偷删掉，见 §8 第 5 条）。

**跨端收尾（见 §6.7，这几条必须两条链路都真跑一遍）**

38. 手机开始录音 → **在电脑上打开 Synapse** → 手机那条**没有被收尾**，还在正常录；手机上点完成后时长正确、**语音视图有真实波形**。
39. 桌面开始录音 → **在手机上打开 App** → 桌面那条没有被收尾；桌面点完成后一切正常。
40. 两台设备上各有一条未收尾的录音（模拟：两端各录一半后都强杀）→ 各自重新打开，**各自收尾自己那条**，时长和波形都对，不串。
41. 只有一端有残片时，另一端启动**不会**把它收尾。

**命名**

42. 手机端全仓搜不到「会议」两个字——用户可见文案、注释、文档都干净，只有 `Meeting` 这种内部类型名和 `Features/Meeting/` 目录名除外。
