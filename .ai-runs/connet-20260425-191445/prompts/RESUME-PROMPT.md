# RESUME-PROMPT

Resume unattended Codex run `.ai-runs/connet-20260425-191445`.

Read `manifest.yml`, `state/heartbeat.json`, `state/PROGRESS.md`, and git status. Reconcile git commits with checkpoints before doing new work. Append a recovery entry to `logs/recovery.log` and `reports/COMPLETION-REPORT.md`.
