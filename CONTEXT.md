# Synapse

Synapse 管理可供编辑器与 Agent 使用的内容、能力和自动化执行模型。

## 智能体

**Agent 本地引用**：
助手消息中被识别为指向当前计算机文件系统对象的可操作引用；相对目标以所属 Agent 对话的项目根目录解释，绝对目标保持其绝对位置。引用可以携带行列定位信息，但文件系统操作只作用于底层目标。识别只表达引用形态，不保证目标存在、可访问或适用于每一种引用操作；它不包含用户消息、代码块、工具调用结果或未被识别的普通文本。
_避免使用_：本地文件链接、路径文本

**Agent 本地引用默认打开**：
用户明确选择让操作系统按桌面默认方式打开 Agent 本地引用目标的操作；它不是安全预览，普通目录通常由文件管理器打开，但应用包或可执行对象的最终行为由操作系统决定。成功只表示系统请求已被接受，不表示默认应用已经启动、聚焦或加载目标。
_避免使用_：在文件管理器中打开、安全打开

**Agent 本地引用文件夹定位**：
用户明确选择让文件管理器显示并尽可能选中 Agent 本地引用条目的操作；它不打开目标，目标是符号链接时定位链接条目本身而不是链接目标。成功只表示系统调用正常返回，不表示文件管理器已经聚焦或实际选中目标。
_避免使用_：打开所在文件夹、打开目标

**跟随对话**：
智能体不固定供应商和模型，运行时继承所属对话选择的供应商和模型。
_避免使用_：未指定、默认模型、自动选择

**指定模型**：
智能体显式绑定的供应商和模型档位；运行时覆盖所属对话的选择，实际模型随供应商的档位映射更新，失效时不得自动转换为跟随对话。
_避免使用_：固定模型、默认模型、智能体模型

## 内容安装

**安装目标**：
Skill、Rule 等内容通过适配器写入的目标产品或运行环境，例如 Codex、Claude Code、Cursor；该术语不表示 Synapse 智能体。
_避免使用_：Agent、智能体

## 能力与应用

**Capability**：
一个具有稳定身份和独立契约版本、可由一个或多个入口依赖的执行契约；它的存在不自动产生 MCP Tool、System App、Workflow Node、Automation Action 或 Deep Link。
_避免使用_：MCP 工具、系统应用、能力包

**Built-in Capability Package**：
Synapse 随构建交付的内置注册单位；它通过一个权威 manifest 显式声明自身能力、包版本和全部可选 surface，即使某类 surface 为空也必须声明。物理上暂时位于历史 `desktop/app-capabilities` 目录不表示它是 System App。
_避免使用_：内置应用、MCP 包、目录插件

**剪贴板**：
Package ID 为 `clipboard`、service ID 为 `core.clipboard` 的 Built-in Capability Package；它通过 Electron 默认剪贴板在当前操作系统的标准系统剪贴板中写入或读取纯文本，并以 `app.clipboard.text.write` 与 `app.clipboard.text.read` 表达两个相互独立、始终可用且不可单独关闭的 Capability。V1 只提供两个在节点选择器中可发现的 Workflow Node，不注册 System App、Automation Action、MCP Tool 或 Deep Link；两个节点不是自包含节点，分享导入时必须满足来自 `synapse.builtin` 的对应 Capability `>= 1.0.0`，缺失或版本不兼容时按现有兼容性契约阻断。它不操作 Linux selection、macOS Find pasteboard 或 HTML、RTF、图片、文件列表等其它格式，也不泛指各界面内部的复制或粘贴交互。
_避免使用_：复制按钮、剪贴板工具

**剪贴板文本读取**：
`clipboard_text_read` 节点执行时一次性取得当前操作系统剪贴板中的纯文本；它是不改变状态但可能暴露正文的高风险 Capability，使用可选的 `in` 端口排序，从 `out` 端口提供文本主输出与公开结构化 `{ text }`。剪贴板为空或只含非文本内容时，空字符串是有效读取结果，不属于执行失败。读取结果最多为 1 MiB UTF-8 字节，超限时整体失败，不截断或返回部分文本；该上限是 `readText()` 返回后的结果边界，不是原生读取前的内存配额。原生结果包含 NUL 或非法 Unicode 时也整体失败。完整文本只存在于当前 Workflow Engine 执行和下游数据流；实时界面及 Workflow MCP 状态或结果沿用约 10,000 字节的统一有界预览与截断标记，读取节点自身的持久化快照则完全省略 `output` 与 `outputs.text`。显式传给后续节点的内容按后续节点规则处理。
_避免使用_：剪贴板内容读取、空剪贴板错误

**剪贴板文本写入**：
`clipboard_text_write` 节点把非空配置文本经过不产生日志的显式变量绑定解析后写入当前操作系统剪贴板；它是改变进程外共享状态的高风险 Capability。节点输入连线只表达执行顺序，不会被隐式用作待写入文本。插值不裁剪、规范化或改写内容；未绑定变量、最终空字符串、NUL 和非法 Unicode 在访问前被拒绝。变量解析结果超过 1 MiB UTF-8 字节时也拒绝写入并保留原剪贴板；Tab、CR、LF、其它有效 Unicode 控制字符及空白组成的非空文本仍原样写入。完整解析值只在 Engine 执行期间存在；实时事件、运行状态、Workflow MCP 结果与持久化节点结果中的 `input` 固定为 `{ variables: {} }`，但快照内嵌 Workflow 定义仍保留作者已持久化的原始模板和变量引用，以维持既有历史与重跑语义。写入只调用一次原生 `writeText(text)`，不预读、合并或保留已有 HTML、RTF、图片、文件列表等其它格式；成功只保证本次纯文本已交给原生调用且同步返回，不回读验证或保证其它格式仍存在、内容随后未被覆盖，也不保证 Synapse 退出后继续存在。只有原生访问前的输入拒绝或取消保证剪贴板不变；`writeText()` 抛错时返回稳定失败但剪贴板状态未知，不预读旧值、恢复或二次读取。退出时不延迟进程、不 flush、补写或恢复。`out` 端口只提供公开结构化 `{ success: true }`，不重复返回文本、长度或剪贴板快照。
_避免使用_：隐式复制上游输出、剪贴板自动写入

**剪贴板工作流授权**：
用户显式编排并运行或启用剪贴板节点即授权该 Workflow 执行对应读写；执行时不经过 Clipboard 专用 PermissionGuard，也不逐次确认。UI、Automation 与现有 Workflow MCP 使用相同节点语义并沿用各自 Workflow 入口的授权和审计；Agent 只有在用户当前明确要求创建或运行剪贴板流程时才可加入或触发节点。`workflow_call` 使用子 Workflow 最新版本，其中的剪贴板节点不需要父流程重存或专用确认。外来 Workflow 必须在分享与导入时递归揭示实际子流程中的剪贴板读取和写入风险；持久化的写入文本属于敏感配置，变量引用本身不属于敏感内容。
_避免使用_：剪贴板运行时授权、剪贴板权限弹窗

**剪贴板审计**：
使用 `clipboard.read` 或 `clipboard.write` 动作记录一次已接受的剪贴板访问；`source` 固定为 `workflow`，actor 来自 Workflow Engine 且缺失时使用系统 `workflow-engine` 身份，嵌套执行记录实际子 Workflow、当前 run 和节点身份。审计不保存入口 channel、父调用栈、正文、正文派生信息或名称；UI、Workflow MCP、Automation 等入口差异由外层 Workflow 审计表达。输入在访问前被拒绝时不产生审计，审计故障不改变节点结果。
_避免使用_：剪贴板内容日志、剪贴板授权记录

**剪贴板访问接受点**：
Workflow 节点在进入共享剪贴板 core service 时越过的不可撤销边界；边界前取消、输入拒绝，或缺少非空可信 `workflowId`、`runId`、`nodeId` 时不访问剪贴板或产生审计，身份字段缺失固定返回 `INTERNAL_ERROR` 且不使用未知身份占位。边界后同步读写一次并保留其结果，不重试、撤销或恢复旧剪贴板。
_避免使用_：可取消剪贴板调用、剪贴板事务

**剪贴板 core service**：
主进程中由两个剪贴板节点共享的 `core.clipboard` 实例；它在 Electron 就绪后通过单一原生适配器同步读写，不依赖窗口、Renderer、存储或 IPC。service 不缓存正文、旧值或最近读写值，完整文本只由当前 Workflow Engine 和必要下游临时引用，运行结束释放业务引用并交由 JavaScript GC；V1 不设置正文 TTL、清理定时器或不可实现的安全清零承诺。适配器不可用时 service 仍保持注册并报告 degraded，具体读写返回稳定失败。
_避免使用_：Renderer 剪贴板服务、剪贴板 IPC

**剪贴板并发**：
每次原生文本读取或写入都是一次同步、不可分割的调用，service 不提供队列、锁、事务或 Clipboard 专用限流，也不按频率、突发量或冷却时间静默抑制访问。Workflow 节点间的先后顺序和是否执行只由现有可执行图规则表达：无入边节点仍须经出边到达结束节点，从主路径显式连出的叶子按既有副作用分支执行，完全游离的节点不访问剪贴板。执行频率、并发和 Automation 调度由现有编排层治理；读取结果只是调用瞬间的快照，写入结果不保证内容之后未被其它节点或应用覆盖。
_避免使用_：剪贴板事务、剪贴板全局顺序

**剪贴板错误**：
剪贴板节点失败时返回的稳定、不可重试载荷；错误码限于 `INVALID_INPUT`、`TEXT_TOO_LARGE`、`READ_FAILED`、`WRITE_FAILED`、`CANCELLED` 和 `INTERNAL_ERROR`。除空剪贴板或无文本读取仍成功外，前五个非取消错误均按现有节点失败规则令 Workflow 失败，`CANCELLED` 按现有取消规则传播；副作用叶子失败也不会因主路径完成而被忽略。载荷不包含原生异常、正文、实际长度或其它内容派生信息，只有输入错误可以附带受限的字段与原因枚举，其中 `forbidden_character` 专指 NUL。V1 不增加 `continueOnError`、私有错误端口或降级开关。
_避免使用_：原生剪贴板异常、剪贴板错误详情

**剪贴板日志**：
`core.clipboard` 只以固定阶段与固定原因记录适配器、原生读写或审计故障，不记录成功、输入拒绝、取消、调用身份、正文、正文派生信息或原生异常详情。
_避免使用_：剪贴板操作日志、剪贴板正文诊断

**能力 Surface**：
把 Capability 接入特定产品入口的显式贡献，包括 Workflow Node、Automation Action、MCP Tool、System App 和 Deep Link；Workflow 的 Panel/Card 与 Automation 的 Config Form 属于各自 surface 的 Renderer 贡献，不是独立应用界面。
_避免使用_：能力本身、目录入口、自动暴露

**System App Surface**：
能力包可选声明的单个系统应用入口；surface 是否存在、启动器是否发现、是否允许固定或默认进入 Dock、窗口是否可打开以及是否允许 Deep Link 必须分别声明，不能从 renderer 目录、App ID 或 discovery 推断。
_避免使用_：Capability Package、Renderer 页面、启动器条目

**能力注册状态**：
能力包及其版本化契约是否随当前 Synapse 构建完成注册；它不表示运行服务当前健康、用户已经授权或存在任何可见入口。
_避免使用_：能力可用、能力启用、节点可见

**能力可用状态**：
已注册能力的运行实现是否已在当前平台成功初始化并可以接受执行；服务故障可以使能力 unavailable，但不得删除既有节点和 Action 的身份或令编辑器无法打开其配置。
_避免使用_：能力注册、用户授权、入口可见

**Surface Discovery**：
Workflow Node 或 Automation Action 是否出现在新建选择器中的声明；隐藏只阻止创建入口发现，不改变已有定义的执行身份、能力注册或可用状态。
_避免使用_：能力启停、执行权限、定义删除

## 问题反馈

**问题反馈**：
用户向 Synapse 私下报告所遇问题或改进建议的单向沟通能力；它不提供公开阅读、回复、评论或处理进度查询。
_避免使用_：留言板、讨论区、客服工单

**问题反馈记录**：
一次问题反馈提交后形成、仅供管理员查看的私密记录。
_避免使用_：留言、帖子、讨论

**匿名问题反馈提交**：
不要求提交者拥有或登录 Synapse 用户账号的问题反馈提交；“匿名”不表示提交入口无需滥用防护，也不保证服务端无法观察必要的请求元数据。
_避免使用_：无校验提交、不可追踪提交

**AI 主动问题反馈**：
Agent 在有具体、可说明的运行事实，或已核实的 Synapse 代码、公开文档与实际行为矛盾表明 Synapse 产品、能力契约或内置 Synapse Skill 可能存在问题时，主动建议用户提交的问题反馈；普通校验、权限、限流、单次网络问题、第三方或编辑器错误、用户项目错误、操作失误、无证据猜测和纯审美改进不属于该概念。
_避免使用_：自动上报、普遍错误收集

**用户主动问题反馈**：
用户明确要求提交、且用户认为与 Synapse 产品或内置 Synapse Skill 有关的问题反馈；Agent 可以指出归因疑点，但不能以自身判断替用户否决。
_避免使用_：任意留言、通用匿名信箱

**问题反馈确认稿**：
完成上下文整理和隐私检查后，由 Agent 向用户完整展示、准备原样提交的纯文本正文；它不包含记录身份、接收时间等服务端运行字段。
_避免使用_：反馈预览、提交摘要

**问题反馈正文**：
问题反馈记录中由用户确认并原样保存的完整多行纯文本内容；它至少说明发生场景、实际情况以及认为存在问题的理由，可以使用由 Skill 组织的可读段落，但这些段落不构成服务端可解析的结构，也不作为 Markdown、HTML 或链接解释。
_避免使用_：反馈字段集、反馈表单数据、反馈摘要

**问题反馈确认**：
用户在看到完整问题反馈确认稿后，明确授权按该正文提交；任何正文修改都会使已有确认失效，必须展示新的完整确认稿并重新取得确认。
_避免使用_：长期授权、默认同意、隐式确认

**问题反馈隐私检查**：
在问题反馈提交前，由 Skill 进行语义审查，并由 MCP 边界与服务端按同一权威政策分别执行确定性检查的分层防护；任一层发现风险都会拒绝原文，且不自动修改、回显或记录正文及片段。该检查不保证识别任意自然语言中的全部隐私。
_避免使用_：隐私保证、自动脱敏、绝对安全检查

## 通知能力

**System Notifier**：
拥有通用、单向、非交互式系统通知投递、默认行为设置、MCP 能力和工作流节点的系统应用；它的投递能力不依赖应用窗口，也不提供通知中心或投递状态界面。通知点击或按钮属于具体业务流程时，由对应模块自己拥有，不进入 System Notifier。
_避免使用_：系统通知中心、通知历史应用

**系统通知投递**：
请求当前计算机的操作系统最多发起一次当前进程内原生通知尝试的单向副作用；请求通过稳定 API 的输入校验后，MCP 与工作流调用方只获得固定成功应答，该应答不表示尝试已执行或通知已发送、展示、点击或阅读。调用错误仍会失败；底层投递问题不改变调用结果。它不建立持久队列，不重试、补发或重放，也不保存通知历史或处理通知交互回调。
_避免使用_：通知任务、通知结果、系统消息记录

**系统通知内容**：
系统通知投递的跨平台稳定内容，严格由必填、非空白且无首尾空白的单行标题与正文组成；标题最多 64 个 Unicode 码点，正文最多 256 个 Unicode 码点，非法或超限时拒绝且不自动规范化、裁剪或修改。调用来源属于执行上下文，不属于通知内容；平台特有参数也不属于公共契约，平台差异由内部适配器和用户默认设置吸收。
_避免使用_：平台通知选项、Electron 通知参数

**系统通知默认设置**：
System Notifier 对所有通知投递统一应用的启用状态与静音偏好；停用会抑制原生通知尝试但不改变合法调用结果，静音只表达系统偏好而不保证操作系统遵守。
_避免使用_：调用参数、平台通知配置

**系统通知调用结果**：
合法的系统通知投递调用按 fire-and-forget 契约完成后返回的固定 `{ success: true }`；它不携带通知身份、影响数量、平台状态或任何发送、展示、交互和失败事实。
_避免使用_：发送结果、投递结果、展示结果

**系统通知输入错误**：
系统通知内容未通过稳定输入契约时返回的脱敏 `INVALID_INPUT`；它只标识固定字段与原因，不包含原值、片段、长度或校验器内部信息。
_避免使用_：投递错误、Zod 错误

**系统通知调用授权**：
MCP 工具被调用或工作流作者放置通知节点即构成当前系统通知投递的授权，不再经过 Synapse 逐次确认。合法调用只审计可信调用身份、入口、工作流运行位置和内容长度等元数据，不保存通知标题、正文、摘要或哈希。
_避免使用_：系统通知二次授权、通知正文审计

**系统通知防轰炸限流**：
只按可信调用身份与入口计数的当前进程内投递抑制机制；它不读取或保存通知内容，超限调用仍产生固定成功结果和无正文审计。限流状态不向调用方公开，也不执行内容去重或合并。
_避免使用_：通知调用失败、通知内容去重、通知合并

## 文档能力

**文本提取**：
从受支持的本地文档中按原始顺序取得已有文本层，并执行最小确定性规范化的统一任务；PDF 与 DOCX 共用同一能力身份。它不包含 OCR、语义改写、Unicode 兼容归一化或版面重建。
_避免使用_：文档解析、PDF 解析应用、DOCX 解析应用

**文本提取结果**：
文本提取返回的完整规范化正文、文档格式、文件名、源文件大小及可用的格式专属元数据；它不包含完整源路径。没有文本层的文档仍产生正文为空字符串的成功结果。
_避免使用_：解析片段、源文件记录

**文本写入文件**：
接收文本内容和本机绝对目标路径，将文本一次性按 `utf8` 或 `utf16le` 保存；任意扩展名和无扩展名路径都可使用，目标已存在时只有调用方显式允许覆盖才可替换。它不按路径解析、校验或改写内容，结果中的 `format` 仅表示小写末尾扩展名，无扩展名时为空字符串。
_避免使用_：文件生成、任意文件写入、二进制文件写入

**文本写入文件节点**：
文本写入文件能力的工作流入口；只通过显式变量绑定解析 `path` 与 `text`，按路径末尾扩展名决定格式，成功时以规范实际路径作为主输出并提供统一结构化写入结果。
_避免使用_：格式生成节点、隐式上游文本保存节点

## JSON 处理

**JSON Repair**：
把一次文本输入转换为一份已修复并验证的 JSON 文本的系统应用；其稳定 App ID 为 `json-repair`，中文操作与 Workflow 节点使用“JSON 修复”。它在启动器可见但不默认固定到 Dock，复用共享尺寸与按 App ID 管理的单实例窗口，不声明 deep link 或打开参数；MCP 与 Workflow 调用不打开、聚焦或唤醒窗口。Dock 排序只是不稳定的展示细节，实施时按现状避让冲突。它不使用“清理”“净化”或“安全”命名。
_避免使用_：JSON Repairer、JSON 清理器、JSON 净化器

**JSON Repair 工作台**：
JSON Repair 系统 App 的单视图响应式界面；宽窗口左右并列“输入文本”与“JSON 文本”，窄窗口上下排列。输入为空白或 UTF-8 超过 128 KiB 时禁用“修复 JSON”，输入变化同时清除旧结果与旧错误；成功结果出现后才提供“复制 JSON”，失败只显示共享固定错误。界面保持单层，不提供 tabs、历史、格式化、Schema、文件或其它策略入口，空输出不重复显示字段标题。
_避免使用_：JSON 修复控制台、JSON 修复结果占位、JSON 修复工具箱

**JSON 文本修复**：
JSON Repair 的唯一纯转换能力，对外身份为 `app.json_repair.text.repair`；MCP 工具 `app_json_repair_text_repair`、Workflow 节点 `json_repair_text_repair`、IPC 和界面共同解析 `core.json-repair`。它使用规范动作 `repair`，不修改外部状态、不标记高风险且不经过 PermissionGuard。
_避免使用_：JSON 生成、JSON 清理、分入口修复服务

**JSON 修复输入**：
JSON 文本修复接收的严格 `{ text: string }`；`text` 必填、非空白、必须是合法 UTF-16且 UTF-8 编码最多 128 KiB，并在进入修复管线前保持原样。它允许多行与控制内容，不接受未知字段或选择模式、格式化、Schema、批量提取等策略参数；Schema 字符长度只作预筛，运行时 UTF-8 字节数是权威限制。
_避免使用_：JSON 修复选项、JSON 原生对象输入、批量输入

**JSON 修复 MCP 工具**：
只在用户任务明确需要修复 JSON 时调用的 `app_json_repair_text_repair`；它接收唯一 JSON 修复输入，成功只返回 `{ json }`。直接调用必须原样传入 `text`，不得预处理输入、解析后重序列化输出、当作安全净化或 Schema 校验，也不得自动重试；Workflow 作者显式配置节点不受直接 Agent 调用意图限制。工具描述必须明确修复是 best-effort，启发式过程可能改写语义，结果虽经最终 JSON 校验但仍是不可信、未净化且不表示 Schema 合规；V1 不拆分提取、校验、格式化、文件或批量工具，也不返回 `affected`。运行时注册、内置 Synapse Skill 指南、API/Workflow 参考和契约测试必须同批落地。
_避免使用_：通用文本预处理工具、JSON 工具套件、隐式 JSON 修复

**JSON 修复 IPC**：
Renderer 通过 `window.synapse.jsonRepair.text.repair({ text })` 调用的唯一 invoke 方法，其 operation ID 为 `app.json_repair.text.repair`。公开 bridge 类型保持严格输入；IPC transport 接受 unknown 后由 handler 立即使用共享领域校验器，以确保非法调用也返回统一 `INVALID_INPUT`，而不是通用 IPC 校验错误。handler 只补充可信 UI actor/source 并解析同一个 core service，成功返回 `{ ok: true, result: { json } }`，失败返回 `{ ok: false, error }`，不提供事件或其它方法。
_避免使用_：JSON 修复 IPC 工具集、IPC 专用校验错误、Renderer 修复逻辑

**JSON 修复执行边界**：
在 Electron 主进程的无状态 `core.json-repair` 内完成的一次有界同步转换；接受点前检查取消信号，进入 core service 后不可中途取消。service 无硬依赖，只在 AuditSink 之后按软顺序启动并将其作为可选端口；Database/MCP 聚合服务与 Workflow Engine 对 core service 建立硬依赖，IPC 也解析同一实例。它不依赖数据库、PermissionGuard、窗口、网络或存储，不提供无意义的 lifecycle/health 方法，也不提供运行时备用修复器。V1 不使用 Worker、队列、伪超时或硬耗时承诺，未来可在不改变 capability 语义的前提下调整隔离方式。
_避免使用_：JSON 修复任务队列、可取消修复、修复超时

**JSON 修复上游**：
`@synapse/desktop` 精确依赖的 `repair-json-stream@1.3.1` 公共包；Synapse 直接调用其公开修复与提取导出，只拥有编排、资源限制和最终校验，不复制上游源码。版本升级必须人工审阅并通过固定兼容语料、契约测试和 packaged Electron 解析冒烟，分发时保留 MIT 许可信息。
_避免使用_：内嵌 JSON 修复器、浮动 JSON 修复依赖、Git 依赖

**JSON 修复审计**：
对每次进入 `core.json-repair` 的调用使用 `json.repair` 动作和 `app.json_repair.text.repair` resource 写入的一条无正文审计；成功记为 `allowed`，处理失败记为 `failed`。审计只保留可信入口身份、输入/成功输出字节数或稳定错误码，不保存输入、输出、候选、摘要、哈希、修复细节或解析位置；输入拒绝与接受点前取消不审计，AuditSink 故障也不改变修复结果。
_避免使用_：修复正文审计、裸 `repair` 权限动作、JSON 修复授权

**JSON 修复节点**：
Workflow 中 `json_repair_text_repair` 类型的 JSON 文本修复入口；配置严格包含支持现有变量语法的 `text` 与 `VariableBinding[]`，运行时通过无正文日志的安全插值入口得到唯一 JSON 修复输入。节点在插值前和共享输入校验后检查取消，成功以已修复并验证的 JSON 文本作为主输出并提供 `{ json }`，卡片、日志和错误不展示输入或变量值。
_避免使用_：JSON 提取节点、JSON 清理节点、批量 JSON 节点

**单结果 JSON 处理**：
一次文本输入最多产生一份已修复并验证的 JSON 文本；多个嵌入候选按原文顺序逐一修复并验证，采用第一个成功结果，不作为批量结果返回。整段输入本身是多根 JSON 序列时，允许修复为一个数组值；V1 不允许调用方选择候选策略。
_避免使用_：批量 JSON 提取、多结果 JSON 修复

**JSON 候选**：
一次 JSON 处理可能采用的输入片段；整段输入在去除允许的外围包装后可以任意 JSON 根值成为候选，嵌入普通文本时则只把对象或数组片段视为候选，不猜测自然语言中的数字、布尔值、`null` 或引号片段。
_避免使用_：任意 JSON 字面量搜索、自然语言标量提取

**JSON Markdown 包装**：
JSON Repair 在上游修复前可确定性拆除的单一完整 fenced block；开始行只能是三个反引号或三个反引号加大小写不敏感的 `json`，结束 fence 必须是独立一行的三个反引号，支持 LF/CRLF并保留内部正文。其它语言、属性、缩进、波浪线、多 block 或不完整 fence 不由 Synapse 解包，也不因此拒绝，而是原样进入上游 best-effort 管线。
_避免使用_：通用 Markdown 解析、宽松 fence 修复、Markdown 内容清理

**已修复并验证的 JSON 文本**：
经过 JSON 修复与最终解析校验、UTF-8 编码不超过 1 MiB且对象与数组合计嵌套不超过 128 层的权威文本结果；每个修复阶段都在最终解析前检查完整文本大小和字符串感知的结构深度，超限会终止整次调用，不截断、不部分成功也不继续尝试候选。深度扫描只负责资源计数，合法性仍以 `JSON.parse()` 为准，V1 不另设节点数量上限。解析值只用于验证，不跨 UI、MCP 或 Workflow 边界返回，也不重新序列化覆盖该文本。生成过程不执行输入，但文本内容仍是不可信数据，不表示业务语义正确、内容已净化或符合任何 Schema；所有合法键值均予保留，包括 `__proto__`、`constructor` 和 `prototype`。
_避免使用_：JSON 原生结构、安全 JSON、可信 JSON、已净化 JSON、Schema 合规 JSON

**JSON 修复错误**：
JSON 文本修复未产生成功结果时返回的稳定载荷，包含固定 `code`、固定 `message` 与恒为 `false` 的 `retryable`；固定映射为 `INVALID_INPUT`/“JSON 修复输入无效。”、`INPUT_TOO_LARGE`/“输入文本超过 128 KiB 限制。”、`OUTPUT_TOO_LARGE`/“修复后的 JSON 文本超过 1 MiB 限制。”、`MAX_DEPTH_EXCEEDED`/“修复后的 JSON 嵌套超过 128 层限制。”、`NO_JSON_FOUND`/“未找到可修复的 JSON 数据。”、`JSON_REPAIR_FAILED`/“无法产出有效的 JSON 文本。”、`NON_FINITE_NUMBER`/“JSON 包含非有限数值。”、`CANCELLED`/“JSON 修复已取消。”、`INTERNAL_ERROR`/“JSON 修复失败。”。只有 `INVALID_INPUT` 追加受限的 `request`/`text` 字段和固定原因；载荷不包含原生解析错误、上游异常文本、输入内容、候选片段、实际长度、深度、候选数量或解析位置，各入口也不向固定文案拼接这些信息。输出大小与深度错误在候选阶段立即终止；解析失败与非有限数只淘汰当前候选并允许后续候选成功。全部失败时依次优先返回曾出现的 `NON_FINITE_NUMBER`、已有提取候选或上游异常对应的 `JSON_REPAIR_FAILED`，最后才是 `NO_JSON_FOUND`；`INTERNAL_ERROR` 只表示 Synapse 自身不变量或未分类缺陷。
_避免使用_：SyntaxError、上游修复异常、原始输入错误

**整段多根 JSON 序列**：
整段输入由多个根 JSON 值构成、并由 JSON 修复能力转换为一个数组值的输入形态；它仍只产生一个 API 结果，不等同于从普通文本中批量提取多个候选。
_避免使用_：嵌入 JSON 批量结果、多结果响应

## 脚本运行

**JavaScript 运行**：
把用户源码作为一个普通 JavaScript 脚本文件交给当前版本的非 DOM JavaScript 宿主执行的基础能力；它使用宿主天然提供的标准语言与网络能力，不提供 Node.js、Electron 或本机文件 API，也不建立网络、路径或 API 白名单。
_避免使用_：受限 JavaScript、Web API 白名单、安全沙箱、网络 DSL

**脚本文件运行**：
把持久化源码视为一个会从头到尾执行一次的文件，而不是函数体、表达式、节点 DSL 或 API 调用；运行入口可以有少量用于工作流输入和单一结果的稳定桥接，但不得改变脚本所属宿主的普通语言、模块和副作用语义。
_避免使用_：脚本函数、代码节点 DSL、受控代码片段

**脚本稳定性保护**：
脚本运行的超时、取消、一次性执行单元、有限输出与全局并发只用于避免脚本拖垮 Synapse；它不是用户权限、资源授权或宿主 API 契约。
_避免使用_：脚本权限、运行沙箱、资源 grant

**脚本运行输入**：
工作流或 Automation 向一次脚本文件运行提供的一份严格 JSON 值；它不包含可信调用身份、内部运行状态或 Synapse 控制通道。
_避免使用_：权限上下文、内部运行对象、宿主配置

**脚本运行结果**：
一次脚本文件运行通过该宿主唯一公开结果机制产生的单个严格 JSON 值；日志、标准错误、退出状态和指标不属于结果，数组与对象也不触发隐式分流。
_避免使用_：多结果消息、日志结果、隐式 fan-out

**Node.js CLI 结果**：
Node.js 脚本以退出码 `0` 正常结束时，其允许前后 JSON 空白但只包含一个 JSON 文档的 stdout；stderr 是日志，合法 stdout 出现后脚本仍须正常退出，后台 handle 导致不退出时运行仍会超时。
_避免使用_：Node.js return、最后一行结果、stdout 日志

**JavaScript Worker 结果**：
JavaScript Worker 收到一次 JSON input 后发出的第一次 `postMessage` 值；它必须是严格 JSON，产生候选后运行即进入终止，console 只属于日志。
_避免使用_：JavaScript return、最后表达式、console 结果

**工作流公共结构化输出**：
工作流节点在 manifest 中预先声明、并在成功运行时按声明名称提供的 JSON 兼容值；运行时偶然出现的键、日志、调试信息和会话元数据不属于公共结构化输出。
_避免使用_：节点完整 outputs、运行时字段发现、调试输出

**工作流结构化值绑定**：
通过节点、公共输出名称和字符串或非负整数路径段精确引用工作流公共结构化输出的类型保留绑定；空路径引用整个值，缺少节点、输出、路径或成功结果时在目标节点执行前失败，不执行隐式字符串化或 JSON 编解码。
_避免使用_：模板变量、点号路径、缺失值回退

**Node.js 脚本执行**：
把用户源码作为真实 Node.js 脚本文件、以当前操作系统用户权限和所选 CommonJS 或 ESM 模式执行的基础能力；其标准全局、模块解析、文件、网络、进程和动态代码行为尽量等同同版本的 `node <script>`，Synapse 不提供包管理、依赖安装或安全沙箱。
_避免使用_：受控 Node.js 宿主、Node 风格 DSL、Node.js 沙箱、内置模块白名单

**Node.js 本机依赖**：
真实 Node.js 模块解析在脚本工作目录中找到、但不由 Synapse 安装、打包、声明或保证可移植的本地模块；分享能力依赖不包含这些模块。
_避免使用_：Synapse npm 依赖、内置第三方包、禁止的第三方代码

**JavaScript 代码执行风险**：
JavaScript 运行可以执行动态代码并产生网络等外部副作用，但不具有 Node.js、Electron 或本机文件能力；本地作者保存并运行脚本即表达执行意图，外来工作流仍须在首次执行前展示完整代码和副作用风险。运行时隔离和资源上限保护 Synapse 稳定性，不构成网络或业务权限系统。
_避免使用_：纯计算、无副作用 JavaScript、restricted_code_with_brokered_network

**Node.js 执行单元**：
一次 Node.js 脚本文件运行所使用且绝不复用的真实 Node 子进程；它隔离脚本崩溃并允许 Synapse 在超时或取消时终止主脚本进程，但对脚本创建的 detached 后台进程只作 best-effort 清理。
_避免使用_：受控 runner、Node Worker 池、进程树沙箱

**外来脚本确认**：
导入的 Workflow 在首次执行其可达脚本前进行的一次整体风险确认；本地保存并手动运行脚本或保存并启用 Automation 已表达执行授权，不再产生文件、网络、Secret 或历史的独立授权。
_避免使用_：逐资源授权、本地执行信任、NetworkGrant、FilesystemGrant

**脚本运行内容保存**：
运行结果与日志默认进入既有运行历史的普通持久化行为；用户可以用一个普通开关关闭内容保存，而无需敏感历史授权或基于 Secret 使用情况自动改变结果可用性。
_避免使用_：敏感历史授权、Secret 污点、默认省略结果

## HTML 生成

**EJS 模板配置**：
由 App 或 MCP 调用方显式提交、或由工作流作者持久化维护，并按官方 EJS 字符串渲染语义执行的 HTML 模板代码；工作流运行时只向它提供动态结构化数据，不从上游替换整份模板。它使用固定选项且不支持 include 或模板文件加载；“受信任”表示作者明确选择执行，不表示模板安全或渲染环境构成安全沙箱。
_避免使用_：普通模板数据、上游动态模板、安全模板

**HTML 模板数据**：
一次 EJS 渲染接收的顶层 JSON 对象；它必须能严格 JSON 序列化，不能包含循环引用、函数、`BigInt` 或其它非 JSON 值，模板固定通过 `data` 根对象访问其字段，不把顶层键注入词法作用域。工作流中的上游节点以只包含该对象的严格 JSON 文本传递它，Markdown 代码围栏、说明文字、空值、数组和标量都不属于 HTML 模板数据。
_避免使用_：模板变量文本、Markdown JSON、任意 JSON 值

**HTML 生成结果**：
HTML 模板完成渲染后得到的完整 HTML 字符串，或该字符串可靠写入目标文件后返回的路径及文件事实；它只证明生成完成，不表示内容安全、可信、可访问、符合 HTML 标准或可由浏览器正常运行。
_避免使用_：安全 HTML、已验证网页、可运行网页

**EJS HTML 生成**：
把 EJS 模板配置与 HTML 模板数据渲染为完整 HTML 字符串、且生成器入口自身不调用文件写入或打开能力的操作，对外身份为 `app.html_generator.ejs.generate`；这不限制模板 JavaScript 自行产生外部副作用。
_避免使用_：HTML 文件生成、EJS 文件生成

**EJS HTML 文件生成**：
先执行 EJS HTML 生成、再组合共享文本文件写入能力保存 HTML 生成结果的能力，对外身份为 `app.html_generator.ejs_file.generate`；`ejs_file` 不表示从 `.ejs` 文件读取模板。
_避免使用_：仅生成 HTML、EJS 模板文件生成

## 终端

**终端生命周期状态**：
终端会话所关联运行时的权威阶段，取值为 `running`、`stopping`、`ended`、`failed` 或 `lost`；它只表达运行时存续事实，不表达终端是否请求输入或调用方是否持有写入租约。
_避免使用_：等待输入状态、终端注意状态

**终端结束事实**：
终端会话结束、失败或失联时保留的原因、时间、退出码、信号及适用的停止请求来源；非零退出码属于前台程序结果，不自动构成终端基础设施失败。
_避免使用_：终端生命周期状态、命令执行结果

**终端注意状态**：
根据可追溯证据判断终端当前是否需要外部输入的观测结果，取值为 `waiting`、`not_waiting` 或 `unknown`；它独立于终端生命周期状态，缺少可靠证据时必须为 `unknown`。
_避免使用_：等待输入布尔值、终端运行状态

**终端注意证据**：
支撑终端注意状态的可说明事实，包括交互类型、原因、置信度、检测时间及对应输出位置；终端沉默本身不构成 `waiting` 或 `not_waiting` 的充分证据。
_避免使用_：提示词猜测、静默超时结论

**终端注意检测器**：
被动消费终端结构化标记或协议、前台程序身份、渲染状态和提示结构的版本化判断器；强证据必须可验证，中等证据必须组合多个来源，弱信号只用于诊断，初始版本允许大量 `unknown` 且不注入 Shell integration。
_避免使用_：提示词正则、沉默检测、Shell 注入

**终端注意证据水位**：
注意结果所依赖的 `throughOutputSeq`、`sizeRevision`、模式及渲染状态位置；输入、新输出、resize、渲染降级或模式变化会按依赖关系使旧证据失效，必须在新水位重新确认。
_避免使用_：检测时间、新鲜布尔值

**终端统一对象空间**：
UI 与本机 MCP 共同发现和操作的同一组终端分组、会话及历史；它不创建 MCP 专属副本，本机 MCP 沿用本地用户权限，但每次操作仍必须使用对应的语义 capability 并遵守租约、修订和生命周期约束。
_避免使用_：MCP 终端、Agent 专属终端、远程终端服务

**终端域结构修订**：
由 `terminalDomainRevision` 标识的全局单调持久低频结构位置，只在分组、命令、会话身份与成员关系、低频元数据和可查询操作集合变化时推进；输出和会话运行状态使用各自水位，统一安全域的授权修订不归 Terminal 所有。
_避免使用_：全局输出序列、授权修订、会话状态修订

**终端域结构事件**：
结构事实持久提交后发布的无正文失效通知，携带域修订、事件类型、对象及其修订、时间、来源和操作身份；列表快照返回对应域修订，消费者通过连续事件增量更新并在缺口、重连或恢复后有界重同步。
_避免使用_：权威事件日志、正文事件、无界订阅

**终端会话身份**：
由不可变且不复用的 `sessionId` 标识的一次终端会话；进程结束后该身份只承载元数据和保留历史，重新启动必须产生新身份，删除后身份与历史一并失效。
_避免使用_：终端标题、可重启会话 ID、PTY ID

**终端操作授权**：
用户对当前任务中具体终端操作表达的意图边界；本机 MCP 不需要在 Synapse 内再次登录、认证或申请 Terminal grant，但 Agent 仍不得把创建、读取、输入、停止和删除互相推导，写入还必须取得会话租约。
_避免使用_：Terminal grant、二次认证、对象所有权

**终端 MCP 调用方**：
由传输层分配稳定 `clientId` 和 actor kind 的本机 HTTP MCP 实例；该身份不是认证凭证，不要求用户配置 token，用于租约归属、幂等、配额和审计，并沿用 Synapse 本地 MCP 用户权限。
_避免使用_：Terminal token、匿名调用方、远程认证身份

**终端权限族**：
把终端发现、状态读取、敏感元数据读取、输出读取、命令正文读取、命令启动、会话创建与覆盖创建、语义控制、原始输入、尺寸调整、正常与强制终止、元数据管理、分组与命令管理、会话与分组删除分别表达的正交操作类别；它们用于 capability 描述、权限检查和审计，不要求本机 MCP 逐类申请 grant，也不代替写入租约和用户任务范围。
_避免使用_：Terminal grant、终端角色、终端完整权限

**终端语义能力**：
遵循 `app.terminal.<subdomain>.<action>` 身份、且只承载一个主要权限语义的终端 capability；状态、输出、控制租约、语义输入、原始输入、正常与强制终止、删除预览与提交等高差异操作不得通过 `mode`、`force` 或 `includeOutput` 参数合并。
_避免使用_：终端万能工具、模式化终端操作

**终端能力发现**：
本机 MCP 调用方通过 `app.terminal.capabilities.get` 读取的平台实现、契约版本、风险与限制元数据；它不要求认证或 Terminal grant，且不泄露对象、actor、使用量、路径或密钥细节，`supported` 只表示平台可用。
_避免使用_：授权查询、终端对象发现、认证探测

**终端 Legacy 兼容适配器**：
在明确兼容窗口内把旧终端 capability 映射到新核心服务、身份、权限、租约和审计契约的受限入口；它必须提供机器可发现的弃用和替代信息，无法安全映射时返回迁移错误，不保留或复制旧业务逻辑。
_避免使用_：旧版终端服务、静默兼容

**终端能力契约版本**：
让客户端明确判别并协商终端 capability 请求与响应语义的版本标识；新旧响应不能依靠可选字段存在与否猜测版本，legacy 移除必须通过能力版本与文档同步流程。
_避免使用_：应用版本、字段探测版本

**终端领域结果封套**：
所有新 Terminal tool 在 HTTP MCP 和 IPC 共用的固定 `contractVersion` 响应结构；副作用前拒绝返回机器稳定 error，任何可能已创建、写入或投递的请求返回带资源、操作、修订和接受摘要的可追踪 outcome，调用方不得依赖人类 message 判断。
_避免使用_：传输错误语义、自由文本异常

**终端副作用结果**：
请求可能已经改变外部状态时使用的 `accepted`、`partial`、`delivery_uncertain`、`no_op` 或 `failed_after_identity_created` 等稳定 outcome；`retryable` 只允许调用方重新决策，绝不表示 partial 或不确定投递可自动重放。
_避免使用_：成功布尔值、可重试错误

**终端普通创建**：
通过 `app.terminal.session.create` 按 UI 普通“+”共用的启动快照建立会话；无分组时解析统一默认快照，有分组时严格校验 `expectedLaunchRevision` 并继承分组快照，不接受 cwd、Shell、环境或初始尺寸等启动覆盖，title 只作为显示字段。
_避免使用_：任意终端创建、带覆盖创建

**终端覆盖创建**：
通过 `app.terminal.session_override.create` 携带显式 `overrides` 和覆盖意图建立会话的受控操作；cwd、Shell、环境及显式初始尺寸等影响启动或 PTY/TUI 行为的项目逐项进入脱敏启动事实和审计，并与对应覆盖及 resize 授权策略协调。
_避免使用_：普通终端创建、可选启动参数

**终端创建幂等身份**：
由 `clientId`、capability、调用方幂等键和规范化请求摘要共同确定、并只在明确保留期内有效的创建重试身份；同键同请求返回原 `sessionId`，同键不同请求明确冲突，不同调用方之间不能碰撞。
_避免使用_：全局幂等键、永久请求身份

**终端自动化控制租约**：
绑定运行中终端会话、稳定 `clientId` 和可信 `controllerInstanceId` 的短时、可续期独占写入资格；同一会话同时最多存在一个，同一客户端的不同 Agent 任务仍是不同控制者，超时、主动释放、会话终止、删除、撤权或用户接管时立即失效。
_避免使用_：终端所有权、永久锁、操作授权

**终端控制器实例**：
由可信调用上下文提供或与服务端签发句柄绑定的 `controllerInstanceId`，用于区分同一 MCP 安装或客户端内并行运行的 Agent 任务；调用方不能仅通过任意 tool 参数自行声明另一实例身份。
_避免使用_：clientId、Agent 名称、调用方自报实例

**终端租约操作句柄**：
全局唯一且不复用的 `leaseId`，用于让已认证控制器引用一次具体租约；它不是认证凭据，普通日志和审计只保留脱敏形式，旧句柄的迟到操作不得影响更新的租约修订。
_避免使用_：租约密钥、访问令牌

**终端用户接管**：
用户显式收回运行中终端的输入控制并使现有自动化控制租约失效；它防止用户输入与自动化输入在未察觉时交错，不代替紧急停止或其它操作授权。
_避免使用_：自动抢占、并发输入

**终端输入修订**：
终端服务接受一次输入后生成的单调递增会话内位置，用于把已接受输入与后续输出及审计关联；它不证明前台程序已经处理或完成该输入。
_避免使用_：命令结果、执行完成标识

**终端语义输入动作**：
自动化控制者向终端提交的有序 `text` 或固定枚举 `key` 动作；`text` 不携带 CR、LF、ESC、NUL 等控制字符，按键编码由终端服务统一完成，发送命令只是 `text` 后跟 Enter 的便利表达。
_避免使用_：原始输入、命令执行

**终端原始输入**：
为常规语义动作无法覆盖的场景保留的高风险输入逃生口；它通过单个规范 Base64 载荷表达候选字节，但只承诺底层 PTY 已证明可无损写入的字节集合，具有独立权限边界，不作为普通命令、多行粘贴或 TUI 控制的默认入口。
_避免使用_：语义输入、通用终端写入

**终端原始输入编码能力**：
服务端公开的 `supportedEncoding` 与 `limitations`，用于说明当前平台和 PTY 后端可无损接受的 raw 字节集合；Base64 只编码请求载荷，不证明任意二进制透明传递，无法证明无损的序列必须在写入前拒绝。
_避免使用_：Base64 二进制通道、任意字节支持

**终端括号粘贴**：
通过 `app.terminal.session_input.paste` 在终端模拟状态以新鲜证据确认 bracketed-paste 已启用时提交的有界 UTF-8 多行文本；服务端生成起止帧且绝不退化为 literal paste，未知、关闭或证据过期时在写入前拒绝。
_避免使用_：普通文本输入、原始输入、多行命令执行

**终端粘贴交付不确定**：
括号粘贴起止帧开始提交后发生、且无法确认已交付边界的中途失败结果；它返回 `delivery_uncertain`、失败位置和必要修订，调用方不得自动重试，以免重复正文或让前台程序停留在未闭合粘贴模式。
_避免使用_：普通部分成功、可安全重试失败

**终端输入接受结果**：
终端服务连续处理一组语义输入动作后返回的事实摘要，标识操作、输入修订、接受时间以及已接受到的动作和字节位置；它允许表达中途失败，不承诺整组输入全成或全败，也不表示前台程序已经处理输入。
_避免使用_：输入事务、命令结果

**终端输入顺序前置条件**：
语义输入请求携带、且必须精确等于当前值的 `expectedInputRevision`；同一租约的并发请求只有一个能占用该修订，其余明确冲突，输入顺序不依赖网络到达先后。
_避免使用_：请求时间顺序、隐式串行

**终端输入幂等窗口**：
服务端保存输入操作结果或防重复 tombstone 的明确有限时段；窗口内同一调用方作用域幂等键与同一规范化请求返回原结果而不再写入，不同请求明确冲突，窗口外调用方必须依据最新输入修订重新决策且不得盲目重放。
_避免使用_：永久 exactly-once、无限幂等

**终端尺寸修订**：
由 `sizeRevision` 标识的会话内单调 PTY 尺寸位置；UI 或 MCP 的实际尺寸变化都会推进它和 `stateRevision`，自动化 resize 必须精确匹配预期修订，同尺寸 no-op 和幂等重试不推进。
_避免使用_：输入修订、输出水位、窗口大小

**终端自动化尺寸调整**：
通过 `app.terminal.session.resize` 在同时具备 `session.resize`、`session.control` 和当前有效租约时修改运行中 PTY 尺寸的操作；成功只证明 PTY 接受受限的 cols/rows，不表示前台程序完成重绘。
_避免使用_：窗口缩放、显示设置

**终端前台中断**：
通过 Ctrl+C 语义输入尝试中断终端前台任务的操作；它只证明输入已被接受，不改变终端会话生命周期，也不保证前台程序响应。
_避免使用_：停止终端、终止会话

**终端正常终止**：
请求结束 Synapse 管理的 PTY 会话并使其进入 `stopping` 的生命周期操作；它不要求写入租约、不自动升级为强制终止，只有确认退出后会话才进入 `ended`。
_避免使用_：Ctrl+C、强制终止、删除会话

**终端强制终止**：
调用方明确发起、用于升级尚未结束会话的高风险生命周期操作；它拥有独立操作身份和审计记录，不得由超时策略静默触发。
_避免使用_：自动超时终止、普通停止

**终端终止能力矩阵**：
由平台终止适配器根据当前打包 node-pty/runtime 的目标平台集成测试公开的正常与强制终止支持事实、作用范围和限制；无法证明的模式返回对应 `*_unsupported`，不得回退到更危险或同义路径。
_避免使用_：跨平台统一 kill、类型声明支持

**终端终止投递不确定**：
终止请求向平台运行时发出后无法确认是否已被接受的操作结果；它保留并持续暴露真实生命周期和操作状态，不提前写入 `ended`，也不把不确定请求自动升级或重复发送。
_避免使用_：终止成功、终止失败、已结束

**终端异步操作状态**：
独立于会话生命周期持久化的 `pending_delivery`、`delivered`、`delivery_uncertain`、`completed` 或 `failed` 操作阶段；`completed` 必须携带最终生命周期和原因，不能掩盖 `ended`、`failed` 与 `lost` 的区别。
_避免使用_：会话状态、操作成功布尔值

**终端操作查询**：
通过 `app.terminal.operation.get` 按操作身份读取脱敏状态、时间、关联操作、错误和当前生命周期的能力；调用方仍须对原资源拥有 `state.read`，不能凭 `operationId` 探测对象存在性。
_避免使用_：审计查询、公开操作句柄

**终端会话删除**：
从终端统一对象空间移除一个已处于 `ended`、`failed` 或 `lost` 的会话身份、元数据和保留输出的独立高风险操作；它不终止运行时，也不继承输入租约或停止权限。
_避免使用_：停止并删除、关闭终端

**终端分组删除计划**：
针对一个非空终端分组生成的有时效级联删除计划；外部预览有界脱敏，服务端固化完整会话与命令集合、相关修订、生命周期事实和预计范围。自动输出淘汰只缩小删除字节时可以继续提交，任何增加或未知变化都会使计划失效。
_避免使用_：直接级联删除、删除并强杀

**终端删除 tombstone**：
会话或分组删除后在明确有限窗口内保留的脱敏操作与幂等事实，用于安全重试和查询；窗口外统一返回不泄露历史存在性的结果，不承诺永久保存已删除对象身份。
_避免使用_：永久删除记录、可恢复对象

**终端会话摘要**：
用于 `session.list` 有界发现的最小非状态信息，只包含身份、显示标题、分组、创建时间和来源等；它不包含生命周期、注意状态、租约、输出事实或敏感详情。
_避免使用_：终端状态摘要、终端会话详情

**终端会话状态摘要**：
用于 `session_state.list` 的固定形状批量状态信息，在最小发现摘要上增加生命周期、带新鲜度与水位的注意状态、脱敏租约占用及状态和输出水位；它需要 `discover + state.read`，不包含输出正文。
_避免使用_：终端会话摘要、终端输出

**终端分组摘要**：
用于 `group.list` 有界发现的 groupId、名称、必要修订、成员数和命令数；它不包含启动敏感值或命令正文。
_避免使用_：分组启动详情、命令列表

**终端命令摘要**：
用于 `group_command.list` 的 commandId、显示名称和 commandRevision 等非正文信息；它受命令资源范围约束，不授予命令正文读取能力。
_避免使用_：命令正文、命令详情

**终端集合游标**：
绑定一次终端集合查询的排序、过滤和搜索条件的不透明分页位置；条件变化后不得复用，会话列表以不可变的创建时间和会话身份稳定排序。
_避免使用_：页码、会话序号、跨查询游标

**终端分组启动修订**：
由 `launchRevision` 标识的分组启动语义版本，只在启动目录、Shell、环境等会影响新会话的设置变化时推进；名称或成员变化可以推进整体分组修订，但不应制造启动冲突。
_避免使用_：分组修订、命令修订

**终端会话元数据修订**：
由 `metadataRevision` 标识的会话显示及归属元数据版本；rename 等操作精确匹配该修订，实际变化同时推进 `stateRevision`，规范化同值不推进。未来移组还必须推进来源和目标分组的成员修订。
_避免使用_：状态水位、分组修订

**终端分组成员修订**：
由 `membershipRevision` 标识的分组成员集合版本；直接创建、删除或显式移组推进它和整体 `groupRevision`，名称、启动设置、输出或会话状态变化不推进它。
_避免使用_：分组修订、会话状态修订

**终端命令集合修订**：
由 `commandCollectionRevision` 标识的分组命令集合读取、删除计划和授权判断版本；命令创建、删除以及任何影响这些语义的单项更新都会推进它和 `groupRevision`，它不代替每条命令自己的 `commandRevision`。
_避免使用_：commandSetRevision、单条命令修订

**终端分组命令修订**：
由 `commandRevision` 标识的一条分组命令语义版本；通过命令创建会话时，它与分组启动修订分别校验，任一相关语义变化都会产生明确冲突。
_避免使用_：分组启动修订、会话修订

**终端已保存输入序列**：
分组命令正文经 CRLF 规范化后形成的有界 UTF-8 逻辑行序列；命令启动创建交互 Shell 后把每行连续作为 `text + Enter` 提交，它不是 shell 脚本、`shell -c` 或多行事务，空行和尾随换行具有明确提交语义。
_避免使用_：启动脚本、Shell 命令事务

**终端命令交付操作**：
由 `commandDeliveryOperationId` 标识、使用新会话同一 `inputRevision` 的一次性已保存输入提交；它由 `command.launch` 授权而不授予后续控制，允许表达 failed、部分接受或 `delivery_uncertain`，幂等重试不得新建会话或重投正文。
_避免使用_：命令执行结果、会话创建结果

**终端启动快照**：
新会话建立前从分组或全局默认值解析、校验并固定的启动事实；会话记录所采用的启动与命令修订及脱敏快照，后续配置变化不得回写已有会话。
_避免使用_：当前分组设置、动态启动配置

**终端执行边界**：
由 `executionIsolation=none` 和 `executionIdentity=current_os_user` 明示的初始运行权限；普通 Shell 控制可能执行当前 OS 用户可执行的任意命令，终端或分组授权范围不是文件、网络或进程沙箱，保存命令也不限制其 OS 副作用。
_避免使用_：安全 Shell、受限命令环境

**终端服务配额**：
按全局、client、controller 或 session 约束运行会话、租约、observe、操作速率、输入字节、输出、模拟器、检查点和持久化空间的可配置限制；超限在接受前明确拒绝新负载，不建立隐藏队列、不撤销已接受操作，也不自动终止现有会话。
_避免使用_：进程资源限制、自动闲置终止

**终端配额归因**：
MCP 创建的运行会话持续计入创建 `clientId` 直至进入终态的资源核算事实；连接断开不释放，它只服务配额和审计，不产生会话访问所有权。
_避免使用_：会话所有者、授权范围

**终端环境解析器**：
UI 与 MCP 共用的 `TerminalEnvironmentResolver`，从经三平台验证的基础来源和允许项正向构造子进程环境，并按统一敏感变量来源排除 Synapse 内部凭据；额外变量只能由用户显式配置，敏感值加密且审计不记录值。
_避免使用_：复制 process.env、环境变量 denylist

**终端状态水位**：
由 `stateRevision` 表达的会话内单调持久状态位置；生命周期、注意状态、租约及其它可观察状态变化都会推进它，重启恢复也不得重置或倒退。
_避免使用_：输出序列、输入修订、临时事件编号

**终端观察**：
以状态水位和输出水位为边界返回的一致会话快照或有界等待结果；每个调用方独立推进游标，读取不消费共享输出，超时只表示观察期间没有已知变化。
_避免使用_：事件消费、等待输入判定、永久订阅

**终端状态读取**：
通过 `app.terminal.session_state.get` 在 `state.read` 授权下读取生命周期、注意状态、调用方可见租约摘要、状态水位和输出水位的操作；它永不返回输出正文。
_避免使用_：终端详情、终端输出读取

**终端状态观察**：
通过 `app.terminal.session.observe` 在 `state.read` 授权下有界等待状态或输出水位变化的操作；它可以返回变化类型、输出区间和缺口元数据，但不返回输出字节。
_避免使用_：输出观察、订阅消费

**终端输出观察**：
通过 `app.terminal.session_output.observe` 在同时具备 `state.read` 与 `output.read` 时返回一致状态快照和有界输出增量的操作；响应形状固定，不因调用方拥有其它权限而静默增删字段。
_避免使用_：状态观察、可选输出观察

**终端租约可见摘要**：
状态读取或观察中按调用方身份脱敏的自动化控制占用事实；持有者可以看到自己的租约详情，其它观察者默认只看到是否占用及必要状态，不泄露另一调用方的 `clientId`。
_避免使用_：租约审计记录、控制者身份

**终端输出流**：
终端会话当前保留区间内按顺序记录的原始 PTY 输出；它包含终端控制与重绘语义，通过序列范围和缺口信息显式说明已丢失内容，不等同于永久完整历史或人类可读文本。
_避免使用_：完整终端历史、终端 transcript、终端渲染视图

**终端渲染视图**：
按照终端模拟语义从输出流派生的有界普通文本视图，区分当前可见屏幕与滚动区，并标识覆盖到的输出位置及任何降级原因；它用于观察用户可见状态，不取代终端输出流。
_避免使用_：ANSI 清理文本、原始终端输出、权威终端历史

**终端核心模拟状态**：
由 Terminal 核心服务独立于 Renderer 消费 PTY 原始有序流并维护的有界屏幕、光标、模式和 scrollback 状态；它绑定输出水位、尺寸修订及模拟器实现版本，是 MCP 渲染视图和注意检测的事实来源。
_避免使用_：Renderer xterm 状态、ANSI 清理结果

**终端模拟器检查点**：
只引用已可靠持久化输出、并携带序列、尺寸、模拟器版本和校验事实的有界派生状态；恢复校验失败即弃用，缺少可重建前缀时视图明确 degraded，且检查点正文受输出相同的权限、删除和配额边界约束。
_避免使用_：永久渲染历史、完整终端备份

**终端输出保留策略**：
以可配置的单会话滚动上限和终端域全局配额决定哪些输出块仍可读取；自动淘汰只影响输出并保留会话身份及必要元数据，具体缺口必须通过保留区间和累计淘汰事实显式呈现。
_避免使用_：永久历史、会话清理策略、固定 10 MiB 承诺

**终端结构化存储**：
由 `app.terminal.groups`、`commands`、`sessions`、`operations`、`idempotency` 等 DataRepository namespace 保存的版本化身份、manifest、修订、配额索引和操作状态；每条记录含 schemaVersion 并注册 schema，认证授权不属于该数据域。
_避免使用_：terminal-state.json、终端输出文件

**终端正文块存储**：
仅用于受配额原始 PTY 输出和模拟器检查点正文的 Terminal 专属文件存储，是对系统 App 默认 DataRepository 边界的有限例外；块以内部身份、大小、摘要、类型和所属会话绑定，路径不暴露给 Renderer、IPC 或 MCP，且正文保持敏感数据等级。
_避免使用_：通用文件存储、公开终端文件、SQLite 输出正文

**终端持久化保护状态**：
由 `persistenceProtection` 表达的终端敏感正文加密可用性；安全存储不可用时终端和当次内存输出仍可运行，但不形成可重启正文历史，保存命令和需恢复原值的敏感启动配置必须拒绝且不得回退明文。
_避免使用_：终端可用状态、备份状态

**终端加密正文块**：
通过 Synapse 统一加密能力使用 OS safeStorage 保护的数据密钥和分块认证加密持久化的输出、检查点、派生 scrollback、命令正文或环境值；每块独立 nonce/tag，AAD 绑定块、会话、类型和 schema 版本，摘要只作索引而不代替认证。
_避免使用_：摘要保护、Terminal 配置密钥、明文块

**终端存储迁移标记**：
旧 `terminal-state.json` 经过精确字节备份、源摘要复查、完整迁移验证和原子切换后持久化的永久幂等事实；成功后旧源不得再次导入，避免用户清空新数据后历史复活，失败时保留旧源并允许重试。
_避免使用_：临时迁移缓存、按启动自动导入

**终端 Legacy 迁移基线**：
为无 schemaVersion、无修订和无操作事实的旧终端数据建立的新版本起点；它显式标记迁移前输入、来源、启动修订和缺失结束事实为 unknown/unclassified，只从可证明字段生成新结构，不补造 actor、授权、租约、注意或操作历史。
_避免使用_：完整历史、默认修订

**终端 Legacy 输出编码**：
以 `legacy-js-string-utf8` 标记的旧 JavaScript 字符串输出迁移方式；它保留能证明的旧序列并加密存储，但不声称恢复 node-pty 原始任意字节，序列断裂或正文缺失必须形成 gap 和累计丢失事实。
_避免使用_：原始 PTY 字节、完整输出恢复

**终端普通备份投影**：
普通 `synapse-backup-v1` 中经过 Terminal 专用规划和校验导出的结构与配置视图；它排除输出块、模拟器检查点、派生 scrollback 和短期运行态，恢复后以 `backup_excluded` 缺口和 degraded 视图明确表达正文缺失，不能留下指向未包含块的可用 manifest。
_避免使用_：完整终端备份、DataRepository 原样导出

**终端完整历史备份**：
未来可能提供、显式包含敏感输出与检查点正文且具有大小提示、加密保护和专用格式的独立备份流程；它不属于初始普通配置备份。
_避免使用_：synapse-backup-v1、默认终端备份

## 应用更新

**更新承接链接**：
长期稳定为 `https://synapse.d2.pub/desktop/update` 的普通 HTTPS 链接；它打开更新承接页，不携带目标版本，也不直接等同于唤醒客户端的自定义协议地址。
_避免使用_：更新下载链接、Synapse 深链

**更新承接页**：
`synapse.d2.pub` 上不要求登录、也不属于 Dashboard 布局的独立公开页面；它先要求用户自行确认没有正在进行的任务，只有用户点击更新后才尝试唤醒 Synapse 并把更新意图交给客户端。未自动打开客户端更新页面时只提示用户前往 Synapse 设置手动更新，不提供网页安装包下载。
_避免使用_：下载页、跳转页

**更新触发确认**：
用户在更新承接页确认可以中断当前工作并主动点击更新；它授权客户端进入完整更新流程，但不表示客户端已经检测或保证没有正在进行的任务。
_避免使用_：任务安全检查、自动更新授权

**更新深链**：
稳定以 `synapse://update` 表达客户端更新意图；它不携带目标版本或流程开关，也不表示设置页面的内部导航地址。只有携带有效更新凭证时才授权完整自动更新，裸深链只打开客户端更新页面。
_避免使用_：设置更新页链接、更新承接链接

**更新凭证**：
官方更新承接页在用户点击更新后取得的短时授权；客户端验证通过后才可从更新深链进入完整自动更新流程。
_避免使用_：登录凭证、更新版本、任务安全检查

## 工作流

**工作流布局方向**：
工作流画布中控制流的全局视觉方向；`horizontal` 表示从左向右，`vertical` 表示从上向下，并统一决定节点连接点方位与自动布局方向。它是工作流文档的产品语义，不使用布局算法的方向代码表达。
_避免使用_：Dagre 方向、LR/TB、节点布局方向

**工作流布局方向变更**：
把工作流布局方向与按新方向计算的全部节点坐标共同替换的一次原子草稿变更；成功后适应画布但不自动保存，布局计算失败则方向与坐标均保持不变。
_避免使用_：连接点切换、方向字段更新、自动保存布局

**工作流控制流方向**：
工作流边从 `from` 节点指向 `to` 节点的稳定执行方向，Switch 分支由边的 `branch` 继续标识；它不因工作流布局方向或连接点方位变化而反转，也不改变边身份。
_避免使用_：画布方向、连接点方向、边的视觉朝向

**工作流自动布局**：
依据工作流当前布局方向重新计算全部节点坐标的统一能力；UI 使用持久化方向，MCP 省略方向时同样使用持久化方向，显式兼容参数则同时改变工作流布局方向与坐标。布局算法代码只存在于实现边界，不属于持久化产品模型。
_避免使用_：Dagre 布局、节点坐标整理、仅 UI 布局

**工作流布局状态**：
由工作流布局方向与全部节点坐标共同构成、随工作流文档保存和修订的画布状态；普通加载、分享导入和运行历史只还原该状态，只有工作流布局方向变更或工作流自动布局会重新计算坐标。
_避免使用_：画布偏好、临时布局、运行布局

**提示词节点思考开关**：
控制提示词节点是否显式禁用模型思考；开启表示不传递思考控制参数并跟随模型默认行为，关闭表示要求模型禁用思考。
_避免使用_：强制开启思考、思考档位

**文本节点**：
不调用模型或脚本，在工作流中通过输出模板将固定文本与显式绑定的变量拼接为唯一的文本输出；空模板输出空字符串，而不是空值或缺失结果。它具有可选连接的控制流输入和单一输出端口，既可作为起始节点，也可位于分支或串行流程中；上游连线不形成隐式文本数据输入，单一输出端口可连接并触发多个下游节点。
_避免使用_：文本生成节点、Prompt 节点

**默认应用打开能力**：
接收一个已存在的本机绝对非符号链接普通文件路径，并委托操作系统关联的默认应用打开该文件；相对路径、目录、`file://` 地址、网络链接、多文件和指定应用不属于该能力，文件扩展名不受限制。系统应用、MCP、工作流和应用深链调用统一使用 `path` 参数并共享同一个能力语义与执行边界；操作系统接受请求即视为成功并返回该路径，不表示默认应用已经启动、聚焦或加载文件。
_避免使用_：工作流专属打开逻辑、默认浏览器能力

**默认应用打开节点**：
默认应用打开能力的工作流入口。它按能力包契约持久化 `path`，解析工作流变量并提交一次真实打开请求；成功时以本次提交的绝对文件路径作为主输出和结构化 `path` 输出，重新运行工作流会再次发起请求。
_避免使用_：默认浏览器节点、链接打开节点、文件地址打开节点

**应用深链调用**：
外部链接通过 Synapse URL Scheme 指定一个系统应用 Action 及其参数，请求本机 Synapse 执行该 Action；只有应用显式声明允许深链触发的 Action 才属于此入口，不能借此调用任意应用能力。调用链接允许直接编写和长期使用，Synapse 不要求二次确认、签名、Origin、来源或调用者可信性校验；安全边界只包括 Action 的显式暴露声明、参数契约和必要运行条件。
_避免使用_：Schema 唤醒、通用 MCP 深链、任意能力调用

**工作流分享**：
同事之间传递工作流文档，并通过重新绑定接收方环境中的模型、项目及其它环境依赖，尽量复现发送方的工作流程；它不要求复制发送方本地标识，也不等同于完整数据备份。
_避免使用_：工作流备份、工作流克隆

**工作流依赖映射**：
导入时把分享包记录的发送方环境依赖替换为接收方环境中的可用依赖；映射方向固定为“发送方来源 → 接收方本地目标”。
_避免使用_：反向映射、复制本地标识

**工作流模型引用**：
工作流中实际使用的一个发送方供应商与实际模型组合，以及使用该组合的工作流默认设置和节点位置；同一实际模型不因模型档位不同而拆分，只有无法确定实际模型时才以档位区分。导入方可以一次为同一引用的所有位置选择一个本地供应商与模型。只有谱系历史映射和本地唯一实际模型匹配可以自动完成；其它同名、档位或默认模型只能作为用户显式选择，不得静默补全。分享包不携带供应商 Base URL、环境变量、API Key 或其它连接配置。
_避免使用_：模型槽位

**工作流项目引用**：
工作流中实际使用的一个发送方项目，以及使用该项目的工作流默认设置和节点位置；导入方可以把不同来源项目分别映射到本地项目，也可以把多个来源项目合并映射到同一个本地项目。继承默认项目与节点显式指定项目的关系在映射后保持不变。分享包不记录发送方本机路径、Git 远端地址或凭据；导入依次复用谱系映射、匹配规范化 Git 远端的哈希指纹、按项目类型与唯一名称预选，仍不明确时由用户选择。
_避免使用_：统一默认项目

**工作流本地参数预设**：
接收方为某个本地工作流保存的运行参数值；它与上次运行值都不属于分享包。谱系原地更新时保留已有预设，不自动改写或删除；导入计划只提示与新版参数不兼容的预设，用户选择该预设运行时必须修正缺失、类型、基数或资源可用性问题。只有子工作流满足自动清理条件并被删除时，才连同其预设删除。
_避免使用_：分享参数值、自动迁移预设

**工作流分享依赖集**：
一次工作流分享包含一个或多个入口工作流，以及它们通过工作流调用节点递归引用的全部子工作流；当前界面只允许选择一个入口，但分享包格式使用入口数组为未来的批量分享保留兼容空间。导入时为整组工作流重新分配本地身份并重写内部调用关系。导出必须对整个依赖集使用已保存、已迁移、已校验的一致修订集合；任一子工作流存在未保存编辑、缺失、循环调用或超过当前五层调用限制时阻止导出，同一子工作流只打包一次，确认期间修订变化必须刷新预检。
_避免使用_：仅根工作流、孤立子工作流

**工作流关联 Automation**：
以工作流作为执行动作的独立 Automation；它的触发器、启用状态、作用域、工作目录、策略和运行历史不属于当前工作流分享依赖集。导出预检只提示关联数量，当前分享包不携带也不在导入后创建 Automation。原地更新工作流时必须用新版参数重新校验接收方已有的关联 Automation：兼容项保持不变，不兼容项随导入事务禁用并标记需要更新，但保留配置和运行历史。未来只能通过显式选择、导入后默认禁用的 Automation 模板能力扩展支持。
_避免使用_：工作流子依赖、自动导入调度

**工作流分享包兼容性**：
分享包容器的 major 版本定义不可兼容的结构边界，包内必需能力决定当前 Synapse 是否可以完整导入；缺少必需能力时必须阻止导入，不得通过忽略节点、字段或附件降级导入。分享包只携带工作流数据，不携带节点实现、插件代码、可执行文件或安装脚本；缺少的必需能力只能通过 Synapse 信任的能力来源单独安装并重新预检，导入不得静默安装或执行代码。
_避免使用_：尽力导入、静默降级

**工作流分享包完整性**：
manifest 对每个声明的包内文件记录 SHA-256，导入必须拒绝文件缺失、重复、摘要不符或存在未声明文件。摘要只证明包内内容自洽和传输未损坏，不证明发送者身份；没有可信签名时界面必须显示来源未验证。格式为未来基于制品摘要的可选签名保留扩展点，声明为必需但无法验证的签名能力必须阻止导入。
_避免使用_：可信来源、作者认证

**节点分享契约**：
每种工作流节点必须声明自身的分享能力、环境依赖、可携带资源、敏感配置、导入重写要求和可移植性诊断；分享服务统一聚合这些声明并负责权限、归档、映射、校验和持久化。即使节点完全自包含，也必须显式声明。显式 shell 由节点声明为必须满足的本地运行时能力，缺失时不得自动转换脚本语义；未指定 shell 则继承接收方默认。脚本文本内部的平台专用语法只提示人工确认，不做启发式阻断。
_避免使用_：集中硬编码节点类型、节点自行导出

**工作流分享敏感字面值**：
直接持久化在工作流文档中的 Token、密码、请求头、脚本环境变量或其它敏感字面值；它们被视为作者有意分享的工作流内容，导出时保持原样，但导出预检必须只按位置提示风险且不得在日志或 manifest 中复制值。
_避免使用_：自动脱敏、自动排除

**工作流外部文件依赖**：
工作流节点或参数通过本地路径引用、但不包含在当前分享包中的文件或目录；导出预检必须列出这些依赖并告知作者另行转交，导入方按来源路径分组选择本机文件或目录并批量替换所有引用。节点路径字段以已绑定的参数或上游节点输出变量为根时，该字段属于运行时派生路径，分享依赖归属于变量来源，不得把插值表达式本身重复识别为本地资源；字面路径和静态变量路径仍按外部文件依赖处理。分享包格式只为未来可选的附件能力保留扩展空间。
_避免使用_：分享包附件、自动打包文件

**工作流 Drive 资源依赖**：
工作流引用但不复制进分享包的 Drive 文件或目录；接收方可访问原资源及指定版本时自动沿用，否则当前导入流程必须映射到同类型本地资源，固定版本不得静默漂移到最新版。未来增加 Drive 选择器后才允许改映射到另一个可访问的 Drive 资源。
_避免使用_：Drive 附件、自动复制 Drive 文件

**工作流临时资源依赖**：
无法跨设备复用身份的 staged 文件或目录；分享包只记录其类型和使用位置，当前导入方必须映射到本地稳定替代资源。
_避免使用_：沿用 staged ID

**工作流内联文件**：
把文件字节直接保存在工作流文档中的资源；当前分享包不支持附件，因此检测到内联文件时必须阻止导出，直到作者改用外部资源。未来只能通过显式附件能力扩展支持。
_避免使用_：静默删除、隐式附件

**工作流分享说明**：
面向接收方的可编辑纯文本说明，默认根据工作流描述、运行参数、外部文件依赖和环境前置条件在本地确定性生成；它不得复制敏感字面值，也不依赖模型生成。
_避免使用_：AI 生成说明、导出日志

**工作流导入计划**：
在写入任何数据前生成的完整预检结果，依次覆盖内容、风险与兼容、模型、项目、外部依赖和最终变更确认；它包含新建、原地更新、内部引用重写、旧子工作流清理、备份及全部映射，只有计划完整有效时才能原子提交。提交前必须重新检查活动运行和活动编辑；待更新或清理的工作流自身正在运行，或处于任一活动工作流的递归调用链中时，必须等待运行结束或由用户自行取消，不得让导入自动中止运行。受影响工作流存在未保存编辑或正在保存时也必须阻止提交，不得自动丢弃草稿；无未保存修改的编辑器在提交后自动加载新定义。存储提交必须校验预期修订，防止预检与写入之间的并发覆盖。
_避免使用_：边读边导入、部分导入

**工作流分享谱系**：
标识同一分享来源跨多次导出保持不变的身份；每次导出另有独立制品身份，并记录各来源工作流的修订标识。导入关系保存在工作流正文之外，用于识别重复包和后续版本；同一谱系的新修订以分享来源为权威，经过可恢复备份后原地更新，不检测或保留接收方的本地修改。原地更新必须保留已有运行快照。每个谱系只保留最近一次成功更新前的导入撤销点；仅当相关工作流仍处于刚导入修订时允许原子恢复工作流、被清理子工作流、谱系映射和被禁用的 Automation 状态，运行历史始终保留。新版移除的子工作流只有在既无谱系外引用也无运行历史时才自动清理；存在任一保留理由时解除谱系关系并保留。删除入口时可以默认清理满足相同条件的谱系子工作流；任何仍被其它工作流调用的目标都不得删除。谱系最后一个成员消失后删除来源关系及本地映射。导入结果再次导出时创建新谱系，并通过派生来源关联原谱系和来源制品。
_避免使用_：本地工作流 ID、导出文件名

**工作流运行快照**：
一次工作流运行结束时保存的脱敏工作流定义、修订标识、参数和节点结果；它按本地工作流身份归档，并使用当次运行的定义展示历史结构，不依赖该工作流当前有多少节点。运行快照用于运行历史查看和受约束的再次运行，不是工作流编辑版本历史。
_避免使用_：工作流版本历史、当前工作流定义

**工作流文档**：
可以在本地持久化、导入导出和运行快照之间传递的完整工作流定义。
_避免使用_：工作流包、工作流存储记录

**工作流 schema 版本**：
标识工作流文档数据结构的 SemVer 版本；本地数据、导出文件、导入文件和运行快照共享同一版本语义。
_避免使用_：工作流版本、修订版本、导出包版本

**工作流 schema contract**：
决定工作流文档可持久化结构及其语义的稳定契约，包括工作流字段、参数、资源引用、节点类型和节点配置。
_避免使用_：工作流类型、节点 schema

**工作流修订标识**：
标识工作流一次保存结果的值；它不表示数据结构版本，不参与数据迁移。
_避免使用_：schema 版本、数据版本

**工作流包格式**：
标识工作流导入导出文件的容器协议；它不代替工作流 schema 版本。
_避免使用_：工作流 schema 版本

**工作流包格式版本**：
标识工作流包容器结构的 SemVer 版本；它只在包容器契约变化时升级，不跟随工作流 schema 版本联动。
_避免使用_：工作流 schema 版本、应用版本

**Legacy 工作流文档**：
尚未携带工作流 schema 版本的历史工作流文档；它需要经过真实迁移和完整校验才能成为当前工作流文档。
_避免使用_：旧版工作流、V1 工作流

**Legacy 工作流存储**：
早期 Synapse 在资源仓库 `workflows/` 目录中按工作流和保存版本分别存放 JSON 文件的历史存储方式；它与当前统一的 DataRepository 工作流存储不同。
_避免使用_：旧工作流格式、Legacy 工作流文档

**工作流迁移状态**：
独立于工作流正文保存的迁移诊断记录，按工作流和源摘要记录目标版本、迁移结果及脱敏错误；它不是工作流文档的一部分，也不能代替原始数据或迁移备份。
_避免使用_：工作流加载错误、工作流元数据

**未来工作流文档**：
schema 版本高于当前 Synapse 所支持版本的工作流文档；当前版本不解释、不运行且不改写其内容。
_避免使用_：损坏工作流、无效工作流

**工作流兼容节点**：
为保持已有工作流语义而继续受支持、但不再允许新建的 deprecated 节点类型；只有存在语义等价的显式迁移后才能删除。
_避免使用_：废弃数据、无效节点

**工作流资源参数**：
引用文件或文件夹的工作流参数；其基数由参数独立配置，默认为单值，也可显式允许多选。
_避免使用_：文件路径参数

**单值工作流资源参数**：
只引用一个文件或文件夹的工作流资源参数。

**多值工作流资源参数**：
引用一个非空、有序且不重复的同类文件或文件夹集合；即使集合中只有一个资源，它仍然是多值参数，资源顺序属于参数语义。
_避免使用_：可单可多参数

**资源参数兼容**：
两个工作流资源参数的资源种类和基数均相同；文件与文件夹、单值与多值之间都不构成兼容关系。
_避免使用_：可转换、基本兼容
