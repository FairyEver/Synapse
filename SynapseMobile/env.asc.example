# App Store Connect API 凭证模板。
#
# 用法：把本文件复制成同目录下的 .env.asc，填上真实值。
#
# 两份文件都放在 SynapseMobile/ 下、紧挨着 scripts/asc.mjs。注意文件名不对称，
# 这不是笔误：根 .gitignore:14 的 `.env.*` 规则会把 `.env.asc` 挡在提交之外，
# 但它同样会挡掉任何以 `.env.` 开头的名字——所以模板必须以 `env.` 开头才能被提交。
#
# 凭证来源：App Store Connect → 用户和访问 → 集成 → App Store Connect API。
# 生成密钥时 .p8 只能下载一次，务必存好（本机在 ~/Secrets/apple/synapse/）。
# 一旦丢失，只能吊销该密钥重新生成。
#
# ASC_KEY_ID     密钥页上那一列「密钥 ID」，10 位
# ASC_ISSUER_ID  密钥页顶部「Issuer ID」，UUID 形式，团队级、所有密钥共用
# ASC_KEY_PATH   .p8 私钥的绝对路径
#
# 本仓库是公开的，不要把真实值提交进来。

ASC_KEY_ID=XXXXXXXXXX
ASC_ISSUER_ID=00000000-0000-0000-0000-000000000000
ASC_KEY_PATH=/absolute/path/to/AuthKey_XXXXXXXXXX.p8
