# 9.0 源码映射

CC Connect 源码基准路径：`/Users/liyang/Documents/code/demo/cc-connect-main`。

## Bridge 核心

- CC Connect `core/bridge.go:26` `BridgeServer`
  - Synapse：`desktop/electron/services/bridge-adapter/bridge-adapter-service.ts` `BridgeAdapterService`。
  - 说明：Synapse 不新建全局单例，通过 `core.bridge-adapter` descriptor 接入 `ServiceRegistry`。

- CC Connect `core/bridge.go:45` `bridgeAdapter`
  - Synapse：`BridgeAdapterConnection`。
  - 说明：保存 platform、capabilities、metadata、连接状态、registeredAt、lastSeenAt；同名 adapter 连接会替换旧连接。

- CC Connect `core/bridge.go:57` `bridgeReplyCtx`
  - Synapse：`ReplyTarget.replyCtx` 中的 `{ kind: "bridge", platform, projectId, sessionKey, replyCtx, capabilities }`。
  - 说明：AgentRuntime 收到 Bridge message 时记忆 ReplyTarget，后续 Agent event 和 side-channel send 都复用该 target。

- CC Connect `core/bridge.go:95` `bridgeMsg`
  - Synapse：`desktop/electron/services/bridge-adapter/bridge-protocol.ts` `bridgeBaseSchema`。

- CC Connect `core/bridge.go:99` `bridgeRegister`
  - Synapse：`bridgeRegisterSchema`、`parseBridgeRegister()`。
  - 行为：第一帧必须 register，platform 必填，capabilities 默认补 `text`，metadata 做 token/secret/authorization 过滤。

- CC Connect `core/bridge.go:107` `bridgeMessage`
  - Synapse：`bridgeMessageSchema` -> `AgentMessage`。
  - 行为：`session_key/user_id/content/reply_ctx/project/images/files` 结构化校验；本阶段 Bridge 入站附件明确返回 `unsupported_attachment`。

- CC Connect `core/bridge.go:121` `bridgeCardAction`
  - Synapse：`bridgeCardActionSchema`、`BridgeAdapterService.handleCardAction()`。
  - 行为：支持 `perm:<requestId>:allow` / `perm:<requestId>:deny`，走现有 `AgentRuntimeService.respondPermission()`，actor 标记为 `user id=bridge:<platform>`。

- CC Connect `core/bridge.go:158` `NewBridgeServer`
  - Synapse：`BridgeAdapterService` 默认 path `/bridge/ws`，默认 preferred port `9810`，默认 bind `127.0.0.1`。

- CC Connect `core/bridge.go:194` `Start`
  - Synapse：`BridgeAdapterService.start()` 通过 `NetworkServiceRegistry.register(...)` 启动 WebSocket 与 `/bridge/sessions` HTTP handler。

- CC Connect `core/bridge.go:614` `handleWS`
  - Synapse：`BridgeAdapterService.acceptWebSocket()`。
  - 行为：支持 `Authorization: Bearer`、`X-Bridge-Token`、query `token`。

- CC Connect `core/bridge.go:630` `handleConnection`
  - Synapse：`BridgeAdapterService.handleWebSocket()`。
  - 行为：第一帧 register，后续按 type 分发 `message`、`card_action`、`ping`，未知 type 返回结构化 `error` 且不崩溃。

- CC Connect `core/bridge.go:663` capabilities 默认 `text`
  - Synapse：`normalizeCapabilities()`。

- CC Connect `core/bridge.go:678` 同名 adapter 替换旧连接
  - Synapse：`BridgeAdapterService.registerAdapter()`。

- CC Connect `core/bridge.go:686` `register_ack`
  - Synapse：register 成功返回 `{ type: "register_ack", ok: true }`，失败返回 `{ type: "register_ack", ok: false, code, error }`。

- CC Connect `core/bridge.go:750` `bridgeAdapter.handleMessage`
  - Synapse：`BridgeAdapterService.handleMessage()`。
  - 行为：project 显式优先；未给 project 且只有一个配置项目时 fallback；无法唯一确定则结构化错误。

- CC Connect `core/bridge.go:819` `bridgeAdapter.handleCardAction`
  - Synapse：`BridgeAdapterService.handleCardAction()`。

## Bridge capabilities

- CC Connect `core/bridge_capabilities.go`
  - Synapse：`BridgeAdapterService.capabilitiesSnapshot()`。
  - 说明：只实现最小 `capabilities_snapshot` 子集：host + projects，commands 为空；不暴露命令、技能、provider secret。

## Bridge 出站

- CC Connect `BridgePlatform.Reply/Update/Typing`
  - Synapse：`BridgeAdapterService.dispatchAgentEvent()`。
  - 映射：
    - `text/result` -> `reply`
    - `thinking` -> `typing`，adapter 缺 `typing` 时忽略
    - `toolUse/toolResult` -> `update_message`，缺 capability 时 fallback `reply`
    - `permissionRequest` -> `card`，缺 capability 时 fallback `reply`
    - `error` -> `error`

- CC Connect `core/bridge.go` card fallback/native card
  - Synapse：`sendPermission()` 中 card/text fallback。

## Bridge sessions

- CC Connect `/bridge/sessions`、`/bridge/sessions/` handlers
  - Synapse：`BridgeAdapterService.handleSessionsHttp()`。
  - 映射：
    - GET list -> `AgentRuntimeService.listSessions()`
    - POST create -> `AgentRuntimeService.createSession()`
    - POST switch -> `AgentRuntimeService.switchSession()`
    - DELETE -> `AgentRuntimeService.deleteSession()`
  - 说明：复用 session repository，不绕过 AgentRuntime。

## 本地 side-channel send

- CC Connect `core/api.go:18` `APIServer`
  - Synapse：`desktop/electron/services/side-channel/side-channel-service.ts` `SideChannelService`。
  - 说明：本阶段用 localhost HTTP `/send`，通过 `NetworkServiceRegistry` 注册，不做 Unix socket、cron、relay。

- CC Connect `core/api.go:29` `SendRequest`
  - Synapse：`SideChannelSendRequest`。
  - 字段：`project/projectId`、`sessionKey/session_key`、`message`、`images`、`files`。

- CC Connect `core/api.go:132` `handleSend`
  - Synapse：`SideChannelService.handleHttp()` + `send()`。
  - 行为：只接受 POST；token 鉴权；body 限制；message/images/files 至少一个；project/session target 缺失返回结构化错误。

- CC Connect `core/engine.go:7090` `SendToSessionWithAttachments`
  - Synapse：`SideChannelService.send()`。
  - 说明：通过 AgentRuntime 记忆的 `ReplyTarget` 找到当前 session；通过注册 dispatcher 投递；成功/失败写 outbox，失败写 audit/warn。

- CC Connect `cmd/cc-connect/send.go`
  - Synapse：本阶段只实现 API 与 env 注入，不实现 `synapse send` CLI wrapper；报告中不声明 CLI 已存在。

## Agent env 注入

- CC Connect `core/engine.go:2370` per-session env 注入
  - Synapse：`AgentExecutionContext.sessionEnv`。

- CC Connect `core/engine.go:2373` `CC_PROJECT`、`core/engine.go:2374` `CC_SESSION_KEY`
  - Synapse：`SideChannelService.getAgentEnv()` 注入：
    - `CC_PROJECT`
    - `CC_SESSION_KEY`
    - `SYNAPSE_PROJECT`
    - `SYNAPSE_SESSION_KEY`
    - `SYNAPSE_SIDE_CHANNEL_URL`
    - `SYNAPSE_SIDE_CHANNEL_TOKEN`

- CC Connect `core/interfaces.go:58` `AgentSystemPrompt`
  - Synapse：本阶段未加入 prompt 注入点；只通过 env 暴露 send API，避免污染用户消息。

- CC Connect `cmd/cc-connect/send.go:132` / `:135`
  - Synapse：Codex/Claude adapter 都合并 per-session env，并把新增 env key 加入受控 allowlist。

## runtime/network

- CC Connect `core/bridge.go` / `core/api.go` 直接用 Go HTTP server
  - Synapse：`desktop/electron/runtime/network/local-host.ts`。
  - 说明：`http.createServer` 只出现在 `runtime/network`，业务 service 只提供 `handleHttp`、`acceptWebSocket`、`handleWebSocket`。

## 测试映射

- CC Connect `core/bridge_test.go` register/auth/message/reply/replace/session 测试
  - Synapse：`desktop/electron/services/bridge-adapter/__tests__/bridge-protocol.test.ts`、`bridge-adapter-service.test.ts`。

- CC Connect side-channel send 测试语义
  - Synapse：`desktop/electron/services/side-channel/__tests__/side-channel-service.test.ts`。

- Agent env 注入
  - Synapse：`desktop/electron/services/agent-runtime/__tests__/codex-exec.test.ts`、`claude-code.test.ts`、`agent-runtime-service.test.ts`。
