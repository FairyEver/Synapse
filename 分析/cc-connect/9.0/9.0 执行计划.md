# 9.0 执行计划

## 子任务

1. [已完成] 基线检查。
2. [已完成] 源码映射。
3. [已完成] runtime/network 承载层评估与必要补强。
4. [已完成] Bridge protocol 类型和校验。
5. [已完成] BridgeAdapterService 连接注册与状态。
6. [已完成] Bridge 入站 message/card_action。
7. [已完成] Bridge 出站 reply/update/typing。
8. [已完成] Bridge session 最小协议。
9. [已完成] side-channel send 服务。
10. [已完成] Agent env 注入。
11. [已完成] 附件策略。
12. [已跳过] UI 最小状态展示。
13. [已完成] 测试闭环。
14. [已完成] 最终报告与提交。

## 当前假设

- 本阶段只做 Bridge adapter 和本地 side-channel `send`，不做 Feishu/Lark、Cron、Relay 或多工作区路由。
- UI 不是本阶段闭环的必要条件；若不新增 renderer 状态展示，则第 12 项记录为跳过。
- 所有网络入口通过 `NetworkServiceRegistry` 注册，业务服务只提供 handler/protocol 逻辑。

## 完成说明

- runtime/network：新增 `local-host.ts`，统一承载 localhost HTTP 与 WebSocket upgrade。
- Bridge：新增 `desktop/electron/services/bridge-adapter/`，支持 register/message/card_action/ping、adapter replacement、sessions REST、reply/update/typing/card/text fallback。
- side-channel：新增 `desktop/electron/services/side-channel/`，支持本地 `/send`、token 鉴权、ReplyTarget 记忆、dispatcher、outbox/audit 与附件策略。
- Agent env：Codex/Claude adapter 合并 `AgentExecutionContext.sessionEnv`，并将新增 env keys 加入 allowlist。
- UI：未新增 renderer UI，因此未触碰 shadcn/Radix/Tailwind 表现层。
