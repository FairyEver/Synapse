# 8.0 测试记录

## 基线测试

- `pnpm desktop:typecheck`
  - 时间：第四步 A 代码实现前。
  - 结果：通过。
  - 说明：未发现前置 typecheck 失败。

## 受影响测试

- `pnpm desktop:typecheck`
  - 时间：实现首轮代码后。
  - 结果：失败。
  - 失败摘要：
    - `desktop/electron/modules/agent/ipc.ts` 中 IPC handler 的 `request` 被推断为 `unknown`。
    - `desktop/electron/services/reply-target/outbox-service.ts` 引用的 `OutboxPayloadV1` 未从 data-repo 入口导出。
  - 处理：见 `8.0 问题修复记录.md`。
- `pnpm desktop:typecheck`
  - 时间：修复首轮 typecheck 后。
  - 结果：失败。
  - 失败摘要：`desktop/electron/modules/agent/__tests__/ipc.test.ts` 的 `ctx.resolve` 测试桩不满足泛型签名。
  - 处理：见 `8.0 问题修复记录.md`。
- `pnpm desktop:typecheck`
  - 时间：修复 Agent IPC 测试桩后。
  - 结果：通过。
- `pnpm --dir desktop exec vitest run electron/runtime/security/__tests__/security.test.ts electron/services/reply-target/__tests__/outbox-service.test.ts electron/services/agent-runtime/__tests__/command-router.test.ts electron/services/agent-runtime/__tests__/agent-runtime-service.test.ts electron/modules/agent/__tests__/ipc.test.ts`
  - 时间：受影响范围测试。
  - 结果：通过，5 个测试文件，29 个用例。
- `rg -n "style=|#[0-9a-fA-F]{3,8}|rgb\\(|hsl\\(|gradient|✨|🚀|⚡|bg-\\[|text-\\[|shadow-" desktop/src/modules/agent desktop/src/App.tsx`
  - 时间：UI 约束静态检查。
  - 结果：无命中。

## 全量测试

- `pnpm desktop:typecheck`
  - 结果：通过。
- `pnpm desktop:test`
  - 结果：通过，55 个测试文件，371 个用例。

## Hard constraints

- `pnpm desktop:check:hard-constraints`
  - 结果：通过，输出 `All hard-constraint checks passed.`
