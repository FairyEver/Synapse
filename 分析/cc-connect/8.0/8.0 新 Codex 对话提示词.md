# 8.0 新 Codex 对话提示词

把下面整段复制到新的 Codex 对话中，用来启动第四步 A：

```text
你现在在仓库 `/Users/liyang/Documents/code/github/Synapse` 中工作。

先做这些准备：
1. 执行 `git status --short --branch`，确认当前状态。
2. 当前仓库可能有上一轮生成的 `分析/cc-connect/3.1 整体开发顺序.md` 和 `分析/cc-connect/3.2 后续阶段复核与调整建议.md` 文档变更，这些是有效产物，不要丢弃、不要回滚。
3. 从当前状态新建分支 `第四步A`。如果分支已存在，先停下来说明当前分支和状态，不要强制覆盖。
4. 阅读 `AGENTS.md`，并且如果本步骤触碰 UI，必须阅读 `.claude/rules/design.md` 和 `.claude/rules/ui-rules.md`。

本次任务是执行：
`分析/cc-connect/8.0/8.0 第四步A执行任务书.md`

本阶段目标：
- 实现本地 Agent Management IPC。
- 实现 Renderer Chat 薄界面。
- 实现 AuditSink 持久化。
- 实现 ReplyTarget / Outbox 基础层。
- 补齐 slash command 策略边界。
- 所有过程产物都放在 `分析/cc-connect/8.0/` 下。

执行前必须阅读：
- `分析/cc-connect/3.1 整体开发顺序.md`
- `分析/cc-connect/3.2 后续阶段复核与调整建议.md`
- `分析/cc-connect/8.0/8.0 第四步A执行任务书.md`
- `分析/cc-connect/4.0/`
- `分析/cc-connect/5.0/`
- `分析/cc-connect/6.0/`
- `分析/cc-connect/7.0/`

执行前还必须重新阅读 CC Connect 源码。只要是在复刻 CC Connect 行为，就必须先找到对应源码，不允许凭空实现。重点看：
- `/Users/liyang/Documents/code/demo/cc-connect-main/core/management.go`
  - `ManagementServer`
  - `buildHandler`
  - `handleStatus`
  - `handleProjectSessions`
  - `handleProjectSend`
  - `handleProjectProviders`
- `/Users/liyang/Documents/code/demo/cc-connect-main/core/engine.go`
  - `SendToSessionWithAttachments`
  - slash command / unknown command 处理逻辑
- `/Users/liyang/Documents/code/demo/cc-connect-main/core/interfaces.go`
  - `ReplyContextReconstructor`
- `/Users/liyang/Documents/code/demo/cc-connect-main/core/bridge.go`
  - 只读取回复目标和消息流思想，本阶段不实现 Bridge。

实现约束：
- 不允许新增全局单例。
- 不允许在新代码中裸用 `ipcMain.handle/on`。
- 不允许裸用 `webContents.send`。
- Renderer 只能通过 `window.synapse.*` 使用 Electron 能力。
- 不允许暴露 raw `ipcRenderer`。
- 不允许把 filesystem、shell、provider secret 暴露给 Renderer。
- UI 必须使用现有 shadcn/Radix 和 Tailwind token。
- 禁止自定义颜色、hex/rgb/hsl 字面色、内联 style、AI 渐变、装饰 emoji、卡片套卡片。
- 新业务模块放在 `desktop/src/modules/`，不要放进 `desktop/src/features/`。
- 不要启动 dev server、浏览器预览、Chrome DevTools、Playwright 或运行 App 页面，除非我明确要求。

本阶段不要做：
- Bridge WebSocket server/client。
- `BridgeAdapterService` side-channel send。
- 飞书/Lark。
- Cron/Scheduler。
- Relay。
- 多工作区路由。
- Provider 完整配置页。

过程产物要求：
在 `分析/cc-connect/8.0/` 下维护：
- `8.0 执行计划.md`
- `8.0 源码映射.md`
- `8.0 测试记录.md`
- `8.0 问题修复记录.md`
- `8.0 最终报告.md`

测试闭环：
1. 先测试。
2. 发现问题后记录到 `8.0 问题修复记录.md`。
3. 修改问题。
4. 重复测试。
5. 直到没有已知失败，或明确记录无法解决的外部阻塞。

至少尝试运行：
```bash
pnpm desktop:typecheck
pnpm desktop:test
pnpm desktop:check:hard-constraints
```

如果因为环境或既有问题不能运行或不能通过，必须写清楚具体命令、失败原因、是否由本阶段引入。

完成后请给我：
- 当前分支名。
- 产生的提交 hash。如果你没有提交，说明原因。
- 修改文件摘要。
- 测试结果。
- 是否具备进入 `9.0 第四步B` 的前提。
```
