# 4.0 测试记录

## 脚本确认

已检查 `package.json` 与 `desktop/package.json`：

- `pnpm desktop:typecheck` -> `pnpm --filter @synapse/desktop run typecheck`
- `pnpm desktop:test` -> `pnpm --filter @synapse/desktop run test`
- `pnpm desktop:check:hard-constraints` -> `pnpm --filter @synapse/desktop run check:hard-constraints`

## 执行记录

### 1. 受影响测试首轮

命令：

```bash
pnpm --filter @synapse/desktop test -- desktop/electron/runtime/data-repo/__tests__/schemas.test.ts desktop/electron/runtime/project-container/__tests__/scoped-data-repo.test.ts desktop/electron/runtime/process/__tests__/controlled-runner.test.ts desktop/electron/runtime/network/__tests__/registry.test.ts desktop/electron/bootstrap/__tests__/registry.test.ts desktop/electron/runtime/service-registry/__tests__/registry.test.ts desktop/electron/bootstrap/__tests__/descriptors.test.ts
```

结果：失败。Vitest 实际运行了全量测试，`desktop/tests/unit/phase-0.2-integration.test.ts` 2 个断言失败。

- `backup export/import round-trips JSON + JSONL + SQLite + encrypted namespaces`：旧 audit/conversation 测试数据被新业务 schema validator 拒绝，导入后为空。
- `merge import keeps existing items intact and adds new ones`：旧 audit 测试数据被 validator 拒绝。

### 2. 受影响测试复测

命令：

```bash
pnpm --dir desktop exec vitest run electron/runtime/data-repo/__tests__/schemas.test.ts electron/runtime/project-container/__tests__/scoped-data-repo.test.ts electron/runtime/process/__tests__/controlled-runner.test.ts electron/runtime/network/__tests__/registry.test.ts electron/bootstrap/__tests__/registry.test.ts electron/runtime/service-registry/__tests__/registry.test.ts electron/bootstrap/__tests__/descriptors.test.ts tests/unit/phase-0.2-integration.test.ts
```

结果：通过。

- Test Files: 8 passed
- Tests: 61 passed

### 3. 类型检查首轮

命令：

```bash
pnpm desktop:typecheck
```

结果：失败。

- `desktop/electron/runtime/data-repo/factory.ts`：JSONL backend 泛型需要带 `id`。
- `desktop/electron/runtime/process/controlled-runner.ts`：闭包赋值导致 error 变量被控制流缩窄为 `never`。
- `desktop/electron/runtime/project-container/scoped-data-repo.ts`：`projectId` scoped filter 需要先转 `unknown`。

### 4. 类型检查复测

命令：

```bash
pnpm desktop:typecheck
```

结果：通过。

### 5. 全量测试

命令：

```bash
pnpm desktop:test
```

结果：通过。

- Test Files: 45 passed
- Tests: 323 passed

备注：测试中出现 Node SQLite experimental warning，属于现有测试环境提示，不影响结果。

### 6. Hard Constraints

命令：

```bash
pnpm desktop:check:hard-constraints
```

结果：通过。

- `All hard-constraint checks passed.`
