# 4.0 新 Codex 对话提示词

把下面整段原样复制到新的 Codex 对话中：

```text
你现在在仓库 `/Users/liyang/Documents/code/github/Synapse` 中工作，当前任务是执行 `分析/cc-connect/3.1 整体开发顺序.md` 里的“第零步：地基整理”。

请先阅读并严格遵守：

1. `AGENTS.md`
2. `分析/cc-connect/3.1 整体开发顺序.md`
3. `分析/cc-connect/4.0/4.0 第零步执行任务书.md`

CC Connect 源码位置是：

`/Users/liyang/Documents/code/demo/cc-connect-main`

最高优先级要求：

- 每开发一个模块或机制，都必须先去 CC Connect 源码中找到对应逻辑，并记录文件、行号、方法名、设计依据。
- 只有纯使用 Synapse 当前已有基础设施时，才可以不查 CC Connect 对应逻辑。
- 如果是在复刻 CC Connect 的方法，就必须查它的原始源码，不能只凭分析文档或记忆实现。
- 本阶段所有计划、源码映射、测试记录、问题记录、修复记录、最终报告，都必须放在 `分析/cc-connect/4.0/`。
- 实际生产代码必须按 Synapse 项目结构修改，不要把生产代码写进 `分析/cc-connect/4.0/`。
- 只执行第零步，不要提前实现 AgentRuntime、Feishu、Cron、Bridge UI 或多工作区。

第零步目标：

1. 补齐全局 runtime 服务注册。
2. 补齐 project-scoped persistence。
3. 把 providers/projects/connectors/conversations/audit/outbox 等占位 schema 升级成后续可用的业务 schema。
4. 定义并落地 Agent 子进程启动边界。
5. 定义并落地本地网络服务边界。

执行流程：

1. 先生成 `分析/cc-connect/4.0/4.0 源码映射.md`，把 Synapse 当前文件、CC Connect 对应源码、设计差异、采用方案写清楚。没有完成源码映射，不要大规模改代码。
2. 再生成 `分析/cc-connect/4.0/4.0 执行计划.md`，列出修改文件、目的、风险、测试和验收标准。
3. 按计划外科手术式修改代码，复用现有结构，不做相邻重构，不引入无关依赖，不做 UI。
4. 测试：运行相关单元测试、类型检查、hard constraints。推荐先查 `package.json` 确认脚本，再执行 `pnpm desktop:typecheck`、`pnpm desktop:test`、`pnpm desktop:check:hard-constraints` 或实际存在的等价命令。
5. 把测试结果写入 `分析/cc-connect/4.0/4.0 测试记录.md`。
6. 如果测试发现问题，把问题写入 `分析/cc-connect/4.0/4.0 问题修复记录.md`，修复后重复测试，一直测到没有已知问题，或只剩明确与本任务无关的历史失败。
7. 最后生成 `分析/cc-connect/4.0/4.0 最终报告.md`，说明完成内容、源码依据、修改文件、测试结果、已修复问题、剩余风险，以及是否可以进入 3.1 的第一步 AgentRuntime MVP。

禁止事项：

- 禁止直接开始做 Feishu。
- 禁止直接开始做 Cron。
- 禁止直接开始做 AgentRuntime MVP。
- 禁止让平台或 renderer 直接调用 CLI。
- 禁止跳过 CC Connect 源码映射。
- 禁止只写计划不改代码，除非发现第零步计划本身不可执行，并在报告中给出硬阻塞证据。
- 禁止执行破坏性 Git 操作，例如 `git reset --hard`、强推、删除用户文件。

现在开始执行。先读文件和源码，先产出 4.0 源码映射与执行计划，再动代码。
```
