# 5.0 测试记录

## 脚本确认

- `pnpm desktop:typecheck` -> `pnpm --filter @synapse/desktop run typecheck`
- `pnpm desktop:test` -> `pnpm --filter @synapse/desktop run test`
- `pnpm desktop:check:hard-constraints` -> `pnpm --filter @synapse/desktop run check:hard-constraints`
- 本次改动文件单独 ESLint 使用 `pnpm --dir desktop exec eslint <files...>`

## 执行记录

### 1. 第零步收尾验证

命令：

```bash
pnpm desktop:typecheck
pnpm desktop:test
pnpm desktop:check:hard-constraints
```

结果：全部通过。

- Typecheck: passed
- Vitest: 45 files passed, 323 tests passed
- Hard constraints: passed

随后提交第零步改动：

```bash
git commit -m "chore: complete zero step foundation"
git switch -c "第一步"
```

### 2. 受影响测试首轮

命令：

```bash
pnpm --dir desktop exec vitest run electron/runtime/process/__tests__/controlled-runner.test.ts electron/services/agent-runtime/__tests__/codex-exec.test.ts electron/services/agent-runtime/__tests__/agent-runtime-service.test.ts electron/bootstrap/__tests__/registry.test.ts
```

结果：通过。

- Test Files: 4 passed
- Tests: 19 passed

覆盖：

- Codex args builder 新会话、resume、model/provider/baseUrl/effort。
- Codex JSONL parser 的 thread/text/thinking/tool/error/result 映射。
- AgentRuntime service fake runner 闭环、threadId 保存、resume 参数、权限拒绝。
- ControlledProcessRunner stdin 和 stdout line handler。

### 3. 类型检查首轮

命令：

```bash
pnpm desktop:typecheck
```

结果：失败。

- `desktop/electron/services/agent-runtime/agent-runtime-service.ts` 引入了未从 data-repo 入口导出的 `ConversationHistoryEntryV1`。

### 4. 类型检查复测

命令：

```bash
pnpm desktop:typecheck
```

结果：通过。

### 5. 本次改动文件单独 ESLint

命令：

```bash
pnpm --dir desktop exec eslint electron/runtime/process/controlled-runner.ts electron/runtime/process/__tests__/controlled-runner.test.ts electron/services/agent-runtime/agent-runtime-service.ts electron/services/agent-runtime/adapters/codex-exec.ts electron/services/agent-runtime/index.ts electron/services/agent-runtime/__tests__/codex-exec.test.ts electron/services/agent-runtime/__tests__/agent-runtime-service.test.ts electron/bootstrap/descriptors.ts electron/bootstrap/__tests__/registry.test.ts
```

结果：通过。

### 6. 全量测试

命令：

```bash
pnpm desktop:test
```

结果：通过。

- Test Files: 47 passed
- Tests: 335 passed

备注：测试中仍有 Node SQLite experimental warning，属于现有测试环境提示。

### 7. Hard Constraints

命令：

```bash
pnpm desktop:check:hard-constraints
```

结果：通过。

- `All hard-constraint checks passed.`
