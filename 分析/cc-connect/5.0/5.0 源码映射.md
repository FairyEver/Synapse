# 5.0 源码映射

本文件记录“第一步：本机 AgentRuntime MVP”的 Synapse 实现与 CC Connect 原始源码对应关系。CC Connect 路径均基于 `/Users/liyang/Documents/code/demo/cc-connect-main`。

## 1. Agent 输入/输出类型

Synapse 实现：

- `desktop/electron/services/agent-runtime/types.ts:12` 定义 `AgentMessage`。
- `desktop/electron/services/agent-runtime/types.ts:24` 定义 `AgentEvent` 基类。
- `desktop/electron/services/agent-runtime/types.ts:36` 到 `:79` 定义 `text/thinking/toolUse/toolResult/result/error`。
- `desktop/electron/services/agent-runtime/types.ts:96` 定义 `AgentAdapter`。

CC Connect 对应：

- `core/message.go:140` 的 `Message`：统一平台入站消息，包含 `SessionKey/Platform/UserID/UserName/Content/ReplyCtx/ModeOverride`。
- `core/message.go:160` 的 `EventType`：统一事件类型。
- `core/message.go:187` 的 `Event`：统一 Agent 输出，包含文本、工具、结果、错误、SessionID。
- `core/interfaces.go:233` 的 `Agent` 和 `core/interfaces.go:243` 的 `AgentSession`：Agent 工厂和活跃 CLI session 边界。

迁移取舍：

- 本步只保留本机闭环必需字段；`attachments/replyCtx/modeOverride` 保留扩展位。
- 暂不实现 `permission_request`，Claude Code 权限回写留到第二步。

## 2. 受控进程边界

Synapse 实现：

- `desktop/electron/runtime/process/controlled-runner.ts:45` 扩展 `ControlledProcessRunRequest.stdin` 和 line handlers。
- `desktop/electron/runtime/process/controlled-runner.ts:121` 到 `:155` 保持 `PermissionGuard.check()` 和拒绝审计。
- `desktop/electron/runtime/process/controlled-runner.ts:160` 到 `:210` 在受控 runner 内启动进程、处理 stdout/stderr、写入 stdin。
- `desktop/electron/runtime/process/controlled-runner.ts:263` 到 `:280` 记录成功/失败 audit。
- `desktop/electron/runtime/process/controlled-runner.ts:334` 到 `:371` 实现完整行回调。

CC Connect 对应：

- `agent/codex/session.go:111` 到 `:128`：`exec.CommandContext` 设置 cwd/env/stdin/stdout/stderr 并启动 Codex。
- `agent/codex/session.go:229` 到 `:275`：`readLoop` 读取 stdout JSONL，进程失败时把 stderr 映射为 error event。
- `agent/codex/session.go:277` 到 `:300`：`readJSONLines` 按行读取 stdout。

迁移取舍：

- Synapse 不在业务服务里直接 `child_process.spawn`，而是在第零步 runner 内补 stdin 与 line streaming。
- env 仍走 allowlist，audit 不记录 prompt 原文，只记录 `stdinBytes`。

## 3. Codex exec 参数构造

Synapse 实现：

- `desktop/electron/services/agent-runtime/adapters/codex-exec.ts:61` 到 `:109` 调用受控 runner。
- `desktop/electron/services/agent-runtime/adapters/codex-exec.ts:112` 到 `:153` 实现 `buildCodexExecArgs()`。

CC Connect 对应：

- `agent/codex/session.go:92` 的 `codexSession.Send`：每轮启动 Codex 子进程。
- `agent/codex/session.go:106` 到 `:117`：根据当前 thread 判断是否 resume，并将 prompt 写入 stdin。
- `agent/codex/session.go:166` 到 `:214`：`buildExecArgs` 生成 `codex exec` / `codex exec resume` 参数。

迁移取舍：

- 本步按用户要求新会话使用 `--json --cd <workDir> -`。
- resume 不加 `--cd`，依赖 runner `cwd`，与 CC Connect `session.go:204` 的注释一致。
- 已支持 `model/provider/baseUrl/effort/mode`，但 ProviderConfigService 留到第三步。

## 4. Codex JSONL 事件映射

Synapse 实现：

- `desktop/electron/services/agent-runtime/adapters/codex-exec.ts:159` 到 `:213` 定义 parser 状态、thread id、pending text、最终结果。
- `desktop/electron/services/agent-runtime/adapters/codex-exec.ts:215` 到 `:247` 映射顶层事件。
- `desktop/electron/services/agent-runtime/adapters/codex-exec.ts:249` 到 `:273` 映射 `item.started`。
- `desktop/electron/services/agent-runtime/adapters/codex-exec.ts:275` 到 `:335` 映射 `item.completed`。
- `desktop/electron/services/agent-runtime/adapters/codex-exec.ts:337` 到 `:347` 处理 pending text/thinking flush。
- `desktop/electron/services/agent-runtime/adapters/codex-exec.ts:377` 到 `:395` 实现 `extractItemText`。

CC Connect 对应：

- `agent/codex/session.go:302` 到 `:365`：`handleEvent` 处理 `thread.started/turn.started/item.started/item.completed/turn.completed/turn.failed/error`。
- `agent/codex/session.go:368` 到 `:404`：pending `agent_message` 在工具前作为 thinking、turn 完成时作为 text。
- `agent/codex/session.go:414` 到 `:451`：`handleItemStarted` 将 `command_execution/function_call` 映射为 tool use。
- `agent/codex/session.go:453` 到 `:551`：`handleItemCompleted` 映射 reasoning、agent_message、command result、function result 和其他工具。
- `agent/codex/session.go:900` 到 `:925`：`extractItemText` 从数组字段提取 text。

迁移取舍：

- `error` 顶层事件在 Synapse 中会生成 `AgentEvent.error`，这是本步测试要求；CC Connect 原实现多为日志记录。
- `turn.completed` 的 Synapse `result` 带聚合文本，便于本机 MVP 直接返回最终结果；CC Connect 由 Engine 在 `processInteractiveEvents` 聚合文本。

## 5. AgentRuntime service 与 conversation 持久化

Synapse 实现：

- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:29` 到 `:74` 实现 `send()` 主流程。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:77` 到 `:104` 创建或读取 conversation。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:106` 到 `:148` 写入用户历史、agentSessionId/threadId、assistant 历史和 past ids。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:165` 到 `:181` 将 AgentEvent 发到 project-scoped EventBus。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:189` 到 `:223` 生成稳定 conversation id 与 past id。

CC Connect 对应：

- `core/engine.go:1465` 的 `handleMessage`：平台消息进入统一主流程。
- `core/engine.go:2076` 的 `processInteractiveMessageWith`：写用户 history，创建/复用 agent session，调用 `Send` 并处理事件。
- `core/engine.go:2324` 的 `getOrCreateInteractiveStateWith`：从 session 的 AgentSessionID 恢复或创建 AgentSession。
- `core/engine.go:2579` 的 `processInteractiveEvents`：消费 Agent event，聚合文本、保存 session id、发送最终回复。
- `core/session.go:17` 的 `Session`：保存 `AgentSessionID/PastAgentSessionIDs/History`。
- `core/session.go:137` 的 `CompareAndSetAgentSessionID`：保存首次获取到的 Agent 原生 session id。
- `core/session.go:544` 的 `Save` 与 `core/session.go:618` 的 `load`：持久化/恢复 session snapshot。

迁移取舍：

- 本步不实现 session lock、busy queue、permission pending，这些对应 CC Connect `engine.go:1728`、`:1785`、`:3112`，留到第二步。
- 本步用第零步 `conversations` schema 存 `agentSessionId/threadId/history`，不复刻 CC Connect JSON 文件。

## 6. Project-scoped 服务注册

Synapse 实现：

- `desktop/electron/services/agent-runtime/index.ts:37` 到 `:54` 定义 `agent.runtime` project-scoped service。
- `desktop/electron/bootstrap/descriptors.ts:325` 到 `:343` 在 `core.project-containers` 创建时注册 `agent.runtime`。

CC Connect 对应：

- `cmd/cc-connect/main.go:246` 附近创建每个项目的 `core.NewEngine(...)`。
- `cmd/cc-connect/main.go:721`、`:771`、`:976` 附近将 Cron/Bridge/API 按 project 注册到已有 Engine。

迁移取舍：

- Synapse 采用 ProjectContainerRegistry 承载 project-scoped AgentRuntime，不新增平行全局 singleton。
- `core.project-containers` 增加对 `core.permission-guard`、`core.audit-sink` 的启动依赖，确保打开项目时能创建受控 runner。
