# 6.0 源码映射

本文件记录第二步实现与 CC Connect 原始源码的对应关系。CC Connect 路径基于 `/Users/liyang/Documents/code/demo/cc-connect-main`。

## 1. 会话持久化

Synapse 实现：

- `desktop/electron/runtime/data-repo/schemas/placeholders.ts:197` 增加 `ConversationResumePolicyV1`。
- `desktop/electron/runtime/data-repo/schemas/placeholders.ts:199` 到 `:214` 扩展 `ConversationEntryV1`，保存 `agentType`、`agentSessionId`、`pastAgentSessionIds`、`resumePolicy`、history、userMeta、active、name。
- `desktop/electron/services/agent-runtime/session-repository.ts:31` 到 `:220` 新增 `AgentSessionRepository`，基于 `conversations` namespace 管理 active session、history、userMeta、agent session id 和清理当前 agent session id。

CC Connect 对应：

- `core/session.go:17` 到 `:30` 的 `Session` 保存 `AgentSessionID`、`AgentType`、`PastAgentSessionIDs`、`History`。
- `core/session.go:44` 到 `:81` 的 agent session id 设置和 past id 记录。
- `core/session.go:181` 到 `:204` 的 snapshot 保存 active session、session names、userMeta。
- `core/session.go:322` 到 `:349` 的 `SwitchToAgentSession`。
- `core/session.go:673` 到 `:707` 的 `InvalidateForAgent` 清理不匹配 agent 的当前 session id。

迁移取舍：

- 不复刻 CC Connect 的 JSON 文件存储，继续使用 Synapse 第零步已有 `conversations` namespace。
- 本步保留 `resumePolicy` 状态和清理方法，不实现 `/new`、切 Agent、切 Provider 命令本身。

## 2. Session lock 和 pending queue

Synapse 实现：

- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:47` 到 `:60` 定义 per-session runtime state 和默认队列上限。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:77` 到 `:105` 将普通消息进入同一 session 队列；busy 且超过上限时返回错误。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:199` 到 `:241` 串行处理队列，每个 turn 开始时才写入 user history 和 Agent。

CC Connect 对应：

- `core/session.go:32` 到 `:42` 的 `TryLock/Unlock`。
- `core/engine.go:1624` 到 `:1665` busy 时进入 queue，不直接写 stdin。
- `core/engine.go:1728` 到 `:1778` 的 `queueMessageForBusySession`，队列上限为 5。
- `core/engine.go:3112` 到 `:3191` 的 EventResult 后按顺序处理 queued message。

迁移取舍：

- 本步没有实现 `/btw` 中途写 stdin，只保留长驻 session 的 `send()` 扩展点。
- Renderer/connector 仍只看到 `send()` promise；队列内部保证同一 `sessionKey` 串行。

## 3. Permission pending

Synapse 实现：

- `desktop/electron/services/agent-runtime/types.ts:80` 到 `:87` 增加 `permissionRequest` 事件。
- `desktop/electron/services/agent-runtime/types.ts:136` 到 `:158` 定义 allow/deny 和 pending permission 类型。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:107` 到 `:188` 保存 pending permission，并提供 `respondPermission()` 回写。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:129` 到 `:156` 禁止非 user actor 直接 allow，并在 allow 前走 `PermissionGuard.check()`。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:455` 到 `:477` 写入 permission audit。

CC Connect 对应：

- `core/message.go:160` 到 `:187` 的 `EventPermissionRequest`。
- `core/engine.go:1881` 到 `:1968` 的 pending permission 用户响应处理。
- `core/engine.go:2865` 到 `:2929` 的 permission request pending、prompt 和等待 resolved。
- `agent/claudecode/session.go:600` 到 `:636` 的 `RespondPermission` 写 `control_response`。

迁移取舍：

- Synapse 本步只提供 runtime pending 和回写接口，不做平台按钮、卡片或 UI。
- deny 可由非 user actor 触发；allow 只能由 user actor 触发。

## 4. Claude Code adapter MVP

Synapse 实现：

- `desktop/electron/runtime/process/controlled-runner.ts:73` 到 `:79` 定义长驻 `ControlledProcessSession`。
- `desktop/electron/runtime/process/controlled-runner.ts:122` 到 `:189` 新增 `ControlledProcessRunner.start()`，仍先走 PermissionGuard 和 audit。
- `desktop/electron/services/agent-runtime/adapters/claude-code.ts:36` 到 `:80` 通过受控 runner 启动 Claude Code。
- `desktop/electron/services/agent-runtime/adapters/claude-code.ts:82` 到 `:104` 构造 `--input-format stream-json --output-format stream-json --permission-prompt-tool stdio` 参数。
- `desktop/electron/services/agent-runtime/adapters/claude-code.ts:126` 到 `:159` 写 user JSON 和 `control_response`。
- `desktop/electron/services/agent-runtime/adapters/claude-code.ts:195` 到 `:285` 解析 stream-json、`control_request` 和 result。

CC Connect 对应：

- `agent/claudecode/session.go:57` 到 `:73` 的 Claude Code stream-json 参数。
- `agent/claudecode/session.go:209` 到 `:323` 的 stdout read loop。
- `agent/claudecode/session.go:324` 到 `:516` 的 `control_request`、assistant、result 映射。
- `agent/claudecode/session.go:517` 到 `:599` 的 user JSON stdin 写入。
- `agent/claudecode/session.go:600` 到 `:636` 的 `control_response`。
- `agent/claudecode/claudecode.go:381` 到 `:426` 的 `StartSession`。

迁移取舍：

- 本步不迁移 provider proxy、router、allowedTools/disallowedTools、附件落盘、run_as_user。
- 进程启动没有在业务 adapter 里直接 spawn，而是经 `ControlledProcessRunner.start()`。

## 5. 输入和输出治理

Synapse 实现：

- `desktop/electron/services/agent-runtime/governance.ts:21` 到 `:46` 定义统一治理配置模型。
- `desktop/electron/services/agent-runtime/governance.ts:83` 到 `:121` 提供入站消息统一评估和 outgoing 入口。
- `desktop/electron/services/agent-runtime/governance.ts:123` 到 `:181` 覆盖 allowlist、群聊 @、dedupe/旧消息、rate limit、disabled commands、admin allowlist、banned words。
- `desktop/electron/services/agent-runtime/governance.ts:184` 到 `:323` 实现 sliding-window rate limiter、role policy、message dedupe、outgoing token bucket。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:84` 到 `:88` 在进入 Agent turn 前调用 governance。

CC Connect 对应：

- `core/engine.go:733` 到 `:866` 的 disabled commands、admin allowlist、banned words、rate limit 配置。
- `core/engine.go:1531` 到 `:1548` 的 rate limit 和 banned words 入站检查。
- `core/engine.go:3467` 到 `:3482` 的 disabled command 判断。
- `core/ratelimit.go:1` 到 `:103` 的 sliding-window rate limiter。
- `core/outgoing_ratelimit.go:1` 到 `:132` 的 outgoing token bucket。
- `core/user_roles.go:1` 到 `:249` 的 role policy。
- `core/dedup.go:1` 到 `:51` 的 dedupe 和旧消息过滤。

迁移取舍：

- 不在 Feishu/Lark connector 中散写治理逻辑；本步只建立平台无关服务入口。
- Outgoing limiter 先提供非阻塞 `allowOutgoing()` 能力，后续 connector/outbox 可选择等待或排队策略。
