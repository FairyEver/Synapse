# 13.0 测试记录

## 1. 过程验证

- `pnpm --filter @synapse/desktop run generate:ipc`
  - 结果：通过。
  - 说明：生成 `desktop/electron/generated/ipc-channels.generated.ts`，新增 Agent `listCommands` 与 `openReference` IPC channel。

- `pnpm --filter @synapse/desktop exec tsc -p tsconfig.electron.json --noEmit`
  - 初次结果：失败。
  - 原因：`ReferenceViewOptions` readonly 字段被当作可变对象赋值；Feishu admin 标记调用传入了可能为 `undefined` 的 `userId`。
  - 修复后结果：通过。

- `pnpm --filter @synapse/desktop exec tsc -p tsconfig.json --noEmit`
  - 结果：通过。

- 相关测试命令：
  - `pnpm --filter @synapse/desktop run test -- electron/services/agent-runtime/__tests__/codex-exec.test.ts electron/services/agent-runtime/__tests__/command-router.test.ts electron/services/agent-runtime/__tests__/command-registry.test.ts electron/services/agent-runtime/__tests__/skill-registry.test.ts electron/services/agent-runtime/__tests__/references.test.ts electron/services/bridge-adapter/__tests__/bridge-adapter-service.test.ts electron/services/bridge-adapter/__tests__/bridge-protocol.test.ts electron/services/connectors/__tests__/feishu-reply-service.test.ts electron/runtime/data-repo/__tests__/schemas.test.ts`
  - 初次结果：失败。
  - 修复后结果：通过。
  - 统计：71 个 test files，423 条 tests。

## 2. 任务书要求的最终验证

- `pnpm desktop:typecheck`
  - 结果：通过。

- `pnpm desktop:test`
  - 结果：通过。
  - 统计：71 个 test files，423 条 tests。

- `pnpm desktop:check:hard-constraints`
  - 结果：通过。
  - 输出：`All hard-constraint checks passed.`

- `pnpm desktop:check:ipc-codegen`
  - 初次结果：失败。
  - 原因：`electron/generated/ipc-channels.generated.ts` 已重新生成，但脚本检查 working tree diff，需要暂存生成文件。
  - 处理：只暂存 `desktop/electron/generated/ipc-channels.generated.ts`。
  - 最终结果：通过。
  - 输出：`IPC codegen output is in sync with electron/generated/ipc-channels.generated.ts`

## 3. 未执行项

- 未启动 dev server。
- 未启动 Electron。
- 未启动浏览器、Chrome DevTools、Playwright 或运行界面。

