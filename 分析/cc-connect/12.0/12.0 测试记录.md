# 12.0 测试记录

## 1. 命令结果

- `pnpm desktop:typecheck`
  - 结果：通过。
  - 说明：renderer、electron、test tsconfig 均通过。

- `pnpm desktop:test`
  - 结果：通过。
  - 统计：68 个 test files，412 条 tests 全部通过。

- `pnpm desktop:check:hard-constraints`
  - 结果：通过。
  - 输出：`All hard-constraint checks passed.`

- `pnpm desktop:check:ipc-codegen`
  - 第一次结果：失败。
  - 原因：脚本会重新生成 `desktop/electron/generated/ipc-channels.generated.ts`，并要求生成文件相对 git index 无 diff；当时生成产物已更新但未暂存。
  - 修复：暂存 `desktop/electron/generated/ipc-channels.generated.ts` 后复跑。
  - 最终结果：通过。
  - 输出：`IPC codegen output is in sync with electron/generated/ipc-channels.generated.ts`

## 2. 补充命令

- `pnpm desktop:generate:ipc`
  - 结果：通过。
  - 说明：新增 Feishu automation IPC 后刷新生成文件。
