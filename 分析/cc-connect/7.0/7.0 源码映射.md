# 7.0 源码映射

CC Connect 源码基于 `/Users/liyang/Documents/code/demo/cc-connect-main`。

## 1. Provider 数据结构与解析

CC Connect：

- `config/config.go:346` `ProviderModelConfig`
- `config/config.go:353` `ProviderConfig`
- `config/config.go:370` `CodexProviderConfig`
- `config/config.go:947` `ResolveProviderRefs`
- `config/config.go:988` `ProviderConfig.ResolveForAgent`

Synapse：

- `desktop/electron/runtime/data-repo/schemas/placeholders.ts` 扩展 `SecretEntryV1`、`ProviderEntryV1` 和 provider options。
- `desktop/electron/services/provider-config/types.ts` 定义 ProviderConfigService 对外类型和 runtime view。
- `desktop/electron/services/provider-config/provider-config-service.ts:33` 新增 `ProviderConfigService`。
- `desktop/electron/services/provider-config/provider-config-service.ts:80` 解析 provider refs、project inline provider、agentType 过滤和 active provider。
- `desktop/electron/services/provider-config/provider-config-service.ts:155` 解析 runtime view。

迁移取舍：

- Synapse 不使用 TOML config 作为业务主存储，沿用 `DataRepository` 的 `providers` / `secrets` namespace。
- project inline provider 使用 `provider:<projectId>:<providerId>` 存储，并通过 `options.providerId` 保持对外 provider id。

## 2. 模型切换

CC Connect：

- `core/provider.go:3` `GetProviderModels`
- `core/provider.go:11` `GetProviderModel`
- `core/provider.go:23` `SetProviderModel`
- `core/engine.go:5902` `cmdModel`
- `core/engine.go:6043` `parseModelSwitchArgs`
- `core/engine.go:6068` `switchModelOnAgent`

Synapse：

- `desktop/electron/services/provider-config/provider-config-service.ts:129` `setActiveModel`，优先更新 active provider，未配置 active provider 时更新 project active model。
- `desktop/electron/services/agent-runtime/command-router.ts:54` `/model` 列表与切换。
- `desktop/electron/services/agent-runtime/command-router.ts:153` `parseModelSwitchArgs`。
- `desktop/electron/services/agent-runtime/command-router.ts:166` 支持 index、alias、model id 解析。

迁移取舍：

- Synapse 暂不做 card 渲染，仅返回文本命令结果。
- 未知模型字符串会按直接 model id 持久化，保持 CC Connect 允许直接输入 model id 的行为。

## 3. 模式切换

CC Connect：

- `core/engine.go:6197` `cmdMode`
- `core/engine.go:6287` `applyLiveModeChange`
- `agent/codex/codex.go:623` Codex permission modes
- `agent/claudecode/claudecode.go:674` Claude Code permission modes

Synapse：

- `desktop/electron/services/provider-config/provider-config-service.ts:146` `setActiveMode`。
- `desktop/electron/services/agent-runtime/command-router.ts:83` `/mode` 列表与切换。
- `desktop/electron/services/agent-runtime/command-router.ts:182` 按 agentType 返回 Codex / Claude Code mode 列表。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:227` `resetSession` 关闭 live session 并清空当前 agent session id。

迁移取舍：

- 第三步采用更可控策略：`/mode` 持久化后关闭当前 live session，下一轮使用新 mode；不做 live mode hot-swap。

## 4. Codex runtime view 与配置写入

CC Connect：

- `agent/codex/provider_config.go:12` `ensureCodexProviderConfig`
- `agent/codex/provider_config.go:41` `ensureCodexAuth`
- `agent/codex/session.go:166` `buildExecArgs`
- `agent/codex/codex.go:335` 启动前写 Codex provider config/auth
- `agent/codex/codex.go:589` `providerEnvLocked`

Synapse：

- `desktop/electron/services/provider-config/provider-config-service.ts:155` 生成 Codex runtime view。
- `desktop/electron/services/provider-config/codex-runtime.ts:18` `prepareCodexRuntime`。
- `desktop/electron/services/provider-config/codex-runtime.ts:33` 构造 `[model_providers.<name>]`。
- `desktop/electron/services/provider-config/codex-runtime.ts:56` upsert provider section，保留其他 section。
- `desktop/electron/services/provider-config/codex-runtime.ts:126` 文件写入前检查 `PermissionGuard` 并记录 `AuditSink`。
- `desktop/electron/services/agent-runtime/index.ts:112` runtime view 映射到 `CodexExecAdapter` options。

迁移取舍：

- 不引入 TOML 依赖，按 CC Connect 的 section upsert 语义做最小文本替换。
- `auth.json` 按 `0o600` 写入。

## 5. Claude Code runtime view

CC Connect：

- `agent/claudecode/claudecode.go:854` `SetProviders`
- `agent/claudecode/claudecode.go:860` `SetActiveProvider`
- `agent/claudecode/claudecode.go:897` `providerEnvLocked`
- `agent/claudecode/claudecode.go:951` `runtimeEnvLocked`
- `core/providerproxy.go:36` `NewProviderProxy`

Synapse：

- `desktop/electron/services/provider-config/provider-config-service.ts:155` 生成 Claude Code runtime view。
- `desktop/electron/services/provider-config/provider-config-service.ts:438` `buildRuntimeEnv`。
- `desktop/electron/services/agent-runtime/adapters/claude-code.ts:82` 已有 `buildClaudeCodeArgs`，第三步测试覆盖 runtime view 到 args/env。

迁移取舍：

- 第三步实现 `ANTHROPIC_BASE_URL`、`ANTHROPIC_AUTH_TOKEN`、`ANTHROPIC_API_KEY`、`ANTHROPIC_MODEL` 与 custom env。
- thinking provider proxy 未迁移；后续需要通过 `NetworkServiceRegistry` 设计本地代理边界后再做。

## 6. 命令路由

CC Connect：

- `core/engine.go:3502` `/new`
- `core/engine.go:3512` `/status`
- `core/engine.go:5902` `/model`
- `core/engine.go:6197` `/mode`
- `core/engine.go:7632` card action 渲染入口

Synapse：

- `desktop/electron/services/agent-runtime/command-router.ts:26` `AgentCommandRouter`。
- `desktop/electron/services/agent-runtime/command-router.ts:40` 仅处理 `/model`、`/mode`、`/new`、`/status`，未知命令返回错误。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:104` 先建立/获取 conversation，再经 governance 判定。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:110` command router 在 queue/adapter 前处理命令。
- `desktop/electron/services/agent-runtime/agent-runtime-service.ts:548` 普通消息进入 Agent 前解析 runtime view 并注入 adapter。

迁移取舍：

- 命令处理顺序为：conversation -> governance -> command router -> queue/adapter，符合 7.0 推荐顺序。
- 未知 slash command 策略固定为“不支持错误，不转发给 Agent”。
