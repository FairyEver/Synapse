# 14.0 前置检查

## 1. 检查结论

13.0 产物存在，且从报告内容看已经完成 14.0 所需前提。

已检查文件：

- `分析/cc-connect/13.0/13.0 最终报告.md`
- `分析/cc-connect/13.0/13.0 测试记录.md`
- `分析/cc-connect/13.0/13.0 问题修复记录.md`
- `分析/cc-connect/13.0/13.0 前置检查.md`

13.0 已记录完成：

- Renderer 长任务 stream preview / compact progress。
- `/show` 本地引用渲染与安全读取。
- CommandRegistry、Custom Commands、SkillRegistry。
- 命令/技能发布到 Renderer、Bridge、Feishu。
- Agent-native slash allowlist。
- Scheduler、Cron、Heartbeat、Feishu 主动回传。
- Feishu 多工作区、频道绑定、AgentRuntime workspace 隔离。

## 2. 13.0 测试记录

13.0 测试记录显示以下命令通过：

- `pnpm desktop:typecheck`
- `pnpm desktop:test`
- `pnpm desktop:check:hard-constraints`
- `pnpm desktop:check:ipc-codegen`

`pnpm desktop:test` 记录为 71 个测试文件、423 个测试通过。

## 3. 当前工作区状态

当前工作区存在 13.0 遗留的未提交改动和未跟踪文件。它们视为已有成果，不回退、不覆盖。

本阶段只在必要范围内追加 14.0 实现，并把过程产物放在 `分析/cc-connect/14.0/`。

## 4. 阻塞判断

未发现阻塞 14.0 的核心缺口：

- AgentRuntime queue/session、side session 已存在。
- ReplyTarget/Outbox 已存在。
- Side-channel 已通过 `NetworkServiceRegistry` 注册，并已有本地 token。
- Scheduler exec 已走 `PermissionGuard`、`AuditSink`、`ControlledProcessRunner`。
- Feishu 主动回传已存在。

需要在 14.0 中补齐的能力：

- `run_as_user` 的进程隔离包装、preflight、audit probe。
- Webhook 外部入口。
- Relay side-channel 入口、relay run/session 管理、timeout/drain。
- `/compress` 与 auto-compress。
- 运维诊断与日志目录定位。

