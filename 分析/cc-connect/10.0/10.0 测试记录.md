# 10.0 测试记录

## 基线测试

- 通过：`pnpm desktop:typecheck`
- 通过：`pnpm desktop:test`
  - 59 个测试文件通过。
  - 393 个测试用例通过。
- 通过：`pnpm desktop:check:hard-constraints`
  - `All hard-constraint checks passed.`

## 最终测试

- 通过：`pnpm desktop:typecheck`
  - renderer、electron、test tsconfig 均通过。
- 通过：`pnpm --filter @synapse/desktop exec vitest run electron/services/connectors electron/modules/connectors`
  - 新增 Feishu/Connector 定向测试通过。
- 通过：`pnpm desktop:test`
  - 65 个测试文件通过。
  - 402 个测试用例通过。
- 通过：`pnpm desktop:check:hard-constraints`
  - `All hard-constraint checks passed.`
- 通过：`pnpm desktop:check:ipc-codegen`
  - `IPC codegen output is in sync with electron/generated/ipc-channels.generated.ts`
  - 注：第一次运行因生成文件尚未 stage 而失败；stage 后复跑通过。
