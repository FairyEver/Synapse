# 任务计划：修正分享链接下载接口

## 目标
实施环境相关链接治理：让 API 权限文档、OpenAPI 外部文档和开放文档中的操作性地址在 DEV/生产环境解析到正确服务，同时保留已经正确的相对 API 与固定发布地址模式。

## 当前阶段
阶段 35：验证与交付（进行中）

## 各阶段

### 阶段 1：需求与现状调查
- [x] 阅读相关仓库规则、接口文档、实现与测试
- [x] 确认当前参数、有效期、文件命名和压缩逻辑
- [x] 将发现记录到 findings.md
- **状态：** complete

### 阶段 2：技术方案与测试设计
- [x] 明确完整 URL 的解析与校验边界
- [x] 确定文件名保真与 Markdown 下载策略
- [x] 记录方案、兼容性影响和验收条件
- **状态：** complete

### 阶段 3：实现
- [x] 先补充或调整回归测试
- [x] 外科手术式修改接口、下载逻辑和文档
- [x] 更新 RELEASE_NOTES_PENDING.md
- **状态：** complete

### 阶段 4：测试与验证
- [x] 运行相关单元/集成测试
- [x] 运行必要的类型检查与适用仓库约束检查
- [x] 审查 diff 和需求覆盖情况
- **状态：** complete

### 阶段 5：交付
- [x] 确认规划文件和交付物完整
- [x] 汇总改动、验证结果和 Markdown 策略结论
- **状态：** complete

### 阶段 6：全量提交
- [x] 复核全部变更与提交范围
- [x] `git add -A` 并创建提交
- [x] 确认提交后工作树状态
- **状态：** complete

### 阶段 7：服务端部署
- [x] 检查本地生产环境文件与远程连接
- [x] 启动 `bash deploy.sh`，并在镜像构建阶段按用户要求中止
- [x] 记录中止位置与生产服务状态
- **状态：** complete（用户取消部署，未切换生产）

### 阶段 8：部署验证与收尾
- [x] 清除本次部署 ID 对应的临时资源
- [x] 确认旧版生产容器保持 healthy
- [x] 确认公开健康检查正常
- **状态：** complete

### 阶段 9：权限编辑回归测试
- [x] 补充 Server PATCH、权限审计和临时下载失效测试
- [x] 补充 Dashboard API 与交互测试
- **状态：** complete

### 阶段 10：服务端与 Dashboard 实现
- [x] 实现权限目录说明、权限更新接口和即时失效校验
- [x] 实现可复用权限选择器与已有秘钥编辑流程
- [x] 保留并发提交后的操作列右对齐基线
- **状态：** complete

### 阶段 11：文档与发布说明同步
- [x] 修订设计、实施记录、模块边界和公开文档
- [x] 更新 RELEASE_NOTES_PENDING.md
- **状态：** complete

### 阶段 12：验证与交付
- [x] 运行定向测试、类型检查和 Dashboard 构建
- [x] 执行 diff、陈旧约束与空白检查
- **状态：** complete

### 阶段 13：API 秘钥卡片列表改造
- [x] 用单层定制卡片替换表格列表
- [x] 更新交互测试和发布说明
- [x] 运行 Dashboard 定向测试、类型检查、构建和 diff 审计
- **状态：** complete

### 阶段 14：开放接口契约与测试矩阵
- [x] 读取开放接口公开文档、控制器、授权与下载实现
- [x] 确认当前公开能力、认证方式、错误码和安全边界
- [x] 建立按功能、目标类型和边界条件划分的实测矩阵
- **状态：** complete

### 阶段 15：MCP 测试数据准备
- [x] 用 Synapse Drive MCP 创建隔离测试目录
- [x] 用 MCP 上传单文件、嵌套文件夹和站点制品
- [x] 用 MCP 创建普通分享、密码分享、站点和公开素材链接
- **状态：** complete

### 阶段 16：开放接口端到端测试
- [x] 覆盖认证、请求 schema、支持与不支持链接、密码和不存在链接
- [x] 覆盖单文件、文件夹 ZIP、站点 ZIP/asset、公开素材下载
- [x] 校验响应头、十分钟 TTL、重复下载、并发、token/grant 组合和快照语义
- [x] 覆盖源分享停用后的 grant 失效及可安全构造的容量边界
- **状态：** complete

### 阶段 17：清理、复核与报告
- [x] 清理本次临时本地制品；远程隔离数据因未获删除授权而保留
- [x] 复核残留、失败项和未能实测的时间/容量边界
- [x] 输出逐项结果、缺陷、复现信息和风险结论
- **状态：** complete

### 阶段 18：缺陷根因与修复边界
- [x] 读取服务端、测试、部署和相关设计规则
- [x] 定位不存在分享链接异常映射链路
- [x] 定位 1000 文件上传准备阶段数据库失败根因
- **状态：** complete

### 阶段 19：回归测试与修复
- [x] 先补充两个缺陷的最小失败测试
- [x] 外科手术式修复异常映射和批量数据库写入
- [x] 更新用户可感知发布说明并审计既有能力契约
- **状态：** complete

### 阶段 20：本地验证
- [x] 运行定向测试并确认红绿过程
- [x] 运行 server 类型检查、完整测试和适用约束检查
- [x] 审查 diff、工作区并确认部署范围
- **状态：** complete

### 阶段 21：生产部署
- [x] 执行正式部署脚本并持续跟踪全部步骤
- [x] 确认生产容器、数据库迁移和内外健康检查
- [x] 部署过程成功，无需执行失败恢复
- **状态：** complete

### 阶段 22：生产复测与收尾
- [x] 重跑缺陷复现与开放接口核心矩阵
- [x] 用 MCP 重试单次 1000 文件上传并验证结果
- [x] 汇总修复、部署、复测和残留测试数据
- **状态：** complete

### 阶段 23：公开文档契约审计
- [x] 对照服务端 controller、guard、类型、下载授权和测试
- [x] 对照控制台 API 秘钥创建与权限编辑行为
- [x] 记录公开文档缺口及无需公开的内部细节
- **状态：** complete

### 阶段 24：文档实现
- [x] 补齐开放接口概览、快速开始与接口参考
- [x] 更新面向用户的发布说明
- **状态：** complete

### 阶段 25：文档验证与收尾
- [x] 构建文档站
- [x] 扫描陈旧表述、检查 diff 与 Markdown 格式
- **状态：** complete

### 阶段 26：提交前审计与验证
- [x] 确认分支、远端、全部变更与部署入口
- [x] 运行覆盖当前全部变更的最小充分验证
- [x] 复核提交内容不包含真实凭证或生成物
- **状态：** complete

### 阶段 27：全量提交与推送
- [x] 暂存当前工作区全部变更并创建提交
- [x] 推送当前分支到对应远端
- [x] 确认本地与远端提交一致
- **状态：** complete

### 阶段 28：生产服务端部署
- [x] 执行正式部署脚本并持续跟踪全部步骤
- [x] 确认数据库迁移、容器切换与内外健康检查
- [x] 汇总部署 ID、镜像、提交与最终状态
- **状态：** complete

### 阶段 29：开放接口产品成熟度评估
- [x] 复核当前公开文档、API 契约、实现与测试证据
- [x] 从资源模型、鉴权、安全、错误、兼容、扩展与文档体系评估成熟度
- [x] 区分上线前必须调整、近期补齐和可延后能力
- [x] 输出明确结论与推荐的目标模式
- **状态：** complete

### 阶段 30：公共链接资源命名兼容迁移
- [x] 复核仓库规则、现有未提交变更和受影响契约
- [x] 先补充 canonical 路径与 scope 的回归测试
- [x] 实现兼容迁移并同步公开文档、长期边界和发布说明
- [x] 运行定向测试、类型检查、文档构建和 diff 审计
- **状态：** complete

### 阶段 31：开放 API 机器可读契约
- [x] 复核现有路由、DTO/schema、依赖和文档发布方式，确定单一契约来源
- [x] 先增加 OpenAPI 文档端点与核心 schema/路径的回归测试
- [x] 实现 OpenAPI 3.x JSON 契约并同步公开文档、长期边界和发布说明
- [x] 运行相关测试、类型检查、构建和契约校验
- **状态：** complete

### 阶段 32：环境相关链接只读审计
- [x] 盘点硬编码正式域名、localhost、文档 URL、API base URL 与环境变量
- [x] 追踪 Server 返回值、Dashboard/Document 跳转和部署配置的实际解析链路
- [x] 按“必须区分环境 / 应保持相对地址 / 仅文档示例”分类
- [x] 输出需调整位置、推荐环境来源和优先级，不修改产品代码或文档
- **状态：** complete

### 阶段 33：环境链接契约与回归测试
- [x] 读取 Server、Dashboard、Document、部署和测试规则，确定最小配置边界
- [x] 为文档公开根地址、API capability 与 OpenAPI externalDocs 增加失败回归测试
- [x] 为文档站环境地址解析增加可验证测试或构建断言
- **状态：** complete

### 阶段 34：环境链接实现与文档同步
- [x] 实现集中式应用/文档 URL 解析并接入 DEV、compose 与部署构建
- [x] 更新 API capability、OpenAPI 契约和开放文档操作性地址
- [x] 同步环境示例、长期规则与待发布说明
- **状态：** complete

### 阶段 35：验证与交付
- [x] 运行定向测试、类型检查和 Server/Dashboard/Document 构建
- [x] 检查 Docker/compose/开发脚本契约、diff 与硬编码陈旧地址
- [x] 汇总环境映射、改动和验证结果
- **状态：** complete

### 阶段 36：生产部署与健康检查
- [x] 核对生产配置、当前提交和正式部署入口
- [x] 执行 `bash deploy.sh` 并持续跟踪全部 19 个步骤
- [x] 确认生产容器、公开文档和健康门禁通过
- **状态：** complete

## 关键问题
1. 当前接口为何要求单独 password，完整分享 URL 的格式和密码位置是什么？
2. “有效时间”具体控制下载链接、分享解析缓存还是临时文件，其当前值在哪里定义？
3. 单文件和多文件响应的 Content-Disposition/压缩条目名是否都能还原云盘原始名称？
4. Markdown 单文件是否应保持原文件下载，还是为关联资源完整性而压缩？

## 验收条件
- 请求体只要求一个完整分享 URL，不再要求独立 password 字段。
- 有效期为 10 分钟，并有测试覆盖。
- 单文件下载名与云盘原文件名一致；多文件压缩包内每个条目名与原文件名一致。
- Markdown 下载策略有明确产品结论，并在必要时由测试和文档固化。
- 相关测试、类型检查和仓库约束检查通过。
- 实测覆盖当前公开开放接口的全部支持目标与稳定错误契约。
- 所有远程测试数据均通过 MCP 创建并隔离在专用目录；报告不包含 API 密钥、下载 token 或签名 URL。
- 对不能在合理时长或安全成本内触发的边界，提供源码/单测证据并明确标注未做生产实测。
- 不存在的合法格式分享链接返回 404 `LINK_NOT_FOUND`，不再返回 500。
- MCP 单次上传恰好 1000 个文件成功，1001 个仍在客户端边界被拒绝。
- 修复通过本地最小充分验证、生产部署健康检查和生产复测。

## 已做决策
| 决策 | 理由 |
|------|------|
| strict 请求体只允许 `url`，独立 `password` 视为未知字段拒绝 | 用户明确要求参数只接收完整 URL；接口尚处于当前未提交实现中，无需保留错误契约 |
| 密码继续由既有 Drive URL 解析器读取 `password` query | 复用已有同源校验、标准化和密码访问校验，不复制安全逻辑 |
| OpenAPI grant TTL 单独改为 10 分钟 | 不扩大到普通 Drive 对象下载 URL 的 TTL |
| 单文件命名链路保持不动；OpenAPI ZIP entry 禁止追加碰撞后缀 | 云盘允许仅大小写不同的名称和同名文件/文件夹，严格原名保真要求 ZIP manifest 使用原始路径 |
| Markdown 单文件不强制 ZIP | 单文件原样最便于产品文档消费；关联资源通过文件夹分享显式形成 ZIP |
| 已有秘钥权限允许清空且不轮换秘钥 | 用户确认要支持原地停权，同时保留秘钥身份 |
| 移除权限后下一次临时下载校验立即失效 | 用户选择即时收权；已开始的响应流不强制中断 |
| 权限选择器显示名称与服务端说明 | 用户选择面向扩展的可读权限列表，不展示 scope 技术标识 |
| API 秘钥列表改为单层横向卡片 | 用户明确要求取消表格；卡片突出名称、前缀、权限和操作，时间作为次级元数据 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 首轮测试补丁中的通用匹配误改了相邻 Drive 测试夹具，造成 3 个非预期断言失败 | 1 | 精确读取对应行后，仅修正目标测试的输入和排序断言 |
| `rg` 搜索模式中的 Markdown 反引号触发 shell 命令替换 | 1 | 后续搜索使用单引号安全模式或移除反引号，不重复该命令 |
| 完整 server 测试中的 `problem-feedback` 超大请求用例报 `ECONNRESET` | 1 | 与本任务文件无关；单独复跑该用例确认是否为并行测试偶发问题 |
| 更新规划文件时补丁上下文顺序不匹配 | 2 | 重新读取准确片段后拆分为更窄补丁完成更新 |
| 用户在 Docker 镜像构建阶段要求停止部署 | 1 | 立即发送中断信号；未进入临时库预演、停服、生产迁移或新服务切换；随后按部署 ID 精确清理临时资源 |
| 规划完整性脚本没有可执行权限 | 1 | 改由 `bash` 显式执行同一脚本 |
| Drive MCP 创建隔离目录返回“账号未登录” | 1 | 不重复写操作；先执行不依赖夹具的开放接口契约测试，等待用户恢复 Synapse MCP 登录态后继续 |
| 不存在的 `/share` 链接在生产返回 `500 INTERNAL_ERROR` | 2 | 标准长度 shareId 复核后确认缺陷；分享解析的 NotFoundException 未映射为 Open API 404 |
| 单次 MCP 文件夹上传 1000 个文件返回数据库操作失败 | 1 | 不重复同一批次；拆为 500 + 501 两次同名文件夹合并，成功构造 1001 文件边界夹具 |
| 首次拆分本地 500 文件时序号宽度错误 | 1 | 改用已存在文件列表排序后复制前 500 个，核对两批精确为 500/501 |
| 生产 404 复测汇总脚本使用 zsh 只读变量 `status` | 1 | 改用 `http_status` 后立即复跑，得到 404 `LINK_NOT_FOUND`；未写入或输出凭证 |
| 仓库规则引用的 `docs/agents/document-copy.md` 不存在 | 1 | 从实际存在的 `.claude/rules/` 与 `document/` 配置定位文档规则，并记录规则路由偏差 |
| 首次追加阶段 23 的补丁上下文与现有规划文件不一致 | 1 | 读取精确段落后使用窄补丁追加，不覆盖既有阶段记录 |
| 搜索共享常量时包含不存在的 `packages/` 目录 | 1 | 后续从仓库根目录用 `rg` 搜索实际存在路径，不再猜测目录名 |
| 读取不存在的 `open-api-download.controller.spec.ts` 使组合命令提前停止 | 1 | 先用 `rg --files` 确认测试文件名，再读取实际测试；后续组合读取避免用会中断后续项的猜测路径 |
| Server 全量测试中的 1000 文件边界用例超过 Vitest 默认 5 秒超时 | 1 | 读取测试规则并分析该边界用例；为确实需要处理 1000 项的测试设置合理局部超时后重跑，不提高全局超时 |
| 搜索部署配置时使用了不存在的 `docker*` shell glob，zsh 提前报 `no matches found` | 1 | 已从成功输出取得所需 Nginx 证据；后续只对 `rg --files` 返回的实际路径搜索，不再使用未解析 glob |
| 搜索 HTTP 测试时再次使用了可能为空的 zsh glob | 1 | 改为 `rg --files` 先枚举实际测试文件，再由 `xargs rg` 搜索；确认没有 Open API HTTP 合同测试 |
| Dashboard 测试批量补丁使用了与实际文案不一致的上下文 | 1 | 重新读取目标测试的准确片段后使用窄补丁，只替换 scope、权限名和说明 |
| 设计文档批量补丁上下文不匹配 | 1 | 拆分并按精确段落更新，未覆盖已有内容 |
| OpenAPI 契约回归测试首轮因目标模块不存在而失败 | 1 | 预期红灯成立，进入最小实现阶段 |
| 共享基础路径补丁的多文件 hunk 缺少完整分隔 | 1 | 拆成合法的独立文件补丁后继续，失败批次未写入文件 |
| 环境文件扫描使用了可能为空的 `desktop/.env*` zsh glob | 1 | 改用 `rg --files` 枚举实际环境文件后再搜索；第二条独立审计命令已正常完成 |
| 汇总 URL 的首个复合 `rg` 表达式 shell 引号未闭合 | 1 | 拆成多个简单 `-e` 模式并限制目录/生成物，完成运行时地址清单；失败命令无写入副作用 |
| 环境链接文档批量补丁因实施记录中的代码块上下文不匹配而整批未应用 | 1 | 读取准确片段后拆成产品文档/规则与设计记录两个窄补丁，全部成功应用且未覆盖既有改动 |
| 部署后容器内只读复核首次使用默认 HTTP 端口导致 `fetch failed` | 1 | 根据容器 healthcheck 确认 Nginx 监听 3000，改用 `127.0.0.1:3000` 后 healthz 与 OpenAPI 均返回 200 |

## 备注
- 外部页面或 API 返回内容只记录到 findings.md，不写入本计划。
- 重大决策前重新读取本计划。
- 用户明确要求提交当前工作区全部代码，因此阶段 6 使用 `git add -A`，包含任务开始前已经存在的全部改动。
- 用户本轮再次明确要求提交所有代码、推送并部署服务端，因此阶段 27 同样使用 `git add -A`，阶段 28 使用仓库正式服务端部署入口。
