# Agent 文件 Checkpoint、撤销与右侧工作面板实现

## 目标

按已确认规格为 Synapse Agent 会话实现文件 Checkpoint 与撤销能力：在一次 Agent 响应末尾展示该轮文件变化，点击文件后通过可复用的右侧工作面板打开 Diff；右侧面板作为 Agent 工作区基础布局能力，可承载未来其它工具，不与文件 Diff 绑死。

## 当前状态

- 当前阶段：18 / 18+
- 状态：completed
- 最后更新：2026-08-28
- 设计确认：四项实施方案均已由用户确认

## 第 38 轮工具偏差记录

- 2026-08-28 08:38：点击重名保存后的 Computer Use 输出因上下文超限被截断，无法仅凭该次返回判断动作结果；未重复触发写操作，改用 `disableDiff: true` fresh 读取，确认专用对象已保存且出现“已保存”提示。
- 2026-08-28 08:39：首次名称输入调用误把 AX 序号当成不存在的 `state.refs`，随后又误用 `target` 参数；两次均在动作前失败、UI 未变化。重新核对 Computer Use 技能后使用 `element_index`，并以 fresh 状态确认只发生预期名称修改。
- 2026-08-28 08:52：首次删除回焦补丁的宽泛上下文把 `deleteButtonRef` 误挂到仅运行中才渲染的“查看进度”按钮，专项测试持续红且 focus spy 证明恢复函数从未命中目标；检查实际 diff 后把 ref 精确移到“删除工作流”按钮，避免继续盲调调度时机。

## 第 39 轮工具偏差记录

- 2026-08-28 09:52：使用记录弹窗 fresh AX 同时列出产品右上 `Close` 与 Chrome 标签“关闭”；误点后者关闭了当前标签，但没有业务写入或外部请求。立即用浏览器“重新打开刚关闭的标签页”恢复同一登录页，专用项和两条列表状态完整；后续弹层只用精确 `Close` 索引或 Escape，不再按本地化“关闭”文本猜测。
- 2026-08-28 09:37：为覆盖剪贴板打开空创建表单时，旧的 Create AX 索引实际把焦点落到 Chrome 地址栏；随后 `set_value` 只写入普通固定文本，`sky.keypress` 因方法不存在在按键前失败。核对 Computer Use 技能后改用 `press_key("super+a"/"super+c")`，成功用同一普通文本覆盖剪贴板；没有按 Return、没有导航或创建业务数据。之后显式恢复本地 API 密钥 URL并刷新，页面状态正常。
- 2026-08-28 09:24：Chrome 绿色按钮的本地化 secondary action `缩放窗口` 连续两次返回成功但窗口尺寸未变化；改用历史记录中的英文 action 被工具在动作前拒绝，无 UI 副作用。停止重复 secondary action，改用已核对参数的窗口右边缘拖拽，一次得到 958×768。
- 2026-08-28 09:26：关闭主题设置后的第一次状态仍处于关闭动画树，随后复用其 Close 索引被 Computer Use 在动作前拒绝；fresh 读取确认面板已实际关闭，主题切换已生效且无额外 UI 副作用。后续弹层关闭均先等 fresh 树再取索引。

## 第 40 轮工具偏差记录

- 2026-08-28 10:16：同一 fresh 树中连续对输入/输出两个 stepper 调用 `set_value`，只有第一项 1.25 生效，第二项保持空白；没有保存。fresh 核对后按一动作一读取重新设置输出 2.5 成功，后续数值字段不再批量复用同一 AX 树索引。
- 2026-08-28 10:16：首次单独设置“缓存读”0.25 后立即读取仍为空，等待并用同一 fresh 索引重试一次才生效；没有保存或其它字段变化。后续每次 stepper 设置后留稳定等待并 fresh 核值，避免把 Computer Use 输入时序误判为产品丢值。
- 2026-08-28 10:20：用点击输入框、⌘A、`type_text("1.5")` 编辑已存输入时，中间 fresh 显示 `1.`，补键 `5` 后 fresh 显示 1.5，但随后保存 pending/稳定帧实际为 1.55，说明 Computer Use 键入尾字符异步重复。只影响唯一专用行且可恢复，其他字段不变；后续用逐键操作并额外稳定等待、fresh 二次核对恢复 1.25。
- 2026-08-28 10:20：尝试用 Computer Use 原子 `paste("1.25")` 替换已选 1.55 时，服务以“可能与用户粘贴冲突”中止；fresh 确认字段仍为 1.55 且只保持选中，没有非预期内容或保存。未重试 paste，改为一次 `type_text("1.25")` 后等待并二次 fresh 核对。
- 2026-08-28 10:14：同一 REPL 脚本先用字符串成功设置模型模式，随后把 number stepper 的 `value` 误传为 number 类型，Computer Use 在第二步以 `Invalid params` 拒绝；fresh 核对确认模式只写入一次、价格仍为空、未保存。之后按 Skill schema 仅用字符串 `"-1"` 设置价格，不重复模型动作。
- 2026-08-28 10:08：第二次输出价格管理截图时误以为先前 `if` 块内的 REPL `var r40fs/r40url` 绑定可在后续 cell 复用，读取文件前即抛出 `r40fs is not defined`；没有 Computer Use 动作、不影响 UI 或数据。后续显式写入 `globalThis` 或在同一 cell 内重新 import，不重复依赖该局部绑定。
- 2026-08-28 10:22：尝试将 number stepper 通过 `set_value("")` 清空时，Computer Use 在输入前以 `invalidNumber("")` 拒绝，因此后续 `type_text("abc")` 未执行，草稿仍为 -1，无数据副作用。后续不再用 AX `set_value` 给 number 输入空字符串，而用合法数字直接覆盖；非法文本边界按原生 number input 无法接受记录。

## 第 41 轮工具偏差记录

- 2026-08-28 11:25：最终核对原生 Window 菜单后，连续三种 Escape 键名字均被 Computer Use 在动作前以未知键拒绝，菜单与产品数据未变化；随后点击菜单中唯一主窗项安全收起，并 fresh 确认启动台与焦点基线。
- 2026-08-28 11:15：首次关闭独立用量窗口时，macOS Window 菜单仍处于展开态，fresh 树没有原生关闭按钮，解析得到空索引；Computer Use 在点击前以“坐标必须为有限值”拒绝，无 UI 或数据副作用。随后选择当前窗口菜单项收起菜单，fresh 定位真实 close button 后只关闭一次。
- 2026-08-28 11:10：把窗口根节点误当作绿色全屏按钮调用 `zoom the window`，Computer Use 在动作前以“不支持的 secondary action”拒绝，窗口和数据均未变化；fresh 树确认实际全屏按钮后才执行公开缩放动作。
- 2026-08-28 11:09：等待同一次 CC 手动刷新 30 秒的 node_repl 调用达到工具超时并重置内核；这段代码只有等待与只读状态获取，没有重复刷新或 UI 副作用。重新导入 Computer Use 句柄后确认原刷新仍由应用继续执行。
- 2026-08-28 11:15：首次组合读取 Codex 明细时把未过滤的 initial diff 与稳定摘要一起输出，工具回传因此包含会话标识；没有业务写入，也未进入 findings/progress、测试或最终报告。后续 Computer Use 只在 node_repl 内计数、筛选结构行后输出，不再回传整棵明细树。

## 成功标准

- 明确 Checkpoint 的定义、生命周期、持久化、恢复与撤销语义，不把 Git 工作区快照误当成普通 `git checkout`。
- 明确右侧工作面板应落在哪一层布局、如何与左侧导航/中间会话/窗口宽度协作。
- 复用现有 Git Diff 组件或底层开源组件，避免重复实现编辑器级 Diff。
- 给出会话时间线、主进程服务、preload IPC、renderer 状态与数据模型的完整边界。
- 覆盖并发编辑、未跟踪文件、删除/重命名、二进制/大文件、会话恢复、撤销确认和失败恢复。
- UI 保持现有组件库与布局纪律，不引入自定义配色或破坏会话输入区。
- 产出可直接拆票开发的设计文档，并记录用户确认的产品选择。

## 阶段

- [x] 0. 完成并确认正式设计规格与四项产品选择。
- [x] 1. 复核实现落点、现有测试与依赖边界，建立实施基线。
- [x] 2. 抽取共享 Diff renderer，并声明 `diff@8.0.4` 为 desktop 直接生产依赖。
- [x] 3. 实现可复用工作区辅助面板与 Agent 工作区壳，覆盖宽屏分栏和窄屏详情模式。
- [x] 4. 实现 SDK 文件检查点捕获、sidecar 持久化、受限 Diff 生成及 IPC。
- [x] 5. 实现时间线 Checkpoint 卡片、postlude 排序与右侧 Diff 审查。
- [x] 6. 实现 latest-only 两阶段安全撤销、状态事件、权限与审计。
- [x] 7. 补齐回归测试、规则/API 文档、发布说明与打包验证。
- [x] 8. 修复 SDK `sessionStore` 与文件 Checkpoint 的启动冲突，保留首轮 Checkpoint 并完成回归验证。
- [x] 9. 修复 Checkpoint postlude 误恢复会话运行状态的问题，并完成回归验证。
- [x] 10. 使用电脑操作技能完成真实客户端全链路与边界自动化测试，发现问题后修复并复测。
- [x] 11. 完成 Git 工作台、共享 Diff、项目终端入口与 System App 窗口联动的第 2 轮真实用户视角 C-to-C 自动化测试；确认缺陷则修复并实机复测。
- [x] 12. 完成 Drive 桌面/网页第 3 轮真实用户视角 C-to-C 自动化测试；覆盖上周 Drive、MDXEditor、下载与大目录边界，确认缺陷则修复并实机复测。
- [x] 13. 完成设置、编辑器扫描与 Dashboard Console 第 4 轮真实用户视角 C-to-C 自动化测试；覆盖 Live 重连、布局边界、扫描排序与 Open API 文档导航，确认缺陷则修复并实机复测。
- [x] 14. 完成 Agent MCP/router、工具时间线、会话导出、旧附件脱敏与跨会话恢复的第 5 轮真实用户视角 C-to-C 自动化测试；修复 macOS ZIP 元数据污染并实机复测。
- [x] 15. 完成键盘焦点、Escape、宽窄布局状态保持与 System App 返回焦点的第 6 轮快速验收；修复 Agent 文件审查面板键盘关闭与焦点恢复。
- [x] 16. 从第 7 轮开始持续创建独立 Codex 任务，使用 Computer Use 覆盖上周改动影响的剩余真实用户路径；每轮发现问题即在同任务修复并实机复测，循环至 2026-08-27 18:00。
  - [x] 16.1 第 7 轮完成 System App 壳层、IDE/Terminal/Agent 独立窗口、焦点恢复与加载状态验收；两项真实缺陷已补回归、最小修复并实机闭环。
  - [x] 16.2 第 8 轮覆盖 Agent 普通/自定义会话创建、Persona/Provider/模型选择、附件与图片入口、会话级选择持久化、新旧会话恢复、上下文与用量、斜杠菜单、宽窄布局及加载/空/错状态；修复恢复会话耗时退化为 0 秒并完成实机闭环。
  - [x] 16.3 第 9 轮覆盖上周设置/配置重构影响面：设置分组与字段行、键鼠与宽窄布局、普通偏好编辑/校验/保存/取消/恢复、配置备份安全导出/恢复、Live 手动动作及 App Reset 确认取消；发现问题在同任务最小修复并实机闭环。
  - [x] 16.4 第 10 轮覆盖上周文档站迁移、Open API 概览、公共链接下载文档、机器可读 OpenAPI 契约、复制 Markdown、页内锚点、深链/刷新/新标签/历史/404/外部部署链接，以及宽窄浏览器下导航、目录、代码块、表格、长 URL/JSON、复制按钮与键盘焦点；发现问题在同任务最小修复并实机闭环。
  - [x] 16.5 第 11 轮覆盖上周 Agent attachment 影响面：文件夹、多文件与混合类型、隐藏项/子目录/空文件、重复选择、预览移除重加、空文本发送、运行中切换/停止/失败重试、恢复脱敏、暂存生命周期，以及 size/count/type/symlink/不可读边界；发现问题在同任务最小修复并实机闭环。
  - [x] 16.6 第 12 轮覆盖 Drive 协作与历史能力：专用 Markdown/文本资源、多版本、评论锚点、分享与公开访问、版本/评论/分享组合路径、宽窄布局、键盘与长内容边界；发现问题在同任务最小修复并实机闭环。
  - [x] 16.7 第 13 轮完成 Dashboard API 密钥与 Open API 授权的只读与提交前检查：真实设置入口、有数据态、创建表单校验、权限文档、宽窄布局与键盘焦点；修复 Escape 取消后焦点丢失。凭据创建、调用、下载授权与撤销按安全边界未执行，需用户在目标任务中单独确认。
  - [x] 16.8 第 14 轮深测 Drive 富 Markdown 编辑、图片与 Mermaid 生命周期、文件/文件夹下载和大目录生命周期；仅操作 `sy-c2c-r14-*` 专用资源，发现稳定缺陷后在同任务最小修复并实机闭环。
  - [x] 16.9 第 15 轮验收 Drive 层级列表一致性、编辑器扫描与 Skill 排序、Agent 事件连续性；仅操作 `sy-c2c-r15-*` 专用资源，发现稳定缺陷后在同任务最小修复并实机闭环。
  - [x] 16.10 第 16 轮执行 18:00 前累计修复综合回归：跨 System App/Agent、设置、Drive 与文档站验证焦点、加载、恢复和宽窄布局相互不干扰；仅操作 `sy-c2c-r16-*`，发现稳定缺陷后在同任务最小修复并实机闭环。
  - [x] 16.11 第 17 轮完成 18:00 前 Web 表面最终快速回归：覆盖本地/公开文档 Copy Markdown、中文/英文 404、Open API 文档与机器契约、无效公开分享、955↔1357 响应式及键盘边界；确认公开文档部署落后于当前本地修复，未改源码或部署，浏览器已恢复原始 Console 文件页。
  - [x] 16.12 第 18 轮完成 Dashboard 最终只读冒烟：覆盖实际侧栏、网盘、设置、API 密钥弹窗、Open API 文档入口、历史、刷新、焦点与 955↔1357 布局；未发现新缺陷、未产生数据或配置变更。
  - [x] 16.13 第 19 轮完成无效 Console 深链错误边界：中文 404、刷新、页面/浏览器返回、Tab 与 958px 布局均正常；未发现新缺陷，Chrome 恢复唯一网盘页。
  - [x] 16.14 第 20 轮完成 18:00 前最终只读状态确认：唯一 Console 网盘文件页、1357×768、无弹窗/临时标签/加载错误，刷新后数据表正常恢复；未修改任何内容。
- [ ] 17. 按用户延长后的停止条件继续创建独立 Codex 任务，使用 Computer Use 覆盖上周改动影响的剩余真实用户路径；每轮发现问题即在同任务最小修复并实机复测，持续循环至 2026-08-28 17:00。
  - [x] 17.1 第 21 轮恢复桌面主链并深测 Agent 文件 Checkpoint/撤销/右侧面板、Git/Terminal/System App 组合路径；修复撤销确认取消后焦点丢失和撤销中计数短暂归零，完成真实 UI 与源码回归，专用文件及独立窗口已清理。
  - [x] 17.2 第 22 轮完成配置备份文件面板取消、重复命名/覆盖确认、空/损坏/错误 schema 预检、普通备份结构排除、设置/System App/App Reset/Live 组合持久化深测；修复导出取消与导入失败后的两项键盘回焦缺陷，并完成实机与源码回归。
  - [x] 17.3 第 23 轮深测 Agent/Git/Terminal/System App 多窗口的标题可辨识性、项目归属、窗口复用/关闭与返回焦点；修复独立窗标题同名、复用后路由陈旧与 Agent Git 关闭回焦三项缺陷，并完成实机与源码回归。
  - [x] 17.4 第 24 轮深测 Agent MCP/router、工具时间线、reply target/outbox、主窗/独立会话窗口事件连续性与导出恢复组合；发现稳定缺陷则在同任务最小修复并实机闭环。
  - [x] 17.5 第 25 轮深测 Agent Checkpoint 与 Git 共用 Diff 的键盘/辅助功能状态、长行/二进制/截断降级、宽窄布局和焦点保持；确认稳定问题则在共享组件最小修复并实机闭环。
  - [x] 17.6 第 26 轮深测 App Reset、Drive 删除、Git 丢弃与 Agent 撤销等危险操作的延迟确认、默认安全焦点、禁用/倒计时、Escape/取消回焦、重复打开和窄屏一致性；共享与入口级稳定缺陷均已最小修复并完成 HMR 实机闭环。
  - [x] 17.7 第 27 轮深测 Dashboard Drive 的大目录分页、系统视图、公开素材/回收站、浏览器历史与刷新、行内键盘事件，以及创建/软删除后的列表连续性；仅操作 `sy-c2c-r27-*` 专用资源，发现稳定缺陷则在同任务最小修复并实机闭环。
  - [x] 17.8 第 28 轮深测 Agent 附件的原生文件/文件夹选择、取消回焦、多选/重复/同名、选择后文件变化、移除与重加、会话/独立窗连续性及真实发送链；只使用 `sy-c2c-r28-*` 纯合成夹具，修复未发送附件跨会话泄漏并完成实机/回归闭环；因原生列视图持续无法安全选中发送夹具，本轮按“至多一次”边界保持 0 次发送。
  - [x] 17.9 第 29 轮深测设置中的项目添加/编辑/移除、原生目录选择取消、重复与失效路径、项目列表和 Dock/Agent/Git/Terminal 归属联动、IDE/Knowledge Base 扫描及宽窄键盘连续性；仅使用 `sy-c2c-r29-*` 专用目录，配置已移除且夹具已移入可恢复 Trash；三项焦点缺陷均完成红灯、最小修复与 HMR 实机闭环。
  - [x] 17.10 第 30 轮使用独立普通 Git 仓库夹具深测 Git 工作台的加载、未跟踪/修改/暂存选择、提交校验、历史 Diff、分支创建/切换、刷新、专用改动丢弃、无远端状态、多窗口和键盘/宽窄连续性；禁止 worktree，所有 Git 写操作仅限 `sy-c2c-r30-*` 夹具并最终可恢复清理。
  - [x] 17.11 第 31 轮深测 Terminal 的专用项目与会话创建、命令输入输出、中断/退出、导航恢复、独立窗口复用、关闭/删除确认、宽窄键盘连续性和普通备份边界；仅运行 `sy-c2c-r31-*` 无网络安全命令，最终移除专用配置与会话并可恢复清理。
    - [x] 主任务实现审查追加闭环：删除会话、删除分组与放弃更改三处确认统一危险语义；补齐三处回归，并修复取消删除分组后的键盘回焦。
  - [x] 17.12 第 32 轮深测 Agent 会话管理完整生命周期：只操作 `sy-c2c-r32-*` 专用空会话，覆盖连续创建、命名/重命名、排序/分组/选中回退、归档/恢复/删除、独立窗口复用/关闭、960↔1350 与键盘/焦点/状态边界；不向 Provider 发送消息，最终清理全部专用会话、草稿、弹层和独立窗。
  - [x] 17.13 第 33 轮深测 System App 启动台与 Dock 个性化联动：记录并最终恢复既有 Dock 固定集合与精确顺序，覆盖应用列表、搜索/过滤、键盘、专用应用固定/取消固定/重排、Dock 与启动台打开、激活态、HMR、960↔1350、单一独立窗复用/关闭和返回焦点；不触碰业务数据或全局重置。
  - [x] 17.14 第 34 轮深测 Persona 与快捷输入完整生命周期及 Agent Composer 联动：只操作 `sy-c2c-r34-*` 专用 Persona、快捷输入、空会话和草稿，覆盖创建/校验/取消/编辑/保存/排序或启停/删除、HMR/导航恢复、Agent 选择与安全插入、键盘/焦点、960↔1350 和单一独立 System App 窗复用；不向 Provider 发送消息并最终清理全部专用数据。
  - [x] 17.15 第 35 轮深测 Secrets System App 完整生命周期：只操作 `sy-c2c-r35-*` 专用非敏感占位 Secret，覆盖创建/校验/遮罩显隐/编辑/删除、导航与 HMR 安全默认、960↔1350、单一独立窗复用/关闭回焦，以及普通配置备份/重置边界；最终从真实 UI 清理全部专用数据并恢复单一 960×768 主窗。
  - [x] 17.16 第 36 轮深测本地数据库 System App 完整生命周期：只操作 `sy_c2c_r36_db`、`sy_c2c_r36_table` 及三条专用记录，覆盖数据库/表 schema/行数据校验、编辑与删除安全路径、HMR/导航恢复、960↔1350、单一独立窗复用/关闭回焦，并最终从真实 UI 永久清理全部专用数据。
  - [x] 17.17 第 37 轮深测 Automation/Scheduler System App：只操作禁用且无外部副作用的 `sy_c2c_r37_automation`，覆盖创建/校验/编辑取消与保存、列表与历史、导航/HMR、960↔1350、单一独立窗复用/关闭回焦，并最终从真实 UI 永久清理专用自动化。
  - [x] 17.18 第 38 轮深测 Workflow System App：只操作 `sy_c2c_r38_workflow` 专用工作流，覆盖创建、编辑器、节点/连线校验、保存/取消、列表、HMR、宽窄布局与独立窗口；仅在源码审计能证明为纯本地无副作用时运行，否则不运行，最终从真实 UI 清理专用数据。
  - [x] 17.19 第 39 轮深测 Dashboard API 密钥真实数据生命周期：只创建并最终撤销 `sy-c2c-r39-*` 专用密钥，不读取或操作既有密钥，不在日志/计划中记录明文；覆盖创建、一次性展示/复制反馈、重命名、权限、使用记录、刷新、宽窄布局、撤销与焦点恢复，并确认既有基线不变。
  - [x] 17.20 第 40 轮深测价格管理 System App：记录原价格规则只读基线，只新增、编辑并最终删除一条 `sy-c2c-r40-*` 专用模型匹配规则；覆盖数字校验、保存/HMR、模型覆盖筛选、宽窄布局、独立窗口和删除回焦，不导入预设、不清空或改动既有规则，最终精确恢复原集合。
  - [x] 17.21 第 41 轮深测用量监控及价格下游展示：只读覆盖 CC/Codex 的今日、概览、时间、模型、项目、工具、记录与会话详情，验证未计价/估算费用、筛选、刷新、宽窄/深浅主题和独立窗口；不修改会话、Provider、价格规则或本地记录，不在计划中抄录提示词、路径等私密正文。
  - [x] 17.22 第 42 轮深测 IDE 管理与编辑器扫描写入链：完成项目配置、三类资源创建/校验、扫描、项目作用域安装/卸载、宽窄/主题与独立窗口；修复 6 类稳定焦点/校验问题，主线程撤回无法证明来源的空共享文件删除；专项 70/70、typecheck、hard constraints、diff check 全绿。专用安装、项目和目录已清理，三个专用资源因资源 Git 同步超时仍各保留 1 个，后续仅在同步恢复时通过 UI 删除。
  - [x] 17.23 第 43 轮深测隔离本地 Git 远端工作流：先对三个 r42 专用资源各最多尝试一次 UI 清理；随后只在普通非 worktree 的 r43 本地仓库与本地 bare remote 中覆盖 fetch/pull/push/sync/upstream、分支切换、冲突与错误恢复，不访问或写入任何真实远端，最终用 Finder 将所有专用夹具移入废纸篓。
  - [x] 17.24 第 44 轮深测 Agent 安全会话、文件 Checkpoint 与导出隐私：修复嵌套项目 runtime `PWD/cwd` 错位与导出绝对路径泄漏；完成专用会话、独立窗口、导出覆盖、响应式/主题和清理，专项 43/43 及静态门禁全绿。因 Provider 第三次未执行工具，真实 Checkpoint/撤销/停止转入第 45 轮续验。
  - [x] 17.25 第 45 轮验收 Agent 项目隔离修复并补完 Checkpoint/停止链：使用全新 `sy-c2c-r45-*` 普通项目和专用会话，最多两次无害发送；真实确认文件只落专用项目并完成 Checkpoint/Diff/撤销闭环。首轮隔离失败后按约用第二次发送闭环文件链，因此停止链转入第 46 轮；专用数据与夹具均已清理。
  - [x] 17.26 第 46 轮只用全新 `sy-c2c-r46-*` 普通项目与专用会话完成 Agent 真实停止链：最多一次无文件副作用的本地等待发送，覆盖运行态、停止操作、停止后的消息/草稿/按钮/导航重进/独立窗一致性、无错误提示与会话可继续性；发现稳定缺陷则在同任务最小修复并实机闭环，最终清理专用会话、项目与夹具。
  - [x] 17.27 第 47 轮只用全新 `sy-c2c-r47-*` 普通项目与专用会话补验已启动子进程的停止：最多一次发送，只允许一次精确的本地输出后等待命令；从 UI 单次授权并确认启动标记后停止，验证子进程无残留、权限不扩张、时间线取消语义、导航/独立窗一致性与完整清理。
  - [x] 17.28 第 48 轮只用全新 `sy-c2c-r48-*` 普通项目与专用会话验收 Agent 权限拒绝链：最多一次发送，对精确无害等待命令从真实权限卡选择拒绝，验证命令从未启动、拒绝与取消语义不混淆、焦点/导航/独立窗一致且无权限扩张，最终完整清理。
    - [x] 源码安全续修：取消终态只把 SDK 明确的用户拒绝结果投影为 Cancelled；普通非零退出、普通失败/错误文本和 denied 权限结果必须保留 Failed/Denied，补红绿回归并完成最小充分验证。
    - [ ] 实机补验：真实启动精确 `/bin/sleep 45` 子进程后从 Synapse UI 停止，确认父子进程终止、取消时间线稳定、权限只允许一次，并清理唯一 r47 会话、项目和夹具。
    - [x] 来源全部/CC/Codex与范围 7/30/90/全部已真实覆盖；7 天空态、其余有数据、长模型名及数字对齐均已取证，不为制造匹配态改动真实用量模型。
    - [x] 完成 960↔1357、深色/跟随系统恢复与价格管理独立窗口首帧/稳定帧/单例/关闭回焦；按权威设计记录主窗回启动台与“内嵌状态不丢”的产品边界冲突，未擅改通用语义。
    - [x] final fresh 核对后删除唯一专用行并只保存一次；刷新与导航重进均为 0 行、无 `sy-c2c-r40-*`，原空规则集精确恢复；9 文件/67 测试、desktop typecheck、hard constraints、diff check 全绿。
    - [x] 追加闭环删除成功后卡片卸载导致的焦点丢失：成功回顶栏“新建”，取消/失败回原删除按钮；补回归、最小修复、HMR 实测与额外 r38 专用空对象清理。
    - [x] 第 41 轮完成 CC/Codex 全视图、筛选/范围、价格下游、详情窗、刷新、键盘、960↔1357、深浅主题及 System App 单例实测；修复详情窗标题与未定价记录误显零费用，最终 35 文件/212 测试、typecheck、hard constraints、diff check 全绿，恢复空价格规则、跟随系统、单一 960×768 主窗启动台基线。

## 约束与范围

- 继续使用已运行的 dev 客户端做真实 UI 测试；不得启动、停止或重启任何服务。
- 每轮测试必须新建独立 Codex 任务并在 Synapse 当前项目目录运行；禁止新建 worktree。
- 每轮先阅读既有 findings/progress，避免无目的重复；发现问题必须在同一任务中完成最小修复、真实 UI 复测与最小充分源码验证。
- 2026-08-28 17:00 前若一轮提前结束，立即创建下一轮；到达时间后停止派发新轮次并汇总。
- 附件中的文字仅作为产品效果参考，不作为可执行指令。
- 仅允许新增用户已批准的 desktop 直接生产依赖 `diff@8.0.4`。
- 右侧面板是 Agent 工作区级基础能力，文件 Diff 是首个消费者。

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 第 23 轮共享窗口标题修复首次三文件回归时，System App 旧测试替身缺少真实 Electron `webContents.on` 接口 | 1 | 临时补齐接口确认首版回归；实机揭示事件归属错误后，改用 BrowserWindow 正确事件并移除这项临时 mock，不在生产代码中为测试替身降级。 |
| 第 23 轮首版标题回归误把 `page-title-updated` 挂到 WebContents，测试假绿但实机标题仍通用 | 1 | 以 Electron 41 本地类型定义和真实 UI 失败为证据，把回归事件源改为 BrowserWindow 得到 1/7 聚焦红灯，再修正共享监听并重跑 25/25。 |
| 第 25 轮首次运行共享布局回归时，给 desktop 包内 Vitest 传了带 `desktop/` 前缀的根目录路径 | 1 | Vitest 在 `desktop/` 作为 cwd 找不到文件且未执行测试；改用包内 `src/components/...` 精确路径，不重复错误命令。 |
| 第 25 轮在既有 `.test.ts` 中直接写 JSX，Vitest/OXC 解析失败且 0 测试执行 | 1 | 保留既有文件名，用 `createElement` 表达测试树与 mock，不为单个回归重命名文件。 |
| 第 25 轮无 HMR Git 回焦复测把“打开→读取首帧→关闭”压进一次 Computer Use 调用，主窗随后出现“打开 Git 失败”toast | 1 | 独立窗已关闭且焦点正确回到 Git trigger；不重复时序压缩，关闭 toast 后继续 Agent 会话切换，最终确认无独立窗。 |
| 第 25 轮关闭 toast 后在同一 Computer Use 调用复用旧 AX 索引点击“审查” | 1 | toast 关闭已使 AX 树重建，第二个动作被 stale id 拒绝；重新读取完整树后只使用新索引，不重复跨树复用。 |
| 第 25 轮折叠组键盘复测使用了 Computer Use 不接受的 `Space` 键名 | 1 | 改用其声明的 xdotool 语法 `space` 后，展开、Return 收起与焦点保持一次通过；错误调用未产生 UI 副作用。 |
| 第 35 轮首次按 PNG 固定偏移读取 Computer Use 截图尺寸，但当前截图实际是 JPEG | 1 | 窗口 zoom 已成功且焦点保持；检查文件头确认 JPEG，改用 JPEG SOF 标记解析尺寸，不重复 PNG 假设。 |
| 第 35 轮最终尺寸复核把截图 file URL 误按 data URL 拆分 | 1 | 只读解析失败、UI 无副作用；检查 URL 类型后用已加载的 fs 读取本地 JPEG，再交给既有 SOF 解析器，确认 960×768。 |
| 第 37 轮复测 Cron 取消时，首次点击“编辑”只移动焦点，随后复用预期 Dialog 索引误切换了专用项“周二” | 1 | 立即用 fresh 树精确点回“周二” on，已恢复七天全选，不保存误改；后续每次打开弹层后必须先确认 fresh 树出现弹层再用其索引。 |
| 第 38 轮首次按共享 bundle id `com.github.Electron` 读取 Synapse 状态被多个 Electron 安装歧义拒绝 | 1 | 未执行任何 UI 动作；按工具返回的候选改用当前 Synapse 仓库 Electron 41.2.1 精确 `.app` 路径，后续不再使用共享 bundle id。 |
| 第 38 轮 Workflow 关闭回焦首版仅在 `onCloseAutoFocus` 同步聚焦，专项测试仍显示最终焦点被 Radix 清理覆盖 | 1 | 改为捕获 `beforeunload` 前焦点，并在受控关闭完成后的下一动画帧恢复；专项 1/1 与 HMR 实机均通过。 |
| 第 38 轮空名保存回焦测试首次直接赋值原生 input，未触发 React 受控输入更新，保存 IPC 未调用 | 1 | 只影响测试基建；改用 HTMLInputElement 原生 value setter + input 事件，得到聚焦断言的精确产品红灯后再修复。 |
| 第 27 轮首次组合追加 findings/progress 的补丁在文件切换前误带空 hunk 标记 | 1 | 补丁未写入任何文件；改为只用连续且完整的 Update File hunk，不重复错误结构。 |
| 第 27 轮在虚拟表格物理末端点击顶部 tab 索引时，Chrome 仍报告 Find 命中文本被选中，无法证明 tab 获焦 | 1 | 没有视图或数据变化；滚回页面顶部读取 fresh AX，再从可见 tab 继续，不复用末端位置假设。 |
| 第 27 轮 findings/progress 组合补丁因进度行措辞与实际文件不完全一致而未匹配 | 2 | 两次原子补丁均未写入；用 `rg`/`sed` 读取精确行后按真实文本追加，不重复猜测上下文。 |
| 第 27 轮空态夹具表单首次使用大写 `Shift+Tab` 被 Computer Use 以键名无效拒绝 | 1 | UI 前拒绝、未提交表单；改用 xdotool 语法 `shift+Tab` 后成功，不再重复错误键名。 |
| 第 28 轮首次用显示名 `Synapse` 读取客户端状态超时 | 1 | 未执行 UI 动作；按 Computer Use 规则立即枚举运行应用，确认目标为 `com.github.Electron`，后续改用该 bundle id，不重复显示名。 |
| 第 28 轮用共享 bundle id `com.github.Electron` 读取时命中多 Electron 歧义 | 1 | 未执行 UI 动作；错误返回四个精确 app 路径，改用当前 Synapse 仓库 Electron 41.2.1 的绝对 `.app` 路径并成功读取，不再用共享 bundle id。 |
| 第 28 轮 fresh AX 输出用不存在的固定标题子串切片，只返回末字符 `l` | 1 | 只读操作、无 UI 副作用；立即改为完整树读取，确认附件菜单其实已展开。后续不按可变会话标题切片 AX 文本。 |
| 第 28 轮选择 `single.txt` 后首次 click 调用的 node_repl 包装脚本末尾误写 `null.` | 1 | JavaScript 在解析阶段失败，未执行任何 UI 动作；修正为合法输出处理后再用 fresh 当前索引，不重复错误脚本。 |
| 第 28 轮原生列视图点击 AX `single.txt` 索引却把选择移到本轮 `unsupported.sy-c2c-r28` | 1 | 只影响同一安全夹具目录；未打开/提交。停止依赖该列视图文本索引，改用 Go to Folder 精确路径后按 Return/打开按钮，且每步 fresh 复核。 |
| 第 28 轮修复后 HMR 主窗连续保持空白 renderer | 2 | 两种实现、回归/typecheck 与系统日志均排除稳定 runtime crash；未重启应用或服务，仅执行一次公开 View→Reload，下一自然帧恢复同一 r21，泄漏附件已清空。 |
| 第 28 轮首次 desktop typecheck 把新增 `vi.fn` 推断为零参数元组 | 1 | 只给测试 mock 补 `draftScopeId` 参数类型；生产代码无类型错误，重跑 desktop typecheck 全绿。 |
| 第 28 轮独立窗原生 column view 的 AX 与可见坐标均未选中 `send-one.txt` | 2 | 两次均只在 r28 纯合成目录且 Open 保持禁用；按收敛要求立即 Cancel，不继续相对键循环、不发送任何附件。 |
| 第 28 轮清理后枚举整个 `~/.Trash` 被 macOS 隐私拒绝 | 1 | move 已完成、工作区源路径已消失；改用两个唯一 Trash 绝对路径做存在性检查，夹具目录与源变化文件均确认可恢复。 |
| 第 28 轮附件菜单 fresh diff 后立即点击“附加文件”索引仍被报告失效 | 1 | 菜单保持打开且未选择；不重复 index 点击，改用已验证的 `Down` 聚焦首项 + `Return` 键盘激活。 |
| 第 28 轮原生列视图按 screenshot 的 `single.txt` 可见坐标仍选到安全未知扩展 | 1 | 只在 r28 合成列内移动选择、未提交；停止坐标点行，改用当前选中项的原生 Up/Down 键相对导航并逐帧确认。 |
| 第 28 轮读取 Up 后状态的 node_repl 包装再次误写 `null.` | 1 | 解析前失败、UI 未变化；立即用合法脚本读取，确认 Up 已选中安全 `trash-before-send.txt`。后续复制固定合法输出尾，不再手写变体。 |
| 多文件源码组合输出两次超过上下文并被截断 | 2 | 改用 `rg` 定位符号，再按短区间读取；不再整段并发拼接大文件。 |
| 首次按顶层 `node_modules/@anthropic-ai/...` 查找 SDK 类型失败 | 1 | 使用 pnpm 虚拟存储中的 0.3.245 精确路径核对类型。 |
| `node_repl` 截图复用块内临时导入变量时报 `fs2 is not defined` | 1 | 不依赖先前块内变量；后续截图调用重新导入或保存到 `globalThis`。 |
| 向 xterm 写入命令未生效（AX `paste`、AX `type_text`、画布坐标聚焦后 `type_text`） | 3 | 三次均确认没有文件副作用；Computer Use 当前只能可靠观察/切换 xterm，无法键入。停止重复，改用 `apply_patch` 创建专用 fixture，客户端仍负责真实状态刷新与审查。
| 最终组合 `git diff` 预览因既有规划文件历史过大被截断 | 1 | 改用 `git diff --check`、规格首尾抽查和定向 `rg` 完成一致性验证。 |
| 配额回归测试把“缺少 patch 属性”写成对象包含 `patch: undefined` | 1 | 改用显式 `toBeUndefined()` 验证属性值，保留对象包含断言检查其余状态。 |
| 配额回归测试误把 persistCapture 返回的首个 superseded 状态事件当成新检查点 | 1 | 按 `turnId` 与 `available` 状态选择新事件，不依赖 append-only 事件数组位置。 |
| 首次替换 Claude SDK 测试时未匹配组合 type import | 1 | 读取文件实际导入结构后，改用更窄的行级补丁。 |
| 额外枚举 ASAR 内容时本地无法直接导入 `@electron/asar` | 1 | 不追加临时依赖；以仓库正式 `check:packaged-asar` 成功结果作为打包边界验收。 |
| 文档部署链接测试误用根目录 Vitest，命令不可用 | 1 | 确认测试文件使用 `node:test`，改为 `node --test` 后 4/4 通过。 |
| Agent 独立窗口首版中性加载态持续不结束 | 1 | 定位为 refresh 期间重复 `selectSession` 使请求失效；加载期不再重复选择，已选中目标时跳过切换，并以真实窗口验证可从“加载中”进入完整会话。 |
| 视觉会话第二次文件选择时 AX `icon.png` 单击/双击未选中 | 2 | 文件面板保持打开且无副作用；停止重复点击，改走键盘搜索并限定当前 `assets` 目录。 |
| 原生文件搜索建议“Name Contains icon.png”单击未提交 | 1 | 搜索范围已正确限定 `assets`；不重复点建议，改用 Return 提交当前搜索词。 |
| `assets` 原生搜索提交后未出现可访问结果 | 1 | 不继续等待/重复搜索；退出搜索后复用首轮已成功的 Go to Folder 精确文件路径。 |
| Computer Use 对 slash menu 使用 `ENTER` / `RETURN` 大写键名失败 | 2 | 按技能规定改用 xdotool 风格 `Return`；本轮不重复该无效键名，视觉会话草稿通过 `set_value` 清理，Enter 插入语义已有 Persona 原路径证据。 |
| 热更新后复用旧 AX element id 打开项目菜单失败 | 1 | 立即重新读取完整 AX 树并使用新索引继续；客户端没有发生动作或状态改变。 |
| Computer Use 向 Chrome 发送 `alt+Left` 未触发历史后退 | 1 | 页面与 URL 均无变化；不重复同一键盘合成路径，改用浏览器真实返回/前进按钮验证 history 状态。 |
| 第 11 轮首次 `node_repl` 应用筛选表达式被工具语法解析拒绝 | 1 | 未执行任何桌面动作；不重复复杂模板表达式，直接按已知应用名读取当前 Synapse 状态。 |
| 第 11 轮首次规划文件双文件补丁因多余 hunk 标记被拒绝 | 1 | 文件未变化；改用两个连续且语法完整的 Update File hunk 追加，不重复错误补丁结构。 |
| 第 11 轮追加两次 UI 观察时误带不存在的表头定位 | 1 | 原子补丁未写入任何文件；先读取三个规划文件的真实位置，再分别以稳定标题和末行定位。 |
| 第 11 轮尝试用显示名或共享 bundle id 定位 Synapse | 2 | 前者不是可用应用名，后者命中多个 Electron；均未执行桌面动作，改用当前仓库 Electron 可执行文件的精确绝对路径。 |
| 第 11 轮用菜单键盘 Down 序列重加文件夹时误进“附加文件” | 1 | 原生文件面板立即 Escape 取消且未选取任何文件；改用菜单可见坐标点选“附加文件夹”，不重复不稳定的菜单初始焦点假设。 |
| 第 11 轮误把 Computer Use 滚动参数写为坐标 delta 形式 | 3 | 均在参数校验阶段失败且无 UI 动作；重新查技能示例后确认应使用 `element_index + direction + pages`，会话切换改用可访问元素索引直接点击。 |
| 第 11 轮复用前一棵 AX 树的附件菜单项索引失败 | 1 | 只有菜单已打开，未选择文件；立即读取当前菜单新索引后继续，不再跨树复用子项索引。 |
| 第 11 轮首次尝试恢复窗口宽度时复用了旧全屏按钮索引 | 1 | secondary action 在校验阶段拒绝且窗口未变化；读取当前完整 AX 树后按新索引执行系统 zoom，成功恢复 960px。 |
| 第 11 轮尝试用坐标拖拽窗口边缘时未匹配 drag 参数 | 4 | 均在参数校验阶段失败且窗口未变化；改用窗口绿色按钮公开的 `zoom the window` secondary action 完成 960↔1360 往返。 |
| 第 11 轮两次把 macOS 快捷键写成独立 modifier 参数 | 2 | 均无 UI 副作用；按 Computer Use 技能改用 xdotool 风格单字符串 `super+shift+g`，原生 Go to Folder 随后成功。 |
| 第 11 轮 Renderer 热更新后 Computer Use 拒绝复用旧激活状态 | 1 | 未执行任何 UI 动作；先重新读取完整应用状态，再沿当前索引继续恢复复测。 |
| 第 11 轮精确清理命令被安全层拒绝 `rm -rf`，BSD `chmod` 不接受 `--` | 2 | 首次命令整体未执行；改为把唯一专用目录移入唯一 Trash 路径，再用精确路径 `find -depth -delete` 永久清理，工作区与 Trash 均无残留。 |
| 第 13 轮首次组合追加计划/发现/进度补丁未匹配 findings 末行 | 1 | 原子补丁未写入任何文件；读取三个文件实际末尾后按稳定位置分别追加。 |
| 第 14 轮首次生成 PPM 时 JS 字符串把换行写成字面 `\\n`，`sips` 与 `ffmpeg` 均拒绝解码 | 2 | 先检查十六进制确认夹具格式错误；用 `apply_patch` 按真实多行文本重建 PPM，再由已安装 ffmpeg 做格式转换，得到可验证的 4×4 PNG。 |
| 第 14 轮首次关闭已完成上传面板时 AX 树已自动切回 Drive，旧 Close 索引失效 | 1 | 未发生 UI 副作用；立即重新读取完整树，确认面板其实已关闭且目录已出现在根列表，不再复用旧索引。 |
| 第 14 轮 MDXEditor 源码 textarea 的 `select_text` 未命中目标句，键入内容误落到 Mermaid 首行 | 1 | 改动仅处于未保存草稿；读取可见完整值后用同一 textarea 的 `set_value` 校正并完成目标编辑，再经 UI 保存和刷新验证，无错误内容进入版本。 |
| 第 14 轮安全图片剪贴板粘贴无法经 Computer Use 稳定到达编辑器 | 3 | 工具仅原生支持 text/md/html 剪贴板；按上游指令停止重复 Finder/Preview 尝试，不改用接口绕过，记录为工具限制并确认无远端图片副作用。 |
| 第 15 轮上传完成面板在读取后自动关闭，下一次点击 Close 时索引失效 | 1 | 未发生额外 UI 动作；立即读取当前界面确认实际状态，不再复用旧索引。 |

## 第 45 轮完成记录（2026-08-28）

- [x] 实机复现嵌套普通项目被祖先 Git 提升、首轮 Write 落错 Synapse 根。
- [x] 按现有 SDK 契约增加精确工作区 system prompt，并为 Write/Edit/MultiEdit/NotebookEdit 增加 cwd/additionalDirectories PreToolUse 边界；不把项目路径表述为通用 OS/Bash 沙箱。
- [x] 第二次 Provider Write 落入唯一 r45 夹具，生成真实 1-file Checkpoint；完成 Diff、响应式、键盘、Escape 回焦、撤销 Escape/取消/确认、导航、独立窗与主题验收。
- [x] 因首轮失败，第二次按约优先重试文件任务；停止链未发送，Provider 总次数保持 2。
- [x] 永久删除唯一 r45 会话、移除唯一 r45 项目；Finder 将夹具和根误写移入未清空废纸篓，两处原路径均不存在。
- [x] 14 文件 / 389 专项、desktop typecheck、hard constraints、git diff --check 全绿；共享既有改动未回退。

## 第 45 轮 symlink 安全续修（已完成）

- [x] 核对 PermissionGuard、Checkpoint、attachment staging 的 realpath/安全父路径契约。
- [x] 先补目录 symlink 越界、目标文件 symlink、正常新建/既存、additional directory、不可解析父目录红灯。
- [x] 最小实现真实根与最近存在父目录校验，文档精确说明 TOCTOU 保证边界。
- [x] 跑 session/security 专项、desktop typecheck、hard constraints、git diff --check。

## 第 47 轮工具偏差记录

- 2026-08-28 15:xx：首次追加停止终态观察的组合补丁因手写上下文漏了“父 shell 与 sleep”中的空格而被 `apply_patch` 原子拒绝，三份规划文件和源码均未变化；读取真实末行后已使用精确上下文记录，不再猜测。
- 2026-08-28 15:xx：随后首次向 `task_plan.md` 补记该偏差时，误把小节标题猜成“symlink 硬链接安全验证”，再次被 `apply_patch` 原子拒绝；读取文件真实末尾后改用“第 45 轮 symlink 安全续修（已完成）”精确锚点，仍未造成 UI、规划或源码副作用。
- 2026-08-28 16:05：为兼容恢复态缺省 `success` 的窄补丁只按首个 `return result.success === false` 匹配，误改到相邻的“固定拒绝句”分支，导致新增恢复态测试按预期继续红；检查实际函数后立即把固定拒绝分支恢复为严格 `false`，只在空中断结果分支允许缺省值。未影响运行中应用状态或既有数据。
- 2026-08-28 16:09：首次把“状态可缺省”的源码与测试补丁合并时漏写第二个 `Update File` 标头，`apply_patch` 因在源码文件找不到测试上下文而原子拒绝；立即用两个精确文件段重试，没有文件或 UI 副作用。
- 2026-08-28 16:18：Computer Use 在最终 HMR 读取前返回 `native pipe closed before response`，该次没有 UI 动作；仅重新连接并 fresh 读取现有窗口成功，未启动、停止或重启 Synapse。

## 第 47 轮清理闸门

- [x] 唯一会话已通过真实 UI 进入永久删除确认层；目标精确为 `sy-c2c-r47-agent-running-stop-session`，文案明确“永久删除 / 无法撤销”，默认焦点安全地停在“取消”。
- [x] 获得动作时确认后，只删除该会话；随后移除唯一 r47 项目配置，并用 Finder 将唯一夹具移入未清空废纸篓。
- [x] 最终确认项目数恢复 6、源路径不存在、无 r47 会话/草稿/权限/运行进程/独立窗，主窗恢复 960×768 且主题跟随系统。

## 第 48 轮工具偏差记录

- 2026-08-28 16:xx：Impeccable 要求读取当前模块代表实现时，首次猜测了不存在的 `desktop/src/modules/agent/components/conversation-workspace.tsx`；`sed` 仅报文件不存在，未执行 UI、源码或配置写入。随后用 `rg --files` 定位真实 `agent-conversation-workspace.tsx`，不再猜路径。
- 2026-08-28 16:xx：首次按共享 bundle id `com.github.Electron` 读取现有 Synapse 界面，被多个 Electron 安装歧义在动作前拒绝；没有启动应用或执行 UI 动作。按工具返回的当前仓库 Electron 41.2.1 精确 `.app` 路径重新读取成功，后续固定使用该路径。
- 2026-08-28 16:xx：首次两次构造含单引号、双引号和 `\\n` 的提示词 node_repl 脚本时，手写嵌套转义均在 JavaScript 解析阶段报 `Unexpected identifier 'printf'`，UI 未获得任何动作、composer 保持空。改为在外层用 `JSON.stringify(prompt)` 生成合法代码后一次写入，并用 fresh AX 原值逐字相等断言确认；后续复杂文本不再手写多层转义。
- 2026-08-28 16:53：最终只读核验中 `test -f` 已确认废纸篓内精确 README 存在，但随后 `find` 枚举同一废纸篓目录被 macOS 隐私权限拒绝；未执行任何写入或重试绕过。结合 Finder 真实“移到废纸篓”后源项消失、源路径不存在和精确文件存在，清理证据完整。

## 第 48 轮清理闸门

- [x] 跨导航与独立窗口稳定复现拒绝结果误标 Failed；补精确 SDK 拒绝句红灯并完成纯 renderer 最小修复，HMR 原路径已显示 Denied。
- [x] 完成相关回归、desktop typecheck、hard constraints、diff-check，并复核导航/独立窗 HMR 状态。

- [x] 唯一会话通过真实 UI 进入精确永久删除确认层，核对名称、不可撤销文案与安全默认焦点后停下请求动作时确认。
- [x] 获得确认后只删除 r48 会话、移除 r48 项目，并用 Finder 将唯一夹具移入未清空废纸篓。
- [x] 最终确认项目数恢复 6、源路径不存在、无 r48 会话/草稿/权限/进程/独立窗，主窗恢复 960×768 且主题跟随系统。

## 第 18 阶段：2026-08-27 至 2026-08-28 全量修复 Review（已完成）

- [x] 锁定范围：两日无新增 Git commit；审查基线为 `a473f7ce5b6b264a6bba1432871080b82e6237cc`，对象为相对该基线的全部未提交改动。
- [x] 从 `findings.md`、`progress.md`、设计文档与发布说明还原每项修复意图及两日时间线。
- [x] 并行完成 Standards 与 Spec 两轴审查，主线复核高风险实现、跨模块误改和测试盲区。
- [x] 仅对确认存在的真实问题补红灯并作最小修复，同步发布说明。
- [x] 运行最小充分测试、typecheck、hard constraints 与 diff-check，形成周四/周五最终汇报。
