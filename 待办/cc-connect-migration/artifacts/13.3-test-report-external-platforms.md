# 13.3 测试报告：外部平台完整迁移

更新时间：2026-04-26 13:21 CST

## 自动化测试

| 命令 | 结果 |
| --- | --- |
| `pnpm --filter @synapse/desktop exec vitest run tests/unit/connector-qr.test.ts tests/unit/connectors.test.ts tests/unit/connectors-project-model.test.ts` | pass，31 tests |
| `pnpm --filter @synapse/desktop exec vitest run tests/unit/connector-qr.test.ts tests/unit/connectors.test.ts tests/unit/connectors-project-model.test.ts tests/unit/product-entry.test.ts` | pass，36 tests |
| `pnpm desktop:typecheck` | pass |
| `pnpm desktop:check:hard-constraints` | pass |
| `git diff --check` | pass |
| `pnpm desktop:check:ipc-codegen` | pass |

## 覆盖项

- Feishu begin 成功。
- Feishu init 不支持 `client_secret`。
- Feishu `slow_down`。
- Feishu `access_denied`。
- Feishu `expired_token`。
- Feishu poll transport failure 收敛为 `failed` session。
- Feishu poll 识别 Lark tenant brand 并切换到 Lark domain。
- Feishu completed 保存 `app_id/app_secret`，config 只保留 `secretRefs.app_secret`。
- Weixin begin 成功。
- Weixin `wait/scaned/expired/confirmed`。
- Weixin confirmed 缺 `bot_token` 或 `ilink_bot_id` 报错。
- QR UI 能渲染二维码 SVG。
- 成功后项目平台列表出现 configured 记录。
- secret 明文不进入项目 config。
- 手动平台 secret 字段通过 main 侧 secret store 写入，不明文持久化到 config。

## 说明

真实外部扫码不由自动化测试伪造。自动化只使用 mock HTTP 验证状态机、secretRef 和保存链路。
