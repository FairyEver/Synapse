# S06-B06 Execution Report: 访问控制、脱敏和限速

## 批次信息

- 批次编号：S06-B06
- 覆盖 CC ID：CC-023、CC-024
- 状态：done，等待独立 commit

## 原源码对照

读取的 CC Connect 源码和测试：

- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/user_roles.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/redact.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/ratelimit.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/outgoing_ratelimit.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/config.example.toml`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/user_roles_test.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/redact_test.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/ratelimit_test.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/outgoing_ratelimit_test.go`

对应迁移点：

- CC 的 `allow_from` 空值和 `*` permit-all 语义映射为 `allowListAllows` 和 `AccessPolicyService.checkUser`。
- CC 的 user role 解析顺序（显式用户、default role、wildcard、无匹配）映射为 `AccessRoleManager.resolveRole`。
- CC 的 `disabled_commands` 规范化、slash 去除、大小写折叠、builtin alias/prefix 匹配和 wildcard 扩展映射为 `resolveDisabledCommands`。
- CC 的 env/args/token 脱敏映射为 `redactEnv`、`redactArgs`、`redactToken`。
- CC 的入站 sliding-window rate limiter 和 role-first/global-fallback keying 映射为 `SlidingWindowRateLimiter` 和 `AccessPolicyService.checkIncomingRate`。
- CC 的出站 per-platform token bucket、default burst `ceil(max_per_second)` 和 disabled override 映射为 `OutgoingRateLimiter`。

## 实现摘要

- 新增 `desktop/electron/services/access-policy-service.ts`：
  - access allow list、role manager、disabled command resolver。
  - inbound sliding-window rate limiter。
  - outbound token bucket rate limiter。
  - env/args/token redaction helpers。
- 新增 `desktop/tests/unit/access-policy-golden.test.ts`，覆盖 allow_from、roles、role validation、disabled commands 和 redaction。
- 新增 `desktop/tests/unit/rate-limit.test.ts`，覆盖 inbound window、role-first/global fallback 和 outbound bucket 行为。

## 验证

- `pnpm --filter @synapse/desktop run test -- desktop/tests/unit/access-policy-golden.test.ts desktop/tests/unit/rate-limit.test.ts`：通过，实际运行 50 个 test files / 345 tests。
- `pnpm desktop:typecheck`：通过。
- `pnpm desktop:check:hard-constraints`：通过。
- `pnpm desktop:check:ipc-codegen`：通过。
- `git diff --check`：通过。

## 约束检查

- 未启动 dev server、Electron 应用、浏览器或 Playwright。
- 未新增依赖。
- 未写入 token、pattern 或 secret 明文到日志。
- 未绕过 PermissionGuard/AuditSink；本批只新增无副作用策略原语。
- 未将任何 CC ID 标记为 dropped。
