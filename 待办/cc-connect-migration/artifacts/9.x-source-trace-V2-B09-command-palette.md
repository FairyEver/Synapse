# 9.x Source Trace - V2-B09 命令面板

生成时间：2026-04-26 10:15:08 CST

## 批次范围

- Batch：V2-B09
- 名称：命令面板
- 范围：会话页命令面板、命令目录、命令执行入口、命令结果面板、高风险命令权限提示。

## 旧源码读取记录

### Web 命令面板

- `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Chat/CommandPalette.tsx`
  - 读取目的：命令面板 UI、命令分组、键盘行为、过滤逻辑。
  - 还原证据：
    - `slashCommands` 使用 `session`、`settings`、`info`、`advanced` 分组。
    - 关键命令包含 `/new`、`/list`、`/switch`、`/current`、`/history`、`/stop`、`/model`、`/reasoning`、`/mode`、`/lang`、`/provider`、`/status`、`/help`、`/doctor`、`/version`、`/commands`、`/dir`、`/cron`、`/heartbeat`、`/alias`、`/config`、`/skills`、`/upgrade`、`/delete-mode`。
    - 输入 `query` 按命令名和 label 过滤；`ArrowDown`、`ArrowUp`、`Enter`、`Escape` 分别控制选择和关闭。
    - 打开面板会重置 `query` 和 active index；点击外部关闭。

### Web 命令结果

- `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Chat/CommandResultPanel.tsx`
  - 读取目的：命令执行结果显示形态。
  - 还原证据：
    - `CommandResult` 字段：`command`、`content`、`format: text | markdown | card | buttons`、`card?`、`buttons?`。
    - 结果以侧栏/面板展示，可关闭；命令标题和 icon 从 `slashCommands` 查找。
    - Markdown、card、button 三类结果有独立渲染；按钮通过 `onCardAction` 回调。
  - 迁移约束：旧源码有自定义颜色和第三方 Markdown 依赖；3S 侧使用现有 shadcn、Tailwind token、轻量 Markdown/Code renderer。

### ChatView 命令入口

- `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Chat/ChatView.tsx`
  - 读取目的：输入框如何触发命令面板、命令如何进入发送/结果链路。
  - 还原证据：
    - 空输入键入 `/` 会打开命令面板。
    - `handleCmdSelect` 关闭命令面板；连接态下调用 `bridgeSend(cmd.cmd)`。
    - `/new`、`/stop`、`/switch`、`/delete-mode`、`/upgrade` 等 chat commands 走消息流；其他命令首个 reply/card/buttons 进入 command result panel。
    - Chat 输入区有独立命令按钮打开面板。

### Core 命令注册与自定义命令

- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/command.go`
  - 读取目的：自定义命令覆盖、命令文件读取、参数展开。
  - 还原证据：
    - `CommandRegistry` 支持配置命令和 agent markdown command 文件，配置命令覆盖 agent 文件。
    - 命令名 hyphen/underscore 归一；agent 文件路径通过 abs dir prefix 防路径穿越。
    - 空 agent command 文件跳过；描述取第一个非空行并截断到 60 runes。
    - 模板展开支持 `{{1}}`、`{{1:default}}`、`{{2*}}`、`{{2*:default}}`、`{{args}}`、`{{args:default}}`。
    - 无 placeholder 时，把用户参数以两个换行追加到 prompt 后。

### Core 命令执行和权限边界

- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/engine.go`
  - 读取目的：内置命令目录、别名、前缀匹配、禁用命令、高风险命令。
  - 还原证据：
    - 内置命令包含 ``。
    - `matchPrefix` 先精确匹配，再唯一前缀匹配；前缀歧义返回空。
    - 高风险命令：`shell`、`show`、`dir`、`restart`、`upgrade`、`web`、`diff`。
    - `isAdmin` 对空 `admin_from` fail-closed。
    - `handleCommand` 会先解析字段、匹配前缀，再检查项目/角色禁用命令；禁用时发送 disabled reply 并记录 audit。
    - 非管理员执行高风险命令会被阻止并返回 admin required。
    - 未知命令返回 unknown notice；自定义命令和 skill 命令通过 registry 解析。
    - `renderCommandsCard` 会列出自定义命令的 tag/desc，并展示卡片 note。

### Core 权限测试

- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/engine_test.go`
  - 读取目的：旧实现边界条件。
  - 还原证据：
    - `disabled_commands=["*"]` 禁用所有内置命令。
    - 禁用命令支持带 slash 输入。
    - `admin_from` 为空时高风险命令默认拒绝。
    - 非管理员会被 `/shell`、`/restart`、`/upgrade`、`/commands addexec` 和 custom exec 阻止。
    - `/help` 等非高风险命令不受管理员限制。

### Bridge 能力发布

- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/bridge_capabilities.go`
  - 读取目的：Web 端命令目录发布形态。
  - 还原证据：
    - `GetBridgePublishedCommands` 发布未禁用的内置命令和自定义命令，并按名称排序。
    - 返回字段包含 `name`、`description`、`source: builtin/custom`、`requiresWorkspace`、`argsMode`。
    - 旧实现尚未发布 skill richer command model。

## 3S 侧迁移决策

- 命令目录使用 3S 已有 `CC_BUILTIN_COMMANDS` 和 `AccessPolicyService` 的 `resolveDisabledCommands/isCommandDisabled` 语义，保持旧实现的 slash/alias/wildcard 禁用行为。
- 命令面板接到现有 `agentSessions` bridge domain，不新增 renderer 直连 IPC。
- 安全执行范围只接本批次可证明的轻量命令：
  - `/new`：在当前项目/session key 新建会话并切换 UI。
  - `/status`、`/current`、`/model`、`/provider`、`/commands`、`/help`、`/list`、`/history`、`/cron`、`/skills`、`/heartbeat`：返回本地可得信息。
- 高风险命令 `shell/show/dir/restart/upgrade/web/diff` 不直接执行；先返回 `permission_required`，确认后仍不会绕过未接入的 runtime。
- 未知命令、歧义前缀、禁用命令返回错误/禁用状态，不凭空执行。

## 待验证项

- TypeScript 类型检查。
- 单元测试覆盖：命令目录过滤、`/status` 结果、`/new` 会话切换、高风险命令权限提示、禁用命令阻止。
