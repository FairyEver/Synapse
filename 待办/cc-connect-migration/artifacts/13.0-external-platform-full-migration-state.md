# 13.0 外部平台完整迁移状态

更新时间：2026-04-26 13:21 CST

## 当前状态

pass_with_waiting_user_scan

## 通知

- Bark：sent。

## 批次进度

| 批次 | 状态 | 结果 |
| --- | --- | --- |
| S13-B01 Source Trace | done | 已生成 `13.1-source-trace-external-platforms.md` |
| S13-B02 main 侧 QR onboarding | done | Feishu/Lark/Weixin begin/poll/save 已迁到 Electron main typed IPC |
| S13-B03 UI QR 状态机 | done | UI 显示真实二维码，支持等待、已扫码、保存中、已连接、过期、拒绝、错误、重试 |
| S13-B04 Secret 保存闭环 | done | app_secret/token 写入 encrypted `secrets` 命名空间，项目配置只保存 `secretRefs` |
| S13-B05 手动平台表单 | done | 基于 connector descriptors 覆盖 telegram、discord、slack、dingtalk、wecom、qq、qqbot、line、weibo |
| S13-B06 测试和 Computer Use | done | 自动验证通过；Computer Use 已确认 Feishu/Weixin 真实二维码显示、取消重试不空白、Telegram 手动保存可用 |

## 当前验证

- `pnpm --filter @synapse/desktop exec vitest run tests/unit/connector-qr.test.ts tests/unit/connectors.test.ts tests/unit/connectors-project-model.test.ts`：pass，31 tests。
- `pnpm desktop:typecheck`：pass。
- `pnpm desktop:check:hard-constraints`：pass。
- `git diff --check`：pass。
- `pnpm desktop:check:ipc-codegen`：pass。

## Computer Use 复测

- 使用 Windsurf 底栏 `kill` 后点击 `dev` 启动 Synapse；未点击 `bump`。
- `cc-v2-acceptance-project` 下添加 Feishu，点击开始设置后显示可扫描二维码，不是纯等待文案。
- 取消 Feishu 后再次开始设置，二维码重新显示，无空白。
- 添加 Weixin，点击开始设置后显示可扫描二维码。
- Telegram 手动表单使用安全输入保存，返回平台列表后仍为 configured，列表不显示 token 明文。
- Feishu/Lark/Weixin 真实保存完成仍等待用户手机扫码，状态记录为 `waiting_user_scan`。

## 待完成

- 最终 commit。
