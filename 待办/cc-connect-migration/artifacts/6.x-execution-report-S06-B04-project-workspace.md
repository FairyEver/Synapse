# S06-B04 Execution Report: 项目和工作区模型

## 批次信息

- 批次编号：S06-B04
- 覆盖 CC ID：CC-006、CC-030
- 状态：done，等待独立 commit

## 原源码对照

读取的 CC Connect 源码和测试：

- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/projectstate.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/workspace_binding.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/workspace_state.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/config.example.toml`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/projectstate_test.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/workspace_binding_test.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/tests/integration/multi_workspace_shared_test.go`
- `/Users/liyang/Desktop/code-guide/cc-connect-main/core/engine_test.go`

对应迁移点：

- `ProjectStateStore` 的 `work_dir_override` 和 `workspace_dir_overrides` 映射为 3S project config 的 `workDirOverride`、`workspaceDirOverrides`，保留配置默认 `workDir` 以支持 reset。
- `WorkspaceBindingManager` 的 project binding 优先、shared fallback、legacy channel key fallback 和 unbind 行为映射为 `project-workspace-service`。
- `multi-workspace` 的 `base_dir` 与 `work_dir` 冲突规则已在 S06-B03 导入预览中校验，本批承接为项目 draft 的 `mode/baseDir/path`。
- 缺失路径不在本批越权读取真实文件，改为显式 `ProjectPathInspector` 注入，由调用方在授权上下文中判断。

## 实现摘要

- 新增 `desktop/electron/services/project-workspace-service.ts`：
  - 旧 CC project preview 到 3S project config/default project draft。
  - project `workDirOverride`、workspace work_dir override。
  - workspace binding、unbind、project 优先 + shared fallback lookup、legacy channel key 兼容。
  - 显式路径状态 inspector。
- 扩展 `desktop/src/types/config.ts`、`desktop/src/lib/config.ts`、`desktop/src/constants/defaults.ts`：
  - `defaultProjectId`、`workspaceBindings`。
  - project 的 `mode/workDir/baseDir/source/workspaceDirOverrides`。
  - config sanitize 和 patch 保留新增字段。
- 更新 `desktop/electron/services/config-backup-service.ts`，避免备份导入丢失新增 project/workspace 字段。
- 新增 `desktop/tests/unit/project-workspace.test.ts` 覆盖 CC-006、CC-030 行为。

## 验证

- `pnpm --filter @synapse/desktop run test -- desktop/tests/unit/project-workspace.test.ts`：通过，实际运行 47 个 test files / 329 tests。
- `pnpm desktop:typecheck`：通过。
- `pnpm desktop:check:hard-constraints`：通过。
- `pnpm desktop:check:ipc-codegen`：通过。

## 约束检查

- 未启动 dev server、Electron 应用、浏览器或 Playwright。
- 未新增依赖。
- 未推送、未重置、未删除用户未确认文件。
- 未将任何 CC ID 标记为 dropped。
- 本批未读取真实 workspace 内容；路径状态通过显式 inspector 注入。
