# 12.2 v2 人工验收缺陷修复定向复测

复测时间：2026-04-26 12:42-12:45 CST
复测方式：Computer Use 操作真实桌面 Synapse Electron。
启动方式：Windsurf 底栏 `kill` 后点击 `dev`。
禁止项：未点击 Windsurf `bump`；未删除用户或测试数据。

## 环境

- 仓库：`/Users/liyang/Documents/code/github/Synapse`
- 应用：Electron dev app，URL `127.0.0.1:5173/`
- 重启次数：1 次，本轮为加载修复后的代码进行定向复测。

## A-8 QR 保存草稿后列表出现

步骤：
- 进入 `连接`。
- 选择已有 `cc-v2-acceptance-project`。
- 打开 `添加平台`。
- 选择 `Feishu`。
- 点击 `开始设置`，状态进入 `等待扫码`。
- 点击 `保存草稿`。
- 点击 `完成` 关闭弹窗。
- 打开项目 `平台` tab。

结果：
- 项目总览平台数从 0 更新到 1。
- 项目列表中 `cc-v2-acceptance-project` 显示 `1 平台`。
- 平台列表出现 `Feishu / feishu / draft / 停用`。

结论：pass。

## A-9 Telegram token 保存后列表出现且 token 不明文

步骤：
- 在同一项目 `平台` tab 点击 `添加平台`。
- 保持 `Telegram`。
- 在 secure token 输入框输入测试 token。
- 点击 `保存`。

结果：
- token 输入控件为 secure field，输入内容以圆点显示。
- 保存后项目总览平台数更新到 2。
- 项目列表中 `cc-v2-acceptance-project` 显示 `2 平台`。
- 平台列表出现 `cc-v2-acceptance-project-telegram / telegram / configured / 启用 / *`。
- 平台列表未显示 token 明文。

结论：pass。

## C-8 命令面板打开不白屏

步骤：
- 进入 `会话`。
- 保持已有会话选中。
- 点击输入区旁 `命令` 按钮。

结果：
- 命令面板正常打开。
- 面板显示会话、设置、信息、高级分组。
- 可见 `/status`、`/skills` 等命令。
- 应用未白屏，a11y 树仍完整。

结论：pass。

## C-9 `/status` 可执行

步骤：
- 在命令面板搜索 `status`。
- 选择并执行 `/status`。

结果：
- 结果 Sheet 标题为 `状态`。
- 命令为 `/status`。
- 状态 badge 为 `完成`。
- 内容显示项目、路径、会话、Agent、权限和禁用命令。

结论：pass。

## C-10 `/skills` 指向 `技能 > 项目扫描`

步骤：
- 关闭 `/status` 结果 Sheet。
- 再次打开命令面板。
- 搜索 `skills`。
- 选择并执行 `/skills`。

结果：
- 结果 Sheet 标题为 `Skills`。
- 命令为 `/skills`。
- 状态 badge 为 `完成`。
- 内容显示 `进入技能 > 项目扫描。`。

结论：pass。

## 总结

本轮定向复测 A-8、A-9、C-8、C-9、C-10 全部通过。CUA-001、CUA-002、CUA-003、CUA-004 的发布阻塞状态解除。
