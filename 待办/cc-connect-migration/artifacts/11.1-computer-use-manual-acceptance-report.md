# 11.1 Computer Use 人工验收报告

## 总结结论

blocked

A-E 均已按 Computer Use 操作执行到可执行边界，但发布候选不建议放行。主要阻塞是：平台保存后不可见/疑似未持久化，以及会话命令入口点击后白屏，导致 `status` 和 `/skills` 不能验收。未发现测试 API key/token 在列表、Raw TOML 或导出日志中明文泄漏；未发现高风险 restart 绕过确认。

## 环境和启动方式

- 仓库：`/Users/liyang/Documents/code/github/Synapse`
- 应用：Synapse Electron，URL 显示为 `127.0.0.1:5173/`
- 启动方式：Computer Use 操作 Windsurf 底栏 `dev`
- 重启方式：C 段命令入口白屏后，Computer Use 操作 Windsurf `kill`，等待 Electron 退出，再点 `dev`
- 重启次数：1
- 禁止项执行情况：未点击 Windsurf `bump`；未用 shell 启动或杀应用；未使用 Playwright、Chrome DevTools 或浏览器自动化；未修改 `desktop/` 业务代码

## A-E 验收结果总览

| 段落 | 通过数 | 失败数 | 阻塞数 | 风险数 | 结论 |
|---|---:|---:|---:|---:|---|
| A. 项目和连接 | 7 | 2 | 0 | 1 | fail |
| B. Provider | 7 | 0 | 0 | 1 | pass_with_risks |
| C. 会话和命令 | 6 | 1 | 2 | 2 | blocked |
| D. 自动化 | 9 | 0 | 1 | 2 | pass_with_risks |
| E. 系统设置和诊断 | 8 | 0 | 0 | 1 | pass_with_risks |

## 逐步验收记录

### A. 项目和连接

1. 打开 3S：通过 Windsurf `dev` 启动成功，Electron 应用可操作，pass。
2. 进入连接：顶部导航进入 `连接`，pass。
3. 查看运行概览：初始为项目 1、平台 0、Agent 0；创建项目后变为项目 2、平台 0、Agent 0，pass。
4. 新建项目：使用 `cc-v2-acceptance-project`，workdir 选择 `desktop`，agent 类型使用界面可用项，pass。
5. 保存后确认项目列表：项目列表出现测试项目，pass。
6. 进入项目详情：详情页显示正确 workdir，pass。
7. 打开添加平台：平台页可打开添加弹窗，pass。
8. 选择 QR 平台：Feishu 进入 `等待扫码`；点击 `保存草稿` 后 toast 显示成功，但平台列表仍为 `暂无平台`，fail。
9. 选择手动 token 平台：Telegram token 输入框遮蔽明文；点击保存后返回空列表且无明确错误，fail。未观察到 token 明文泄漏，risk 为持久化不可验证。

### B. Provider

1. 进入设置 > 服务商：pass。
2. 新增 provider：填写测试 provider、Base URL、model、API key，pass。
3. 保存后检查列表：列表出现 `cc-v2-acceptance-provider`，pass。
4. API key 脱敏：列表密钥列为 `已保存密钥引用`，未显示明文，pass。
5. 绑定全局 provider：测试项目服务商页可绑定全局 provider，pass。
6. 新增项目自定义 provider：项目 provider 可新增并显示，pass。
7. CC-Switch 导入预览：粘贴测试 JSON 后出现预览表，重复项显示 `已存在` 且 `导入 0` 禁用，pass。
8. 风险：本次只验证了粘贴 JSON 预览，未验证真实本地 CC-Switch 数据源自动导入，risk。

### C. 会话和命令

1. 进入会话：会话列表可见历史 session，pass。
2. 选择测试项目或创建测试会话：界面未见项目选择器，但可新建当前 session，risk。
3. 打开历史 session 抽屉/列表：历史列表可见，pass。
4. 新建会话：点击 `新会话` 后列表新增 session，pass。
5. 发送测试消息：输入并发送 `hello cc v2 acceptance`，用户消息进入会话，pass。
6. 记录发送结果：UI 显示明确错误 `agent runtime is not connected`，按已知 runtime 未接风险处理，pass_with_risk。
7. 富消息区域：错误事件区域出现 `错误 #1 agent runtime is not connected`，pass。
8. 打开命令面板并搜索 `status`：点击输入区旁 `命令` 后应用白屏，fail。
9. 执行安全命令：因命令面板白屏未能执行，blocked。
10. 执行 `/skills`：因命令面板白屏未能执行，blocked。

### D. 自动化

1. 进入自动化：pass。
2. 新建 Prompt Cron：pass。
3. 选择测试项目、schedule、prompt、session key、权限模式：可选择测试项目并填写，pass。
4. 保存后确认任务列表：列表出现任务、项目、下次运行和最近结果列，pass。
5. 测试启停任务：启用/停用状态可切换，pass。
6. 编辑任务：prompt 改为 edited 后列表标题更新，pass。
7. 删除本次测试任务：删除属于本地数据删除，已请求用户确认；未确认前未删除，blocked。
8. 新建 Exec Cron：切换 Exec 后出现 `Exec 任务保存前需要确认。`，pass。
9. 配置 Heartbeat：测试项目 heartbeat 可保存，pass。
10. 新增 Hook：Command Hook 保存前提示需要权限确认，保存后列表出现，pass。
11. 最近运行或状态反馈：Heartbeat 手动运行后运行次数从 0 到 1；Hook 未点击测试以避免无额外确认执行本地命令，risk。

### E. 系统设置和诊断

1. 进入设置 > CC Connect：pass。
2. 修改无风险配置：Preview Interval 从 1500 改为 1600，pass。
3. 保存后刷新/切换验证：保存后 Raw TOML 更新为 `interval_ms = 1600`，reload 后仍为 1600，pass。
4. 查看脱敏 Raw TOML：provider secret 为 `***REDACTED***`，未发现测试 API key/token 明文，pass。
5. 尝试 reload：点击后配置刷新且保持保存值；未观察到明确成功 toast，risk。
6. 尝试 restart：点击后出现 `重启 CC Connect` 确认框，点击取消，未执行重启，pass。
7. Bridge/Webhook/Local API/Management API 诊断：均可见状态、地址和能力说明，pass。
8. daemon/logs/doctor/update/install-source 诊断：可见 daemon、update、doctor 表；doctor 行为 `通过`，pass。
9. 日志导出入口：设置 > 调试存在 `导出全部日志`，导出成功到 Downloads，并提示已导出 31 个日志文件，pass。

## 缺陷清单摘要

- CUA-001：QR 平台保存草稿提示成功，但列表仍为空，疑似未持久化。
- CUA-002：Telegram token 平台保存后列表仍为空，且无明确错误反馈。
- CUA-003：会话命令入口点击后白屏，需要重启应用。
- CUA-004：`status` 和 `/skills` 因命令入口白屏无法验收。

## 风险清单摘要

- Chat runtime 未接通但错误反馈明确，属于已知风险。
- Provider/Raw TOML 未泄漏密钥，但平台 token 因保存失败无法验证持久化链路。
- CC-Switch 只验证粘贴 JSON 预览，未验证真实本地导入。
- Heartbeat 手动运行只看到计数变化，仍需结合真实 runtime 验证执行语义。
- Command Hook 测试按钮可能执行本地命令，本轮未在无额外确认下点击。
- Prompt Cron 删除等待用户确认，测试数据清理未闭环。
- CC Connect reload 生效但缺少明显成功反馈。

## 是否发现敏感信息明文泄漏

未发现。检查范围包括 provider 列表、项目 provider 列表、Raw TOML、CC-Switch 预览表和导出的日志 zip。测试 API key/token 只在用户主动输入控件或粘贴 textarea 中出现；普通列表、Raw TOML 和导出日志未出现明文。

## 是否发现高风险操作绕过确认

未发现。Exec Cron 和 Command Hook 均有权限/风险提示；CC Connect restart 先弹确认框，本轮取消后未执行。

## 是否发现空壳入口

发现疑似断链入口，不按已确认裁剪点豁免：

- 平台保存链路显示成功或返回页面，但平台列表为空，项目平台计数仍为 0。
- 会话命令入口点击后白屏，导致命令功能无法使用。

其余已确认裁剪点未当作缺陷：旧 Web Admin 登录、Provider 推荐市场、Skills 商店、完整 Raw TOML 编辑器、旧 daemon/API 控制面等均未作为本轮缺陷。

## 是否需要进入修复阶段

需要。建议先修复 CUA-003 命令入口白屏和 CUA-001/CUA-002 平台保存不可见问题，再重新执行 A、C 以及 D 清理闭环。
