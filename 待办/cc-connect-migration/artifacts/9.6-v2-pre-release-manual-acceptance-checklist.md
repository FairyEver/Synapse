# 9.6 v2 发布前人工验收清单

- Date: `2026-04-26`
- Scope: CC Connect v2 release readiness
- Baseline commit before this check: `52e70e8 stage09-v2(V2-B15): 最终收口审计 [F-001..F-058]`

## 代码状态核对

- `git status --short`: 初始检查为空，工作区干净。
- `git log --oneline --grep='stage09-v2(V2-B' --all`: 已确认 `V2-B00` 到 `V2-B15` 均有提交。
- 本次只修正 `/skills` 命令过期文案，并生成发布前清单/交接更新；未新增 V2 业务范围。

## 必须先确认的裁剪点

| F ID | 推荐结论 | 确认项 |
| --- | --- | --- |
| F-004 | drop | 不复制旧 Web Admin 登录页；3S 使用 Electron 本地权限边界。 |
| F-027 | merge | Provider 推荐列表只作为预设助手，不做 provider 商店。 |
| F-028 | merge | per-agent provider 预设字段承接在预设助手中，不做独立生态页。 |
| F-030 | defer | 推荐 Skills 不阻塞首轮发布，后续并入 3S 技能生态。 |
| F-051 | merge | Raw TOML 只做脱敏查看/导入，不做完整编辑器。 |
| F-058 | merge | daemon、doctor、logs、update、install-source 融入设置/诊断，不复制 npm wrapper 产品入口。 |

## A. 项目和连接

- [ ] 打开 3S。
- [ ] 进入连接。
- [ ] 查看运行概览，确认项目数、平台数、最近会话不是硬编码。
- [ ] 新建项目，填写名称、workdir、agent 类型。
- [ ] 在项目列表看到新项目。
- [ ] 进入项目详情。
- [ ] 打开添加平台。
- [ ] 选择 QR 平台，确认看到 idle/等待/错误等状态入口。
- [ ] 选择手动 token 平台，确认看到 token、allow users、group/shared session。
- [ ] 取消或保存测试配置，确认不保存敏感明文。

通过标准：

- 每步都有真实 UI 反馈。
- 不出现无动作按钮。
- secret 不在 renderer 明文持久化。

## B. Provider

- [ ] 进入设置 > 服务商。
- [ ] 新增 provider。
- [ ] 填写名称、base URL、模型、agent type、API key。
- [ ] 保存 provider。
- [ ] 查看 API key 是否脱敏或显示为 secretRef。
- [ ] 回到项目详情 > 服务商。
- [ ] 绑定全局 provider。
- [ ] 新增项目自定义 provider。
- [ ] 打开 CC-Switch 导入预览，确认冲突/预览反馈存在。

通过标准：

- API key 不以明文出现在列表、Raw TOML、日志或通知中。
- 项目详情能看到 provider 绑定结果。

## C. 会话和命令

- [ ] 进入会话。
- [ ] 选择一个项目会话。
- [ ] 打开历史 session 抽屉。
- [ ] 切换 session。
- [ ] 新建会话。
- [ ] 输入消息并发送。
- [ ] 查看发送中、事件、结果或错误。
- [ ] 查看 thinking/tool/permission/error 等富消息。
- [ ] 响应一次权限请求。
- [ ] 打开命令面板并搜索 `status`。
- [ ] 执行安全命令并查看结果。
- [ ] 执行 `/skills`，确认结果指向 `技能 > 项目扫描`。

通过标准：

- 会话数据不是硬编码。
- 发送失败有可恢复反馈。
- 命令结果可见。

## D. 自动化

- [ ] 进入自动化。
- [ ] 新建 Prompt Cron。
- [ ] 选择项目、schedule、prompt、session key、权限模式。
- [ ] 保存后在任务列表看到任务。
- [ ] 启停任务。
- [ ] 编辑任务。
- [ ] 删除任务。
- [ ] 新建 Exec Cron，确认出现权限提示。
- [ ] 切换 Heartbeat，配置项目 heartbeat。
- [ ] 切换 Hooks，新增 HTTP hook 或 command hook。
- [ ] 查看最近运行或状态。

通过标准：

- Exec/command hook 必须出现权限提示。
- 最近运行或状态可见。
- 删除和高风险操作有明确确认。

## E. 系统设置和诊断

- [ ] 进入设置 > CC Connect。
- [ ] 修改附件发送、preview、rate limit、log level。
- [ ] 保存后刷新设置，确认值持久化。
- [ ] 查看脱敏 Raw TOML。
- [ ] 尝试 reload，确认有操作反馈。
- [ ] 尝试 restart，确认出现明确确认提示。
- [ ] 查看 Bridge 诊断状态。
- [ ] 查看 Webhook 诊断状态。
- [ ] 查看 Local API socket 路径、权限和端点。
- [ ] 查看 Management API 端点和 token 状态。
- [ ] 查看 daemon/logs/doctor/update/install-source 诊断。
- [ ] 进入调试或日志入口，导出日志。

通过标准：

- Raw TOML 不显示 secret/token/API key 明文。
- 高风险操作不直接执行。
- 操作结果有反馈。

## 发布判定

- [ ] `F-004,F-027,F-028,F-030,F-051,F-058` 裁剪点已确认。
- [ ] A-E 人工验收全部通过。
- [ ] 未发现迁移范围内空壳入口。
- [ ] 未发现 secret/token/API key 明文泄漏。
- [ ] 未发现高风险操作绕过确认。
- [ ] 自动验证仍通过：`desktop:typecheck`、`desktop:check:hard-constraints`、`desktop:check:ipc-codegen`。

以上全部完成后，可将 CC Connect v2 迁移标记为发布候选。
