# Source Trace: V2-B02 project detail platform status

批次：`V2-B02`  
名称：项目详情和平台状态  
生成时间：2026-04-26 09:05 CST

## 本批目标

- 在 3S `连接` 模块中补齐项目详情结构。
- 详情包含概览、平台、服务商、Heartbeat、设置。
- 项目设置可查看并保存 agent/workdir/权限/语言/访问控制等基础字段。
- 删除项目必须有确认，不允许误触。

## 已读取 CC Connect 原源码

| 路径 | 已确认内容 |
| --- | --- |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Projects/ProjectDetail.tsx` | 项目详情 Tab 为 `overview`、`providers`、`heartbeat`、`settings`；概览显示平台状态和会话数；Provider Tab 展示项目 provider；Heartbeat Tab 展示 paused/running、interval、run/error count；Settings Tab 覆盖 agent type、work_dir、permission mode、context indicator、reply footer、inject sender、language、admin_from、disabled_commands、platform allow_from；删除项目必须打开确认框后调用 `deleteProject()`。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Projects/PlatformSetupQR.tsx` | 添加平台入口会进入 QR 二级流程，并携带 `projectName`、`workDir`、`agentType` 保存。B02 只保留平台状态和详情结构，实际向导由 B03 实现。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Projects/PlatformManualForm.tsx` | 手动平台保存调用 `addPlatformToProject(projectName, { type, options, work_dir, agent_type })`。B02 不保存平台凭证。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/api/projects.ts` | `ProjectDetail` 字段包含 `agent_type`、`work_dir`、`platforms`、`sessions_count`、`active_session_keys`、`heartbeat`、`settings`；`ProjectSettingsUpdate` 包含 `language`、`admin_from`、`disabled_commands`、`work_dir`、`mode`、`agent_type`、`show_context_indicator`、`reply_footer`、`inject_sender`、`platform_allow_from`。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/config/config.go` | `ProjectConfig` / `AgentConfig` / `PlatformConfig` 定义项目、agent、platform 数据；`ProjectSettingsUpdate` 和 `SaveProjectSettings()` 更新 agent type、work_dir、permission mode、language、admin_from、disabled_commands、platform allow_from；`buildRawProjectSpans()` 证明平台和 agent 配置以 project 为边界解析。 |

## 搜索证据

执行过：

```text
rg -n "type Tab|overview|providers|heartbeat|settings|platforms|Add platform|handleSaveSettings|deleteProject|platform_allow_from|disabled_commands|admin_from|work_dir|agent_type|show_context_indicator|inject_sender" web/src/pages/Projects/ProjectDetail.tsx web/src/pages/Projects/PlatformSetupQR.tsx web/src/pages/Projects/PlatformManualForm.tsx web/src/api/projects.ts config/config.go -g'*.tsx' -g'*.ts' -g'*.go'
```

## 还原逻辑

| CC Connect 逻辑 | 3S 本批处理 |
| --- | --- |
| 项目详情以 Tab 组织概览、Provider、Heartbeat、Settings。 | 3S 连接模块项目详情改为 `Tabs`，包含概览、平台、服务商、Heartbeat、设置。 |
| 平台状态展示 type + connected。 | 3S 使用 `platformConnections` 展示平台名称、状态和启用态；无平台时只显示空态。 |
| Settings 可更新 agent/workdir/权限/语言/访问控制。 | 3S 保存到 `config.global.projects` 的项目字段中。 |
| 删除项目需要确认。 | 3S 使用 `AlertDialog` 确认后删除配置中的项目。 |

## 3S 落点

| 文件 | 计划处理 |
| --- | --- |
| `desktop/src/types/config.ts` | 增加项目设置、Heartbeat、Provider refs 等可选字段。 |
| `desktop/src/modules/connectors/project-model.ts` | 增加项目设置更新和 summary 字段。 |
| `desktop/src/modules/connectors/index.tsx` | 项目详情 Tab、设置表单、平台状态、删除确认。 |
| `desktop/tests/unit/connectors-project-model.test.ts` | 覆盖设置更新、平台状态 summary 和删除候选。 |

## 无法确认内容

- B02 不实现 Provider 绑定、Heartbeat 启停、平台新增保存；这些在后续批次承接。B02 只展示状态和保存项目基础设置。
