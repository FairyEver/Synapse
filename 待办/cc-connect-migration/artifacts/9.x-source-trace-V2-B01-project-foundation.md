# Source Trace: V2-B01 project foundation

批次：`V2-B01`  
名称：Project 数据和 UI 基础  
生成时间：2026-04-26 09:02 CST

## 本批目标

- 在 3S `连接` 模块中建立 CC Connect project 基础视图。
- 项目列表来自 3S 真实配置数据，不使用固定空态或硬编码项目。
- 新建项目第一步保存到 3S 配置数据层，字段覆盖名称、工作目录、Agent 类型。

## 已读取 CC Connect 原源码

| 路径 | 已确认内容 |
| --- | --- |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Dashboard.tsx` | Dashboard 同时调用 `getStatus()` 和 `listProjects()`；指标展示 `version`、`uptime_seconds`、`connected_platforms.length`、`projects_count`；项目摘要展示 `name`、`agent_type`、`platforms`、`sessions_count`。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Projects/ProjectList.tsx` | 项目列表从 `listProjects()` 读取；新增项目向导第一步包含项目名称、工作目录、Agent 类型；名称输入会用 `/[^a-zA-Z0-9_-]/g` 过滤；Agent 选项包含 `claudecode`、`codex`、`gemini`、`cursor`、`devin`、`acp`、`acp:openclaw`、`opencode`、`qoder`。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/api/projects.ts` | `ProjectSummary` 字段为 `name`、`agent_type`、`platforms`、`sessions_count`、`heartbeat_enabled`；`ProjectDetail` 包含 `work_dir`、`platforms`、`settings` 等。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/core/projectstate.go` | 项目运行态持久化字段包含 `work_dir_override` 和 `workspace_dir_overrides`；状态写入 JSON 文件。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/config/config.go` | `ProjectConfig` 包含 `Name`、`Mode`、`BaseDir`、`Agent`、`Platforms`、`Heartbeat`、访问控制和显示设置；`AgentConfig` 包含 `Type`、`Options`、`ProviderRefs`、`Providers`；`validate()` 要求项目名、agent type、至少一个 platform；`AddPlatformToProject()` 在项目不存在时可用 `agentType` 和 `workDir` 创建项目并写入 `agent.options.work_dir`。 |

## 搜索证据

执行过：

```text
rg -n "type Project|ProjectSummary|listProjects|getProject|AddPlatformToProject|ProjectState|projects|agent_type|work_dir|sessions_count|platforms|uptime|version" web/src/pages/Dashboard.tsx web/src/pages/Projects/ProjectList.tsx web/src/api core/projectstate.go config/config.go -g'*.tsx' -g'*.ts' -g'*.go'
```

## 还原逻辑

| CC Connect 逻辑 | 3S 本批落点 |
| --- | --- |
| Project 是平台、会话、Heartbeat、Provider 的共同父对象。 | `desktop/src/modules/connectors/` 先承接项目列表和运行概览。 |
| 项目基础字段是 name、work_dir、agent_type。 | 扩展 `SynapseProjectConfig` 的可选 `agentType`，并复用既有 `path/workDir` 字段保存工作目录。 |
| 新增项目第一步要求名称、工作目录、Agent 类型。 | 新建项目 Dialog 保存到 `config.global.projects`，通过既有 `window.synapse.config.update` 持久化。 |
| ProjectList 展示 agent、平台数量、sessions_count。 | 项目卡展示 agent、workdir、平台数量；会话数量在本批无真实 session API，显示为待接入状态，避免硬编码数字。 |

## 3S 落点

| 文件 | 计划处理 |
| --- | --- |
| `desktop/src/types/config.ts` | 增加 project 的 CC Connect 可选字段。 |
| `desktop/src/modules/connectors/project-model.ts` | 新增纯函数：Agent 选项、名称清洗、项目创建、项目摘要。 |
| `desktop/src/modules/connectors/index.tsx` | 使用 `useAppConfig()` 渲染运行概览、项目列表、新建项目 Dialog。 |
| `desktop/tests/unit/connectors-project-model.test.ts` | 覆盖项目名称清洗、新建项目字段和 summary 计算。 |
| `desktop/tests/unit/product-entry.test.ts` | 改为渲染纯 UI 组件，避免测试依赖 Electron preload。 |

## 无法确认内容

- 旧 CC Connect 的 `validate()` 要求项目至少有一个 platform；v2 B01 只做项目第一步，平台选择在 `V2-B03`。因此 3S 项目可以先以无平台草稿存在，但必须在 UI 中如实展示平台为 0。
- 本批没有真实 session repository 接入，不能声称会话数量已迁移。
