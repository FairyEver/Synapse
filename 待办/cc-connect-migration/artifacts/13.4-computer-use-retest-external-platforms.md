# 13.4 Computer Use 复测：外部平台完整迁移

更新时间：2026-04-26 13:21 CST

## 方式

- 通过 Windsurf 底栏点击 `kill`，再点击 `dev` 启动 Synapse。
- 未点击 `bump`。
- 复测目标项目：`cc-v2-acceptance-project`。
- 复测完成后再次点击 `kill` 停止 dev。

## 结果

| 项 | 结果 |
| --- | --- |
| 打开连接模块并选择测试项目 | pass |
| Feishu 添加平台后开始设置 | pass，显示可扫描二维码 |
| Feishu 取消后重试 | pass，二维码重新显示，无空白 |
| Weixin 添加平台后开始设置 | pass，显示可扫描二维码 |
| Telegram 手动 token 保存 | pass，使用安全输入，保存后列表仍为 configured |
| secret 明文展示 | pass，平台列表未显示 token 明文 |
| Feishu/Lark/Weixin 真实扫码保存 | waiting_user_scan |

## 观察

- Feishu 与 Weixin 均拿到真实二维码，证明 main 侧 beginQr、typed IPC、preload bridge、renderer QR 渲染链路可用。
- 本轮没有用户手机扫码，因此无法完成真实 `confirmed/success/saveQr` 外部闭环；自动化测试已用 mock 覆盖成功保存和 secretRef 持久化。
- 初次取消扫码后终端出现过一次 poll 相关栈，已追加修复：poll 外部请求异常会返回 `failed` session，并新增单测覆盖。

## 结论

桌面复测通过，真实手机扫码保存状态记录为 `waiting_user_scan`。
