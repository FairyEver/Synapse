# 8.2 CC Connect Admin Screen Map

## 结论

incomplete

本文件区分两类证据：

- `observed`：浏览器实测看到或安全点击过。
- `source-only blocked`：源码反查确认存在，但因 `/login` API 令牌阻塞未能实测。

## 站点地图

| 页面名称 | URL / 路由 | 左侧菜单 | 覆盖状态 | 源码对应 |
|---|---|---|---|---|
| 登录 | `/login` | 不在左侧菜单 | observed | `web/src/pages/Login.tsx` |
| 概览 | `/` | 概览 | blocked | `web/src/pages/Dashboard.tsx` |
| 项目 | `/projects` | 项目 | blocked | `web/src/pages/Projects/ProjectList.tsx` |
| 项目详情 | `/projects/:name` | 项目卡片进入 | blocked | `web/src/pages/Projects/ProjectDetail.tsx` |
| 服务商 | `/providers` | 服务商 | blocked | `web/src/pages/Providers/ProviderList.tsx` |
| 技能 | `/skills` | 技能 | blocked | `web/src/pages/Skills/SkillList.tsx` |
| 对话 | `/chat` | 对话 | blocked | `web/src/pages/Chat/ChatList.tsx` |
| 对话详情 | `/chat/:name` | 对话项目卡片进入 | blocked | `web/src/pages/Chat/ChatView.tsx` |
| 定时任务 | `/cron` | 定时任务 | blocked | `web/src/pages/Cron/CronList.tsx` |
| 系统 | `/system` | 系统 | blocked | `web/src/pages/System/Config.tsx` |

## 左侧菜单覆盖

| 菜单 | 路由 | 状态 | 证据 |
|---|---|---|---|
| 概览 | `/` | blocked | 直接访问后重定向 `/login` |
| 项目 | `/projects` | blocked | 直接访问后重定向 `/login` |
| 服务商 | `/providers` | blocked | 直接访问后重定向 `/login` |
| 技能 | `/skills` | blocked | 直接访问后重定向 `/login` |
| 对话 | `/chat` | blocked | 直接访问后重定向 `/login` |
| 定时任务 | `/cron` | blocked | 直接访问后重定向 `/login` |
| 系统 | `/system` | blocked | 直接访问后重定向 `/login` |

## 页面详图

### 登录

| 项 | 内容 |
|---|---|
| 页面名称 | 登录 |
| URL / 路由 | `/login` |
| 入口路径 | 打开 `http://localhost:9820/` 自动重定向 |
| 覆盖状态 | observed |
| 页面整体布局 | 居中登录卡片，右上角语言和主题工具 |
| 顶部工具 | 语言按钮 `EN`、`中`、`繁`、`日`、`ES`；无文字主题图标按钮 |
| 主内容区 | Logo、标题 `CC-Connect 管理后台`、副标题 `连接到您的 CC-Connect 实例`、登录表单 |
| 字段 | `API 令牌` password，placeholder `mgmt-secret-xxx`；`服务器地址` text optional，placeholder `http://localhost:9820` |
| 按钮和操作 | `连接` 按钮，token 为空时 disabled；语言按钮可点击；主题按钮可点击 |
| 空状态 / 错误 / 加载 | 未触发错误；源码中 token 无效时显示 `login.invalidToken`；提交时显示 loading spinner |
| 滚动区域和底部 | 实测未见独立滚动区域或底部 |
| 源码对应 | `web/src/pages/Login.tsx`、`web/src/store/auth.ts`、`web/src/store/theme.ts` |
| 对应 CC ID | CC-036、CC-021、CC-029 |

### App Shell

| 项 | 内容 |
|---|---|
| 页面名称 | App Shell |
| URL / 路由 | 受保护路由公共布局 |
| 入口路径 | 登录后进入 |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | 左侧 Sidebar、顶部 Header、主内容 Outlet、Footer |
| 顶部工具 | 刷新、语言 Dropdown、主题切换、退出登录 |
| 左侧区域 | 品牌 `CC-Connect`；菜单：概览、项目、服务商、技能、对话、定时任务、系统；底部折叠按钮 |
| 底部区域 | 年份、版本号、GitHub 外链 |
| 源码对应 | `web/src/components/Layout/Layout.tsx`、`Header.tsx`、`Sidebar.tsx`、`Footer.tsx` |
| 对应 CC ID | CC-036、CC-029 |

### 概览

| 项 | 内容 |
|---|---|
| 页面名称 | 概览 |
| URL / 路由 | `/` |
| 入口路径 | 左侧“概览” |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | 统计卡片区、项目区、最近会话区 |
| 顶部工具 | 继承 Header：刷新、语言、主题、退出 |
| 主内容区 | 版本、运行时间、连接平台数、项目数；项目卡片；最近会话卡片 |
| 卡片 / 列表 | StatCard 4 个；项目最多展示 4 个；最近会话最多展示 4 个 |
| 按钮和操作 | `查看全部` 到 `/projects`、`查看全部` 到 `/chat`；项目卡片进入 `/chat/:project`；最近会话卡片进入 `/chat/:project` |
| 空状态 / 错误 / 加载 | loading pulse；error 文本；无项目 EmptyState；无最近会话文本 |
| 源码对应 | `web/src/pages/Dashboard.tsx` |
| 对应 CC ID | CC-001、CC-006、CC-010、CC-036 |

### 项目

| 项 | 内容 |
|---|---|
| 页面名称 | 项目 |
| URL / 路由 | `/projects` |
| 入口路径 | 左侧“项目” |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | 标题 + 新增项目按钮；项目卡片网格；新增项目向导 Modal |
| 主内容区 | 项目卡片显示名称、agent_type、platform badges、session count、heartbeat 状态 |
| 弹窗 / 表单 | Add project Wizard：Step `name`、`platform`、`qr`、`form`、`done` |
| 字段 | Project name text；Working directory text；Agent type select |
| Agent 可选项 | `claudecode`、`codex`、`gemini`、`cursor`、`devin`、`acp`、`acp:openclaw`、`opencode`、`qoder` |
| Platform 可选项 | Feishu/Lark、WeChat、Telegram、Discord、Slack、DingTalk、WeChat Work、QQ OneBot、QQ Bot Official、LINE、Weibo |
| 按钮和操作 | Add project、Next、Back、Cancel、平台选择、QR setup、manual setup |
| 安全跳过 | 创建项目、QR 授权保存、平台凭据保存 |
| 源码对应 | `ProjectList.tsx`、`PlatformSetupQR.tsx`、`PlatformManualForm.tsx`、`platformMeta.ts` |
| 对应 CC ID | CC-002、CC-003、CC-006、CC-037、CC-036 |

### 项目详情

| 项 | 内容 |
|---|---|
| 页面名称 | 项目详情 |
| URL / 路由 | `/projects/:name` |
| 入口路径 | 项目卡片 |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | 返回按钮、项目标题、agent badge、Tab |
| Tab | Overview、Providers、Heartbeat、Settings |
| Overview | 平台连接 badges、Add platform Modal、session count、active session keys |
| Providers | 项目 provider 列表、Add Provider Modal、Link global / Add custom 切换 |
| Heartbeat | 状态、interval、run count、error count、last run、skipped busy、pause/resume、trigger、set interval |
| Settings | Agent settings、General settings、Platform access control、Danger Zone |
| 字段 | language、admin_from、disabled_commands、work_dir、agent_mode、agent_type、show_context_indicator、reply_footer、inject_sender、platform_allow_from |
| 危险操作 | 删除项目、Restart now、trigger heartbeat、pause/resume heartbeat、保存设置、添加 provider、删除 provider |
| 源码对应 | `ProjectDetail.tsx` |
| 对应 CC ID | CC-003、CC-006、CC-014、CC-016、CC-023、CC-029、CC-030、CC-037 |

### 服务商

| 项 | 内容 |
|---|---|
| 页面名称 | 服务商 |
| URL / 路由 | `/providers` |
| 入口路径 | 左侧“服务商” |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | 标题说明、导入 cc-switch、添加 provider、providers/presets Tab、卡片网格 |
| Tab | Providers、Presets |
| 弹窗 | Add/Edit Provider、Delete confirm、CC-Switch Import |
| 字段 | name、API Key、agent_types、base_url、model、models、thinking、per-agent base_url/model/models、codex wire_api |
| 可选项 | agent_types：claudecode/codex/gemini/opencode/cursor/kimi/qoder/acp；thinking：default/enabled/disabled；codex wire_api：default/responses/chat |
| 按钮和操作 | Add、Import CC-Switch、Refresh presets、Edit、Delete、show/hide API key、Add model、Set default model、Add from preset |
| 安全跳过 | 保存 provider、导入 cc-switch、删除 provider |
| 源码对应 | `ProviderList.tsx`、`api/providers.ts` |
| 对应 CC ID | CC-014 |

### 技能

| 项 | 内容 |
|---|---|
| 页面名称 | 技能 |
| URL / 路由 | `/skills` |
| 入口路径 | 左侧“技能” |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | 标题说明、Local/Recommended Tab、项目侧栏、本地技能列表或推荐技能卡片 |
| Tab | Local、Recommended |
| 主内容区 | Local：项目列表、agent_type、skill count、scan dirs、skill cards；Recommended：preset cards |
| 按钮和操作 | 切换项目、切换 Tab、Refresh recommended、外部 source/download links |
| 空状态 / 加载 | loading spinner；无项目技能；无推荐技能 |
| 源码对应 | `SkillList.tsx` |
| 对应 CC ID | CC-013 |

### 对话

| 项 | 内容 |
|---|---|
| 页面名称 | 对话列表 |
| URL / 路由 | `/chat` |
| 入口路径 | 左侧“对话” |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | 项目聊天卡片网格 |
| 卡片字段 | project name、live dot、last message、agent_type、platform badges、session count、time ago |
| 操作 | 点击项目卡片进入 `/chat/:name` |
| 空状态 / 加载 | loading pulse；无对话 EmptyState |
| 源码对应 | `ChatList.tsx` |
| 对应 CC ID | CC-001、CC-010、CC-036 |

### 对话详情

| 项 | 内容 |
|---|---|
| 页面名称 | 对话详情 |
| URL / 路由 | `/chat/:name` |
| 入口路径 | 对话项目卡片 |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | 顶部返回、项目标题、Bridge 状态、session selector；消息滚动区；输入区；SessionDrawer；CommandResultPanel |
| 输入区 | Slash 命令按钮、文本 input、发送按钮 |
| 命令面板 | 搜索框；命令分组 session/settings/info/advanced |
| 命令列表 | `/new`、`/list`、`/switch`、`/current`、`/history`、`/stop`、`/model`、`/reasoning`、`/mode`、`/lang`、`/provider`、`/status`、`/help`、`/doctor`、`/version`、`/whoami`、`/commands`、`/dir`、`/cron`、`/heartbeat`、`/alias`、`/config`、`/skills`、`/upgrade`、`/delete-mode` |
| 消息能力 | markdown、code copy、card、buttons、image、file、streaming、typing、message copy |
| 抽屉 | SessionDrawer：session list、new session、close、select session |
| 安全跳过 | 发送消息、slash 命令、card action、新建 session |
| 源码对应 | `ChatView.tsx`、`CommandPalette.tsx`、`SessionDrawer.tsx`、`CommandResultPanel.tsx` |
| 对应 CC ID | CC-001、CC-007、CC-009、CC-010、CC-011、CC-018、CC-020、CC-027、CC-031 |

### 定时任务

| 项 | 内容 |
|---|---|
| 页面名称 | 定时任务 |
| URL / 路由 | `/cron` |
| 入口路径 | 左侧“定时任务” |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | 标题 + Add 按钮；job cards；Add/Edit Modal |
| 卡片字段 | description/id、cron schedule、silent、mode、project、prompt/exec、last_run、last_error、enabled 状态 |
| 表单字段 | project select、schedule picker、description、task type prompt/exec、prompt textarea、exec input、session_key select、mode select、enabled switch、silent switch |
| Schedule 预设 | Every minute、Every 5/15/30 min、Every hour、Every 2/6 hours、Daily 6/9/12/18/22、Weekdays 9:00、Weekly Mon 9AM、Monthly 1st、Custom expression |
| Mode 可选项 | bypassPermissions、acceptEdits、auto、plan、dontAsk |
| 危险操作 | create/update job、enable/disable、delete job |
| 源码对应 | `CronList.tsx`、`api/cron.ts` |
| 对应 CC ID | CC-015、CC-012、CC-017 |

### 系统

| 项 | 内容 |
|---|---|
| 页面名称 | 系统 |
| URL / 路由 | `/system` |
| 入口路径 | 左侧“系统” |
| 覆盖状态 | source-only blocked |
| 页面整体布局 | Reload / Restart 操作区、Global Settings、Raw Config 折叠区 |
| Global Settings 分区 | General、Display、Stream preview、Rate limit、Log |
| 字段 | language、attachment_send、idle_timeout_mins、thinking_messages、thinking_max_len、tool_messages、tool_max_len、stream_preview_enabled、stream_preview_interval_ms、rate_limit_max_messages、rate_limit_window_secs、log_level |
| 可选项 | language：en/zh/zh-TW/ja/es；attachment_send：default/on/off；log_level：debug/info/warn/error |
| 折叠区域 | Raw Config，显示 TOML/JSON 标记和原始配置 pre |
| 危险操作 | reload config、restart system、save settings |
| 源码对应 | `System/Config.tsx`、`System/GlobalSettings.tsx`、`api/settings.ts` |
| 对应 CC ID | CC-005、CC-021、CC-024、CC-029、CC-032、CC-033、CC-034 |

## 弹窗 / 抽屉 / Popover / Dropdown / Tab 覆盖

| 组件 | 页面 | 类型 | 状态 | 源码 |
|---|---|---|---|---|
| Add Project Wizard | 项目 | Modal | source-only blocked | `ProjectList.tsx` |
| Platform QR Setup | 项目 / 项目详情 | Modal content | source-only blocked | `PlatformSetupQR.tsx` |
| Platform Manual Form | 项目 / 项目详情 | Modal content | source-only blocked | `PlatformManualForm.tsx` |
| Project Detail Tabs | 项目详情 | Tab | source-only blocked | `ProjectDetail.tsx` |
| Add Provider | 项目详情 | Modal | source-only blocked | `ProjectDetail.tsx` |
| Set Heartbeat Interval | 项目详情 | Modal | source-only blocked | `ProjectDetail.tsx` |
| Delete Project Confirm | 项目详情 | Modal | source-only blocked | `ProjectDetail.tsx` |
| Restart Required | 项目详情 | Modal | source-only blocked | `ProjectDetail.tsx` |
| Provider Add/Edit | 服务商 | Modal | source-only blocked | `ProviderList.tsx` |
| Provider Delete Confirm | 服务商 | Modal | source-only blocked | `ProviderList.tsx` |
| CC-Switch Import | 服务商 | Modal | source-only blocked | `ProviderList.tsx` |
| Local / Recommended | 技能 | Tab | source-only blocked | `SkillList.tsx` |
| Command Palette | 对话详情 | Popover | source-only blocked | `CommandPalette.tsx` |
| Session Drawer | 对话详情 | Drawer | source-only blocked | `SessionDrawer.tsx` |
| Command Result Panel | 对话详情 | Drawer/Panel | source-only blocked | `CommandResultPanel.tsx` |
| Cron Add/Edit | 定时任务 | Modal | source-only blocked | `CronList.tsx` |
| Header Language | App Shell | Dropdown | source-only blocked | `Header.tsx` |
| Raw Config | 系统 | Collapsible | source-only blocked | `Config.tsx` |

## 明显 source-only candidate

无独立路由未挂载在 `App.tsx` 中。`BridgeAdapters.tsx` 存在于源码树，但当前 `App.tsx` 没有路由引用；本次未登录无法确认是否有隐藏入口，应标记为 `source-only-candidate`。
