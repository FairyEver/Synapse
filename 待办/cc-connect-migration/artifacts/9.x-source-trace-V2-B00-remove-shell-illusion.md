# Source Trace: V2-B00 remove shell illusion

批次：`V2-B00`  
名称：移除空壳完成假象  
生成时间：2026-04-26 09:18 CST

## 本批目标

- 修正 3S 当前 `会话`、`连接`、`自动化` 三个入口中无 handler 按钮、固定数字和误导性静态能力展示。
- 本批不新增业务能力，只让未接入功能不再伪装成可用。

## 已读取 CC Connect 原源码

| 路径 | 已确认内容 |
| --- | --- |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Projects/ProjectList.tsx` | `listProjects()` 拉取真实项目；`openWizard()` 打开新增项目向导；新增项目包含 `name -> platform -> qr/form` 状态；平台选择用 `handlePlatformSelect()` 进入 QR 或手动表单。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Projects/ProjectDetail.tsx` | 项目详情通过 `getProject()`、`listProviders()`、`getHeartbeat()` 拉取真实数据；添加平台、设置保存、删除项目均有真实 handler。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Projects/PlatformSetupQR.tsx` | QR 流程状态机包含 `idle`、`loading`、`scanning`、`scanned`、`completed`、`expired`、`denied`、`error`、`saving`；开始 QR 设置会调用 Feishu/Weixin begin/poll/save API。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Projects/PlatformManualForm.tsx` | 手动平台表单校验 required 字段，保存时调用 `addPlatformToProject()`；不只是组件本地态。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Chat/ChatList.tsx` | 会话列表由 `listProjects()` + `listSessions(project)` 组成，按最近 session 时间排序；项目卡跳转 `/chat/:name`。 |
| `/Users/liyang/Desktop/code-guide/cc-connect-main/web/src/pages/Cron/CronList.tsx` | Cron 页面通过 `listCronJobs()` / `createCronJob()` / `updateCronJob()` / `deleteCronJob()` 形成列表、创建、编辑、删除、启停闭环。 |

## 搜索证据

执行过：

```text
rg -n "新增项目|Add Project|Add.*Project|添加平台|新建会话|New Session|添加任务|New Task|开始二维码|Telegram|Cron" web/src/pages web/src/components core config cmd platform -g'*.tsx' -g'*.ts' -g'*.go'
```

确认旧 Admin 中同名入口均对应 API、状态机或页面跳转，不是静态按钮。

## 还原逻辑

| CC Connect 逻辑 | 3S 本批处理 |
| --- | --- |
| 项目、会话、Cron 均从 API 加载真实数据。 | 移除当前 3S 固定 `0` 状态卡，不再显示硬编码统计。 |
| 新增项目、添加平台、添加 Cron 均有真实 handler。 | 在真实数据/API 未接入前，移除 `新建会话`、`添加连接`、`新建任务` 等无动作按钮。 |
| 空态只在真实数据为空时出现。 | 当前 3S 空态文案改为短提示，不展示可点击但无效动作。 |
| QR/手动平台能力在旧 Admin 中是二级流程。 | 本批不展示平台/Webhook/Bridge/QR 静态能力卡，避免用户误判为已完成。 |

## 3S 落点

| 文件 | 处理 |
| --- | --- |
| `desktop/src/modules/agent-sessions/index.tsx` | 移除固定统计卡、无 handler 按钮和能力标签，仅保留真实未接入空态。 |
| `desktop/src/modules/connectors/index.tsx` | 移除 `添加连接` 无 handler 按钮和静态能力卡。 |
| `desktop/src/modules/automation/index.tsx` | 移除 `新建任务`、`启用 Heartbeat`、`新建 Hook` 无 handler 按钮和静态标签。 |
| `desktop/tests/unit/product-entry.test.ts` | 更新静态渲染断言，防止 B00 后重新出现误导性按钮/固定统计。 |

## 无法确认内容

- 本批不实现 project、connector、cron 的保存链路；这些分别属于 `V2-B01` 到后续批次。
