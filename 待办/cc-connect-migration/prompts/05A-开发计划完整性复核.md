# 提示词 05A：开发计划完整性复核

把下面提示词完整发送给 Codex。生成 `待办/cc-connect-migration/artifacts/5.1-development-plan.md` 后、开始执行迁移前，必须运行一次。

```text
目标：从开发经理视角复核 待办/cc-connect-migration/artifacts/5.1-development-plan.md 是否可以作为唯一执行依据。只做完整性复核，不写业务代码，不扩展范围。

角色与职责：
你是交付审计负责人。你的职责是复核最终开发计划是否能作为唯一执行依据，重点检查 source inventory → CC ID → 3S 产品基线 → 产品设计 → 工程映射 → 验收项 → 开发阶段是否断链。你不能新增实现任务，不能重新设计产品，不能输出泛泛建议。你的输出必须给出 pass、fail 或 needs-user-confirmation。

请直接在当前仓库中读取和写入下面列出的路径，不要要求用户手动粘贴文件内容。

请读取：

1. 待办/cc-connect-migration/整体标准.md
2. 待办/cc-connect-migration/artifacts/1.1-source-inventory.md
3. 待办/cc-connect-migration/artifacts/1.2-feature-manifest.md
4. 待办/cc-connect-migration/artifacts/2.1-synapse-product-baseline.md
5. 待办/cc-connect-migration/artifacts/2.2-product-design.md
6. 待办/cc-connect-migration/artifacts/3.1-migration-map.md
7. 待办/cc-connect-migration/artifacts/4.1-verification-ledger.md
8. 待办/cc-connect-migration/artifacts/5.1-development-plan.md
9. 待办/cc-connect-migration/artifacts/1.3-open-questions.md
10. 当前仓库 AGENTS.md

请生成：

待办/cc-connect-migration/artifacts/5.2-development-plan-review.md

如果 `待办/cc-connect-migration/artifacts/` 不存在，先创建该目录。

复核规则：

1. 每个 source inventory 正式入口必须映射到至少一个 CC ID，或有明确排除原因。
2. 每个正式 CC ID 必须出现在 product design、migration map、verification ledger、development plan 中。
3. 每个正式 CC ID 必须能追踪到：
   - 来源证据
   - 产品入口或后台归属
   - 工程落点
   - 验收方式
   - 开发阶段
4. 每个开发阶段必须只包含正式 CC ID。
5. 每个开发阶段必须有明确完成条件和验收方式。
6. 每个 needs-user-confirmation 项必须在计划中阻塞执行或明确等待用户确认。
7. 不允许出现没有 CC ID 的开发任务。
8. 不允许出现没有验收项的开发任务。
9. 不允许输出泛泛优化建议。

待办/cc-connect-migration/artifacts/5.2-development-plan-review.md 必须包含：

1. 复核结论：pass / fail / needs-user-confirmation
2. Source Inventory 覆盖检查
3. CC ID 端到端追踪检查
4. 阶段任务完整性检查
5. 验收项覆盖检查
6. 用户确认项检查
7. 阻塞进入执行阶段的问题

如果结论是 pass，必须写：

待办/cc-connect-migration/artifacts/5.1-development-plan.md 可以作为唯一执行依据进入迁移执行。

如果结论是 fail，每个问题必须包含：

文件
ID 或入口
问题
修复要求

如果结论是 needs-user-confirmation，每个问题必须包含：

ID 或入口
需要用户确认的选项
不确认会阻塞哪个阶段
```
