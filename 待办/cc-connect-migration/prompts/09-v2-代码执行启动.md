# 提示词 09：v2 代码执行启动

把下面提示词完整发送给新的 Codex 对话。推荐先执行 `V2-RUN-01`，范围是 `V2-B00` 到 `V2-B03`。

````text
请在 `/Users/liyang/Documents/code/github/Synapse` 仓库中执行 CC Connect 融合到 3S 的 v2 代码迁移第一段。

本次执行范围：`V2-RUN-01`
允许执行批次：`V2-B00`、`V2-B01`、`V2-B02`、`V2-B03`
禁止执行范围：`V2-B04` 及之后的任何批次

角色：
你是一个严谨的 Electron + React + TypeScript 迁移工程负责人。你的任务不是凭空重做一个相似功能，而是把已经跑通的 CC Connect 功能按 3S 的产品结构迁移过来。你必须先看 CC Connect 原源码，再写 3S 代码。拒绝幻觉、拒绝猜测、拒绝空壳入口。

必须读取：
1. `AGENTS.md`
2. `.claude/rules/design.md`
3. `.claude/rules/ui-rules.md`
4. `待办/cc-connect-migration/整体标准.md`
5. `待办/cc-connect-migration/orchestrator/HANDOFF_PROTOCOL.md`
6. `待办/cc-connect-migration/orchestrator/V2_CODE_RUNNER_RULES.md`
7. `待办/cc-connect-migration/artifacts/0.0-latest-handoff.md`
8. `待办/cc-connect-migration/artifacts/8.1-cc-connect-admin-walkthrough.md`
9. `待办/cc-connect-migration/artifacts/8.2-cc-connect-admin-screen-map.md`
10. `待办/cc-connect-migration/artifacts/8.3-cc-connect-admin-interaction-ledger.md`
11. `待办/cc-connect-migration/artifacts/8.4-cc-connect-admin-to-3s-gap-list.md`
12. `待办/cc-connect-migration/artifacts/8.5-cc-connect-admin-layout-and-flow-map.md`
13. `待办/cc-connect-migration/artifacts/8.6-cc-connect-feature-implementation-gap-matrix.md`
14. `待办/cc-connect-migration/artifacts/8.7-product-scope-and-usability-analysis.md`
15. `待办/cc-connect-migration/artifacts/9.0-product-scope-decision-table.md`
16. `待办/cc-connect-migration/artifacts/9.1-v2-product-design.md`
17. `待办/cc-connect-migration/artifacts/9.2-v2-development-plan.md`
18. `待办/cc-connect-migration/artifacts/9.3-v2-acceptance-ledger.md`

CC Connect 原项目固定路径：

```text
/Users/liyang/Desktop/code-guide/cc-connect-main
```

硬性规则：
1. 每个批次写代码前，必须先在 CC Connect 原项目中用 `rg` 搜索并读取相关源码、测试、fixtures、配置示例或文档。
2. 每个批次必须先生成 `9.x-source-trace-<batch-id>-<short-name>.md`，记录实际读取的 CC Connect 文件、函数、字段、默认值、状态机、错误处理和 3S 落点。
3. 找不到本批对应源码证据时必须暂停，不得按计划文件凭空写代码。
4. 不允许引用不存在或没读取过的源码路径、函数名、字段名、API。
5. 不允许只做静态 UI、无 handler 按钮、硬编码数字、固定空态或“看起来完成”的入口。
6. 不启动 Electron、dev server、浏览器、Playwright 或运行时预览，除非用户另行明确授权。
7. 每完成一个批次必须运行验证、写执行报告、更新状态文件、更新 `0.0-latest-handoff.md`、创建 git commit、发送 Bark 通知。
8. 一旦暂停、阻塞、需要用户确认、需要 token/二维码/secret/依赖授权、三次验证失败、或本段范围完成，必须发送 Bark 手机通知。
9. Bark 通知规则读取 `待办/cc-connect-migration/整体标准.md`，通知不得包含 secret、token、API key、Raw TOML、二维码内容或敏感配置。
10. 每个批次 commit message 必须使用 `stage09-v2(<batch-id>): <批次名称> [<F ID 范围>]`，commit body 必须包含 `Source:`、`Verify:`、`Report:`。

本段执行顺序：
1. `V2-B00`：移除空壳完成假象。
2. `V2-B01`：Project 数据和 UI 基础。
3. `V2-B02`：项目详情和平台状态。
4. `V2-B03`：平台连接向导。

执行要求：
1. 严格按 `V2_CODE_RUNNER_RULES.md` 的单批执行循环执行。
2. 每批只做自己的功能，不扩大范围。
3. 每批最多修复 3 次，第三次失败必须暂停并通知。
4. 每批完成后必须提交一次 commit。
5. 本段 `V2-B00` 到 `V2-B03` 全部完成后停止，不要继续 `V2-B04`。

本段完成时必须写入或更新：
1. `待办/cc-connect-migration/artifacts/9.0-v2-code-runner-state.md`
2. `待办/cc-connect-migration/artifacts/9.0-v2-code-runner-log.md`
3. `待办/cc-connect-migration/artifacts/9.0-v2-code-runner-resume-prompt.md`
4. `待办/cc-connect-migration/artifacts/9.0-v2-code-runner-summary.md`
5. `待办/cc-connect-migration/artifacts/0.0-latest-handoff.md`
6. 每批的 `9.x-source-trace-*`
7. 每批的 `9.x-execution-report-*`

最终回复只需要简短说明：
1. 当前完成到哪个批次。
2. 生成了哪些 source trace / report。
3. 提交了哪些 commit。
4. 是否有 blocker。
5. 下一段应该执行哪个 V2-RUN。
6. 是否已发送 Bark 通知。
````
