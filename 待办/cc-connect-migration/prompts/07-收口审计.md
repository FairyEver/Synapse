# 提示词 07：收口审计

把下面提示词完整发送给 Codex。

````text
目标：对 CC Connect 迁移到 3S 做最终收口审计。审计只能判断迁移闭环是否完成，不能重新发散成开放式建议。

角色与职责：
你是发布前收口审计负责人。你的职责是只判断迁移闭环是否完成，检查正式 CC ID、产品设计、工程映射、数据兼容、权限安全、验收账本、golden 样本、人工验收、开发计划、决策日志、发布回滚和最终状态是否闭合，并做新增代码反向覆盖检查。你不能重新发散需求，不能提出新架构设想，不能输出 UI 美化建议。你的输出必须明确 complete 或 incomplete。

请直接在当前仓库中读取和写入下面列出的路径，不要要求用户手动粘贴文件内容。

请读取：

1. 待办/cc-connect-migration/整体标准.md
2. 待办/cc-connect-migration/artifacts/1.1-source-inventory.md
3. 待办/cc-connect-migration/artifacts/1.2-feature-manifest.md
4. 待办/cc-connect-migration/artifacts/2.1-synapse-product-baseline.md
5. 待办/cc-connect-migration/artifacts/2.2-product-design.md
6. 待办/cc-connect-migration/artifacts/3.1-migration-map.md
7. 待办/cc-connect-migration/artifacts/3.2-data-compatibility-plan.md
8. 待办/cc-connect-migration/artifacts/3.3-permission-and-security-map.md
9. 待办/cc-connect-migration/artifacts/4.1-verification-ledger.md
10. 待办/cc-connect-migration/artifacts/4.2-golden-test-cases.md
11. 待办/cc-connect-migration/artifacts/4.3-manual-acceptance-script.md
12. 待办/cc-connect-migration/artifacts/5.1-development-plan.md
13. 待办/cc-connect-migration/artifacts/5.2-development-plan-review.md
14. 待办/cc-connect-migration/artifacts/5.3-decision-log.md
15. 待办/cc-connect-migration/artifacts/5.4-release-and-rollback-plan.md
16. 待办/cc-connect-migration/artifacts/1.4-glossary-and-concept-map.md
17. 待办/cc-connect-migration/artifacts/1.3-open-questions.md
18. 待办/cc-connect-migration/artifacts/6.0-dev-batch-plan.md
19. 待办/cc-connect-migration/artifacts/6.0-code-runner-state.md
20. 待办/cc-connect-migration/artifacts/6.0-code-runner-log.md
21. 所有 待办/cc-connect-migration/artifacts/6.x-execution-report-*.md
22. 当前仓库 AGENTS.md

还必须运行并读取结果：

1. `git log --oneline --decorate --grep='^stage06('`
2. `git status --short`
3. `pnpm desktop:typecheck`
4. `pnpm desktop:check:hard-constraints`
5. `pnpm desktop:check:ipc-codegen`

审计规则：

只允许输出两类问题：

A. manifest 中已有正式 ID 未闭合。
B. 有来源证据的新候选项，需要用户确认是否纳入正式范围。

禁止输出：

1. 泛泛优化建议。
2. 没有证据的可能遗漏。
3. 新架构设想。
4. UI 美化建议。
5. 与 CC Connect 功能全集无关的重构建议。

stage 06 执行证据审计：

1. 每个 `6.0-dev-batch-plan.md` 中的批次都必须有对应 `6.x-execution-report-*.md`。
2. 每个 `6.x-execution-report-*.md` 必须包含“原源码对照”章节。
3. 每个“原源码对照”章节必须列出已读取的 CC Connect 源码路径和测试路径。
4. 每个批次必须有对应 git commit。
5. 每个批次 commit message 必须匹配：

```text
stage06(<批次编号>): <批次名称> [<CC ID 列表>]
```

6. 每个批次 commit body 必须包含 Source、Verify、Report。
7. `git status --short` 必须为空，除非只剩 stage 07 审计文件本身且尚未提交。
8. 如果任一批次缺少源码对照、缺少 CC Connect 源码/测试路径、缺少 commit、commit message 不合规，审计结论必须是 incomplete。
9. 如果任一批次报告只写了计划文件依据，没有列出 `/Users/liyang/Desktop/code-guide/cc-connect-main` 下的源码或测试路径，审计结论必须是 incomplete。

请生成或更新：

待办/cc-connect-migration/artifacts/7.1-final-audit.md
待办/cc-connect-migration/artifacts/7.2-reverse-coverage-check.md

如果 `待办/cc-connect-migration/artifacts/` 不存在，先创建该目录。

待办/cc-connect-migration/artifacts/7.1-final-audit.md 必须包含：

1. 审计结论：complete / incomplete
2. 未闭合正式 ID 列表
3. 产品设计未覆盖 ID 列表
4. 新发现候选项列表
5. 验收账本未通过项
6. 需要用户确认的 dropped / replaced / merged 项
7. 数据兼容、权限安全、golden 样本、人工验收、发布回滚未闭合项
8. stage 06 批次报告完整性检查
9. git commit 链路检查
10. 原源码对照完整性检查
11. 最终验证命令结果
12. 最终是否满足完成定义

待办/cc-connect-migration/artifacts/7.2-reverse-coverage-check.md 必须包含：

1. 迁移期间新增或修改的页面、服务、API、IPC、preload API、数据模型、配置项、测试和状态项
2. 对应 CC ID
3. 对应 decision-log ID
4. 是否属于范围内
5. 范围外项处理方式
6. 对应 stage06 commit
7. 对应执行报告
8. 对应 CC Connect 原源码/测试证据

完成定义：

当 待办/cc-connect-migration/artifacts/1.2-feature-manifest.md 中所有正式 CC ID 都处于 done / merged / replaced / dropped，且 待办/cc-connect-migration/artifacts/4.1-verification-ledger.md 中对应验收项全部通过或有用户确认的豁免，则 CC Connect 迁移到 3S 完成。

同时必须满足：

1. 待办/cc-connect-migration/artifacts/7.2-reverse-coverage-check.md 中所有新增入口都能追溯到 CC ID 或 decision-log。
2. 数据兼容、权限安全、golden 样本、人工验收、发布回滚相关阻塞项均已关闭或有用户确认豁免。
3. 每个 stage 06 批次都有执行报告。
4. 每个 stage 06 批次都有合规 git commit。
5. 每个 stage 06 批次执行报告都有“原源码对照”并列出 CC Connect 源码/测试路径。
6. `git status --short` 没有未提交迁移代码。
7. `pnpm desktop:typecheck`、`pnpm desktop:check:hard-constraints`、`pnpm desktop:check:ipc-codegen` 均通过。

如果满足完成定义，请明确写：

CC Connect 迁移闭环完成。

如果不满足完成定义，请只列出阻塞完成的具体 ID 和证据，不要扩展讨论。

用户通知：

生成 `7.1-final-audit.md` 和 `7.2-reverse-coverage-check.md` 后，如果审计结论是 `incomplete`，必须按 `待办/cc-connect-migration/整体标准.md` 的“用户通知规则”发送 Bark 手机通知，提醒用户查看 `7.1-final-audit.md` 中的阻塞项和下一步决定。如果审计结论是 `complete`，但仍需要用户人工检查或运行应用验收，也必须发送通知。通知结果写入当前 log 或审计文件的记录区。

跨对话交接：

审计完成后必须按 `待办/cc-connect-migration/orchestrator/HANDOFF_PROTOCOL.md` 更新 `待办/cc-connect-migration/artifacts/0.0-latest-handoff.md`，写清审计结论、阻塞项、下一步建议和给另一个 Codex 的短提示词。
````
